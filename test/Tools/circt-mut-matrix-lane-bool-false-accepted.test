// RUN: rm -rf %t && mkdir -p %t/scripts
// RUN: printf '#!/usr/bin/env bash\nset -euo pipefail\necho INVOKED\n' > %t/scripts/run_mutation_matrix.sh
// RUN: chmod +x %t/scripts/run_mutation_matrix.sh
// RUN: bash -lc 'row="lane1\t%t/design.il\t%t/mutations.txt\t%t/tests.tsv\t-\t-\t-"; for _ in $(seq 1 13); do row="${row}\t-"; done; row="${row}\tfalse"; row="${row}\t-"; row="${row}\t-"; row="${row}\t-"; row="${row}\t-"; row="${row}\t-"; row="${row}\t-"; row="${row}\t-"; row="${row}\tfalse"; row="${row}\tfalse"; printf "%b\n" "$row" > %t/lanes.tsv'
// RUN: env CIRCT_MUT_SCRIPTS_DIR=%t/scripts circt-mut matrix --lanes-tsv %t/lanes.tsv > %t/log.txt
// RUN: FileCheck %s < %t/log.txt
//
// CHECK: INVOKED
