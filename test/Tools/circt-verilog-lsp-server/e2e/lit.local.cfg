# This directory contains pytest-based e2e tests, not lit tests.
# Mark the entire directory as unsupported to prevent lit from trying to run
# these files as lit tests. The pytest tests should be run separately with:
#   cd test/Tools/circt-verilog-lsp-server/e2e && pytest
config.unsupported = True
