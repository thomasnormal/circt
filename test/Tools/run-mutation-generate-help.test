// RUN: %S/../../utils/generate_mutations_yosys.sh --help | FileCheck %s

// CHECK: generate_mutations_yosys.sh [options]
// CHECK: --design
// CHECK: --out
// CHECK: --count
// CHECK: --seed
// CHECK: --yosys
// CHECK: --cache-dir
// CHECK: --mode
// CHECK: stuck,invert,connect
// CHECK: --modes
// CHECK: --mode-count
// CHECK: --mode-counts
// CHECK: --mode-weight
// CHECK: --mode-weights
// CHECK: --profile
// CHECK: --profiles
// CHECK: --cfg
// CHECK: --cfgs
// CHECK: --select
// CHECK: --selects
