// RUN: %S/../../utils/generate_mutations_yosys.sh --help | FileCheck %s

// CHECK: generate_mutations_yosys.sh [options]
// CHECK: --design
// CHECK: --out
// CHECK: --count
// CHECK: --seed
// CHECK: --yosys
// CHECK: --mode
// CHECK: --modes
// CHECK: --cfg
// CHECK: --cfgs
// CHECK: --select
// CHECK: --selects
