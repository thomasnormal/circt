// RUN: rm -rf %t && mkdir -p %t/proj
// RUN: printf '[cover]\ndesign = "d.il"\nmutations_file = "m.txt"\ngenerate_mutations = 10\ntests_manifest = "t.tsv"\nwork_dir = "out/cover"\n' > %t/proj/circt-mut.toml
// RUN: not circt-mut run --project-dir %t/proj --mode cover > %t/log.txt 2>&1
// RUN: FileCheck %s < %t/log.txt
//
// CHECK: circt-mut run: [cover] keys 'mutations_file' and 'generate_mutations' are mutually exclusive.
