// RUN: rm -rf %t
// RUN: mkdir -p %t/bin %t/src
// RUN: touch %t/src/a.sv
// RUN: printf '#!/usr/bin/env bash\nset -euo pipefail\nout=\"\"\nwhile [[ $# -gt 0 ]]; do\n  case \"$1\" in\n    -o) out=\"$2\"; shift 2 ;;\n    *) shift ;;\n  esac\ndone\n: \"${out:?}\"\nprintf \"dummy\\n\" > \"$out\"\nexit 0\n' > %t/bin/circt-verilog
// RUN: printf '#!/usr/bin/env bash\nset -euo pipefail\nstate=\"%t/opt.state\"\ncount=0\nif [[ -f \"$state\" ]]; then\n  count=\"$(cat \"$state\")\"\nfi\ncount=$((count + 1))\necho \"$count\" > \"$state\"\nif [[ \"$count\" -eq 1 ]]; then\n  echo \"posix_spawn failed: Permission denied\" >&2\n  exit 126\nfi\nin=\"\"\nout=\"\"\nwhile [[ $# -gt 0 ]]; do\n  case \"$1\" in\n    -o) out=\"$2\"; shift 2 ;;\n    *)\n      if [[ -z \"$in\" ]]; then in=\"$1\"; fi\n      shift ;;\n  esac\ndone\n: \"${in:?}\"\n: \"${out:?}\"\ncp \"$in\" \"$out\"\nexit 0\n' > %t/bin/circt-opt
// RUN: printf '#!/usr/bin/env bash\necho \"BMC_RESULT=UNSAT\"\nexit 0\n' > %t/bin/circt-bmc
// RUN: chmod +x %t/bin/circt-verilog %t/bin/circt-opt %t/bin/circt-bmc
// RUN: printf 'retry_case\ttop\t%t/src/a.sv\t%t/src\t%t/work/retry\n' > %t/cases.tsv
// RUN: env CIRCT_VERILOG=%t/bin/circt-verilog CIRCT_OPT=%t/bin/circt-opt CIRCT_BMC=%t/bin/circt-bmc BMC_RUN_SMTLIB=0 BMC_LAUNCH_RETRY_ATTEMPTS=2 BMC_LAUNCH_RETRY_BACKOFF_SECS=0 OUT=%t/results.tsv %S/../../utils/run_pairwise_circt_bmc.py --cases-file %t/cases.tsv --workdir %t/work > %t/run.log
// RUN: FileCheck %s --check-prefix=LOG < %t/run.log
// RUN: FileCheck %s --check-prefix=RES < %t/results.tsv
// RUN: FileCheck %s --check-prefix=OPTLOG < %t/work/retry_case/circt-opt.log
//
// LOG: Running BMC on 1 pairwise case(s)...
// LOG: pairwise BMC summary: total=1 pass=1 fail=0 xfail=0 xpass=0 error=0 skip=0 unknown=0 timeout=0
// RES: PASS{{[[:space:]]+}}retry_case{{[[:space:]]+}}{{.*}}work/retry{{[[:space:]]+}}pairwise{{[[:space:]]+}}BMC{{[[:space:]]+}}UNSAT
// OPTLOG: runner_command_launch_retry reason=retryable_exit_code_126 attempt=1 delay_secs=0.000
