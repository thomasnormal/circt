// REQUIRES: native_tools

// RUN: cat > %t/design.sv <<'SV'
// RUN: module top(input logic clk, input logic d, output logic q);
// RUN:   always_ff @(posedge clk) q <= d;
// RUN: endmodule
// RUN: SV
// RUN: not circt-mut generate --design %t/design.sv --out %t/mutations.txt --count 1 --seed 0 --native-ops RESET_NEGEDGE_TO_POSEDGE > %t/log.txt 2>&1
// RUN: FileCheck %s --check-prefix=ERR < %t/log.txt

// ERR: circt-mut generate: no applicable native mutation sites for selected operators
