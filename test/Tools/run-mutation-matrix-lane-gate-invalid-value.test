// RUN: rm -rf %t && mkdir -p %t
// RUN: printf 'module top; endmodule\n' > %t/design.il
// RUN: printf '1 M1\n' > %t/mutations.txt
// RUN: printf '#!/usr/bin/env bash\nset -euo pipefail\nwhile [[ $# -gt 0 ]]; do\n  case \"$1\" in\n    -o) out=\"$2\"; shift 2 ;;\n    -d) design=\"$2\"; shift 2 ;;\n    *) shift ;;\n  esac\ndone\ncp \"$design\" \"$out\"\n' > %t/fake-create-mutated.sh
// RUN: chmod +x %t/fake-create-mutated.sh
// RUN: printf '#!/usr/bin/env bash\nset -euo pipefail\necho SURVIVED > result.txt\n' > %t/fake-test.sh
// RUN: chmod +x %t/fake-test.sh
// RUN: printf 't\tbash %t/fake-test.sh\tresult.txt\t^DETECTED$\t^SURVIVED$\n' > %t/tests.tsv
//
// RUN: bash -lc 'row="lane_bad\t%t/design.il\t%t/mutations.txt\t%t/tests.tsv\t-\t-\t-"; for _ in $(seq 1 32); do row="${row}\t-"; done; row="${row}\tmaybe\t-\t-"; printf "%b\n" "$row" > %t/lanes.tsv'
//
// RUN: not %S/../../utils/run_mutation_matrix.sh \
// RUN:   --lanes-tsv %t/lanes.tsv \
// RUN:   --out-dir %t/out \
// RUN:   --create-mutated-script %t/fake-create-mutated.sh \
// RUN:   2>&1 | FileCheck %s --check-prefix=LOG
// RUN: FileCheck %s --check-prefix=RESULTS < %t/out/results.tsv
//
// LOG: Mutation matrix summary: pass=0 fail=1
//
// RESULTS: lane_id{{.*}}status{{.*}}exit_code{{.*}}gate_status{{.*}}config_error_code{{.*}}config_error_reason
// RESULTS-NEXT: lane_bad{{.*}}FAIL{{.*}}1{{.*}}CONFIG_ERROR{{.*}}INVALID_SKIP_BASELINE_OVERRIDE{{.*}}Invalid lane skip_baseline override
