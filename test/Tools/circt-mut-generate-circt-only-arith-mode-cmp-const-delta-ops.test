// RUN: rm -rf %t && mkdir -p %t
// RUN: printf "module top(input logic [3:0] a,b, output logic y0,y1);\n  assign y0 = (a == 3);\n  assign y1 = (4 <= b);\nendmodule\n" > %t/design.sv
// RUN: env -u CIRCT_MUT_ALLOW_THIRD_PARTY circt-mut generate --design %t/design.sv --out %t/mutations.txt --count 64 --seed 0 --modes arith > %t/log.txt
// RUN: grep -q 'NATIVE_CMP_CONST_PLUS_ONE' %t/mutations.txt
// RUN: grep -q 'NATIVE_CMP_CONST_MINUS_ONE' %t/mutations.txt
// RUN: FileCheck %s --check-prefix=LOG < %t/log.txt
//
// LOG: Generated mutations: 64
