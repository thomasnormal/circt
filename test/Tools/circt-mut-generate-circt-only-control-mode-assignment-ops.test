// RUN: rm -rf %t && mkdir -p %t
// RUN: printf 'module top(input logic clk, input logic a,b,c,d, output logic y0,y1);\n  always @(posedge clk) begin\n    y0 = a & b;\n    y1 <= c | d;\n  end\nendmodule\n' > %t/design.sv
// RUN: env -u CIRCT_MUT_ALLOW_THIRD_PARTY circt-mut generate --design %t/design.sv --out %t/mutations.txt --count 120 --seed 37 --modes control > %t/log.txt
// RUN: grep -q 'NATIVE_BA_TO_NBA' %t/mutations.txt
// RUN: grep -q 'NATIVE_NBA_TO_BA' %t/mutations.txt
// RUN: FileCheck %s --check-prefix=LOG < %t/log.txt
//
// LOG: Generated mutations: 120
