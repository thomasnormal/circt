// RUN: rm -rf %t && mkdir -p %t/proj
// RUN: printf '[run]\nwith_report = true\nreport_mode = "cover"\nreport_history = "history.tsv"\nreport_compare = "base.tsv"\n' > %t/proj/circt-mut.toml
// RUN: not circt-mut run --project-dir %t/proj --mode cover 2>&1 | FileCheck %s
//
// CHECK: circt-mut run: report_history shorthand is mutually exclusive with report compare/trend/append selectors
