// RUN: rm -rf %t && mkdir -p %t
// RUN: printf 'module top(input logic a, input logic b, output logic y0, output logic y1);\n  assign y0 = (a == b);\n  assign y1 = (a == b);\nendmodule\n' > %t/design.sv
// RUN: printf '1 NATIVE_EQ_TO_NEQ@2\n' > %t/mutation.txt
// RUN: circt-mut apply -i %t/mutation.txt -o %t/mutant.sv -d %t/design.sv
// RUN: FileCheck %s --check-prefix=MUTANT < %t/mutant.sv
//
// MUTANT: assign y0 = (a == b);
// MUTANT: assign y1 = (a != b);
