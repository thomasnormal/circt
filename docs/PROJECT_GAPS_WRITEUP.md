# Project Gap Writeups

Generated: 2026-02-27 10:21:38 UTC

This report contains one paragraph per scanned gap marker.

## Summary

- Total entries: 2071
- TODO/FIXME-like entries: 682
- Unsupported/unimplemented-like entries: 886

## Gap Paragraphs

### G0001 `utils/check_avip_circt_sim_mode_parity.py:94` (tooling-gap)

At `utils/check_avip_circt_sim_mode_parity.py:94`, the marker `f"invalid allowlist row {line_no}: unsupported mode '{mode}' "` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0002 `utils/run_mutation_cover.sh:406` (tooling-gap)

At `utils/run_mutation_cover.sh:406`, the marker `echo "Unsupported mutant format: $MUTANT_FORMAT (expected il|v|sv)." >&2` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0003 `utils/summarize_circt_sim_jit_reports.py:113` (tooling-gap)

At `utils/summarize_circt_sim_jit_reports.py:113`, the marker `f"invalid allowlist row {line_no}: unsupported mode '{mode}' "` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0004 `unittests/Runtime/MooreRuntimeTest.cpp:10074` (unclassified)

At `unittests/Runtime/MooreRuntimeTest.cpp:10074`, the marker `__uvm_run_test("unimplemented_test", 18);` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0005 `unittests/Runtime/MooreRuntimeTest.cpp:10081` (unclassified)

At `unittests/Runtime/MooreRuntimeTest.cpp:10081`, the marker `EXPECT_NE(output.find("unimplemented_test"), std::string::npos);` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0006 `unittests/Analysis/CMakeLists.txt:1` (todo)

At `unittests/Analysis/CMakeLists.txt:1`, the marker `# TODO: Linting needs slang header fixes` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Linting needs slang header fixes. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0007 `utils/run_opentitan_fpv_circt_lec.py:414` (tooling-gap)

At `utils/run_opentitan_fpv_circt_lec.py:414`, the marker `"case-level LEC health checks cannot yet derive native cover "` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0008 `utils/formal/lib/runner_common.py:117` (tooling-gap)

At `utils/formal/lib/runner_common.py:117`, the marker `f"invalid allowlist row {line_no}: unsupported mode '{mode}' "` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0009 `utils/run_sv_tests_circt_sim.sh:516` (tooling-gap)

At `utils/run_sv_tests_circt_sim.sh:516`, the marker `if grep -q "unsupported\|not yet implemented\|unimplemented" "$sim_log" 2>/dev/null; then` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0010 `utils/run_sv_tests_circt_sim.sh:517` (tooling-gap)

At `utils/run_sv_tests_circt_sim.sh:517`, the marker `result="UNSUPPORTED"` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0011 `tools/circt-sim-compile/circt-sim-compile.cpp:630` (unclassified)

At `tools/circt-sim-compile/circt-sim-compile.cpp:630`, the marker `*rejectionReason = "builtin.unrealized_conversion_cast:unsupported_arity";` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0012 `tools/circt-sim-compile/circt-sim-compile.cpp:1417` (tooling-gap)

At `tools/circt-sim-compile/circt-sim-compile.cpp:1417`, the marker `bool unsupported = false;` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0013 `tools/circt-sim-compile/circt-sim-compile.cpp:1427` (unclassified)

At `tools/circt-sim-compile/circt-sim-compile.cpp:1427`, the marker `skipReason = ("unsupported_call:" + callee->str());` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0014 `tools/circt-sim-compile/circt-sim-compile.cpp:1429` (unclassified)

At `tools/circt-sim-compile/circt-sim-compile.cpp:1429`, the marker `skipReason = "unsupported_call:indirect";` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0015 `tools/circt-sim-compile/circt-sim-compile.cpp:1431` (unclassified)

At `tools/circt-sim-compile/circt-sim-compile.cpp:1431`, the marker `skipReason = ("unsupported_op:" +` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0016 `tools/circt-sim-compile/circt-sim-compile.cpp:1434` (tooling-gap)

At `tools/circt-sim-compile/circt-sim-compile.cpp:1434`, the marker `unsupported = true;` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0017 `tools/circt-sim-compile/circt-sim-compile.cpp:1439` (unclassified)

At `tools/circt-sim-compile/circt-sim-compile.cpp:1439`, the marker `skipReason = "unsupported_op:hw.struct_extract";` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0018 `tools/circt-sim-compile/circt-sim-compile.cpp:1440` (tooling-gap)

At `tools/circt-sim-compile/circt-sim-compile.cpp:1440`, the marker `unsupported = true;` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0019 `tools/circt-sim-compile/circt-sim-compile.cpp:1446` (unclassified)

At `tools/circt-sim-compile/circt-sim-compile.cpp:1446`, the marker `skipReason = "unsupported_op:hw.struct_create";` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0020 `tools/circt-sim-compile/circt-sim-compile.cpp:1447` (tooling-gap)

At `tools/circt-sim-compile/circt-sim-compile.cpp:1447`, the marker `unsupported = true;` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0021 `tools/circt-sim-compile/circt-sim-compile.cpp:1453` (tooling-gap)

At `tools/circt-sim-compile/circt-sim-compile.cpp:1453`, the marker `unsupported = true;` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0022 `tools/circt-sim-compile/circt-sim-compile.cpp:1467` (tooling-gap)

At `tools/circt-sim-compile/circt-sim-compile.cpp:1467`, the marker `unsupported = true;` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0023 `tools/circt-sim-compile/circt-sim-compile.cpp:1480` (tooling-gap)

At `tools/circt-sim-compile/circt-sim-compile.cpp:1480`, the marker `unsupported = true;` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0024 `tools/circt-sim-compile/circt-sim-compile.cpp:1484` (unclassified)

At `tools/circt-sim-compile/circt-sim-compile.cpp:1484`, the marker `skipReason = ("operand_dep_unsupported:" +` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0025 `tools/circt-sim-compile/circt-sim-compile.cpp:1486` (tooling-gap)

At `tools/circt-sim-compile/circt-sim-compile.cpp:1486`, the marker `unsupported = true;` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0026 `tools/circt-sim-compile/circt-sim-compile.cpp:1492` (tooling-gap)

At `tools/circt-sim-compile/circt-sim-compile.cpp:1492`, the marker `if (unsupported)` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0027 `tools/circt-sim-compile/circt-sim-compile.cpp:1497` (tooling-gap)

At `tools/circt-sim-compile/circt-sim-compile.cpp:1497`, the marker `if (unsupported || opsToClone.empty()) {` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0028 `tools/circt-sim-compile/circt-sim-compile.cpp:1498` (tooling-gap)

At `tools/circt-sim-compile/circt-sim-compile.cpp:1498`, the marker `if (unsupported) {` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0029 `tools/circt-sim-compile/circt-sim-compile.cpp:1500` (tooling-gap)

At `tools/circt-sim-compile/circt-sim-compile.cpp:1500`, the marker `skipReason = "unsupported:unknown";` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0030 `tools/circt-sim-compile/circt-sim-compile.cpp:1514` (unclassified)

At `tools/circt-sim-compile/circt-sim-compile.cpp:1514`, the marker `bool cloneUnsupported = false;` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0031 `tools/circt-sim-compile/circt-sim-compile.cpp:1521` (unclassified)

At `tools/circt-sim-compile/circt-sim-compile.cpp:1521`, the marker `cloneUnsupported = true;` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0032 `tools/circt-sim-compile/circt-sim-compile.cpp:1550` (unclassified)

At `tools/circt-sim-compile/circt-sim-compile.cpp:1550`, the marker `cloneUnsupported = true;` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0033 `tools/circt-sim-compile/circt-sim-compile.cpp:1575` (unclassified)

At `tools/circt-sim-compile/circt-sim-compile.cpp:1575`, the marker `if (cloneUnsupported) {` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0034 `tools/circt-sim-compile/circt-sim-compile.cpp:1577` (tooling-gap)

At `tools/circt-sim-compile/circt-sim-compile.cpp:1577`, the marker `cloneSkipReason = "unsupported:unknown";` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0035 `tools/circt-sim-compile/circt-sim-compile.cpp:1592` (tooling-gap)

At `tools/circt-sim-compile/circt-sim-compile.cpp:1592`, the marker `/// This keeps the module-init pipeline conservative while avoiding unsupported` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0036 `tools/circt-sim-compile/circt-sim-compile.cpp:1859` (tooling-gap)

At `tools/circt-sim-compile/circt-sim-compile.cpp:1859`, the marker `/// This is intentionally conservative: unsupported fragments are rendered as a` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0037 `tools/circt-sim-compile/circt-sim-compile.cpp:1872` (tooling-gap)

At `tools/circt-sim-compile/circt-sim-compile.cpp:1872`, the marker `formatStr.append("<unsupported>");` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0038 `tools/circt-sim-compile/circt-sim-compile.cpp:1899` (tooling-gap)

At `tools/circt-sim-compile/circt-sim-compile.cpp:1899`, the marker `formatStr.append("<unsupported>");` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0039 `tools/circt-sim-compile/circt-sim-compile.cpp:1911` (tooling-gap)

At `tools/circt-sim-compile/circt-sim-compile.cpp:1911`, the marker `formatStr.append("<unsupported>");` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0040 `tools/circt-sim-compile/circt-sim-compile.cpp:1923` (tooling-gap)

At `tools/circt-sim-compile/circt-sim-compile.cpp:1923`, the marker `formatStr.append("<unsupported>");` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0041 `tools/circt-sim-compile/circt-sim-compile.cpp:1936` (tooling-gap)

At `tools/circt-sim-compile/circt-sim-compile.cpp:1936`, the marker `formatStr.append("<unsupported>");` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0042 `tools/circt-sim-compile/circt-sim-compile.cpp:1948` (tooling-gap)

At `tools/circt-sim-compile/circt-sim-compile.cpp:1948`, the marker `formatStr.append("<unsupported>");` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0043 `tools/circt-sim-compile/circt-sim-compile.cpp:1961` (tooling-gap)

At `tools/circt-sim-compile/circt-sim-compile.cpp:1961`, the marker `formatStr.append("<unsupported>");` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0044 `tools/circt-sim-compile/circt-sim-compile.cpp:1975` (tooling-gap)

At `tools/circt-sim-compile/circt-sim-compile.cpp:1975`, the marker `formatStr.append("<unsupported>");` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0045 `tools/circt-sim-compile/circt-sim-compile.cpp:1989` (tooling-gap)

At `tools/circt-sim-compile/circt-sim-compile.cpp:1989`, the marker `formatStr.append("<unsupported>");` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0046 `tools/circt-sim-compile/circt-sim-compile.cpp:2009` (tooling-gap)

At `tools/circt-sim-compile/circt-sim-compile.cpp:2009`, the marker `formatStr.append("<unsupported>");` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0047 `tools/circt-sim-compile/circt-sim-compile.cpp:2026` (tooling-gap)

At `tools/circt-sim-compile/circt-sim-compile.cpp:2026`, the marker `formatStr.append("<unsupported>");` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0048 `tools/circt-sim-compile/circt-sim-compile.cpp:3487` (tooling-gap)

At `tools/circt-sim-compile/circt-sim-compile.cpp:3487`, the marker `bool unsupported = false;` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0049 `tools/circt-sim-compile/circt-sim-compile.cpp:3493` (tooling-gap)

At `tools/circt-sim-compile/circt-sim-compile.cpp:3493`, the marker `unsupported = true;` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0050 `tools/circt-sim-compile/circt-sim-compile.cpp:3499` (tooling-gap)

At `tools/circt-sim-compile/circt-sim-compile.cpp:3499`, the marker `if (!unsupported && calleeTy.getNumResults() == 1) {` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0051 `tools/circt-sim-compile/circt-sim-compile.cpp:3504` (tooling-gap)

At `tools/circt-sim-compile/circt-sim-compile.cpp:3504`, the marker `unsupported = true;` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0052 `tools/circt-sim-compile/circt-sim-compile.cpp:3508` (tooling-gap)

At `tools/circt-sim-compile/circt-sim-compile.cpp:3508`, the marker `if (unsupported)` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0053 `tools/circt-sim-compile/circt-sim-compile.cpp:3526` (tooling-gap)

At `tools/circt-sim-compile/circt-sim-compile.cpp:3526`, the marker `unsupported = true;` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0054 `tools/circt-sim-compile/circt-sim-compile.cpp:3532` (tooling-gap)

At `tools/circt-sim-compile/circt-sim-compile.cpp:3532`, the marker `if (unsupported)` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0055 `tools/circt-sim-compile/circt-sim-compile.cpp:3825` (tooling-gap)

At `tools/circt-sim-compile/circt-sim-compile.cpp:3825`, the marker `return 0; // nested unsupported type` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0056 `tools/circt-sim-compile/circt-sim-compile.cpp:3830` (tooling-gap)

At `tools/circt-sim-compile/circt-sim-compile.cpp:3830`, the marker `return 0; // unsupported` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0057 `tools/circt-sim-compile/circt-sim-compile.cpp:4171` (tooling-gap)

At `tools/circt-sim-compile/circt-sim-compile.cpp:4171`, the marker `/// functions containing unsupported ops) without crashing at runtime.` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0058 `tools/circt-sim-compile/circt-sim-compile.cpp:4215` (unclassified)

At `tools/circt-sim-compile/circt-sim-compile.cpp:4215`, the marker `bool sawUnsupportedReferencedExternal = false;` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0059 `tools/circt-sim-compile/circt-sim-compile.cpp:4253` (unclassified)

At `tools/circt-sim-compile/circt-sim-compile.cpp:4253`, the marker `sawUnsupportedReferencedExternal = true;` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0060 `tools/circt-sim-compile/circt-sim-compile.cpp:4259` (unclassified)

At `tools/circt-sim-compile/circt-sim-compile.cpp:4259`, the marker `bool hasUnsupported = false;` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0061 `tools/circt-sim-compile/circt-sim-compile.cpp:4260` (unclassified)

At `tools/circt-sim-compile/circt-sim-compile.cpp:4260`, the marker `std::string unsupportedReason;` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0062 `tools/circt-sim-compile/circt-sim-compile.cpp:4265` (unclassified)

At `tools/circt-sim-compile/circt-sim-compile.cpp:4265`, the marker `hasUnsupported = true;` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0063 `tools/circt-sim-compile/circt-sim-compile.cpp:4266` (unclassified)

At `tools/circt-sim-compile/circt-sim-compile.cpp:4266`, the marker `unsupportedReason = ("parameter " + std::to_string(i) + " has type " +` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0064 `tools/circt-sim-compile/circt-sim-compile.cpp:4271` (unclassified)

At `tools/circt-sim-compile/circt-sim-compile.cpp:4271`, the marker `if (!hasUnsupported && !isa<LLVM::LLVMVoidType>(funcTy.getReturnType())) {` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0065 `tools/circt-sim-compile/circt-sim-compile.cpp:4275` (unclassified)

At `tools/circt-sim-compile/circt-sim-compile.cpp:4275`, the marker `hasUnsupported = true;` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0066 `tools/circt-sim-compile/circt-sim-compile.cpp:4276` (unclassified)

At `tools/circt-sim-compile/circt-sim-compile.cpp:4276`, the marker `if (hasUnsupported)` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0067 `tools/circt-sim-compile/circt-sim-compile.cpp:4277` (unclassified)

At `tools/circt-sim-compile/circt-sim-compile.cpp:4277`, the marker `unsupportedReason = ("return type " + formatType(retTy));` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0068 `tools/circt-sim-compile/circt-sim-compile.cpp:4279` (unclassified)

At `tools/circt-sim-compile/circt-sim-compile.cpp:4279`, the marker `if (hasUnsupported) {` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0069 `tools/circt-sim-compile/circt-sim-compile.cpp:4282` (tooling-gap)

At `tools/circt-sim-compile/circt-sim-compile.cpp:4282`, the marker `"function: unsupported trampoline ABI ("` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0070 `tools/circt-sim-compile/circt-sim-compile.cpp:4283` (unclassified)

At `tools/circt-sim-compile/circt-sim-compile.cpp:4283`, the marker `<< unsupportedReason << ")";` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0071 `tools/circt-sim-compile/circt-sim-compile.cpp:4284` (unclassified)

At `tools/circt-sim-compile/circt-sim-compile.cpp:4284`, the marker `sawUnsupportedReferencedExternal = true;` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0072 `tools/circt-sim-compile/circt-sim-compile.cpp:4289` (unclassified)

At `tools/circt-sim-compile/circt-sim-compile.cpp:4289`, the marker `if (sawUnsupportedReferencedExternal)` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0073 `tools/circt-sim-compile/circt-sim-compile.cpp:7498` (tooling-gap)

At `tools/circt-sim-compile/circt-sim-compile.cpp:7498`, the marker `bool unsupported = false;` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0074 `tools/circt-sim-compile/circt-sim-compile.cpp:7506` (tooling-gap)

At `tools/circt-sim-compile/circt-sim-compile.cpp:7506`, the marker `unsupported = true;` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0075 `tools/circt-sim-compile/circt-sim-compile.cpp:7510` (tooling-gap)

At `tools/circt-sim-compile/circt-sim-compile.cpp:7510`, the marker `if (!unsupported) {` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0076 `tools/circt-sim-compile/circt-sim-compile.cpp:7522` (tooling-gap)

At `tools/circt-sim-compile/circt-sim-compile.cpp:7522`, the marker `unsupported = true;` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0077 `tools/circt-sim-compile/circt-sim-compile.cpp:7525` (tooling-gap)

At `tools/circt-sim-compile/circt-sim-compile.cpp:7525`, the marker `unsupported = true;` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0078 `tools/circt-sim-compile/circt-sim-compile.cpp:7527` (tooling-gap)

At `tools/circt-sim-compile/circt-sim-compile.cpp:7527`, the marker `if (!unsupported) {` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0079 `tools/circt-sim-compile/circt-sim-compile.cpp:7536` (tooling-gap)

At `tools/circt-sim-compile/circt-sim-compile.cpp:7536`, the marker `// If not found in original module (or unsupported type), skip.` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0080 `utils/run_mutation_mcy_examples.sh:1006` (unclassified)

At `utils/run_mutation_mcy_examples.sh:1006`, the marker `tmp_values_file="$(mktemp "${OUT_DIR}/example-history-values.XXXXXX")"` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0081 `utils/run_mutation_mcy_examples.sh:2379` (unclassified)

At `utils/run_mutation_mcy_examples.sh:2379`, the marker `history_detected_file="$(mktemp "${OUT_DIR}/suite-history-detected.XXXXXX")"` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0082 `utils/run_mutation_mcy_examples.sh:2380` (unclassified)

At `utils/run_mutation_mcy_examples.sh:2380`, the marker `history_relevant_file="$(mktemp "${OUT_DIR}/suite-history-relevant.XXXXXX")"` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0083 `utils/run_mutation_mcy_examples.sh:2381` (unclassified)

At `utils/run_mutation_mcy_examples.sh:2381`, the marker `history_coverage_file="$(mktemp "${OUT_DIR}/suite-history-coverage.XXXXXX")"` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0084 `utils/run_mutation_mcy_examples.sh:2382` (unclassified)

At `utils/run_mutation_mcy_examples.sh:2382`, the marker `history_errors_file="$(mktemp "${OUT_DIR}/suite-history-errors.XXXXXX")"` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0085 `frontends/PyRTG/src/pyrtg/support.py:60` (unclassified)

At `frontends/PyRTG/src/pyrtg/support.py:60`, the marker `assert False, "Unsupported value"` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0086 `frontends/PyRTG/src/pyrtg/support.py:117` (unclassified)

At `frontends/PyRTG/src/pyrtg/support.py:117`, the marker `raise ValueError("unsupported type")` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0087 `test/Runtime/uvm/uvm_phase_wait_for_state_test.sv:2` (test-expectation)

At `test/Runtime/uvm/uvm_phase_wait_for_state_test.sv:2`, the marker `// UNSUPPORTED: true` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0088 `test/Runtime/uvm/uvm_phase_aliases_test.sv:2` (test-expectation)

At `test/Runtime/uvm/uvm_phase_aliases_test.sv:2`, the marker `// UNSUPPORTED: true` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0089 `utils/run_avip_circt_sim.sh:249` (tooling-gap)

At `utils/run_avip_circt_sim.sh:249`, the marker `echo "error: unsupported AVIP_SET '$AVIP_SET' (use core8 or all9)" >&2` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0090 `utils/run_avip_circt_sim.sh:269` (tooling-gap)

At `utils/run_avip_circt_sim.sh:269`, the marker `echo "error: unsupported CIRCT_SIM_MODE '$CIRCT_SIM_MODE' (use interpret or compile)" >&2` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0091 `frontends/PyRTG/src/pyrtg/control_flow.py:92` (todo)

At `frontends/PyRTG/src/pyrtg/control_flow.py:92`, the marker `# FIXME: this is very ugly because the MLIR python bindings do now allow us` indicates a gap signal in this path. What's missing: The marker documents unfinished work: this is very ugly because the MLIR python bindings do now allow us. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0092 `frontends/PyRTG/src/pyrtg/control_flow.py:174` (todo)

At `frontends/PyRTG/src/pyrtg/control_flow.py:174`, the marker `# TODO: probably better to delete the variable` indicates a gap signal in this path. What's missing: The marker documents unfinished work: probably better to delete the variable. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0093 `frontends/PyRTG/src/pyrtg/contexts.py:73` (todo)

At `frontends/PyRTG/src/pyrtg/contexts.py:73`, the marker `# TODO: just adding all variables in the context is not particularly nice.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: just adding all variables in the context is not particularly nice.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0094 `frontends/PyRTG/src/pyrtg/contexts.py:87` (todo)

At `frontends/PyRTG/src/pyrtg/contexts.py:87`, the marker `# TODO: we currently just assume "_context_seq" is a reserved prefix, would` indicates a gap signal in this path. What's missing: The marker documents unfinished work: we currently just assume "_context_seq" is a reserved prefix, would. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0095 `utils/run_avip_arcilator_sim.sh:103` (tooling-gap)

At `utils/run_avip_arcilator_sim.sh:103`, the marker `echo "error: unsupported AVIP_SET '$AVIP_SET' (use core8 or all9)" >&2` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0096 `utils/create_mutated_yosys.sh:95` (tooling-gap)

At `utils/create_mutated_yosys.sh:95`, the marker `echo "Unsupported design extension in '$DESIGN_FILE' (expected .il/.v/.sv)." >&2` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0097 `utils/create_mutated_yosys.sh:106` (tooling-gap)

At `utils/create_mutated_yosys.sh:106`, the marker `echo "Unsupported output extension in '$OUTPUT_FILE' (expected .il/.v/.sv)." >&2` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0098 `utils/check_opentitan_connectivity_cover_parity.py:101` (tooling-gap)

At `utils/check_opentitan_connectivity_cover_parity.py:101`, the marker `f"invalid allowlist row {line_no}: unsupported mode '{mode}' "` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0099 `utils/check_opentitan_connectivity_status_parity.py:101` (tooling-gap)

At `utils/check_opentitan_connectivity_status_parity.py:101`, the marker `f"invalid allowlist row {line_no}: unsupported mode '{mode}' "` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0100 `test/CMakeLists.txt:53` (todo)

At `test/CMakeLists.txt:53`, the marker `# TODO: circt-verilog-lsp-server is disabled due to slang API compatibility issues` indicates a gap signal in this path. What's missing: The marker documents unfinished work: circt-verilog-lsp-server is disabled due to slang API compatibility issues. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0101 `utils/run_opentitan_connectivity_circt_bmc.py:120` (tooling-gap)

At `utils/run_opentitan_connectivity_circt_bmc.py:120`, the marker `f"invalid allowlist row {line_no}: unsupported mode '{mode}' "` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0102 `utils/run_opentitan_connectivity_circt_bmc.py:412` (tooling-gap)

At `utils/run_opentitan_connectivity_circt_bmc.py:412`, the marker `"unsupported connectivity rule type in manifest: "` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0103 `frontends/PyCDE/test/test_esi.py:170` (todo)

At `frontends/PyCDE/test/test_esi.py:170`, the marker `# TODO: fixme` indicates a gap signal in this path. What's missing: The marker documents unfinished work: fixme. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0104 `include/circt/Runtime/MooreRuntime.h:4208` (unclassified)

At `include/circt/Runtime/MooreRuntime.h:4208`, the marker `/// @param phase The phase being executed (for passing to super.XXX_phase())` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0105 `frontends/PyCDE/test/test_polynomial.py:120` (todo)

At `frontends/PyCDE/test/test_polynomial.py:120`, the marker `# TODO: before generating all the modules, the IR doesn't verify since the` indicates a gap signal in this path. What's missing: The marker documents unfinished work: before generating all the modules, the IR doesn't verify since the. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0106 `utils/refactor_continue.sh:6` (unclassified)

At `utils/refactor_continue.sh:6`, the marker `TODO_PATH="docs/WHOLE_PROJECT_REFACTOR_TODO.md"` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0107 `utils/refactor_continue.sh:14` (todo)

At `utils/refactor_continue.sh:14`, the marker `--status Show concise plan/todo continuation status (default)` indicates a gap signal in this path. What's missing: The marker documents unfinished work: continuation status (default). The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0108 `utils/refactor_continue.sh:18` (todo)

At `utils/refactor_continue.sh:18`, the marker `--todo PATH Override todo file path` indicates a gap signal in this path. What's missing: The marker documents unfinished work: PATH Override todo file path. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0109 `utils/refactor_continue.sh:41` (todo)

At `utils/refactor_continue.sh:41`, the marker `--todo)` indicates a gap signal in this path. What's missing: The marker documents unfinished work: ). The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0110 `utils/refactor_continue.sh:42` (unclassified)

At `utils/refactor_continue.sh:42`, the marker `TODO_PATH="$2"` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0111 `utils/refactor_continue.sh:61` (unclassified)

At `utils/refactor_continue.sh:61`, the marker `if [[ ! -f "$TODO_PATH" ]]; then` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0112 `utils/refactor_continue.sh:62` (todo)

At `utils/refactor_continue.sh:62`, the marker `echo "todo file not found: $TODO_PATH" >&2` indicates a gap signal in this path. What's missing: The marker documents unfinished work: file not found: $TODO_PATH" >&2. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0113 `utils/refactor_continue.sh:66` (unclassified)

At `utils/refactor_continue.sh:66`, the marker `CANONICAL_PROMPT="continue according to ${PLAN_PATH}, keeping track of progress using ${TODO_PATH}"` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0114 `utils/refactor_continue.sh:81` (unclassified)

At `utils/refactor_continue.sh:81`, the marker `' "$TODO_PATH"` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0115 `utils/refactor_continue.sh:92` (unclassified)

At `utils/refactor_continue.sh:92`, the marker `' "$TODO_PATH"` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0116 `utils/refactor_continue.sh:122` (todo)

At `utils/refactor_continue.sh:122`, the marker `printf 'TODO: %s\n' "$TODO_PATH"` indicates a gap signal in this path. What's missing: The marker documents unfinished work: %s\n' "$TODO_PATH". The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0117 `utils/run_yosys_sva_circt_bmc.sh:142` (unclassified)

At `utils/run_yosys_sva_circt_bmc.sh:142`, the marker `UNSUPPORTED_SVA_POLICY="${UNSUPPORTED_SVA_POLICY:-strict}"` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0118 `utils/run_yosys_sva_circt_bmc.sh:298` (unclassified)

At `utils/run_yosys_sva_circt_bmc.sh:298`, the marker `if [[ "$UNSUPPORTED_SVA_POLICY" != "strict" && "$UNSUPPORTED_SVA_POLICY" != "lenient" ]]; then` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0119 `utils/run_yosys_sva_circt_bmc.sh:299` (unclassified)

At `utils/run_yosys_sva_circt_bmc.sh:299`, the marker `echo "invalid UNSUPPORTED_SVA_POLICY: $UNSUPPORTED_SVA_POLICY (expected strict or lenient)" >&2` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0120 `utils/run_yosys_sva_circt_bmc.sh:2747` (tooling-gap)

At `utils/run_yosys_sva_circt_bmc.sh:2747`, the marker `echo "error: unsupported YOSYS_SVA_MODE_SUMMARY_HISTORY_JSONL_FILE line format in $file at line $lineno" >&2` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0121 `utils/run_yosys_sva_circt_bmc.sh:2867` (tooling-gap)

At `utils/run_yosys_sva_circt_bmc.sh:2867`, the marker `echo "error: unsupported drop-event id hash mode: $drop_events_id_hash_mode" >&2` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0122 `utils/run_yosys_sva_circt_bmc.sh:3026` (tooling-gap)

At `utils/run_yosys_sva_circt_bmc.sh:3026`, the marker `echo "error: unsupported drop-event lock backend: $selected_backend" >&2` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0123 `utils/run_yosys_sva_circt_bmc.sh:3186` (tooling-gap)

At `utils/run_yosys_sva_circt_bmc.sh:3186`, the marker `echo "error: unsupported YOSYS_SVA_MODE_SUMMARY_HISTORY_TSV_FILE header in $file" >&2` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0124 `utils/run_yosys_sva_circt_bmc.sh:8522` (tooling-gap)

At `utils/run_yosys_sva_circt_bmc.sh:8522`, the marker `# In smoke mode we treat sim-only tests as unsupported by BMC and skip them` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0125 `utils/run_yosys_sva_circt_bmc.sh:8616` (unclassified)

At `utils/run_yosys_sva_circt_bmc.sh:8616`, the marker `if [[ "$UNSUPPORTED_SVA_POLICY" == "lenient" ]]; then` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0126 `utils/run_yosys_sva_circt_bmc.sh:8617` (tooling-gap)

At `utils/run_yosys_sva_circt_bmc.sh:8617`, the marker `verilog_args+=("--sva-continue-on-unsupported")` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0127 `utils/run_yosys_sva_circt_bmc.sh:8676` (unclassified)

At `utils/run_yosys_sva_circt_bmc.sh:8676`, the marker `if [[ "$UNSUPPORTED_SVA_POLICY" == "lenient" ]]; then` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0128 `utils/run_yosys_sva_circt_bmc.sh:8677` (tooling-gap)

At `utils/run_yosys_sva_circt_bmc.sh:8677`, the marker `verilog_args+=("--sva-continue-on-unsupported")` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0129 `utils/run_yosys_sva_circt_bmc.sh:8761` (unclassified)

At `utils/run_yosys_sva_circt_bmc.sh:8761`, the marker `if [[ "$UNSUPPORTED_SVA_POLICY" == "lenient" ]]; then` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0130 `utils/run_yosys_sva_circt_bmc.sh:8762` (tooling-gap)

At `utils/run_yosys_sva_circt_bmc.sh:8762`, the marker `bmc_args+=("--drop-unsupported-sva")` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0131 `lib/Runtime/MooreRuntime.cpp:2481` (todo)

At `lib/Runtime/MooreRuntime.cpp:2481`, the marker `// TODO: Implement proper simulation-aware waiting when a simulation` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Implement proper simulation-aware waiting when a simulation. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0132 `lib/Runtime/MooreRuntime.cpp:12227` (todo)

At `lib/Runtime/MooreRuntime.cpp:12227`, the marker `// TODO: Handle array element access via index calculation` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Handle array element access via index calculation. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0133 `lib/Runtime/MooreRuntime.cpp:14402` (todo)

At `lib/Runtime/MooreRuntime.cpp:14402`, the marker `/// 1. Creates the test component using the UVM factory (TODO)` indicates a gap signal in this path. What's missing: The marker documents unfinished work: ). The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0134 `lib/Runtime/MooreRuntime.cpp:17781` (todo)

At `lib/Runtime/MooreRuntime.cpp:17781`, the marker `// TODO: Integrate with HDL access functions if HDL path is set` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Integrate with HDL access functions if HDL path is set. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0135 `lib/Runtime/MooreRuntime.cpp:17823` (todo)

At `lib/Runtime/MooreRuntime.cpp:17823`, the marker `// TODO: Integrate with HDL access functions if HDL path is set` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Integrate with HDL access functions if HDL path is set. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0136 `frontends/PyCDE/test/test_instances.py:124` (todo)

At `frontends/PyCDE/test/test_instances.py:124`, the marker `# TODO: Add back physical region support` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Add back physical region support. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0137 `frontends/PyCDE/test/test_instances.py:156` (todo)

At `frontends/PyCDE/test/test_instances.py:156`, the marker `# TODO: add back anonymous reservations` indicates a gap signal in this path. What's missing: The marker documents unfinished work: add back anonymous reservations. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0138 `utils/check_opentitan_fpv_bmc_evidence_parity.py:111` (tooling-gap)

At `utils/check_opentitan_fpv_bmc_evidence_parity.py:111`, the marker `f"invalid allowlist row {line_no}: unsupported mode '{mode}' "` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0139 `utils/check_opentitan_target_manifest_drift.py:87` (tooling-gap)

At `utils/check_opentitan_target_manifest_drift.py:87`, the marker `f"invalid allowlist row {line_no}: unsupported mode '{mode}' "` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0140 `utils/run_avip_circt_verilog.sh:157` (unclassified)

At `utils/run_avip_circt_verilog.sh:157`, the marker `synth_filelist="$(mktemp /tmp/avip-synth-filelist.XXXXXX.f)"` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0141 `utils/run_avip_circt_verilog.sh:184` (unclassified)

At `utils/run_avip_circt_verilog.sh:184`, the marker `synth_filelist="$(mktemp /tmp/avip-synth-filelist.XXXXXX.f)"` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0142 `include/circt/Transforms/Passes.td:86` (implementation-gap)

At `include/circt/Transforms/Passes.td:86`, the marker ``--map-arith-to-comb`) do not encounter unsupported index-typed arithmetic.` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0143 `unittests/Support/TestReportingTest.cpp:54` (unclassified)

At `unittests/Support/TestReportingTest.cpp:54`, the marker `tc.skip("Not implemented");` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0144 `unittests/Support/TestReportingTest.cpp:58` (unclassified)

At `unittests/Support/TestReportingTest.cpp:58`, the marker `EXPECT_EQ(tc.message, "Not implemented");` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0145 `unittests/Support/TestReportingTest.cpp:195` (unclassified)

At `unittests/Support/TestReportingTest.cpp:195`, the marker `tc.skip("Not implemented");` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0146 `unittests/Support/TestReportingTest.cpp:203` (unclassified)

At `unittests/Support/TestReportingTest.cpp:203`, the marker `EXPECT_NE(output.find("Not implemented"), std::string::npos);` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0147 `utils/check_opentitan_connectivity_contract_fingerprint_parity.py:90` (tooling-gap)

At `utils/check_opentitan_connectivity_contract_fingerprint_parity.py:90`, the marker `f"invalid allowlist row {line_no}: unsupported mode '{mode}' "` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0148 `utils/mutation_mcy/lib/native_mutation_plan.py:57` (tooling-gap)

At `utils/mutation_mcy/lib/native_mutation_plan.py:57`, the marker `raise ValueError("unsupported native ops: " + ", ".join(sorted(set(unknown))))` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0149 `utils/mutation_mcy/lib/drift.sh:646` (unclassified)

At `utils/mutation_mcy/lib/drift.sh:646`, the marker `history_detected_file="$(mktemp "${OUT_DIR}/suite-history-detected.XXXXXX")"` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0150 `utils/mutation_mcy/lib/drift.sh:647` (unclassified)

At `utils/mutation_mcy/lib/drift.sh:647`, the marker `history_relevant_file="$(mktemp "${OUT_DIR}/suite-history-relevant.XXXXXX")"` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0151 `utils/mutation_mcy/lib/drift.sh:648` (unclassified)

At `utils/mutation_mcy/lib/drift.sh:648`, the marker `history_coverage_file="$(mktemp "${OUT_DIR}/suite-history-coverage.XXXXXX")"` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0152 `utils/mutation_mcy/lib/drift.sh:649` (unclassified)

At `utils/mutation_mcy/lib/drift.sh:649`, the marker `history_errors_file="$(mktemp "${OUT_DIR}/suite-history-errors.XXXXXX")"` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0153 `utils/run_regression_unified.sh:120` (tooling-gap)

At `utils/run_regression_unified.sh:120`, the marker `manifest_error "$row_no" "unsupported profile token '$token' (expected smoke|nightly|full|all)"` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0154 `utils/check_opentitan_fpv_objective_parity.py:170` (tooling-gap)

At `utils/check_opentitan_fpv_objective_parity.py:170`, the marker `f"invalid allowlist row {line_no}: unsupported mode '{mode}' "` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0155 `lib/Runtime/uvm-core/src/tlm2/uvm_tlm_time.svh:105` (todo)

At `lib/Runtime/uvm-core/src/tlm2/uvm_tlm_time.svh:105`, the marker `// ToDo: Check resolution` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Check resolution. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0156 `utils/run_opentitan_connectivity_circt_lec.py:130` (tooling-gap)

At `utils/run_opentitan_connectivity_circt_lec.py:130`, the marker `f"invalid allowlist row {line_no}: unsupported mode '{mode}' "` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0157 `utils/run_opentitan_connectivity_circt_lec.py:366` (tooling-gap)

At `utils/run_opentitan_connectivity_circt_lec.py:366`, the marker `"unsupported connectivity rule type in manifest: "` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0158 `utils/internal/checks/wasm_cxx20_contract_check.sh:45` (tooling-gap)

At `utils/internal/checks/wasm_cxx20_contract_check.sh:45`, the marker `echo "[wasm-cxx20-contract] configure script accepted unsupported C++ standard override (17)" >&2` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0159 `utils/run_opentitan_fpv_bmc_policy_workflow.sh:323` (tooling-gap)

At `utils/run_opentitan_fpv_bmc_policy_workflow.sh:323`, the marker `echo "unsupported mode: $MODE" >&2` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0160 `utils/select_opentitan_connectivity_cfg.py:276` (tooling-gap)

At `utils/select_opentitan_connectivity_cfg.py:276`, the marker `f"unsupported connectivity CSV row kind '{first}' in "` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0161 `utils/run_avip_xcelium_reference.sh:94` (tooling-gap)

At `utils/run_avip_xcelium_reference.sh:94`, the marker `echo "error: unsupported AVIP_SET '$AVIP_SET' (use core8 or all9)" >&2` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0162 `utils/check_opentitan_fpv_objective_parity_drift.py:83` (tooling-gap)

At `utils/check_opentitan_fpv_objective_parity_drift.py:83`, the marker `f"invalid allowlist row {line_no}: unsupported mode '{mode}' "` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0163 `utils/generate_mutations_yosys.sh:696` (tooling-gap)

At `utils/generate_mutations_yosys.sh:696`, the marker `echo "Unsupported design extension for $DESIGN (expected .il/.v/.sv)." >&2` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0164 `lib/Runtime/uvm-core/src/tlm2/uvm_tlm2_ifs.svh:76` (implementation-gap)

At `lib/Runtime/uvm-core/src/tlm2/uvm_tlm2_ifs.svh:76`, the marker ``define UVM_TLM_TASK_ERROR "TLM-2 interface task not implemented"` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0165 `lib/Runtime/uvm-core/src/tlm2/uvm_tlm2_ifs.svh:81` (implementation-gap)

At `lib/Runtime/uvm-core/src/tlm2/uvm_tlm2_ifs.svh:81`, the marker ``define UVM_TLM_FUNCTION_ERROR "UVM TLM 2 interface function not implemented"` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0166 `test/firtool/firtool.fir:77` (todo)

At `test/firtool/firtool.fir:77`, the marker `; TODO: This test should be rewritten to not be so brittle around module` indicates a gap signal in this path. What's missing: The marker documents unfinished work: This test should be rewritten to not be so brittle around module. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0167 `unittests/Support/PrettyPrinterTest.cpp:75` (unclassified)

At `unittests/Support/PrettyPrinterTest.cpp:75`, the marker `"float xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"};` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0168 `unittests/Support/PrettyPrinterTest.cpp:204` (unclassified)

At `unittests/Support/PrettyPrinterTest.cpp:204`, the marker `float xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0169 `unittests/Support/PrettyPrinterTest.cpp:237` (unclassified)

At `unittests/Support/PrettyPrinterTest.cpp:237`, the marker `float xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0170 `unittests/Support/PrettyPrinterTest.cpp:247` (unclassified)

At `unittests/Support/PrettyPrinterTest.cpp:247`, the marker `float xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0171 `unittests/Support/PrettyPrinterTest.cpp:279` (unclassified)

At `unittests/Support/PrettyPrinterTest.cpp:279`, the marker `float xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0172 `unittests/Support/PrettyPrinterTest.cpp:285` (unclassified)

At `unittests/Support/PrettyPrinterTest.cpp:285`, the marker `float xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0173 `unittests/Support/PrettyPrinterTest.cpp:304` (unclassified)

At `unittests/Support/PrettyPrinterTest.cpp:304`, the marker `float xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0174 `unittests/Support/PrettyPrinterTest.cpp:317` (unclassified)

At `unittests/Support/PrettyPrinterTest.cpp:317`, the marker `float xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0175 `unittests/Support/PrettyPrinterTest.cpp:321` (unclassified)

At `unittests/Support/PrettyPrinterTest.cpp:321`, the marker `float xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0176 `unittests/Support/PrettyPrinterTest.cpp:335` (unclassified)

At `unittests/Support/PrettyPrinterTest.cpp:335`, the marker `float xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0177 `unittests/Support/PrettyPrinterTest.cpp:339` (unclassified)

At `unittests/Support/PrettyPrinterTest.cpp:339`, the marker `float xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0178 `unittests/Support/PrettyPrinterTest.cpp:350` (unclassified)

At `unittests/Support/PrettyPrinterTest.cpp:350`, the marker `foooooo(int a, int b, int a1, int b1, int a2, int b2, int a3, int b3, int a4, int b4, int a5, int b5, int a6, int b6, int a7, int b7, float xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx);` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0179 `unittests/Support/PrettyPrinterTest.cpp:356` (unclassified)

At `unittests/Support/PrettyPrinterTest.cpp:356`, the marker `baroo(int a, int b, int a1, int b1, int a2, int b2, int a3, int b3, barooga(int a, int b, int a1, int b1, int a2, int b2, int a3, int b3, int a4, int b4, int a5, int b5, int a6, int b6, int a7, int b7, float xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx), int a4, int b4, int a5, int b5, int a6, int b6, int a7, int b7, float xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx);` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0180 `unittests/Support/PrettyPrinterTest.cpp:362` (unclassified)

At `unittests/Support/PrettyPrinterTest.cpp:362`, the marker `wahoo(int a, int b, int a1, int b1, int a2, int b2, int a3, int b3, yahooooooo(int a, int b, int a1, int b1, int a2, int b2, int a3, int b3, int a4, int b4, int a5, int b5, int a6, int b6, int a7, int b7, float xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx), int a4, int b4, int a5, int b5, int a6, int b6, int a7, int b7, float xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx);` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0181 `unittests/Support/PrettyPrinterTest.cpp:671` (unclassified)

At `unittests/Support/PrettyPrinterTest.cpp:671`, the marker `StringToken("xxxxxxxxxxxxxxx"), BreakToken(),` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0182 `unittests/Support/PrettyPrinterTest.cpp:690` (unclassified)

At `unittests/Support/PrettyPrinterTest.cpp:690`, the marker `xxxxxxxxxxxxxxx yyyyyyyyyyyyyyy` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0183 `unittests/Support/PrettyPrinterTest.cpp:697` (unclassified)

At `unittests/Support/PrettyPrinterTest.cpp:697`, the marker `>>xxxxxxxxxxxxxxx yyyyyyyyyyyyyyy` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0184 `unittests/Support/PrettyPrinterTest.cpp:702` (unclassified)

At `unittests/Support/PrettyPrinterTest.cpp:702`, the marker `xxxxxxxxxxxxxxx yyyyyyyyyyyyyyy` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0185 `unittests/Support/PrettyPrinterTest.cpp:709` (unclassified)

At `unittests/Support/PrettyPrinterTest.cpp:709`, the marker `>>>>>>xxxxxxxxxxxxxxx` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0186 `unittests/Support/PrettyPrinterTest.cpp:716` (unclassified)

At `unittests/Support/PrettyPrinterTest.cpp:716`, the marker `>>>>>>xxxxxxxxxxxxxxx` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0187 `unittests/Support/PrettyPrinterTest.cpp:723` (unclassified)

At `unittests/Support/PrettyPrinterTest.cpp:723`, the marker `>>> xxxxxxxxxxxxxxx` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0188 `unittests/Support/PrettyPrinterTest.cpp:730` (unclassified)

At `unittests/Support/PrettyPrinterTest.cpp:730`, the marker `>>> xxxxxxxxxxxxxxx` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0189 `frontends/PyCDE/src/pycde/types.py:576` (todo)

At `frontends/PyCDE/src/pycde/types.py:576`, the marker `# TODO: Adapt this to UInt and SInt.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Adapt this to UInt and SInt.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0190 `unittests/Support/FVIntTest.cpp:26` (unclassified)

At `unittests/Support/FVIntTest.cpp:26`, the marker `ASSERT_EQ(FVInt::fromString("X1", 2).sext(5), FVInt::fromString("XXXX1", 2));` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0191 `unittests/Support/FVIntTest.cpp:104` (unclassified)

At `unittests/Support/FVIntTest.cpp:104`, the marker `auto b = FVInt::fromString("00001111XXXXZZZZ", 2);` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0192 `unittests/Support/FVIntTest.cpp:108` (unclassified)

At `unittests/Support/FVIntTest.cpp:108`, the marker `ASSERT_EQ(a & b, FVInt::fromString("000001XX0XXX0XXX", 2));` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0193 `unittests/Support/FVIntTest.cpp:109` (unclassified)

At `unittests/Support/FVIntTest.cpp:109`, the marker `ASSERT_EQ(a | b, FVInt::fromString("01XX1111X1XXX1XX", 2));` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0194 `unittests/Support/FVIntTest.cpp:110` (unclassified)

At `unittests/Support/FVIntTest.cpp:110`, the marker `ASSERT_EQ(a ^ b, FVInt::fromString("01XX10XXXXXXXXXX", 2));` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0195 `utils/check_opentitan_connectivity_objective_parity.py:151` (tooling-gap)

At `utils/check_opentitan_connectivity_objective_parity.py:151`, the marker `f"invalid allowlist row {line_no}: unsupported mode '{mode}' "` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0196 `test/firtool/spec/refs/read_subelement_add.fir:9` (todo)

At `test/firtool/spec/refs/read_subelement_add.fir:9`, the marker `wire x : {a: UInt<5>, b: UInt<2>} ; XXX: ADDED` indicates a gap signal in this path. What's missing: The marker documents unfinished work: ADDED. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0197 `test/firtool/spec/refs/read_subelement_add.fir:10` (todo)

At `test/firtool/spec/refs/read_subelement_add.fir:10`, the marker `invalidate x ; XXX: ADDED` indicates a gap signal in this path. What's missing: The marker documents unfinished work: ADDED. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0198 `test/firtool/spec/refs/read_subelement_add.fir:11` (todo)

At `test/firtool/spec/refs/read_subelement_add.fir:11`, the marker `define p = probe(x) ; XXX: ADDED` indicates a gap signal in this path. What's missing: The marker documents unfinished work: ADDED. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0199 `test/firtool/dedup-modules-with-output-dirs.fir:7` (todo)

At `test/firtool/dedup-modules-with-output-dirs.fir:7`, the marker `"dirname": "XXX",` indicates a gap signal in this path. What's missing: The marker documents unfinished work: ",. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0200 `test/firtool/dedup-modules-with-output-dirs.fir:18` (test-expectation)

At `test/firtool/dedup-modules-with-output-dirs.fir:18`, the marker `; CHECK: FILE "XXX{{/|\\}}A.sv"` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0201 `test/firtool/dedup-modules-with-output-dirs.fir:72` (todo)

At `test/firtool/dedup-modules-with-output-dirs.fir:72`, the marker `"dirname": "ZZZ/XXX",` indicates a gap signal in this path. What's missing: The marker documents unfinished work: ",. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0202 `test/firtool/dedup-modules-with-output-dirs.fir:95` (test-expectation)

At `test/firtool/dedup-modules-with-output-dirs.fir:95`, the marker `; CHECK: FILE "ZZZ{{/|\\}}XXX{{/|\\}}A.sv"` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0203 `tools/circt-mut/circt-mut.cpp:2368` (tooling-gap)

At `tools/circt-mut/circt-mut.cpp:2368`, the marker `error = (Twine("circt-mut cover: unsupported --mutant-format value for "` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0204 `tools/circt-mut/circt-mut.cpp:6369` (tooling-gap)

At `tools/circt-mut/circt-mut.cpp:6369`, the marker `error = (Twine("circt-mut run: unsupported TOML escape in ") + path +` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0205 `tools/circt-mut/circt-mut.cpp:14642` (tooling-gap)

At `tools/circt-mut/circt-mut.cpp:14642`, the marker `// Keep full compatibility by deferring unsupported/unknown options to the` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0206 `tools/circt-mut/circt-mut.cpp:15299` (tooling-gap)

At `tools/circt-mut/circt-mut.cpp:15299`, the marker `errs() << "circt-mut generate: unsupported design extension for "` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0207 `test/firtool/spec/refs/read_subelement.fir:10` (todo)

At `test/firtool/spec/refs/read_subelement.fir:10`, the marker `wire x : {a: UInt<5>, b: UInt<2>} ; XXX: ADDED` indicates a gap signal in this path. What's missing: The marker documents unfinished work: ADDED. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0208 `test/firtool/spec/refs/read_subelement.fir:11` (todo)

At `test/firtool/spec/refs/read_subelement.fir:11`, the marker `invalidate x ; XXX: ADDED` indicates a gap signal in this path. What's missing: The marker documents unfinished work: ADDED. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0209 `test/firtool/spec/refs/read_subelement.fir:12` (todo)

At `test/firtool/spec/refs/read_subelement.fir:12`, the marker `define p = probe(x) ; XXX: ADDED` indicates a gap signal in this path. What's missing: The marker documents unfinished work: ADDED. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0210 `lib/Runtime/uvm-core/src/tlm1/uvm_tlm_ifs.svh:34` (implementation-gap)

At `lib/Runtime/uvm-core/src/tlm1/uvm_tlm_ifs.svh:34`, the marker ``define UVM_TASK_ERROR "UVM TLM interface task not implemented"` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0211 `lib/Runtime/uvm-core/src/tlm1/uvm_tlm_ifs.svh:35` (implementation-gap)

At `lib/Runtime/uvm-core/src/tlm1/uvm_tlm_ifs.svh:35`, the marker ``define UVM_FUNCTION_ERROR "UVM TLM interface function not implemented"` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0212 `frontends/PyCDE/src/pycde/system.py:122` (todo)

At `frontends/PyCDE/src/pycde/system.py:122`, the marker `# TODO: Ideally, we'd be able to run the cf-to-handshake lowering passes in` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Ideally, we'd be able to run the cf-to-handshake lowering passes in. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0213 `frontends/PyCDE/src/pycde/system.py:249` (todo)

At `frontends/PyCDE/src/pycde/system.py:249`, the marker `# TODO: handle symbolrefs pointing to potentially renamed symbols.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: handle symbolrefs pointing to potentially renamed symbols.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0214 `frontends/PyCDE/src/pycde/system.py:574` (todo)

At `frontends/PyCDE/src/pycde/system.py:574`, the marker `# TODO: This was broken by MLIR. When we have a replacement, use that instead.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: This was broken by MLIR. When we have a replacement, use that instead.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0215 `test/circt-verilog/roundtrip-register-enable.sv:4` (test-expectation)

At `test/circt-verilog/roundtrip-register-enable.sv:4`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0216 `test/firtool/spec/refs/read.fir:10` (todo)

At `test/firtool/spec/refs/read.fir:10`, the marker `wire x : UInt<4> ; XXX: ADDED` indicates a gap signal in this path. What's missing: The marker documents unfinished work: ADDED. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0217 `test/firtool/spec/refs/read.fir:11` (todo)

At `test/firtool/spec/refs/read.fir:11`, the marker `invalidate x ; XXX: ADDED` indicates a gap signal in this path. What's missing: The marker documents unfinished work: ADDED. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0218 `test/firtool/spec/refs/read.fir:12` (todo)

At `test/firtool/spec/refs/read.fir:12`, the marker `define p = probe(x) ; XXX: ADDED` indicates a gap signal in this path. What's missing: The marker documents unfinished work: ADDED. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0219 `utils/run_opentitan_fpv_circt_bmc.py:231` (tooling-gap)

At `utils/run_opentitan_fpv_circt_bmc.py:231`, the marker `"unsupported stopat selector "` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0220 `utils/run_opentitan_fpv_circt_bmc.py:373` (tooling-gap)

At `utils/run_opentitan_fpv_circt_bmc.py:373`, the marker `f"invalid allowlist row {line_no}: unsupported mode '{mode}' "` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0221 `utils/run_opentitan_fpv_circt_bmc.py:1607` (unclassified)

At `utils/run_opentitan_fpv_circt_bmc.py:1607`, the marker `add_contract_error(row, "unsupported_stopat_selector")` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0222 `test/circt-verilog/registers.sv:4` (test-expectation)

At `test/circt-verilog/registers.sv:4`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0223 `test/firtool/spec/refs/probe_export_simple.fir:7` (todo)

At `test/firtool/spec/refs/probe_export_simple.fir:7`, the marker `input in: UInt<5> ; XXX: Added width.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Added width.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0224 `test/firtool/spec/refs/nosubaccess.fir:8` (todo)

At `test/firtool/spec/refs/nosubaccess.fir:8`, the marker `; XXX: Modified to not use input probes, get probe from ext, widths.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Modified to not use input probes, get probe from ext, widths.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0225 `utils/check_opentitan_compile_contract_drift.py:95` (tooling-gap)

At `utils/check_opentitan_compile_contract_drift.py:95`, the marker `f"invalid allowlist row {line_no}: unsupported mode '{mode}' "` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0226 `lib/Runtime/uvm-core/src/tlm1/uvm_tlm_fifo_base.svh:35` (implementation-gap)

At `lib/Runtime/uvm-core/src/tlm1/uvm_tlm_fifo_base.svh:35`, the marker ``define UVM_TLM_FIFO_TASK_ERROR "fifo channel task not implemented"` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0227 `lib/Runtime/uvm-core/src/tlm1/uvm_tlm_fifo_base.svh:36` (implementation-gap)

At `lib/Runtime/uvm-core/src/tlm1/uvm_tlm_fifo_base.svh:36`, the marker ``define UVM_TLM_FIFO_FUNCTION_ERROR "fifo channel function not implemented"` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0228 `test/circt-verilog/memories.sv:4` (test-expectation)

At `test/circt-verilog/memories.sv:4`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0229 `tools/circt-sim/AOTProcessCompiler.h:211` (tooling-gap)

At `tools/circt-sim/AOTProcessCompiler.h:211`, the marker `/// with unsupported ops are skipped.` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0230 `test/circt-verilog/redundant-files.sv:4` (test-expectation)

At `test/circt-verilog/redundant-files.sv:4`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0231 `lib/Runtime/uvm-core/src/tlm1/uvm_sqr_ifs.svh:38` (implementation-gap)

At `lib/Runtime/uvm-core/src/tlm1/uvm_sqr_ifs.svh:38`, the marker ``define UVM_SEQ_ITEM_TASK_ERROR "Sequencer interface task not implemented"` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0232 `lib/Runtime/uvm-core/src/tlm1/uvm_sqr_ifs.svh:39` (implementation-gap)

At `lib/Runtime/uvm-core/src/tlm1/uvm_sqr_ifs.svh:39`, the marker ``define UVM_SEQ_ITEM_FUNCTION_ERROR "Sequencer interface function not implemented"` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0233 `test/firtool/spec/refs/forwarding_refs_upwards.fir:6` (todo)

At `test/firtool/spec/refs/forwarding_refs_upwards.fir:6`, the marker `extmodule Foo : ; XXX: module -> extmodule` indicates a gap signal in this path. What's missing: The marker documents unfinished work: module -> extmodule. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0234 `test/firtool/spec/refs/forwarding_refs_upwards.fir:7` (todo)

At `test/firtool/spec/refs/forwarding_refs_upwards.fir:7`, the marker `output p : Probe<UInt<3>> ; XXX: added width` indicates a gap signal in this path. What's missing: The marker documents unfinished work: added width. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0235 `test/circt-verilog/library-locations.sv:4` (test-expectation)

At `test/circt-verilog/library-locations.sv:4`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0236 `frontends/PyCDE/src/pycde/ndarray.py:221` (todo)

At `frontends/PyCDE/src/pycde/ndarray.py:221`, the marker `# Todo: We should allow for 1 extra dimension in the access slice which is` indicates a gap signal in this path. What's missing: The marker documents unfinished work: We should allow for 1 extra dimension in the access slice which is. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0237 `test/firtool/spec/refs/force_initial.fir:27` (todo)

At `test/firtool/spec/refs/force_initial.fir:27`, the marker `; XXX: modified, workaround inability create non-const node w/literal initializer.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: modified, workaround inability create non-const node w/literal initializer.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0238 `include/circt/Support/TruthTable.h:37` (todo)

At `include/circt/Support/TruthTable.h:37`, the marker `/// TODO: Rename this to MultiOutputTruthTable since for single-output functions` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Rename this to MultiOutputTruthTable since for single-output functions. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0239 `include/circt/Support/TruthTable.h:126` (todo)

At `include/circt/Support/TruthTable.h:126`, the marker `/// FIXME: Currently we are using exact canonicalization which doesn't scale` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Currently we are using exact canonicalization which doesn't scale. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0240 `test/firtool/spec/refs/force_and_release.fir:26` (todo)

At `test/firtool/spec/refs/force_and_release.fir:26`, the marker `; XXX: modified, workaround inability create non-const node w/literal initializer.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: modified, workaround inability create non-const node w/literal initializer.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0241 `test/circt-verilog/command-files.sv:4` (test-expectation)

At `test/circt-verilog/command-files.sv:4`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0242 `test/firtool/spec/refs/define.fir:8` (todo)

At `test/firtool/spec/refs/define.fir:8`, the marker `input p : {x: UInt<1>, flip y : UInt<3>} ; XXX: modified, for init` indicates a gap signal in this path. What's missing: The marker documents unfinished work: modified, for init. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0243 `test/firtool/spec/refs/define.fir:11` (todo)

At `test/firtool/spec/refs/define.fir:11`, the marker `output c : Probe<UInt> ; read-only ref. to register 'r', inferred width. ; XXX: modified, needs width` indicates a gap signal in this path. What's missing: The marker documents unfinished work: modified, needs width. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0244 `test/firtool/spec/refs/define.fir:14` (todo)

At `test/firtool/spec/refs/define.fir:14`, the marker `connect p.y, UInt<3>(0) ; XXX: modified, for init` indicates a gap signal in this path. What's missing: The marker documents unfinished work: modified, for init. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0245 `test/firtool/spec/refs/define.fir:16` (todo)

At `test/firtool/spec/refs/define.fir:16`, the marker `node q = p.x ; XXX: modified, workaround inability create non-const node w/literal initializer.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: modified, workaround inability create non-const node w/literal initializer.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0246 `test/firtool/spec/refs/define.fir:19` (todo)

At `test/firtool/spec/refs/define.fir:19`, the marker `connect r, p.x ; XXX: modified, initialize register` indicates a gap signal in this path. What's missing: The marker documents unfinished work: modified, initialize register. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0247 `frontends/PyCDE/src/pycde/instance.py:168` (todo)

At `frontends/PyCDE/src/pycde/instance.py:168`, the marker `# TODO: make these weak refs` indicates a gap signal in this path. What's missing: The marker documents unfinished work: make these weak refs. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0248 `test/firtool/spec/refs/define-subelement.fir:7` (todo)

At `test/firtool/spec/refs/define-subelement.fir:7`, the marker `input x : UInt<3> ; XXX: width added` indicates a gap signal in this path. What's missing: The marker documents unfinished work: width added. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0249 `test/firtool/spec/refs/define-flip-to-passive.fir:7` (todo)

At `test/firtool/spec/refs/define-flip-to-passive.fir:7`, the marker `input x : {a: UInt<3>, flip b: UInt} ; XXX: width on x.a` indicates a gap signal in this path. What's missing: The marker documents unfinished work: width on x.a. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0250 `test/firtool/spec/refs/define-flip-to-passive.fir:8` (todo)

At `test/firtool/spec/refs/define-flip-to-passive.fir:8`, the marker `output y : {a: UInt, flip b: UInt<3>} ; XXX: width on y.b` indicates a gap signal in this path. What's missing: The marker documents unfinished work: width on y.b. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0251 `include/circt/Support/LTLSequenceNFA.h:289` (implementation-gap)

At `include/circt/Support/LTLSequenceNFA.h:289`, the marker `emitError(loc, "unsupported sequence lowering for block argument");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0252 `include/circt/Support/LTLSequenceNFA.h:395` (implementation-gap)

At `include/circt/Support/LTLSequenceNFA.h:395`, the marker `defOp->emitError("unsupported sequence lowering");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0253 `test/circt-reduce/name-sanitizer.mlir:1` (test-expectation)

At `test/circt-reduce/name-sanitizer.mlir:1`, the marker `// UNSUPPORTED: system-windows` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0254 `frontends/PyCDE/src/pycde/esi.py:95` (todo)

At `frontends/PyCDE/src/pycde/esi.py:95`, the marker `# TODO: figure out a way to verify the ports during this call.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: figure out a way to verify the ports during this call.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0255 `frontends/PyCDE/src/pycde/esi.py:1141` (todo)

At `frontends/PyCDE/src/pycde/esi.py:1141`, the marker `# TODO: implement some fairness.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: implement some fairness.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0256 `unittests/Dialect/Sim/DPIRuntimeTest.cpp:84` (unclassified)

At `unittests/Dialect/Sim/DPIRuntimeTest.cpp:84`, the marker `EXPECT_DOUBLE_EQ(intVal.toDouble(), 42.0);` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0257 `unittests/Dialect/Sim/DPIRuntimeTest.cpp:87` (unclassified)

At `unittests/Dialect/Sim/DPIRuntimeTest.cpp:87`, the marker `EXPECT_DOUBLE_EQ(realVal.toDouble(), 3.14);` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0258 `lib/Runtime/uvm-core/src/seq/uvm_sequencer_base.svh:1149` (todo)

At `lib/Runtime/uvm-core/src/seq/uvm_sequencer_base.svh:1149`, the marker `// TODO:` indicates a gap signal in this path. What's missing: The marker documents deferred or unfinished work on this path, which means behavior here is not fully implemented yet. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0259 `test/circt-reduce/operation-pruner.mlir:1` (test-expectation)

At `test/circt-reduce/operation-pruner.mlir:1`, the marker `// UNSUPPORTED: system-windows` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0260 `test/circt-reduce/must-dedup-children.mlir:1` (test-expectation)

At `test/circt-reduce/must-dedup-children.mlir:1`, the marker `// UNSUPPORTED: system-windows` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0261 `test/circt-reduce/make-symbols-private.mlir:1` (test-expectation)

At `test/circt-reduce/make-symbols-private.mlir:1`, the marker `// UNSUPPORTED: system-windows` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0262 `test/circt-reduce/cse-reduction.mlir:1` (test-expectation)

At `test/circt-reduce/cse-reduction.mlir:1`, the marker `// UNSUPPORTED: system-windows` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0263 `test/circt-reduce/canonicalize-reduction.mlir:1` (test-expectation)

At `test/circt-reduce/canonicalize-reduction.mlir:1`, the marker `// UNSUPPORTED: system-windows` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0264 `frontends/PyCDE/src/pycde/devicedb.py:31` (todo)

At `frontends/PyCDE/src/pycde/devicedb.py:31`, the marker `# TODO: Once we get into non-zero num primitives, this needs to be updated.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Once we get into non-zero num primitives, this needs to be updated.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0265 `frontends/PyCDE/src/pycde/constructs.py:42` (todo)

At `frontends/PyCDE/src/pycde/constructs.py:42`, the marker `# TODO: We assume here that names are unique within a module, which isn't` indicates a gap signal in this path. What's missing: The marker documents unfinished work: We assume here that names are unique within a module, which isn't. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0266 `frontends/PyCDE/src/pycde/bsp/common.py:330` (todo)

At `frontends/PyCDE/src/pycde/bsp/common.py:330`, the marker `a response, the MMIO service will hang. TODO: add some kind of timeout.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: add some kind of timeout.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0267 `frontends/PyCDE/src/pycde/bsp/common.py:356` (todo)

At `frontends/PyCDE/src/pycde/bsp/common.py:356`, the marker `# TODO: make the amount of register space each client gets a parameter.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: make the amount of register space each client gets a parameter.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0268 `frontends/PyCDE/src/pycde/bsp/common.py:359` (todo)

At `frontends/PyCDE/src/pycde/bsp/common.py:359`, the marker `# TODO: only supports one outstanding transaction at a time. This is NOT` indicates a gap signal in this path. What's missing: The marker documents unfinished work: only supports one outstanding transaction at a time. This is NOT. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0269 `frontends/PyCDE/src/pycde/bsp/common.py:530` (todo)

At `frontends/PyCDE/src/pycde/bsp/common.py:530`, the marker `# transaction in flight at once. TODO: enforce this or make it more robust.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: enforce this or make it more robust.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0270 `frontends/PyCDE/src/pycde/bsp/common.py:722` (todo)

At `frontends/PyCDE/src/pycde/bsp/common.py:722`, the marker `# TODO: Implement tag-rewriting.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Implement tag-rewriting.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0271 `frontends/PyCDE/src/pycde/bsp/common.py:743` (todo)

At `frontends/PyCDE/src/pycde/bsp/common.py:743`, the marker `# TODO: Should responses come back out-of-order (interleaved tags),` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Should responses come back out-of-order (interleaved tags),. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0272 `frontends/PyCDE/src/pycde/bsp/common.py:747` (todo)

At `frontends/PyCDE/src/pycde/bsp/common.py:747`, the marker `# identifier. TODO: Implement the gating logic here.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Implement the gating logic here.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0273 `frontends/PyCDE/src/pycde/bsp/common.py:765` (todo)

At `frontends/PyCDE/src/pycde/bsp/common.py:765`, the marker `# TODO: Change this once we support tag-rewriting.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Change this once we support tag-rewriting.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0274 `frontends/PyCDE/src/pycde/bsp/common.py:775` (todo)

At `frontends/PyCDE/src/pycde/bsp/common.py:775`, the marker `# TODO: Don't release a request until the client is ready to accept` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Don't release a request until the client is ready to accept. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0275 `frontends/PyCDE/src/pycde/bsp/common.py:1015` (todo)

At `frontends/PyCDE/src/pycde/bsp/common.py:1015`, the marker `# TODO: re-write the tags and store the client and client tag.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: re-write the tags and store the client and client tag.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0276 `tools/circt-sim/LLHDProcessInterpreterNativeThunkPolicy.cpp:707` (unclassified)

At `tools/circt-sim/LLHDProcessInterpreterNativeThunkPolicy.cpp:707`, the marker `static std::string formatUnsupportedProcessOpDetail(Operation &op) {` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0277 `tools/circt-sim/LLHDProcessInterpreterNativeThunkPolicy.cpp:750` (unclassified)

At `tools/circt-sim/LLHDProcessInterpreterNativeThunkPolicy.cpp:750`, the marker `auto traceUnsupported = []() {` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0278 `tools/circt-sim/LLHDProcessInterpreterNativeThunkPolicy.cpp:752` (unclassified)

At `tools/circt-sim/LLHDProcessInterpreterNativeThunkPolicy.cpp:752`, the marker `std::getenv("CIRCT_SIM_TRACE_JIT_UNSUPPORTED_SHAPES");` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0279 `tools/circt-sim/LLHDProcessInterpreterNativeThunkPolicy.cpp:756` (unclassified)

At `tools/circt-sim/LLHDProcessInterpreterNativeThunkPolicy.cpp:756`, the marker `auto dumpUnsupportedShape = [&](StringRef detail) {` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0280 `tools/circt-sim/LLHDProcessInterpreterNativeThunkPolicy.cpp:757` (unclassified)

At `tools/circt-sim/LLHDProcessInterpreterNativeThunkPolicy.cpp:757`, the marker `if (!traceUnsupported || !state.getProcessOp())` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0281 `tools/circt-sim/LLHDProcessInterpreterNativeThunkPolicy.cpp:766` (tooling-gap)

At `tools/circt-sim/LLHDProcessInterpreterNativeThunkPolicy.cpp:766`, the marker `llvm::errs() << "[JIT-UNSUPPORTED] proc=" << procId << " name=" << procName` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0282 `tools/circt-sim/LLHDProcessInterpreterNativeThunkPolicy.cpp:870` (unclassified)

At `tools/circt-sim/LLHDProcessInterpreterNativeThunkPolicy.cpp:870`, the marker `std::string detail = getUnsupportedThunkDeoptDetail(state);` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0283 `tools/circt-sim/LLHDProcessInterpreterNativeThunkPolicy.cpp:873` (unclassified)

At `tools/circt-sim/LLHDProcessInterpreterNativeThunkPolicy.cpp:873`, the marker `dumpUnsupportedShape(detail);` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0284 `tools/circt-sim/LLHDProcessInterpreterNativeThunkPolicy.cpp:874` (unclassified)

At `tools/circt-sim/LLHDProcessInterpreterNativeThunkPolicy.cpp:874`, the marker `return ProcessThunkInstallResult::UnsupportedOperation;` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0285 `tools/circt-sim/LLHDProcessInterpreterNativeThunkPolicy.cpp:904` (unclassified)

At `tools/circt-sim/LLHDProcessInterpreterNativeThunkPolicy.cpp:904`, the marker `std::string LLHDProcessInterpreter::getUnsupportedThunkDeoptDetail(` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0286 `tools/circt-sim/LLHDProcessInterpreterNativeThunkPolicy.cpp:917` (unclassified)

At `tools/circt-sim/LLHDProcessInterpreterNativeThunkPolicy.cpp:917`, the marker `return (Twine("combinational_unsupported_terminator:") +` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0287 `tools/circt-sim/LLHDProcessInterpreterNativeThunkPolicy.cpp:923` (unclassified)

At `tools/circt-sim/LLHDProcessInterpreterNativeThunkPolicy.cpp:923`, the marker `return (Twine("combinational_unsupported:") +` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0288 `tools/circt-sim/LLHDProcessInterpreterNativeThunkPolicy.cpp:932` (unclassified)

At `tools/circt-sim/LLHDProcessInterpreterNativeThunkPolicy.cpp:932`, the marker `return (Twine("combinational_unsupported:first_op:") +` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0289 `tools/circt-sim/LLHDProcessInterpreterNativeThunkPolicy.cpp:950` (unclassified)

At `tools/circt-sim/LLHDProcessInterpreterNativeThunkPolicy.cpp:950`, the marker `return formatUnsupportedProcessOpDetail(*it);` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0290 `tools/circt-sim/LLHDProcessInterpreterNativeThunkPolicy.cpp:964` (unclassified)

At `tools/circt-sim/LLHDProcessInterpreterNativeThunkPolicy.cpp:964`, the marker `return (Twine("multiblock_unsupported_terminator:") +` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0291 `tools/circt-sim/LLHDProcessInterpreterNativeThunkPolicy.cpp:972` (unclassified)

At `tools/circt-sim/LLHDProcessInterpreterNativeThunkPolicy.cpp:972`, the marker `return (Twine("multiblock_unsupported_terminator:") +` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0292 `tools/circt-sim/LLHDProcessInterpreterNativeThunkPolicy.cpp:979` (unclassified)

At `tools/circt-sim/LLHDProcessInterpreterNativeThunkPolicy.cpp:979`, the marker `return formatUnsupportedProcessOpDetail(*it);` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0293 `tools/circt-sim/LLHDProcessInterpreterNativeThunkPolicy.cpp:1003` (unclassified)

At `tools/circt-sim/LLHDProcessInterpreterNativeThunkPolicy.cpp:1003`, the marker `return formatUnsupportedProcessOpDetail(op);` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0294 `tools/kanagawatool/kanagawatool.cpp:206` (tooling-gap)

At `tools/kanagawatool/kanagawatool.cpp:206`, the marker `// Legalize unsupported operations within the modules.` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0295 `test/arcilator/apb_testbench.sv:2` (test-expectation)

At `test/arcilator/apb_testbench.sv:2`, the marker `// UNSUPPORTED: true` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0296 `frontends/PyCDE/integration_test/test_software/esi_ram.py:23` (todo)

At `frontends/PyCDE/integration_test/test_software/esi_ram.py:23`, the marker `# TODO: I broke this. Need to fix it.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: I broke this. Need to fix it.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0297 `frontends/PyCDE/src/pycde/bsp/xrt.py:351` (todo)

At `frontends/PyCDE/src/pycde/bsp/xrt.py:351`, the marker `# TODO: Test reads > HostMemDataWidthBytes. I'm pretty sure this is wrong` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Test reads > HostMemDataWidthBytes. I'm pretty sure this is wrong. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0298 `frontends/PyCDE/src/pycde/bsp/xrt.py:397` (todo)

At `frontends/PyCDE/src/pycde/bsp/xrt.py:397`, the marker `# TODO: Single writes only support HostMemDataWidthBytes bytes. Fine for` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Single writes only support HostMemDataWidthBytes bytes. Fine for. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0299 `frontends/PyCDE/integration_test/esi_test.py:78` (todo)

At `frontends/PyCDE/integration_test/esi_test.py:78`, the marker `# TODO: Fix snoop_xact to work with unconumed channels.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Fix snoop_xact to work with unconumed channels.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0300 `include/circt/Support/LLVM.h:193` (todo)

At `include/circt/Support/LLVM.h:193`, the marker `// TODO: It is better to use `NOLINTBEGIN/END` comments to disable clang-tidy` indicates a gap signal in this path. What's missing: The marker documents unfinished work: It is better to use `NOLINTBEGIN/END` comments to disable clang-tidy. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0301 `frontends/PyCDE/src/CMakeLists.txt:101` (todo)

At `frontends/PyCDE/src/CMakeLists.txt:101`, the marker `# TODO: this won't work if ESIPrimitives has multiple source files. Figure out` indicates a gap signal in this path. What's missing: The marker documents unfinished work: this won't work if ESIPrimitives has multiple source files. Figure out. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0302 `tools/hlstool/hlstool.cpp:297` (tooling-gap)

At `tools/hlstool/hlstool.cpp:297`, the marker `// Legalize unsupported operations within the modules.` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0303 `tools/hlstool/hlstool.cpp:355` (todo)

At `tools/hlstool/hlstool.cpp:355`, the marker `// TODO: We assert without a canonicalizer pass here. Debug.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: We assert without a canonicalizer pass here. Debug.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0304 `tools/hlstool/hlstool.cpp:395` (todo)

At `tools/hlstool/hlstool.cpp:395`, the marker `// XXX(rachitnigam): Duplicated from doHLSFlowDynamic. We should probably` indicates a gap signal in this path. What's missing: The marker documents unfinished work: (rachitnigam): Duplicated from doHLSFlowDynamic. We should probably. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0305 `tools/circt-sim/AOTProcessCompiler.cpp:1006` (tooling-gap)

At `tools/circt-sim/AOTProcessCompiler.cpp:1006`, the marker `<< "[AOT] Process not compilable: unsupported sim op "` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0306 `tools/circt-sim/AOTProcessCompiler.cpp:1372` (todo)

At `tools/circt-sim/AOTProcessCompiler.cpp:1372`, the marker `// TODO: extract edge kind from wait attributes (posedge/negedge).` indicates a gap signal in this path. What's missing: The marker documents unfinished work: extract edge kind from wait attributes (posedge/negedge).. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0307 `tools/circt-sim/LLHDProcessInterpreterStorePatterns.h:190` (unclassified)

At `tools/circt-sim/LLHDProcessInterpreterStorePatterns.h:190`, the marker `auto thenYield =` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0308 `tools/circt-sim/LLHDProcessInterpreterStorePatterns.h:194` (unclassified)

At `tools/circt-sim/LLHDProcessInterpreterStorePatterns.h:194`, the marker `if (!thenYield || !elseYield || thenYield.getNumOperands() != 1 ||` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0309 `tools/circt-sim/LLHDProcessInterpreterStorePatterns.h:199` (unclassified)

At `tools/circt-sim/LLHDProcessInterpreterStorePatterns.h:199`, the marker `if (!matchFourStateStructCreateLoad(thenYield.getOperand(0), resolveAddr,` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0310 `tools/circt-sim/LLHDProcessInterpreterStorePatterns.h:201` (unclassified)

At `tools/circt-sim/LLHDProcessInterpreterStorePatterns.h:201`, the marker `!matchFourStateCopyStore(thenYield.getOperand(0), resolveAddr, srcAddr))` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0311 `tools/circt-verilog/circt-verilog.cpp:409` (unclassified)

At `tools/circt-verilog/circt-verilog.cpp:409`, the marker `cl::opt<bool> continueOnUnsupportedSVA{` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0312 `tools/circt-verilog/circt-verilog.cpp:410` (tooling-gap)

At `tools/circt-verilog/circt-verilog.cpp:410`, the marker `"sva-continue-on-unsupported",` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0313 `tools/circt-verilog/circt-verilog.cpp:411` (tooling-gap)

At `tools/circt-verilog/circt-verilog.cpp:411`, the marker `cl::desc("Continue lowering when an unsupported SVA construct is "` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0314 `tools/circt-verilog/circt-verilog.cpp:881` (unclassified)

At `tools/circt-verilog/circt-verilog.cpp:881`, the marker `options.continueOnUnsupportedSVA = opts.continueOnUnsupportedSVA;` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0315 `tools/handshake-runner/Simulation.cpp:144` (unclassified)

At `tools/handshake-runner/Simulation.cpp:144`, the marker `out << any_cast<APFloat>(value).convertToDouble();` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0316 `utils/run_formal_all.sh:11` (unclassified)

At `utils/run_formal_all.sh:11`, the marker `run_formal_all_snapshot="$(mktemp "${TMPDIR:-/tmp}/run_formal_all.XXXXXX.sh")"` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0317 `utils/run_formal_all.sh:2241` (tooling-gap)

At `utils/run_formal_all.sh:2241`, the marker `f"refresh policy profiles manifest signer OCSP {field} parse failed for key_id '{manifest_key_id}': unsupported timestamp '{raw}'",` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0318 `utils/run_formal_all.sh:2452` (tooling-gap)

At `utils/run_formal_all.sh:2452`, the marker `f"refresh policy profiles manifest signer CRL nextUpdate parse failed for key_id '{manifest_key_id}': unsupported timestamp '{crl_next_update}'",` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0319 `utils/run_formal_all.sh:7811` (tooling-gap)

At `utils/run_formal_all.sh:7811`, the marker `print(f"unsupported refresh URI scheme '{scheme}'", file=sys.stderr)` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0320 `utils/run_formal_all.sh:8513` (tooling-gap)

At `utils/run_formal_all.sh:8513`, the marker `f"lane state Ed25519 OCSP {field} parse failed for key_id '{target_key_id}': unsupported timestamp '{raw}'",` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0321 `utils/run_formal_all.sh:8716` (tooling-gap)

At `utils/run_formal_all.sh:8716`, the marker `f"lane state Ed25519 CRL nextUpdate parse failed for key_id '{target_key_id}': unsupported timestamp '{crl_next_update}'",` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0322 `utils/run_formal_all.sh:14091` (tooling-gap)

At `utils/run_formal_all.sh:14091`, the marker `f"unsupported rule_type '{rule_type}' in connectivity rules manifest: {rules_path}"` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0323 `utils/run_formal_all.sh:17626` (tooling-gap)

At `utils/run_formal_all.sh:17626`, the marker `f"unsupported status '{status}'"` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0324 `utils/run_formal_all.sh:20404` (tooling-gap)

At `utils/run_formal_all.sh:20404`, the marker `f"{label} unsupported entry kind '{kind}' at {allowlist_path}:{lineno}"` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0325 `utils/run_formal_all.sh:20549` (tooling-gap)

At `utils/run_formal_all.sh:20549`, the marker `f"{label} line {lineno} unsupported selector kind '{kind}' in {budget_path}"` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0326 `utils/run_formal_all.sh:20688` (tooling-gap)

At `utils/run_formal_all.sh:20688`, the marker `f"mutation/LEC lane-map unsupported source kind '{kind}' at {map_path}:{lineno}"` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0327 `utils/run_formal_all.sh:20779` (tooling-gap)

At `utils/run_formal_all.sh:20779`, the marker `f"BMC/LEC contract-fingerprint case-ID map unsupported source kind '{kind}' at {map_path}:{lineno}"` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0328 `tools/circt-bmc/circt-bmc.cpp:159` (unclassified)

At `tools/circt-bmc/circt-bmc.cpp:159`, the marker `static cl::opt<bool> dropUnsupportedSVA(` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0329 `tools/circt-bmc/circt-bmc.cpp:160` (tooling-gap)

At `tools/circt-bmc/circt-bmc.cpp:160`, the marker `"drop-unsupported-sva",` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0330 `tools/circt-bmc/circt-bmc.cpp:161` (unclassified)

At `tools/circt-bmc/circt-bmc.cpp:161`, the marker `cl::desc("Drop assert-like ops tagged with circt.unsupported_sva before "` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0331 `tools/circt-bmc/circt-bmc.cpp:271` (unclassified)

At `tools/circt-bmc/circt-bmc.cpp:271`, the marker `static unsigned dropUnsupportedSvaOps(mlir::ModuleOp module) {` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0332 `tools/circt-bmc/circt-bmc.cpp:272` (unclassified)

At `tools/circt-bmc/circt-bmc.cpp:272`, the marker `constexpr const char *kUnsupportedSvaAttr = "circt.unsupported_sva";` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0333 `tools/circt-bmc/circt-bmc.cpp:275` (unclassified)

At `tools/circt-bmc/circt-bmc.cpp:275`, the marker `if (!op->hasAttr(kUnsupportedSvaAttr))` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0334 `tools/circt-bmc/circt-bmc.cpp:770` (tooling-gap)

At `tools/circt-bmc/circt-bmc.cpp:770`, the marker `// Prune unreachable symbols early so unsupported ops in dead modules do` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0335 `tools/circt-bmc/circt-bmc.cpp:1291` (unclassified)

At `tools/circt-bmc/circt-bmc.cpp:1291`, the marker `if (dropUnsupportedSVA) {` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0336 `tools/circt-bmc/circt-bmc.cpp:1292` (unclassified)

At `tools/circt-bmc/circt-bmc.cpp:1292`, the marker `unsigned dropped = dropUnsupportedSvaOps(*module);` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0337 `tools/circt-bmc/circt-bmc.cpp:1295` (tooling-gap)

At `tools/circt-bmc/circt-bmc.cpp:1295`, the marker `<< " unsupported SVA assert-like op(s)\n";` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0338 `tools/arcilator/arcilator.cpp:906` (todo)

At `tools/arcilator/arcilator.cpp:906`, the marker `// TODO: This should probably be checking if DLTI is set on module.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: This should probably be checking if DLTI is set on module.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0339 `tools/firtool/firtool.cpp:649` (todo)

At `tools/firtool/firtool.cpp:649`, the marker `// TODO: Serialize others.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Serialize others.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0340 `tools/circt-test/circt-test.cpp:320` (tooling-gap)

At `tools/circt-test/circt-test.cpp:320`, the marker `Unsupported,` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0341 `tools/circt-test/circt-test.cpp:660` (tooling-gap)

At `tools/circt-test/circt-test.cpp:660`, the marker `case TestStatus::Unsupported:` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0342 `tools/circt-test/circt-test.cpp:661` (tooling-gap)

At `tools/circt-test/circt-test.cpp:661`, the marker `os << ansiDim << "unsupported" << ansiReset;` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0343 `tools/circt-test/circt-test.cpp:786` (tooling-gap)

At `tools/circt-test/circt-test.cpp:786`, the marker `<< "unsupported attributes: `" << test.attrs` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0344 `tools/circt-test/circt-test.cpp:834` (tooling-gap)

At `tools/circt-test/circt-test.cpp:834`, the marker `return TestStatus::Unsupported;` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0345 `tools/circt-test/circt-test.cpp:1099` (unclassified)

At `tools/circt-test/circt-test.cpp:1099`, the marker `unsigned numUnsupported = 0;` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0346 `tools/circt-test/circt-test.cpp:1111` (tooling-gap)

At `tools/circt-test/circt-test.cpp:1111`, the marker `case TestStatus::Unsupported:` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0347 `tools/circt-test/circt-test.cpp:1112` (unclassified)

At `tools/circt-test/circt-test.cpp:1112`, the marker `++numUnsupported;` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0348 `tools/circt-test/circt-test.cpp:1136` (unclassified)

At `tools/circt-test/circt-test.cpp:1136`, the marker `if (numUnsupported > 0)` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0349 `tools/circt-test/circt-test.cpp:1137` (tooling-gap)

At `tools/circt-test/circt-test.cpp:1137`, the marker `os << "; " << numUnsupported << " unsupported";` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0350 `lib/Runtime/uvm-core/src/reg/uvm_vreg_field.svh:376` (implementation-gap)

At `lib/Runtime/uvm-core/src/reg/uvm_vreg_field.svh:376`, the marker ``uvm_error("RegModel", $sformatf("Cannot call uvm_vreg_field::get_rights() on unimplemented virtual field \"%s\"",` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0351 `lib/Runtime/uvm-core/src/reg/uvm_vreg_field.svh:412` (implementation-gap)

At `lib/Runtime/uvm-core/src/reg/uvm_vreg_field.svh:412`, the marker ``uvm_error("RegModel", $sformatf("Cannot call uvm_vreg_field::write() on unimplemented virtual register \"%s\"",` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0352 `lib/Runtime/uvm-core/src/reg/uvm_vreg_field.svh:554` (implementation-gap)

At `lib/Runtime/uvm-core/src/reg/uvm_vreg_field.svh:554`, the marker ``uvm_error("RegModel", $sformatf("Cannot call uvm_vreg_field::read() on unimplemented virtual register \"%s\"",` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0353 `lib/Runtime/uvm-core/src/reg/uvm_vreg_field.svh:651` (implementation-gap)

At `lib/Runtime/uvm-core/src/reg/uvm_vreg_field.svh:651`, the marker ``uvm_error("RegModel", $sformatf("Cannot call uvm_vreg_field::poke() on unimplemented virtual register \"%s\"",` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0354 `lib/Runtime/uvm-core/src/reg/uvm_vreg_field.svh:755` (implementation-gap)

At `lib/Runtime/uvm-core/src/reg/uvm_vreg_field.svh:755`, the marker ``uvm_error("RegModel", $sformatf("Cannot call uvm_vreg_field::peek() on unimplemented virtual register \"%s\"",` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0355 `lib/Runtime/uvm-core/src/reg/uvm_vreg_field.svh:838` (todo)

At `lib/Runtime/uvm-core/src/reg/uvm_vreg_field.svh:838`, the marker `//TODO - add fatal messages` indicates a gap signal in this path. What's missing: The marker documents unfinished work: add fatal messages. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0356 `tools/circt-lec/circt-lec.cpp:194` (tooling-gap)

At `tools/circt-lec/circt-lec.cpp:194`, the marker `cl::desc("Allow LEC to abstract unsupported LLHD/inout cases"),` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0357 `tools/domaintool/domaintool.cpp:92` (todo)

At `tools/domaintool/domaintool.cpp:92`, the marker `/// TODO: Improve this format to be something less brittle.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Improve this format to be something less brittle.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0358 `tools/domaintool/domaintool.cpp:189` (todo)

At `tools/domaintool/domaintool.cpp:189`, the marker `// TODO: This is brittle and relies on the lowering of FIRRTL classes to` indicates a gap signal in this path. What's missing: The marker documents unfinished work: This is brittle and relies on the lowering of FIRRTL classes to. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0359 `tools/domaintool/domaintool.cpp:327` (todo)

At `tools/domaintool/domaintool.cpp:327`, the marker `// TODO: Improve the structural typing here in favor of something stricter,` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Improve the structural typing here in favor of something stricter,. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0360 `tools/domaintool/domaintool.cpp:429` (todo)

At `tools/domaintool/domaintool.cpp:429`, the marker `// TODO: Implement multi-file output.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Implement multi-file output.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0361 `tools/circt-sim/LLHDProcessInterpreterBytecode.cpp:50` (tooling-gap)

At `tools/circt-sim/LLHDProcessInterpreterBytecode.cpp:50`, the marker `/// contains unsupported operations.` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0362 `tools/circt-sim/LLHDProcessInterpreterBytecode.cpp:78` (tooling-gap)

At `tools/circt-sim/LLHDProcessInterpreterBytecode.cpp:78`, the marker `/// Try to compile a single operation. Returns false if unsupported.` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0363 `tools/circt-sim/LLHDProcessInterpreterBytecode.cpp:664` (tooling-gap)

At `tools/circt-sim/LLHDProcessInterpreterBytecode.cpp:664`, the marker `return false; // Unsupported predicate` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0364 `tools/circt-sim/LLHDProcessInterpreterBytecode.cpp:783` (tooling-gap)

At `tools/circt-sim/LLHDProcessInterpreterBytecode.cpp:783`, the marker `// Unsupported operation.` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0365 `tools/circt-sim/LLHDProcessInterpreterBytecode.cpp:784` (tooling-gap)

At `tools/circt-sim/LLHDProcessInterpreterBytecode.cpp:784`, the marker `LLVM_DEBUG(llvm::dbgs() << "[Bytecode] Unsupported op: " << op->getName()` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0366 `tools/circt-sim/LLHDProcessInterpreterBytecode.cpp:817` (tooling-gap)

At `tools/circt-sim/LLHDProcessInterpreterBytecode.cpp:817`, the marker `<< "[Bytecode] Failed to compile process: unsupported op "` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0367 `tools/domaintool/Handler.h:47` (todo)

At `tools/domaintool/Handler.h:47`, the marker `/// TODO: Figure out how to make this make sense for handlers which want to` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Figure out how to make this make sense for handlers which want to. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0368 `tools/domaintool/ClockSpecJSONHandler.cpp:94` (todo)

At `tools/domaintool/ClockSpecJSONHandler.cpp:94`, the marker `// TODO: Add checks that path is empty.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Add checks that path is empty.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0369 `tools/domaintool/ClockSpecJSONHandler.cpp:113` (todo)

At `tools/domaintool/ClockSpecJSONHandler.cpp:113`, the marker `// TODO: Add checks that path is empty.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Add checks that path is empty.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0370 `tools/domaintool/ClockSpecJSONHandler.cpp:140` (todo)

At `tools/domaintool/ClockSpecJSONHandler.cpp:140`, the marker `// TODO: Add checks that path is empty.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Add checks that path is empty.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0371 `tools/domaintool/ClockSpecJSONHandler.cpp:167` (todo)

At `tools/domaintool/ClockSpecJSONHandler.cpp:167`, the marker `// TODO: Implement this.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Implement this.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0372 `tools/arcilator/BehavioralLowering.cpp:197` (tooling-gap)

At `tools/arcilator/BehavioralLowering.cpp:197`, the marker `<< "unsupported in arcilator BehavioralLowering: "` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0373 `tools/arcilator/BehavioralLowering.cpp:478` (tooling-gap)

At `tools/arcilator/BehavioralLowering.cpp:478`, the marker `<< "unsupported in arcilator BehavioralLowering: llhd.wait "` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0374 `tools/arcilator/BehavioralLowering.cpp:479` (tooling-gap)

At `tools/arcilator/BehavioralLowering.cpp:479`, the marker `"(process suspension lowering not implemented)";` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0375 `tools/arcilator/BehavioralLowering.cpp:501` (tooling-gap)

At `tools/arcilator/BehavioralLowering.cpp:501`, the marker `/// Four-valued constants with X/Z bits remain unsupported in this path.` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0376 `tools/arcilator/BehavioralLowering.cpp:516` (tooling-gap)

At `tools/arcilator/BehavioralLowering.cpp:516`, the marker `<< "unsupported in arcilator BehavioralLowering: moore.constant "` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0377 `tools/arcilator/BehavioralLowering.cpp:517` (tooling-gap)

At `tools/arcilator/BehavioralLowering.cpp:517`, the marker `"with X/Z bits (four-valued constant lowering not implemented)";` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0378 `tools/arcilator/BehavioralLowering.cpp:547` (tooling-gap)

At `tools/arcilator/BehavioralLowering.cpp:547`, the marker `/// semantics are not implemented in this pass yet.` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0379 `tools/arcilator/BehavioralLowering.cpp:1162` (tooling-gap)

At `tools/arcilator/BehavioralLowering.cpp:1162`, the marker `<< "unsupported in arcilator BehavioralLowering: hw.bitcast "` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0380 `lib/Runtime/uvm-core/src/reg/uvm_vreg.svh:781` (implementation-gap)

At `lib/Runtime/uvm-core/src/reg/uvm_vreg.svh:781`, the marker ``uvm_error("RegModel", $sformatf("Cannot call uvm_vreg::get_offset_in_memory() on unimplemented virtual register \"%s\"",` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0381 `lib/Runtime/uvm-core/src/reg/uvm_vreg.svh:793` (implementation-gap)

At `lib/Runtime/uvm-core/src/reg/uvm_vreg.svh:793`, the marker ``uvm_error("RegModel", $sformatf("Cannot get address of of unimplemented virtual register \"%s\".", this.get_full_name()))` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0382 `lib/Runtime/uvm-core/src/reg/uvm_vreg.svh:803` (implementation-gap)

At `lib/Runtime/uvm-core/src/reg/uvm_vreg.svh:803`, the marker ``uvm_error("RegModel", $sformatf("Cannot call uvm_vreg::get_size() on unimplemented virtual register \"%s\"",` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0383 `lib/Runtime/uvm-core/src/reg/uvm_vreg.svh:819` (implementation-gap)

At `lib/Runtime/uvm-core/src/reg/uvm_vreg.svh:819`, the marker ``uvm_error("RegModel", $sformatf("Cannot call uvm_vreg::get_n_memlocs() on unimplemented virtual register \"%s\"",` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0384 `lib/Runtime/uvm-core/src/reg/uvm_vreg.svh:830` (implementation-gap)

At `lib/Runtime/uvm-core/src/reg/uvm_vreg.svh:830`, the marker ``uvm_error("RegModel", $sformatf("Cannot call uvm_vreg::get_incr() on unimplemented virtual register \"%s\"",` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0385 `lib/Runtime/uvm-core/src/reg/uvm_vreg.svh:841` (implementation-gap)

At `lib/Runtime/uvm-core/src/reg/uvm_vreg.svh:841`, the marker ``uvm_error("RegModel", $sformatf("Cannot call uvm_vreg::get_n_maps() on unimplemented virtual register \"%s\"",` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0386 `lib/Runtime/uvm-core/src/reg/uvm_vreg.svh:852` (implementation-gap)

At `lib/Runtime/uvm-core/src/reg/uvm_vreg.svh:852`, the marker ``uvm_error("RegModel", $sformatf("Cannot call uvm_vreg::get_maps() on unimplemented virtual register \"%s\"",` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0387 `lib/Runtime/uvm-core/src/reg/uvm_vreg.svh:863` (implementation-gap)

At `lib/Runtime/uvm-core/src/reg/uvm_vreg.svh:863`, the marker ``uvm_error("RegModel", $sformatf("Cannot call uvm_vreg::is_in_map() on unimplemented virtual register \"%s\"",` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0388 `lib/Runtime/uvm-core/src/reg/uvm_vreg.svh:874` (implementation-gap)

At `lib/Runtime/uvm-core/src/reg/uvm_vreg.svh:874`, the marker ``uvm_error("RegModel", $sformatf("Cannot call uvm_vreg::get_rights() on unimplemented virtual register \"%s\"",` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0389 `lib/Runtime/uvm-core/src/reg/uvm_vreg.svh:885` (implementation-gap)

At `lib/Runtime/uvm-core/src/reg/uvm_vreg.svh:885`, the marker ``uvm_error("RegModel", $sformatf("Cannot call uvm_vreg::get_rights() on unimplemented virtual register \"%s\"",` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0390 `lib/Runtime/uvm-core/src/reg/uvm_vreg.svh:935` (implementation-gap)

At `lib/Runtime/uvm-core/src/reg/uvm_vreg.svh:935`, the marker ``uvm_error("RegModel", $sformatf("Cannot write to unimplemented virtual register \"%s\".", this.get_full_name()))` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0391 `lib/Runtime/uvm-core/src/reg/uvm_vreg.svh:1047` (implementation-gap)

At `lib/Runtime/uvm-core/src/reg/uvm_vreg.svh:1047`, the marker ``uvm_error("RegModel", $sformatf("Cannot read from unimplemented virtual register \"%s\".", this.get_full_name()))` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0392 `lib/Runtime/uvm-core/src/reg/uvm_vreg.svh:1149` (implementation-gap)

At `lib/Runtime/uvm-core/src/reg/uvm_vreg.svh:1149`, the marker ``uvm_error("RegModel", $sformatf("Cannot poke in unimplemented virtual register \"%s\".", this.get_full_name()))` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0393 `lib/Runtime/uvm-core/src/reg/uvm_vreg.svh:1196` (implementation-gap)

At `lib/Runtime/uvm-core/src/reg/uvm_vreg.svh:1196`, the marker ``uvm_error("RegModel", $sformatf("Cannot peek in from unimplemented virtual register \"%s\".", this.get_full_name()))` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0394 `lib/Runtime/uvm-core/src/reg/uvm_vreg.svh:1242` (unclassified)

At `lib/Runtime/uvm-core/src/reg/uvm_vreg.svh:1242`, the marker `$sformat(convert2string, "%sunimplemented", convert2string);` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0395 `lib/Runtime/uvm-core/src/reg/uvm_vreg.svh:1267` (todo)

At `lib/Runtime/uvm-core/src/reg/uvm_vreg.svh:1267`, the marker `//TODO - add fatal messages` indicates a gap signal in this path. What's missing: The marker documents unfinished work: add fatal messages. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0396 `lib/Runtime/uvm-core/src/reg/uvm_reg_sequence.svh:69` (implementation-gap)

At `lib/Runtime/uvm-core/src/reg/uvm_reg_sequence.svh:69`, the marker `// Note- The convenience API not yet implemented.` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0397 `lib/Runtime/uvm-core/src/reg/uvm_reg_backdoor.svh:46` (implementation-gap)

At `lib/Runtime/uvm-core/src/reg/uvm_reg_backdoor.svh:46`, the marker `// to registers and memories that are not implemented in pure SystemVerilog` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0398 `lib/Runtime/uvm-core/src/reg/uvm_reg_predictor.svh:135` (todo)

At `lib/Runtime/uvm-core/src/reg/uvm_reg_predictor.svh:135`, the marker `// TODO: Is it better to replace this with:` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Is it better to replace this with:. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0399 `lib/Runtime/uvm-core/src/reg/uvm_reg_predictor.svh:174` (todo)

At `lib/Runtime/uvm-core/src/reg/uvm_reg_predictor.svh:174`, the marker `// ToDo: Add memory look-up and call <uvm_mem::XsampleX()>` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Add memory look-up and call <uvm_mem::XsampleX()>. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0400 `lib/Runtime/uvm-core/src/reg/uvm_reg_predictor.svh:203` (todo)

At `lib/Runtime/uvm-core/src/reg/uvm_reg_predictor.svh:203`, the marker `// TODO: what to do with subsequent collisions?` indicates a gap signal in this path. What's missing: The marker documents unfinished work: what to do with subsequent collisions?. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0401 `tools/circt-sim/LLHDProcessInterpreterCallIndirect.cpp:4695` (tooling-gap)

At `tools/circt-sim/LLHDProcessInterpreterCallIndirect.cpp:4695`, the marker `// (e.g., unimplemented sequencer interfaces, missing config_db entries).` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0402 `lib/Runtime/uvm-core/src/reg/uvm_reg_model.svh:348` (implementation-gap)

At `lib/Runtime/uvm-core/src/reg/uvm_reg_model.svh:348`, the marker `// if portions of the registers are not implemented.` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0403 `lib/Runtime/uvm-core/src/reg/uvm_reg_model.svh:355` (todo)

At `lib/Runtime/uvm-core/src/reg/uvm_reg_model.svh:355`, the marker `//| |A|xxx| B |xxx| C |` indicates a gap signal in this path. What's missing: The marker documents unfinished work: | B |xxx| C |. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0404 `lib/Runtime/uvm-core/src/reg/uvm_reg_item.svh:104` (todo)

At `lib/Runtime/uvm-core/src/reg/uvm_reg_item.svh:104`, the marker `// TODO: parameterize` indicates a gap signal in this path. What's missing: The marker documents unfinished work: parameterize. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0405 `lib/Tools/circt-debug/DebugSession.cpp:1207` (todo)

At `lib/Tools/circt-debug/DebugSession.cpp:1207`, the marker `// TODO: Set up Ctrl+C handling` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Set up Ctrl+C handling. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0406 `tools/circt-sim/LLHDProcessInterpreterTrace.cpp:3011` (unclassified)

At `tools/circt-sim/LLHDProcessInterpreterTrace.cpp:3011`, the marker `void LLHDProcessInterpreter::maybeTraceJoinAnyImmediate(ProcessId procId,` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0407 `tools/circt-sim/LLHDProcessInterpreterTrace.cpp:3016` (unclassified)

At `tools/circt-sim/LLHDProcessInterpreterTrace.cpp:3016`, the marker `static unsigned joinAnyImmDiagCount = 0;` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0408 `tools/circt-sim/LLHDProcessInterpreterTrace.cpp:3017` (unclassified)

At `tools/circt-sim/LLHDProcessInterpreterTrace.cpp:3017`, the marker `if (joinAnyImmDiagCount >= 30)` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0409 `tools/circt-sim/LLHDProcessInterpreterTrace.cpp:3019` (unclassified)

At `tools/circt-sim/LLHDProcessInterpreterTrace.cpp:3019`, the marker `++joinAnyImmDiagCount;` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0410 `lib/Runtime/uvm-core/src/reg/uvm_reg_field.svh:1612` (todo)

At `lib/Runtime/uvm-core/src/reg/uvm_reg_field.svh:1612`, the marker `// ToDo: Call parent.XsampleX();` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Call parent.XsampleX();. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0411 `lib/Runtime/uvm-core/src/reg/uvm_reg_field.svh:1737` (todo)

At `lib/Runtime/uvm-core/src/reg/uvm_reg_field.svh:1737`, the marker `// ToDo: Call parent.XsampleX();` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Call parent.XsampleX();. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0412 `lib/Runtime/uvm-core/src/reg/uvm_reg_field.svh:2086` (implementation-gap)

At `lib/Runtime/uvm-core/src/reg/uvm_reg_field.svh:2086`, the marker ``uvm_warning("RegModel","RegModel field copy not yet implemented")` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0413 `lib/Runtime/uvm-core/src/reg/uvm_reg_field.svh:2095` (implementation-gap)

At `lib/Runtime/uvm-core/src/reg/uvm_reg_field.svh:2095`, the marker ``uvm_warning("RegModel","RegModel field compare not yet implemented")` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0414 `lib/Runtime/uvm-core/src/reg/uvm_reg.svh:1153` (todo)

At `lib/Runtime/uvm-core/src/reg/uvm_reg.svh:1153`, the marker `/* ToDo: remove register from previous parent` indicates a gap signal in this path. What's missing: The marker documents unfinished work: remove register from previous parent. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0415 `lib/Runtime/uvm-core/src/reg/uvm_reg_map.svh:1987` (todo)

At `lib/Runtime/uvm-core/src/reg/uvm_reg_map.svh:1987`, the marker `// TODO: need to test for right trans type, if not put back in q` indicates a gap signal in this path. What's missing: The marker documents unfinished work: need to test for right trans type, if not put back in q. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0416 `lib/Runtime/uvm-core/src/reg/uvm_reg_map.svh:2126` (todo)

At `lib/Runtime/uvm-core/src/reg/uvm_reg_map.svh:2126`, the marker `// TODO rewrite` indicates a gap signal in this path. What's missing: The marker documents unfinished work: rewrite. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0417 `include/circt/Dialect/HW/Passes.td:162` (implementation-gap)

At `include/circt/Dialect/HW/Passes.td:162`, the marker `"false", "Ignore bitcasts involving unsupported types">` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0418 `lib/Runtime/uvm-core/src/reg/uvm_mem_mam.svh:92` (implementation-gap)

At `lib/Runtime/uvm-core/src/reg/uvm_mem_mam.svh:92`, the marker `// THRIFTY - Reused previously released memory as much as possible (not yet implemented)` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0419 `tools/circt-sim/circt-sim.cpp:2015` (tooling-gap)

At `tools/circt-sim/circt-sim.cpp:2015`, the marker `llvm::errs() << "[circt-sim] Warning: parallel scheduler is unsupported "` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0420 `tools/circt-sim/circt-sim.cpp:3277` (tooling-gap)

At `tools/circt-sim/circt-sim.cpp:3277`, the marker `"watchdog is unsupported on emscripten; using "` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0421 `tools/circt-sim/LLHDProcessInterpreter.h:1518` (unclassified)

At `tools/circt-sim/LLHDProcessInterpreter.h:1518`, the marker `UnsupportedOperation,` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0422 `tools/circt-sim/LLHDProcessInterpreter.h:1687` (tooling-gap)

At `tools/circt-sim/LLHDProcessInterpreter.h:1687`, the marker `/// Emit a concise unsupported-shape detail for deopt telemetry.` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0423 `tools/circt-sim/LLHDProcessInterpreter.h:1689` (unclassified)

At `tools/circt-sim/LLHDProcessInterpreter.h:1689`, the marker `getUnsupportedThunkDeoptDetail(const ProcessExecutionState &state) const;` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0424 `tools/circt-sim/LLHDProcessInterpreter.h:2337` (unclassified)

At `tools/circt-sim/LLHDProcessInterpreter.h:2337`, the marker `void maybeTraceJoinAnyImmediate(ProcessId procId, ForkId forkId) const;` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0425 `lib/Runtime/uvm-core/src/reg/uvm_reg_block.svh:1898` (todo)

At `lib/Runtime/uvm-core/src/reg/uvm_reg_block.svh:1898`, the marker `/* ToDo: Call XsampleX in the parent block` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Call XsampleX in the parent block. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0426 `lib/Runtime/uvm-core/src/reg/uvm_reg_block.svh:2158` (todo)

At `lib/Runtime/uvm-core/src/reg/uvm_reg_block.svh:2158`, the marker `// TODO` indicates a gap signal in this path. What's missing: The marker documents deferred or unfinished work on this path, which means behavior here is not fully implemented yet. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0427 `lib/Runtime/uvm-core/src/reg/uvm_reg_block.svh:2165` (todo)

At `lib/Runtime/uvm-core/src/reg/uvm_reg_block.svh:2165`, the marker `// TODO` indicates a gap signal in this path. What's missing: The marker documents deferred or unfinished work on this path, which means behavior here is not fully implemented yet. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0428 `lib/Runtime/uvm-core/src/reg/uvm_reg_block.svh:2631` (todo)

At `lib/Runtime/uvm-core/src/reg/uvm_reg_block.svh:2631`, the marker ``ifdef TODO` indicates a gap signal in this path. What's missing: The marker documents deferred or unfinished work on this path, which means behavior here is not fully implemented yet. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0429 `lib/Runtime/uvm-core/src/reg/uvm_mem.svh:982` (implementation-gap)

At `lib/Runtime/uvm-core/src/reg/uvm_mem.svh:982`, the marker ``uvm_error("RegModel", "uvm_mem::get_vreg_by_offset() not yet implemented")` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0430 `include/circt/Dialect/Verif/VerifOps.td:213` (todo)

At `include/circt/Dialect/Verif/VerifOps.td:213`, the marker `// TODO: initial values should eventually be handled by init region` indicates a gap signal in this path. What's missing: The marker documents unfinished work: initial values should eventually be handled by init region. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0431 `include/circt/Dialect/Verif/VerifOps.td:287` (implementation-gap)

At `include/circt/Dialect/Verif/VerifOps.td:287`, the marker `elements (i.e., registers and memories) are currently unsupported.` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0432 `lib/Runtime/uvm-core/src/comps/uvm_push_driver.svh:103` (implementation-gap)

At `lib/Runtime/uvm-core/src/comps/uvm_push_driver.svh:103`, the marker `uvm_report_fatal("UVM_PUSH_DRIVER", "Put task for push driver is not implemented", UVM_NONE);` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0433 `include/circt/Dialect/Verif/VerifOpInterfaces.h:1` (todo)

At `include/circt/Dialect/Verif/VerifOpInterfaces.h:1`, the marker `//===- VerifOpInterfaces.h - TODO ---------------===//` indicates a gap signal in this path. What's missing: The marker documents unfinished work: --------------===//. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0434 `lib/Runtime/uvm-core/src/comps/uvm_driver.svh:62` (todo)

At `lib/Runtime/uvm-core/src/comps/uvm_driver.svh:62`, the marker `// TODO: Would it be useful to change this to:` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Would it be useful to change this to:. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0435 `include/circt/Dialect/SystemC/SystemCTypes.td:115` (unclassified)

At `include/circt/Dialect/SystemC/SystemCTypes.td:115`, the marker `def AnySystemCInteger : AnyTypeOf<[AnyInteger, ValueBaseType]>;` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0436 `include/circt/Dialect/HW/HWStructure.td:247` (todo)

At `include/circt/Dialect/HW/HWStructure.td:247`, the marker `module name in Verilog we can use. TODO: This is a hack because we don't` indicates a gap signal in this path. What's missing: The marker documents unfinished work: This is a hack because we don't. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0437 `include/circt/Dialect/SystemC/SystemCTypes.h:52` (implementation-gap)

At `include/circt/Dialect/SystemC/SystemCTypes.h:52`, the marker `/// unsupported types result in a None return value.` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0438 `lib/Runtime/uvm-core/src/macros/uvm_phase_defines.svh:41` (unclassified)

At `lib/Runtime/uvm-core/src/macros/uvm_phase_defines.svh:41`, the marker `// Also, they declare classes (uvm_XXXXX_phase) and singleton instances (XXXXX_ph)` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0439 `lib/Runtime/uvm-core/src/macros/uvm_phase_defines.svh:47` (unclassified)

At `lib/Runtime/uvm-core/src/macros/uvm_phase_defines.svh:47`, the marker `// The uvm_user_xxx_phase() macros are provided for your convenience.` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0440 `lib/Runtime/uvm-core/src/comps/uvm_agent.svh:54` (todo)

At `lib/Runtime/uvm-core/src/comps/uvm_agent.svh:54`, the marker `// TODO: Make ~is_active~ a field via field utils` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Make ~is_active~ a field via field utils. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0441 `include/circt/Dialect/HW/HWOps.h:42` (todo)

At `include/circt/Dialect/HW/HWOps.h:42`, the marker `/// TODO: Move all these functions to a hw::ModuleLike interface.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Move all these functions to a hw::ModuleLike interface.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0442 `include/circt/Dialect/RTG/Transforms/RTGPasses.td:56` (implementation-gap)

At `include/circt/Dialect/RTG/Transforms/RTGPasses.td:56`, the marker `Option<"unsupportedInstructionsFile", "unsupported-instructions-file",` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0443 `include/circt/Dialect/RTG/Transforms/RTGPasses.td:60` (implementation-gap)

At `include/circt/Dialect/RTG/Transforms/RTGPasses.td:60`, the marker `ListOption<"unsupportedInstructions", "unsupported-instructions",` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0444 `include/circt/Dialect/RTG/Transforms/RTGPasses.td:208` (todo)

At `include/circt/Dialect/RTG/Transforms/RTGPasses.td:208`, the marker `user mode tests) (TODO).` indicates a gap signal in this path. What's missing: The marker documents unfinished work: ).. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0445 `include/circt/Dialect/SystemC/SystemCStructure.td:192` (implementation-gap)

At `include/circt/Dialect/SystemC/SystemCStructure.td:192`, the marker `(not yet implemented).` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0446 `include/circt/Dialect/Kanagawa/KanagawaInterfaces.td:97` (todo)

At `include/circt/Dialect/Kanagawa/KanagawaInterfaces.td:97`, the marker `// TODO: @mortbopet: fix once we have a way to do nested symbol` indicates a gap signal in this path. What's missing: The marker documents unfinished work: @mortbopet: fix once we have a way to do nested symbol. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0447 `lib/Tools/circt-verilog-lsp-server/VerilogServerImpl/VerilogDocument.cpp:65` (todo)

At `lib/Tools/circt-verilog-lsp-server/VerilogServerImpl/VerilogDocument.cpp:65`, the marker `// TODO: Re-enable when CIRCTLinting library is available` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Re-enable when CIRCTLinting library is available. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0448 `lib/Tools/circt-verilog-lsp-server/VerilogServerImpl/VerilogDocument.cpp:3252` (todo)

At `lib/Tools/circt-verilog-lsp-server/VerilogServerImpl/VerilogDocument.cpp:3252`, the marker `std::string moduleStub = "\n// TODO: Implement module\n"` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Implement module\n". The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0449 `include/circt/Dialect/Handshake/Visitor.h:47` (implementation-gap)

At `include/circt/Dialect/Handshake/Visitor.h:47`, the marker `op->emitOpError("is unsupported operation");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0450 `include/circt/Dialect/Handshake/Visitor.h:118` (implementation-gap)

At `include/circt/Dialect/Handshake/Visitor.h:118`, the marker `op->emitOpError("is unsupported operation");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0451 `lib/Runtime/uvm-core/src/macros/uvm_object_defines.svh:805` (unclassified)

At `lib/Runtime/uvm-core/src/macros/uvm_object_defines.svh:805`, the marker ``uvm_warning("UVM/FIELDS/NO_FLAG",{"Field macro for ARG uses FLAG without or'ing any explicit UVM_xxx actions. ",behavior}) \` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0452 `lib/Runtime/uvm-core/src/macros/uvm_object_defines.svh:882` (todo)

At `lib/Runtime/uvm-core/src/macros/uvm_object_defines.svh:882`, the marker `/* TODO if(local_success__ && printing matches) */ \` indicates a gap signal in this path. What's missing: The marker documents unfinished work: if(local_success__ && printing matches) */ \. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0453 `lib/Runtime/uvm-core/src/macros/uvm_object_defines.svh:935` (todo)

At `lib/Runtime/uvm-core/src/macros/uvm_object_defines.svh:935`, the marker `/* TODO if(local_success__ && printing matches) */ \` indicates a gap signal in this path. What's missing: The marker documents unfinished work: if(local_success__ && printing matches) */ \. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0454 `lib/Runtime/uvm-core/src/macros/uvm_object_defines.svh:979` (todo)

At `lib/Runtime/uvm-core/src/macros/uvm_object_defines.svh:979`, the marker `/* TODO if(local_success__ && printing matches) */ \` indicates a gap signal in this path. What's missing: The marker documents unfinished work: if(local_success__ && printing matches) */ \. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0455 `lib/Runtime/uvm-core/src/macros/uvm_object_defines.svh:1026` (todo)

At `lib/Runtime/uvm-core/src/macros/uvm_object_defines.svh:1026`, the marker `/* TODO if(local_success__ && printing matches) */ \` indicates a gap signal in this path. What's missing: The marker documents unfinished work: if(local_success__ && printing matches) */ \. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0456 `lib/Runtime/uvm-core/src/macros/uvm_object_defines.svh:1068` (todo)

At `lib/Runtime/uvm-core/src/macros/uvm_object_defines.svh:1068`, the marker `/* TODO if(local_success__ && printing matches) */ \` indicates a gap signal in this path. What's missing: The marker documents unfinished work: if(local_success__ && printing matches) */ \. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0457 `lib/Runtime/uvm-core/src/macros/uvm_object_defines.svh:1161` (todo)

At `lib/Runtime/uvm-core/src/macros/uvm_object_defines.svh:1161`, the marker `/* TODO: Non-decimal indexes */ \` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Non-decimal indexes */ \. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0458 `lib/Runtime/uvm-core/src/macros/uvm_object_defines.svh:1235` (todo)

At `lib/Runtime/uvm-core/src/macros/uvm_object_defines.svh:1235`, the marker `/* TODO: Non-decimal indexes */ \` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Non-decimal indexes */ \. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0459 `lib/Runtime/uvm-core/src/macros/uvm_object_defines.svh:1316` (todo)

At `lib/Runtime/uvm-core/src/macros/uvm_object_defines.svh:1316`, the marker `/* TODO: Non-decimal indexes */ \` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Non-decimal indexes */ \. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0460 `lib/Runtime/uvm-core/src/macros/uvm_object_defines.svh:1387` (todo)

At `lib/Runtime/uvm-core/src/macros/uvm_object_defines.svh:1387`, the marker `/* TODO: Non-decimal indexes */ \` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Non-decimal indexes */ \. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0461 `lib/Runtime/uvm-core/src/macros/uvm_object_defines.svh:1484` (todo)

At `lib/Runtime/uvm-core/src/macros/uvm_object_defines.svh:1484`, the marker `/* TODO: Non-decimal indexes */ \` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Non-decimal indexes */ \. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0462 `lib/Runtime/uvm-core/src/macros/uvm_object_defines.svh:1577` (todo)

At `lib/Runtime/uvm-core/src/macros/uvm_object_defines.svh:1577`, the marker `/* TODO: Non-decimal indexes */ \` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Non-decimal indexes */ \. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0463 `lib/Runtime/uvm-core/src/macros/uvm_object_defines.svh:1688` (todo)

At `lib/Runtime/uvm-core/src/macros/uvm_object_defines.svh:1688`, the marker `/* TODO: Non-decimal indexes */ \` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Non-decimal indexes */ \. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0464 `lib/Runtime/uvm-core/src/macros/uvm_object_defines.svh:1775` (todo)

At `lib/Runtime/uvm-core/src/macros/uvm_object_defines.svh:1775`, the marker `/* TODO: Non-decimal indexes */ \` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Non-decimal indexes */ \. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0465 `lib/Runtime/uvm-core/src/macros/uvm_object_defines.svh:1985` (todo)

At `lib/Runtime/uvm-core/src/macros/uvm_object_defines.svh:1985`, the marker `/* TODO if(local_success__ && printing matches) */ \` indicates a gap signal in this path. What's missing: The marker documents unfinished work: if(local_success__ && printing matches) */ \. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0466 `lib/Runtime/uvm-core/src/macros/uvm_object_defines.svh:2098` (todo)

At `lib/Runtime/uvm-core/src/macros/uvm_object_defines.svh:2098`, the marker `/* TODO: Non-decimal indexes */ \` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Non-decimal indexes */ \. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0467 `lib/Runtime/uvm-core/src/macros/uvm_object_defines.svh:2143` (todo)

At `lib/Runtime/uvm-core/src/macros/uvm_object_defines.svh:2143`, the marker `/* TODO */ \` indicates a gap signal in this path. What's missing: The marker documents unfinished work: */ \. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0468 `lib/Runtime/uvm-core/src/macros/uvm_object_defines.svh:2157` (todo)

At `lib/Runtime/uvm-core/src/macros/uvm_object_defines.svh:2157`, the marker `/* TODO: Non-decimal indexes */ \` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Non-decimal indexes */ \. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0469 `lib/Runtime/uvm-core/src/macros/uvm_object_defines.svh:2372` (todo)

At `lib/Runtime/uvm-core/src/macros/uvm_object_defines.svh:2372`, the marker `/* TODO: Non-decimal indexes */ \` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Non-decimal indexes */ \. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0470 `lib/Runtime/uvm-core/src/macros/uvm_object_defines.svh:2429` (todo)

At `lib/Runtime/uvm-core/src/macros/uvm_object_defines.svh:2429`, the marker `/* TODO: Non-decimal indexes */ \` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Non-decimal indexes */ \. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0471 `lib/Runtime/uvm-core/src/macros/uvm_object_defines.svh:2477` (todo)

At `lib/Runtime/uvm-core/src/macros/uvm_object_defines.svh:2477`, the marker `/* TODO: Non-decimal indexes */ \` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Non-decimal indexes */ \. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0472 `lib/Runtime/uvm-core/src/macros/uvm_object_defines.svh:2539` (todo)

At `lib/Runtime/uvm-core/src/macros/uvm_object_defines.svh:2539`, the marker `/* TODO: Non-decimal indexes */ \` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Non-decimal indexes */ \. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0473 `lib/Tools/circt-verilog-lsp-server/VerilogServerImpl/CMakeLists.txt:23` (todo)

At `lib/Tools/circt-verilog-lsp-server/VerilogServerImpl/CMakeLists.txt:23`, the marker `# TODO: CIRCTLinting needs slang header fixes` indicates a gap signal in this path. What's missing: The marker documents unfinished work: CIRCTLinting needs slang header fixes. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0474 `include/circt/Dialect/RTG/IR/RTGInterfaces.td:42` (todo)

At `include/circt/Dialect/RTG/IR/RTGInterfaces.td:42`, the marker `TODO: properly verify this; unfortunately, we don't have a 'verify' field` indicates a gap signal in this path. What's missing: The marker documents unfinished work: properly verify this; unfortunately, we don't have a 'verify' field. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0475 `lib/Runtime/uvm-core/src/base/uvm_phase.svh:537` (todo)

At `lib/Runtime/uvm-core/src/base/uvm_phase.svh:537`, the marker `// TBD add more useful debug` indicates a gap signal in this path. What's missing: The marker documents unfinished work: add more useful debug. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0476 `lib/Runtime/uvm-core/src/base/uvm_phase.svh:553` (todo)

At `lib/Runtime/uvm-core/src/base/uvm_phase.svh:553`, the marker `local function string m_aa2string(edges_t aa); // TBD tidy` indicates a gap signal in this path. What's missing: The marker documents unfinished work: tidy. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0477 `lib/Runtime/uvm-core/src/base/uvm_phase.svh:762` (todo)

At `lib/Runtime/uvm-core/src/base/uvm_phase.svh:762`, the marker `// TBD error checks if param nodes are actually in this schedule or not` indicates a gap signal in this path. What's missing: The marker documents unfinished work: error checks if param nodes are actually in this schedule or not. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0478 `lib/Runtime/uvm-core/src/base/uvm_phase.svh:1218` (todo)

At `lib/Runtime/uvm-core/src/base/uvm_phase.svh:1218`, the marker `// TODO: Seems like there should be an official way to set these values...` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Seems like there should be an official way to set these values.... The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0479 `lib/Runtime/uvm-core/src/base/uvm_phase.svh:1383` (todo)

At `lib/Runtime/uvm-core/src/base/uvm_phase.svh:1383`, the marker `// TBD full search` indicates a gap signal in this path. What's missing: The marker documents unfinished work: full search. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0480 `lib/Runtime/uvm-core/src/base/uvm_phase.svh:1403` (todo)

At `lib/Runtime/uvm-core/src/base/uvm_phase.svh:1403`, the marker `// TBD full search` indicates a gap signal in this path. What's missing: The marker documents unfinished work: full search. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0481 `lib/Runtime/uvm-core/src/base/uvm_phase.svh:1432` (todo)

At `lib/Runtime/uvm-core/src/base/uvm_phase.svh:1432`, the marker `// TODO: add support for 'stay_in_scope=1' functionality` indicates a gap signal in this path. What's missing: The marker documents unfinished work: add support for 'stay_in_scope=1' functionality. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0482 `lib/Runtime/uvm-core/src/base/uvm_phase.svh:1442` (todo)

At `lib/Runtime/uvm-core/src/base/uvm_phase.svh:1442`, the marker `// TODO: add support for 'stay_in_scope=1' functionality` indicates a gap signal in this path. What's missing: The marker documents unfinished work: add support for 'stay_in_scope=1' functionality. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0483 `lib/Runtime/uvm-core/src/base/uvm_phase.svh:2026` (implementation-gap)

At `lib/Runtime/uvm-core/src/base/uvm_phase.svh:2026`, the marker ``uvm_warning("NOTIMPL","uvm_phase::jump_all is not implemented and has been replaced by uvm_domain::jump_all")` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0484 `include/circt/Dialect/Handshake/HandshakeOps.td:339` (todo)

At `include/circt/Dialect/Handshake/HandshakeOps.td:339`, the marker `@todo: How to support different init types? these have to be stored (and` indicates a gap signal in this path. What's missing: The marker documents unfinished work: How to support different init types? these have to be stored (and. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0485 `include/circt/Dialect/Synth/SynthOps.td:102` (todo)

At `include/circt/Dialect/Synth/SynthOps.td:102`, the marker `// TODO: Restrict to HWIntegerType.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Restrict to HWIntegerType.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0486 `lib/Tools/circt-verilog-lsp-server/VerilogServerImpl/VerilogIndex.cpp:86` (todo)

At `lib/Tools/circt-verilog-lsp-server/VerilogServerImpl/VerilogIndex.cpp:86`, the marker `// TODO: This implementation does not handle expanded MACROs. Return` indicates a gap signal in this path. What's missing: The marker documents unfinished work: This implementation does not handle expanded MACROs. Return. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0487 `lib/Tools/circt-verilog-lsp-server/VerilogServerImpl/VerilogIndex.cpp:449` (todo)

At `lib/Tools/circt-verilog-lsp-server/VerilogServerImpl/VerilogIndex.cpp:449`, the marker `// TODO: Consider supporting other location format. This is currently` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Consider supporting other location format. This is currently. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0488 `lib/Tools/circt-verilog-lsp-server/VerilogServerImpl/VerilogIndex.cpp:472` (todo)

At `lib/Tools/circt-verilog-lsp-server/VerilogServerImpl/VerilogIndex.cpp:472`, the marker `// TODO: Currently doesn't handle expanded macros` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Currently doesn't handle expanded macros. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0489 `lib/Runtime/uvm-core/src/base/uvm_root.svh:1280` (todo)

At `lib/Runtime/uvm-core/src/base/uvm_root.svh:1280`, the marker `// TBD this looks wrong - taking advantage of uvm_root not doing anything else?` indicates a gap signal in this path. What's missing: The marker documents unfinished work: this looks wrong - taking advantage of uvm_root not doing anything else?. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0490 `lib/Runtime/uvm-core/src/base/uvm_root.svh:1281` (todo)

At `lib/Runtime/uvm-core/src/base/uvm_root.svh:1281`, the marker `// TBD move to phase_started callback?` indicates a gap signal in this path. What's missing: The marker documents unfinished work: move to phase_started callback?. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0491 `lib/Runtime/uvm-core/src/base/uvm_objection.svh:1359` (todo)

At `lib/Runtime/uvm-core/src/base/uvm_objection.svh:1359`, the marker `// TODO: change to plusarg` indicates a gap signal in this path. What's missing: The marker documents unfinished work: change to plusarg. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0492 `include/circt/Dialect/HWArith/HWArithOps.td:170` (unclassified)

At `include/circt/Dialect/HWArith/HWArithOps.td:170`, the marker `let arguments = (ins AnyInteger:$in);` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0493 `include/circt/Dialect/HWArith/HWArithOps.td:171` (unclassified)

At `include/circt/Dialect/HWArith/HWArithOps.td:171`, the marker `let results = (outs AnyInteger:$out);` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0494 `lib/Runtime/uvm-core/src/dpi/uvm_hdl_xcelium.c:264` (todo)

At `lib/Runtime/uvm-core/src/dpi/uvm_hdl_xcelium.c:264`, the marker `// FIXME` indicates a gap signal in this path. What's missing: The marker documents deferred or unfinished work on this path, which means behavior here is not fully implemented yet. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0495 `include/circt/Dialect/Moore/MooreOps.td:2225` (implementation-gap)

At `include/circt/Dialect/Moore/MooreOps.td:2225`, the marker `Non-integral data types define other rules which are not yet implemented.` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0496 `include/circt/Dialect/OM/OMAttributes.td:72` (todo)

At `include/circt/Dialect/OM/OMAttributes.td:72`, the marker `// TODO: Use custom assembly format to infer an element type from elements.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Use custom assembly format to infer an element type from elements.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0497 `lib/Runtime/uvm-core/src/dpi/uvm_hdl_vcs.c:615` (todo)

At `lib/Runtime/uvm-core/src/dpi/uvm_hdl_vcs.c:615`, the marker `//TODO:: furture implementation for pure verilog` indicates a gap signal in this path. What's missing: The marker documents unfinished work: : furture implementation for pure verilog. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0498 `lib/Runtime/uvm-core/src/base/uvm_object.svh:743` (unclassified)

At `lib/Runtime/uvm-core/src/base/uvm_object.svh:743`, the marker `extern virtual function void m_unsupported_set_local(uvm_resource_base rsrc);` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0499 `lib/Runtime/uvm-core/src/base/uvm_object.svh:826` (implementation-gap)

At `lib/Runtime/uvm-core/src/base/uvm_object.svh:826`, the marker `uvm_report_error("NOTYPID", "get_type not implemented in derived class.", UVM_NONE);` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0500 `lib/Runtime/uvm-core/src/base/uvm_object.svh:948` (unclassified)

At `lib/Runtime/uvm-core/src/base/uvm_object.svh:948`, the marker `// m_unsupported_set_local` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0501 `lib/Runtime/uvm-core/src/base/uvm_object.svh:952` (unclassified)

At `lib/Runtime/uvm-core/src/base/uvm_object.svh:952`, the marker `function void uvm_object::m_unsupported_set_local(uvm_resource_base rsrc);` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0502 `lib/Runtime/uvm-core/src/base/uvm_resource_base.svh:554` (unclassified)

At `lib/Runtime/uvm-core/src/base/uvm_resource_base.svh:554`, the marker `// if dbg is null, we never called record_xxx_access and so there is nothing to print` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0503 `include/circt/Dialect/FIRRTL/FIRRTLVisitors.h:442` (unclassified)

At `include/circt/Dialect/FIRRTL/FIRRTLVisitors.h:442`, the marker `// Default to chaining visitUnhandledXXX to visitUnhandledOp.` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0504 `lib/Runtime/uvm-core/src/base/uvm_misc.svh:331` (todo)

At `lib/Runtime/uvm-core/src/base/uvm_misc.svh:331`, the marker `// TODO $countbits(value,'z) would be even better` indicates a gap signal in this path. What's missing: The marker documents unfinished work: $countbits(value,'z) would be even better. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0505 `include/circt/Dialect/MSFT/MSFTConstructs.td:28` (todo)

At `include/circt/Dialect/MSFT/MSFTConstructs.td:28`, the marker `// TODO: flesh out description once we've proved this op out.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: flesh out description once we've proved this op out.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0506 `include/circt/Dialect/MSFT/DeviceDB.h:50` (todo)

At `include/circt/Dialect/MSFT/DeviceDB.h:50`, the marker `// TODO: Create read-only version of getLeaf.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Create read-only version of getLeaf.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0507 `include/circt/Dialect/FIRRTL/FIRRTLTypes.td:248` (todo)

At `include/circt/Dialect/FIRRTL/FIRRTLTypes.td:248`, the marker `// TODO: Migrate off of this by making the operands for `PrintfOp` use only` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Migrate off of this by making the operands for `PrintfOp` use only. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0508 `lib/Runtime/uvm-core/src/base/uvm_globals.svh:238` (todo)

At `lib/Runtime/uvm-core/src/base/uvm_globals.svh:238`, the marker `// TODO merge with uvm_enum_wrapper#(uvm_severity)` indicates a gap signal in this path. What's missing: The marker documents unfinished work: merge with uvm_enum_wrapper#(uvm_severity). The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0509 `include/circt/Dialect/Sim/SimOps.td:189` (unclassified)

At `include/circt/Dialect/Sim/SimOps.td:189`, the marker `let arguments = (ins AnyInteger:$value,` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0510 `include/circt/Dialect/Sim/SimOps.td:224` (unclassified)

At `include/circt/Dialect/Sim/SimOps.td:224`, the marker `let arguments = (ins AnyInteger:$value,` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0511 `include/circt/Dialect/Sim/SimOps.td:256` (unclassified)

At `include/circt/Dialect/Sim/SimOps.td:256`, the marker `let arguments = (ins AnyInteger:$value,` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0512 `include/circt/Dialect/Sim/SimOps.td:382` (unclassified)

At `include/circt/Dialect/Sim/SimOps.td:382`, the marker `let arguments = (ins AnyInteger:$value,` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0513 `include/circt/Dialect/Sim/SimOps.td:438` (unclassified)

At `include/circt/Dialect/Sim/SimOps.td:438`, the marker `let arguments = (ins AnyInteger:$value);` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0514 `include/circt/Dialect/Sim/SimOps.td:967` (unclassified)

At `include/circt/Dialect/Sim/SimOps.td:967`, the marker `let results = (outs AnyInteger:$time);` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0515 `include/circt/Dialect/Sim/SimOps.td:1439` (unclassified)

At `include/circt/Dialect/Sim/SimOps.td:1439`, the marker `let arguments = (ins AnyInteger:$delay);` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0516 `include/circt/Dialect/Sim/SimOps.td:1562` (unclassified)

At `include/circt/Dialect/Sim/SimOps.td:1562`, the marker `let arguments = (ins AnyInteger:$keyCount);` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0517 `include/circt/Dialect/Sim/SimOps.td:1589` (unclassified)

At `include/circt/Dialect/Sim/SimOps.td:1589`, the marker `Optional<AnyInteger>:$keyCount` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0518 `include/circt/Dialect/Sim/SimOps.td:1616` (unclassified)

At `include/circt/Dialect/Sim/SimOps.td:1616`, the marker `Optional<AnyInteger>:$keyCount` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0519 `include/circt/Dialect/Sim/SimOps.td:1645` (unclassified)

At `include/circt/Dialect/Sim/SimOps.td:1645`, the marker `Optional<AnyInteger>:$keyCount` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0520 `include/circt/Dialect/Sim/SimOps.td:1818` (unclassified)

At `include/circt/Dialect/Sim/SimOps.td:1818`, the marker `let results = (outs AnyInteger:$count);` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0521 `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:1194` (implementation-gap)

At `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:1194`, the marker `"unsupported llhd.probe on local ref without LLVM value type");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0522 `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:1217` (implementation-gap)

At `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:1217`, the marker `"unsupported conditional drive on local ref in LEC");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0523 `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:1220` (implementation-gap)

At `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:1220`, the marker `"unsupported non-constant drive time on local ref in LEC");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0524 `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:1224` (implementation-gap)

At `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:1224`, the marker `"unsupported llhd.drive on local ref without LLVM value type");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0525 `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:1244` (implementation-gap)

At `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:1244`, the marker `return mux.emitError("unsupported comb.mux on LLHD refs with non-probe "` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0526 `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:1304` (implementation-gap)

At `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:1304`, the marker `"unsupported predecessor for unused block argument in LEC");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0527 `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:1527` (implementation-gap)

At `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:1527`, the marker `"unsupported predecessor for pointer block argument collapse in "` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0528 `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:1834` (implementation-gap)

At `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:1834`, the marker `<< "unsupported comb.mux on divergent LLHD refs";` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0529 `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:1880` (implementation-gap)

At `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:1880`, the marker `"unsupported unrealized conversion on LLHD signal in LEC");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0530 `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:1883` (implementation-gap)

At `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:1883`, the marker `"unsupported LLHD signal cast without LLVM pointer type");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0531 `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:1906` (implementation-gap)

At `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:1906`, the marker `"unsupported load from LLHD signal in LEC");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0532 `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:1909` (implementation-gap)

At `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:1909`, the marker `"unsupported load conversion for LLHD signal in LEC");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0533 `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:1945` (implementation-gap)

At `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:1945`, the marker `"unsupported store to LLHD signal in LEC");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0534 `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:1957` (implementation-gap)

At `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:1957`, the marker `"unsupported LLVM user of LLHD signal cast in LEC");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0535 `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:1964` (implementation-gap)

At `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:1964`, the marker `<< "unsupported LLHD signal use in LEC: " << user->getName();` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0536 `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:2124` (implementation-gap)

At `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:2124`, the marker `return probe.emitError("unsupported LLHD probe path in LEC");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0537 `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:2337` (implementation-gap)

At `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:2337`, the marker `return probe.emitError("unsupported LLHD probe path in LEC");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0538 `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:2372` (implementation-gap)

At `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:2372`, the marker `return driveOp.emitError("unsupported LLHD drive value in LEC");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0539 `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:2384` (implementation-gap)

At `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:2384`, the marker `return probe.emitError("unsupported LLHD probe path in LEC");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0540 `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:2426` (implementation-gap)

At `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:2426`, the marker `return driveOp.emitError("unsupported LLHD register-state drive value");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0541 `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:2434` (implementation-gap)

At `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:2434`, the marker `return probe.emitError("unsupported LLHD probe path in LEC");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0542 `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:2566` (implementation-gap)

At `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:2566`, the marker `"unsupported LLHD drive path update in LEC");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0543 `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:2582` (implementation-gap)

At `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:2582`, the marker `return probe.emitError("unsupported LLHD probe path in LEC");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0544 `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:2742` (implementation-gap)

At `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:2742`, the marker `return driveOp.emitError("unsupported LLHD drive value in LEC");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0545 `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:2750` (implementation-gap)

At `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:2750`, the marker `"unsupported LLHD drive path update in LEC");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0546 `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:2769` (implementation-gap)

At `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:2769`, the marker `return probe.emitError("unsupported LLHD probe path in LEC");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0547 `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:3182` (implementation-gap)

At `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:3182`, the marker `return probe.emitError("unsupported LLHD probe path in LEC");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0548 `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:3209` (implementation-gap)

At `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:3209`, the marker `return probe.emitError("unsupported LLHD probe path in LEC");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0549 `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:3261` (implementation-gap)

At `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:3261`, the marker `return terminator->emitError("unsupported predecessor for LLHD ref");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0550 `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:3311` (implementation-gap)

At `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:3311`, the marker `return sigOp.emitError("unsupported LLHD signal use in LEC");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0551 `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:3327` (implementation-gap)

At `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:3327`, the marker `return probe.emitError("unsupported LLHD probe use in LEC");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0552 `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:3331` (implementation-gap)

At `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:3331`, the marker `return gep.emitError("unsupported GEP pattern for interface signal");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0553 `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:3349` (implementation-gap)

At `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:3349`, the marker `return cast.emitError("unsupported cast for interface signal");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0554 `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:3355` (implementation-gap)

At `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:3355`, the marker `return cast.emitError("unsupported cast use for interface");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0555 `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:3379` (implementation-gap)

At `lib/Tools/circt-lec/StripLLHDInterfaceSignals.cpp:3379`, the marker `return gep.emitError("unsupported interface signal use in LEC");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0556 `include/circt/Dialect/Sim/DPIRuntime.h:225` (unclassified)

At `include/circt/Dialect/Sim/DPIRuntime.h:225`, the marker `double toDouble() const {` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0557 `include/circt/Dialect/Sim/DPIRuntime.h:778` (implementation-gap)

At `include/circt/Dialect/Sim/DPIRuntime.h:778`, the marker `// Fall back to void return for unsupported signatures` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0558 `lib/Tools/circt-lec/LowerLLHDRefPorts.cpp:404` (implementation-gap)

At `lib/Tools/circt-lec/LowerLLHDRefPorts.cpp:404`, the marker `module.emitError("unsupported ref type for driven input port");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0559 `lib/Tools/circt-lec/LowerLLHDRefPorts.cpp:522` (implementation-gap)

At `lib/Tools/circt-lec/LowerLLHDRefPorts.cpp:522`, the marker `inst.emitError("unsupported ref type for output conversion");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0560 `lib/Runtime/uvm-core/src/base/uvm_factory.svh:739` (implementation-gap)

At `lib/Runtime/uvm-core/src/base/uvm_factory.svh:739`, the marker `//| // get_type_name not implemented by macro for parameterized classes` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0561 `lib/Runtime/uvm-core/src/base/uvm_event.svh:466` (todo)

At `lib/Runtime/uvm-core/src/base/uvm_event.svh:466`, the marker `// \todo Remove this note when 6450 is resolved.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Remove this note when 6450 is resolved.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0562 `lib/Tools/circt-lec/LowerLECLLVM.cpp:1717` (implementation-gap)

At `lib/Tools/circt-lec/LowerLECLLVM.cpp:1717`, the marker `"unsupported LLVM aggregate conversion in LEC; add lowering");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0563 `lib/Tools/circt-lec/LowerLECLLVM.cpp:1846` (implementation-gap)

At `lib/Tools/circt-lec/LowerLECLLVM.cpp:1846`, the marker `// declarations/symbols so strict unsupported-op checks only trigger on real` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0564 `lib/Tools/circt-lec/LowerLECLLVM.cpp:1925` (implementation-gap)

At `lib/Tools/circt-lec/LowerLECLLVM.cpp:1925`, the marker `"LEC LLVM lowering left unsupported LLVM operations");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0565 `include/circt/Dialect/SV/SVStatements.td:68` (todo)

At `include/circt/Dialect/SV/SVStatements.td:68`, the marker `// TODO: ODS forces using a custom builder just to get the region terminator` indicates a gap signal in this path. What's missing: The marker documents unfinished work: ODS forces using a custom builder just to get the region terminator. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0566 `include/circt/Dialect/SV/SVStatements.td:124` (todo)

At `include/circt/Dialect/SV/SVStatements.td:124`, the marker `// TODO: ODS forces using a custom builder just to get the region terminator` indicates a gap signal in this path. What's missing: The marker documents unfinished work: ODS forces using a custom builder just to get the region terminator. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0567 `include/circt/Dialect/SV/SVStatements.td:169` (todo)

At `include/circt/Dialect/SV/SVStatements.td:169`, the marker `// TODO: ODS forces using a custom builder just to get the region terminator` indicates a gap signal in this path. What's missing: The marker documents unfinished work: ODS forces using a custom builder just to get the region terminator. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0568 `include/circt/Dialect/SV/SVStatements.td:335` (todo)

At `include/circt/Dialect/SV/SVStatements.td:335`, the marker `// TODO: ODS forces using a custom builder just to get the region terminator` indicates a gap signal in this path. What's missing: The marker documents unfinished work: ODS forces using a custom builder just to get the region terminator. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0569 `include/circt/Dialect/SV/SVPasses.td:36` (implementation-gap)

At `include/circt/Dialect/SV/SVPasses.td:36`, the marker `let summary = "Eliminate features marked unsupported in LoweringOptions";` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0570 `include/circt/Dialect/SV/SVPasses.td:39` (implementation-gap)

At `include/circt/Dialect/SV/SVPasses.td:39`, the marker `unsupported by some tools, e.g. multidimensional arrays. This pass is` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0571 `lib/Tools/circt-lec/ConstructLEC.cpp:54` (todo)

At `lib/Tools/circt-lec/ConstructLEC.cpp:54`, the marker `// FIXME: sanity check the fetched global: do all the attributes match what` indicates a gap signal in this path. What's missing: The marker documents unfinished work: sanity check the fetched global: do all the attributes match what. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0572 `lib/Tools/circt-lec/ConstructLEC.cpp:219` (todo)

At `lib/Tools/circt-lec/ConstructLEC.cpp:219`, the marker `// TODO: don't use LLVM here` indicates a gap signal in this path. What's missing: The marker documents unfinished work: don't use LLVM here. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0573 `lib/Tools/circt-lec/ConstructLEC.cpp:231` (todo)

At `lib/Tools/circt-lec/ConstructLEC.cpp:231`, the marker `// TODO: we should find a more elegant way of reporting the result than` indicates a gap signal in this path. What's missing: The marker documents unfinished work: we should find a more elegant way of reporting the result than. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0574 `include/circt/Dialect/SV/SVTypes.td:18` (todo)

At `include/circt/Dialect/SV/SVTypes.td:18`, the marker `// TODO: The following should go into a `SVTypesImpl.td` if we ever actually` indicates a gap signal in this path. What's missing: The marker documents unfinished work: The following should go into a `SVTypesImpl.td` if we ever actually. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0575 `lib/Runtime/uvm-core/src/base/uvm_printer.svh:1000` (implementation-gap)

At `lib/Runtime/uvm-core/src/base/uvm_printer.svh:1000`, the marker ``uvm_warning("PRINTER_UNKNOWN_RADIX",$sformatf("set_radix_string called with unsupported radix %s",radix))` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0576 `include/circt/Dialect/ESI/ESIStdServices.td:56` (todo)

At `include/circt/Dialect/ESI/ESIStdServices.td:56`, the marker `TODO: ports for out-of-order returns` indicates a gap signal in this path. What's missing: The marker documents unfinished work: ports for out-of-order returns. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0577 `include/circt/Dialect/FIRRTL/FIRParser.h:77` (todo)

At `include/circt/Dialect/FIRRTL/FIRParser.h:77`, the marker `// TODO: This API is super wacky and should be streamlined to hide the` indicates a gap signal in this path. What's missing: The marker documents unfinished work: This API is super wacky and should be streamlined to hide the. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0578 `lib/Runtime/uvm-core/src/base/uvm_port_base.svh:528` (todo)

At `lib/Runtime/uvm-core/src/base/uvm_port_base.svh:528`, the marker `if (end_of_elaboration_ph.get_state() == UVM_PHASE_EXECUTING || // TBD tidy` indicates a gap signal in this path. What's missing: The marker documents unfinished work: tidy. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0579 `lib/Runtime/uvm-core/src/base/uvm_port_base.svh:638` (todo)

At `lib/Runtime/uvm-core/src/base/uvm_port_base.svh:638`, the marker `end_of_elaboration_ph.get_state() == UVM_PHASE_DONE ) begin // TBD tidy` indicates a gap signal in this path. What's missing: The marker documents unfinished work: tidy. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0580 `test/Tools/run-yosys-sva-bmc-unsupported-sva-policy.test:3` (test-expectation)

At `test/Tools/run-yosys-sva-bmc-unsupported-sva-policy.test:3`, the marker `// RUN: printf '#!/usr/bin/env bash\nset -euo pipefail\nif [[ " $* " != *" --sva-continue-on-unsupported "* ]]; then\n echo "missing --sva-continue-on-unsupported" >&2\n exit 9\nfi\necho "// dummy mlir"\n' > %t/fake-verilog.sh` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0581 `test/Tools/run-yosys-sva-bmc-unsupported-sva-policy.test:4` (test-expectation)

At `test/Tools/run-yosys-sva-bmc-unsupported-sva-policy.test:4`, the marker `// RUN: printf '#!/usr/bin/env bash\nset -euo pipefail\nif [[ " $* " != *" --drop-unsupported-sva "* ]]; then\n echo "missing --drop-unsupported-sva" >&2\n exit 9\nfi\nexit 0\n' > %t/fake-bmc.sh` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0582 `test/Tools/run-yosys-sva-bmc-unsupported-sva-policy.test:8` (test-expectation)

At `test/Tools/run-yosys-sva-bmc-unsupported-sva-policy.test:8`, the marker `// RUN: SKIP_VHDL=0 SKIP_FAIL_WITHOUT_MACRO=1 UNSUPPORTED_SVA_POLICY=lenient \` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0583 `test/Tools/run-yosys-sva-bmc-unsupported-sva-policy.test:18` (test-expectation)

At `test/Tools/run-yosys-sva-bmc-unsupported-sva-policy.test:18`, the marker `// RUN: printf '#!/usr/bin/env bash\nset -euo pipefail\nif [[ " $* " == *" --sva-continue-on-unsupported "* ]]; then\n echo "unexpected --sva-continue-on-unsupported in strict mode" >&2\n exit 9\nfi\necho "// dummy mlir"\n' > %t.strict/fake-verilog.sh` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0584 `test/Tools/run-yosys-sva-bmc-unsupported-sva-policy.test:19` (test-expectation)

At `test/Tools/run-yosys-sva-bmc-unsupported-sva-policy.test:19`, the marker `// RUN: printf '#!/usr/bin/env bash\nset -euo pipefail\nif [[ " $* " == *" --drop-unsupported-sva "* ]]; then\n echo "unexpected --drop-unsupported-sva in strict mode" >&2\n exit 9\nfi\nexit 0\n' > %t.strict/fake-bmc.sh` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0585 `test/Tools/run-yosys-sva-bmc-unsupported-sva-policy.test:34` (test-expectation)

At `test/Tools/run-yosys-sva-bmc-unsupported-sva-policy.test:34`, the marker `// RUN: printf '#!/usr/bin/env bash\nset -euo pipefail\nif [[ " $* " != *" --sva-continue-on-unsupported "* ]]; then\n echo "missing --sva-continue-on-unsupported (sim-only)" >&2\n exit 9\nfi\necho "module {}"\n' > %t.sim/fake-verilog.sh` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0586 `test/Tools/run-yosys-sva-bmc-unsupported-sva-policy.test:40` (test-expectation)

At `test/Tools/run-yosys-sva-bmc-unsupported-sva-policy.test:40`, the marker `// RUN: UNSUPPORTED_SVA_POLICY=lenient TEST_FILTER='.*' \` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0587 `test/Tools/circt-verilog/commandline.mlir:29` (test-gap-reference)

At `test/Tools/circt-verilog/commandline.mlir:29`, the marker `// CHECK-DAG: --sva-continue-on-unsupported` indicates a gap signal in this path. What's missing: This test line references an unsupported behavior, indicating a known gap in upstream implementation coverage. How to fix: Implement the missing behavior in production code and update this test to assert functional semantics rather than unsupported status once support is complete.

### G0588 `lib/Runtime/uvm-core/src/base/uvm_component.svh:1655` (implementation-gap)

At `lib/Runtime/uvm-core/src/base/uvm_component.svh:1655`, the marker `// produce message for unsupported types from apply_config_settings` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0589 `lib/Runtime/uvm-core/src/base/uvm_component.svh:1656` (unclassified)

At `lib/Runtime/uvm-core/src/base/uvm_component.svh:1656`, the marker `uvm_resource_base m_unsupported_resource_base = null;` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0590 `lib/Runtime/uvm-core/src/base/uvm_component.svh:1657` (unclassified)

At `lib/Runtime/uvm-core/src/base/uvm_component.svh:1657`, the marker `extern function void m_unsupported_set_local(uvm_resource_base rsrc);` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0591 `lib/Runtime/uvm-core/src/base/uvm_component.svh:2569` (implementation-gap)

At `lib/Runtime/uvm-core/src/base/uvm_component.svh:2569`, the marker ``uvm_warning("COMP/SPND/UNIMP", "suspend() not implemented")` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0592 `lib/Runtime/uvm-core/src/base/uvm_component.svh:2577` (implementation-gap)

At `lib/Runtime/uvm-core/src/base/uvm_component.svh:2577`, the marker ``uvm_warning("COMP/RSUM/UNIMP", "resume() not implemented")` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0593 `lib/Runtime/uvm-core/src/base/uvm_component.svh:3337` (todo)

At `lib/Runtime/uvm-core/src/base/uvm_component.svh:3337`, the marker `// TODO: Update this to use sort_by_precedence_q (Mantis 7354)` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Update this to use sort_by_precedence_q (Mantis 7354). The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0594 `lib/Runtime/uvm-core/src/base/uvm_component.svh:3542` (unclassified)

At `lib/Runtime/uvm-core/src/base/uvm_component.svh:3542`, the marker `// m_unsupported_set_local (override)` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0595 `lib/Runtime/uvm-core/src/base/uvm_component.svh:3545` (unclassified)

At `lib/Runtime/uvm-core/src/base/uvm_component.svh:3545`, the marker `function void uvm_component::m_unsupported_set_local(uvm_resource_base rsrc);` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0596 `lib/Runtime/uvm-core/src/base/uvm_component.svh:3547` (unclassified)

At `lib/Runtime/uvm-core/src/base/uvm_component.svh:3547`, the marker `m_unsupported_resource_base = rsrc;` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0597 `lib/Tools/circt-bmc/LowerToBMC.cpp:3126` (unclassified)

At `lib/Tools/circt-bmc/LowerToBMC.cpp:3126`, the marker `bool hasUnsupportedUse = false;` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0598 `lib/Tools/circt-bmc/LowerToBMC.cpp:3138` (unclassified)

At `lib/Tools/circt-bmc/LowerToBMC.cpp:3138`, the marker `hasUnsupportedUse = true;` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0599 `lib/Tools/circt-bmc/LowerToBMC.cpp:3142` (unclassified)

At `lib/Tools/circt-bmc/LowerToBMC.cpp:3142`, the marker `if (hasUnsupportedUse || hasEnable)` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0600 `lib/Tools/circt-bmc/LowerToBMC.cpp:3312` (todo)

At `lib/Tools/circt-bmc/LowerToBMC.cpp:3312`, the marker `// TODO: don't use LLVM here` indicates a gap signal in this path. What's missing: The marker documents unfinished work: don't use LLVM here. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0601 `include/circt/Dialect/Calyx/CalyxPrimitives.td:543` (unclassified)

At `include/circt/Dialect/Calyx/CalyxPrimitives.td:543`, the marker `let results = (outs AnyInteger:$in, AnyInteger:$out);` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0602 `test/Tools/circt-verilog-lsp-server/workspace-symbols.test:3` (test-expectation)

At `test/Tools/circt-verilog-lsp-server/workspace-symbols.test:3`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0603 `include/circt/Dialect/Calyx/CalyxPasses.td:27` (todo)

At `include/circt/Dialect/Calyx/CalyxPasses.td:27`, the marker `2. TODO(Calyx): If there are multiple writes to a signal, replace the reads` indicates a gap signal in this path. What's missing: The marker documents unfinished work: (Calyx): If there are multiple writes to a signal, replace the reads. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0604 `test/Tools/circt-verilog-lsp-server/workspace-symbol-project.test:3` (test-expectation)

At `test/Tools/circt-verilog-lsp-server/workspace-symbol-project.test:3`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0605 `test/Tools/circt-verilog-lsp-server/uvm-diagnostics.test:3` (test-expectation)

At `test/Tools/circt-verilog-lsp-server/uvm-diagnostics.test:3`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0606 `test/Tools/circt-verilog-lsp-server/types.test:3` (test-expectation)

At `test/Tools/circt-verilog-lsp-server/types.test:3`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0607 `test/Tools/circt-verilog-lsp-server/signature-help.test:3` (test-expectation)

At `test/Tools/circt-verilog-lsp-server/signature-help.test:3`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0608 `include/circt/Dialect/Calyx/CalyxLoweringUtils.h:204` (todo)

At `include/circt/Dialect/Calyx/CalyxLoweringUtils.h:204`, the marker `/// TODO(mortbopet): Add a post-insertion check to ensure that the use-def` indicates a gap signal in this path. What's missing: The marker documents unfinished work: (mortbopet): Add a post-insertion check to ensure that the use-def. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0609 `test/Tools/circt-verilog-lsp-server/rename-variables.test:3` (test-expectation)

At `test/Tools/circt-verilog-lsp-server/rename-variables.test:3`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0610 `lib/Tools/arcilator/pipelines.cpp:98` (todo)

At `lib/Tools/arcilator/pipelines.cpp:98`, the marker `// TODO: maybe merge RemoveUnusedArcArguments with SinkInputs?` indicates a gap signal in this path. What's missing: The marker documents unfinished work: maybe merge RemoveUnusedArcArguments with SinkInputs?. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0611 `lib/Tools/arcilator/pipelines.cpp:111` (todo)

At `lib/Tools/arcilator/pipelines.cpp:111`, the marker `// TODO: LowerClocksToFuncsPass might not properly consider scf.if operations` indicates a gap signal in this path. What's missing: The marker documents unfinished work: LowerClocksToFuncsPass might not properly consider scf.if operations. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0612 `lib/Tools/arcilator/pipelines.cpp:114` (todo)

At `lib/Tools/arcilator/pipelines.cpp:114`, the marker `// TODO: InlineArcs seems to not properly handle scf.if operations, thus the` indicates a gap signal in this path. What's missing: The marker documents unfinished work: InlineArcs seems to not properly handle scf.if operations, thus the. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0613 `test/Tools/circt-verilog-lsp-server/rename-edge-cases.test:3` (test-expectation)

At `test/Tools/circt-verilog-lsp-server/rename-edge-cases.test:3`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0614 `include/circt/Dialect/Calyx/CalyxHelpers.h:49` (todo)

At `include/circt/Dialect/Calyx/CalyxHelpers.h:49`, the marker `// TODO(github.com/llvm/circt/issues/1679): Add Invoke.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: (github.com/llvm/circt/issues/1679): Add Invoke.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0615 `test/Tools/circt-verilog-lsp-server/references.test:3` (test-expectation)

At `test/Tools/circt-verilog-lsp-server/references.test:3`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0616 `test/Tools/circt-verilog-lsp-server/module-instantiation.test:3` (test-expectation)

At `test/Tools/circt-verilog-lsp-server/module-instantiation.test:3`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0617 `test/Tools/circt-verilog-lsp-server/interface.test:3` (test-expectation)

At `test/Tools/circt-verilog-lsp-server/interface.test:3`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0618 `test/Tools/circt-verilog-lsp-server/interface.test:29` (todo)

At `test/Tools/circt-verilog-lsp-server/interface.test:29`, the marker `// TODO: Hover on interface signals not yet implemented` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Hover on interface signals not yet implemented. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0619 `test/Tools/circt-verilog-lsp-server/interface.test:39` (todo)

At `test/Tools/circt-verilog-lsp-server/interface.test:39`, the marker `// TODO: Go-to-definition for interface members not yet implemented` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Go-to-definition for interface members not yet implemented. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0620 `test/Tools/circt-verilog-lsp-server/inlay-hints.test:3` (test-expectation)

At `test/Tools/circt-verilog-lsp-server/inlay-hints.test:3`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0621 `include/circt/Dialect/Comb/CombOps.h:97` (todo)

At `include/circt/Dialect/Comb/CombOps.h:97`, the marker `/// TODO: Support signed division and modulo.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Support signed division and modulo.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0622 `test/Tools/circt-verilog-lsp-server/goto-definition.test:3` (test-expectation)

At `test/Tools/circt-verilog-lsp-server/goto-definition.test:3`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0623 `include/circt/Dialect/Arc/ArcOps.td:333` (unclassified)

At `include/circt/Dialect/Arc/ArcOps.td:333`, the marker `AnyInteger:$address` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0624 `include/circt/Dialect/Arc/ArcOps.td:335` (unclassified)

At `include/circt/Dialect/Arc/ArcOps.td:335`, the marker `let results = (outs AnyInteger:$data);` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0625 `include/circt/Dialect/Arc/ArcOps.td:411` (unclassified)

At `include/circt/Dialect/Arc/ArcOps.td:411`, the marker `AnyInteger:$address` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0626 `include/circt/Dialect/Arc/ArcOps.td:413` (unclassified)

At `include/circt/Dialect/Arc/ArcOps.td:413`, the marker `let results = (outs AnyInteger:$data);` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0627 `include/circt/Dialect/Arc/ArcOps.td:427` (unclassified)

At `include/circt/Dialect/Arc/ArcOps.td:427`, the marker `AnyInteger:$address,` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0628 `include/circt/Dialect/Arc/ArcOps.td:429` (unclassified)

At `include/circt/Dialect/Arc/ArcOps.td:429`, the marker `AnyInteger:$data` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0629 `lib/Target/ExportSystemC/Patterns/SystemCEmissionPatterns.cpp:140` (todo)

At `lib/Target/ExportSystemC/Patterns/SystemCEmissionPatterns.cpp:140`, the marker `// TODO: the 'override' keyword is hardcoded here because the destructor can` indicates a gap signal in this path. What's missing: The marker documents unfinished work: the 'override' keyword is hardcoded here because the destructor can. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0630 `lib/Target/ExportSystemC/Patterns/SystemCEmissionPatterns.cpp:311` (implementation-gap)

At `lib/Target/ExportSystemC/Patterns/SystemCEmissionPatterns.cpp:311`, the marker `p.emitError(op, "member access kind not implemented");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0631 `test/Tools/circt-verilog-lsp-server/find-references.test:3` (test-expectation)

At `test/Tools/circt-verilog-lsp-server/find-references.test:3`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0632 `test/Tools/circt-verilog-lsp-server/find-references-comprehensive.test:3` (test-expectation)

At `test/Tools/circt-verilog-lsp-server/find-references-comprehensive.test:3`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0633 `test/Tools/circt-verilog-lsp-server/document-symbols.test:3` (test-expectation)

At `test/Tools/circt-verilog-lsp-server/document-symbols.test:3`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0634 `test/Tools/circt-verilog-lsp-server/document-highlight.test:3` (test-expectation)

At `test/Tools/circt-verilog-lsp-server/document-highlight.test:3`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0635 `test/Tools/circt-verilog-lsp-server/document-links.test:3` (test-expectation)

At `test/Tools/circt-verilog-lsp-server/document-links.test:3`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0636 `lib/Target/ExportSystemC/Patterns/EmitCEmissionPatterns.cpp:72` (todo)

At `lib/Target/ExportSystemC/Patterns/EmitCEmissionPatterns.cpp:72`, the marker `// TODO: template arguments not supported for now.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: template arguments not supported for now.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0637 `lib/Target/ExportSystemC/Patterns/EmitCEmissionPatterns.cpp:84` (todo)

At `lib/Target/ExportSystemC/Patterns/EmitCEmissionPatterns.cpp:84`, the marker `// TODO: template arguments not supported for now.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: template arguments not supported for now.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0638 `test/Tools/circt-verilog-lsp-server/find-package-import-def.test:4` (test-expectation)

At `test/Tools/circt-verilog-lsp-server/find-package-import-def.test:4`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0639 `test/Tools/circt-mut-forward-generate-cache-fallback.test:4` (test-expectation)

At `test/Tools/circt-mut-forward-generate-cache-fallback.test:4`, the marker `// RUN: CIRCT_MUT_SCRIPTS_DIR=%t/scripts circt-mut generate --native-unsupported foo --design %t/design.v --out %t/mutations.txt --count 2 > %t/out.txt` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0640 `test/Tools/circt-mut-forward-generate-cache-fallback.test:8` (test-expectation)

At `test/Tools/circt-mut-forward-generate-cache-fallback.test:8`, the marker `// CHECK: ARG:--native-unsupported` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0641 `test/Tools/circt-verilog-lsp-server/diagnostic.test:4` (test-expectation)

At `test/Tools/circt-verilog-lsp-server/diagnostic.test:4`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0642 `test/Tools/circt-verilog-lsp-server/find-definition.test:3` (test-expectation)

At `test/Tools/circt-verilog-lsp-server/find-definition.test:3`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0643 `test/Tools/circt-verilog-lsp-server/debounce.test:4` (test-expectation)

At `test/Tools/circt-verilog-lsp-server/debounce.test:4`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0644 `test/Tools/circt-verilog-lsp-server/code-lens.test:3` (test-expectation)

At `test/Tools/circt-verilog-lsp-server/code-lens.test:3`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0645 `test/Tools/circt-verilog-lsp-server/command-files.test:4` (test-expectation)

At `test/Tools/circt-verilog-lsp-server/command-files.test:4`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0646 `lib/Target/ExportSystemC/ExportSystemC.cpp:39` (todo)

At `lib/Target/ExportSystemC/ExportSystemC.cpp:39`, the marker `// Remove invalid characters. TODO: a digit is not allowed as the first` indicates a gap signal in this path. What's missing: The marker documents unfinished work: a digit is not allowed as the first. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0647 `tools/circt-sim/LLHDProcessInterpreter.cpp:26757` (tooling-gap)

At `tools/circt-sim/LLHDProcessInterpreter.cpp:26757`, the marker `return "<unsupported format>";` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0648 `tools/circt-sim/LLHDProcessInterpreter.cpp:27568` (unclassified)

At `tools/circt-sim/LLHDProcessInterpreter.cpp:27568`, the marker `maybeTraceJoinAnyImmediate(procId, forkId);` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0649 `tools/circt-sim/LLHDProcessInterpreter.cpp:40655` (tooling-gap)

At `tools/circt-sim/LLHDProcessInterpreter.cpp:40655`, the marker `// Initialize return slots up-front so unsupported/failed paths never leak` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0650 `tools/circt-sim/LLHDProcessInterpreter.cpp:40717` (unclassified)

At `tools/circt-sim/LLHDProcessInterpreter.cpp:40717`, the marker `static bool warnedUnsupportedTrampolineNativeAbi = false;` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0651 `tools/circt-sim/LLHDProcessInterpreter.cpp:40718` (unclassified)

At `tools/circt-sim/LLHDProcessInterpreter.cpp:40718`, the marker `if (!warnedUnsupportedTrampolineNativeAbi) {` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0652 `tools/circt-sim/LLHDProcessInterpreter.cpp:40720` (tooling-gap)

At `tools/circt-sim/LLHDProcessInterpreter.cpp:40720`, the marker `<< "[circt-sim] WARNING: unsupported trampoline native fallback ABI"` indicates a gap signal in this path. What's missing: The tool path hard-restricts this case and reports it as unsupported, so workflows depending on this mode or op are blocked. How to fix: Extend the tool's accepted schema/operation set and plumb support through all downstream call sites, then add CLI/script regression tests for both supported and invalid inputs.

### G0653 `tools/circt-sim/LLHDProcessInterpreter.cpp:40724` (unclassified)

At `tools/circt-sim/LLHDProcessInterpreter.cpp:40724`, the marker `warnedUnsupportedTrampolineNativeAbi = true;` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0654 `test/Tools/circt-verilog-lsp-server/code-actions.test:3` (test-expectation)

At `test/Tools/circt-verilog-lsp-server/code-actions.test:3`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0655 `test/Tools/circt-verilog-lsp-server/class-hover.test:3` (test-expectation)

At `test/Tools/circt-verilog-lsp-server/class-hover.test:3`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0656 `test/Tools/circt-verilog-lsp-server/rename-comprehensive.test:3` (test-expectation)

At `test/Tools/circt-verilog-lsp-server/rename-comprehensive.test:3`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0657 `test/Tools/circt-verilog-lsp-server/class-definition.test:3` (test-expectation)

At `test/Tools/circt-verilog-lsp-server/class-definition.test:3`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0658 `lib/Target/ExportSystemC/EmissionPrinter.cpp:32` (implementation-gap)

At `lib/Target/ExportSystemC/EmissionPrinter.cpp:32`, the marker `os << "\n<<UNSUPPORTED OPERATION (" << op->getName() << ")>>\n";` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0659 `lib/Target/ExportSystemC/EmissionPrinter.cpp:50` (implementation-gap)

At `lib/Target/ExportSystemC/EmissionPrinter.cpp:50`, the marker `os << "<<UNSUPPORTED TYPE (" << type << ")>>";` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0660 `lib/Target/ExportSystemC/EmissionPrinter.cpp:66` (implementation-gap)

At `lib/Target/ExportSystemC/EmissionPrinter.cpp:66`, the marker `os << "<<UNSUPPORTED ATTRIBUTE (" << attr << ")>>";` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0661 `test/Tools/circt-verilog-lsp-server/procedural.test:3` (test-expectation)

At `test/Tools/circt-verilog-lsp-server/procedural.test:3`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0662 `test/Tools/circt-verilog-lsp-server/workspace-symbol.test:3` (test-expectation)

At `test/Tools/circt-verilog-lsp-server/workspace-symbol.test:3`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0663 `test/Tools/circt-verilog-lsp-server/call-hierarchy.test:3` (test-expectation)

At `test/Tools/circt-verilog-lsp-server/call-hierarchy.test:3`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0664 `test/Tools/circt-verilog-lsp-server/workspace-symbol-fuzzy.test:3` (test-expectation)

At `test/Tools/circt-verilog-lsp-server/workspace-symbol-fuzzy.test:3`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0665 `test/Tools/circt-verilog-lsp-server/member-completion.test:3` (test-expectation)

At `test/Tools/circt-verilog-lsp-server/member-completion.test:3`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0666 `test/Tools/circt-verilog-lsp-server/completion.test:3` (test-expectation)

At `test/Tools/circt-verilog-lsp-server/completion.test:3`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0667 `test/Tools/circt-verilog-lsp-server/uvm-completion.test:3` (test-expectation)

At `test/Tools/circt-verilog-lsp-server/uvm-completion.test:3`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0668 `test/Tools/circt-verilog-lsp-server/type-hierarchy.test:3` (test-expectation)

At `test/Tools/circt-verilog-lsp-server/type-hierarchy.test:3`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0669 `test/Tools/circt-verilog-lsp-server/semantic-tokens.test:3` (test-expectation)

At `test/Tools/circt-verilog-lsp-server/semantic-tokens.test:3`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0670 `test/Tools/circt-verilog-lsp-server/semantic-tokens-comprehensive.test:3` (test-expectation)

At `test/Tools/circt-verilog-lsp-server/semantic-tokens-comprehensive.test:3`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0671 `test/Tools/circt-verilog-lsp-server/rename.test:3` (test-expectation)

At `test/Tools/circt-verilog-lsp-server/rename.test:3`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0672 `test/Tools/circt-verilog-lsp-server/inheritance-completion.test:3` (test-expectation)

At `test/Tools/circt-verilog-lsp-server/inheritance-completion.test:3`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0673 `test/Tools/circt-verilog-lsp-server/rename-refactoring.test:3` (test-expectation)

At `test/Tools/circt-verilog-lsp-server/rename-refactoring.test:3`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0674 `include/circt/Conversion/ImportVerilog.h:231` (implementation-gap)

At `include/circt/Conversion/ImportVerilog.h:231`, the marker `/// If true, continue lowering when an unsupported SVA construct is` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0675 `include/circt/Conversion/ImportVerilog.h:233` (unclassified)

At `include/circt/Conversion/ImportVerilog.h:233`, the marker `bool continueOnUnsupportedSVA = false;` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0676 `test/Tools/circt-verilog-lsp-server/include.test:5` (test-expectation)

At `test/Tools/circt-verilog-lsp-server/include.test:5`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0677 `test/Tools/circt-verilog-lsp-server/hover.test:3` (test-expectation)

At `test/Tools/circt-verilog-lsp-server/hover.test:3`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0678 `test/Tools/run-sv-tests-bmc-smtlib-no-fallback.test:2` (test-expectation)

At `test/Tools/run-sv-tests-bmc-smtlib-no-fallback.test:2`, the marker `// RUN: bash -eu -c $'cat > %t/sv-tests/tests/chapter-16/16.15--smtlib-no-fallback.sv <<\"EOF\"\n/*\n:name: smtlib_no_fallback\n:description: ensures unsupported SMT-LIB export does not retry to native backend\n:type: simulation\n:tags: 16.15\n:unsynthesizable: 1\n*/\nmodule top(input logic clk, input logic a);\n assert property (@(posedge clk) a);\nendmodule\nEOF\n'` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0679 `test/Tools/circt-verilog-lsp-server/formatting.test:3` (test-expectation)

At `test/Tools/circt-verilog-lsp-server/formatting.test:3`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0680 `include/circt/Conversion/Passes.td:178` (todo)

At `include/circt/Conversion/Passes.td:178`, the marker `// TODO: @mortbopet: There is a possible non-neglible speedup that can be achieved` indicates a gap signal in this path. What's missing: The marker documents unfinished work: @mortbopet: There is a possible non-neglible speedup that can be achieved. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0681 `lib/Support/TruthTable.cpp:217` (todo)

At `lib/Support/TruthTable.cpp:217`, the marker `// FIXME: The time complexity is O(n! * 2^(n + m)) where n is the number` indicates a gap signal in this path. What's missing: The marker documents unfinished work: The time complexity is O(n! * 2^(n + m)) where n is the number. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0682 `lib/Support/JSON.cpp:64` (unclassified)

At `lib/Support/JSON.cpp:64`, the marker `json.value(apfloat.convertToDouble());` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0683 `lib/Support/CoverageDatabase.cpp:617` (implementation-gap)

At `lib/Support/CoverageDatabase.cpp:617`, the marker `"Unsupported database version");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0684 `test/Tools/circt-verilog-lsp-server/e2e/lit.local.cfg:2` (test-gap-reference)

At `test/Tools/circt-verilog-lsp-server/e2e/lit.local.cfg:2`, the marker `# Mark the entire directory as unsupported to prevent lit from trying to run` indicates a gap signal in this path. What's missing: This test line references an unsupported behavior, indicating a known gap in upstream implementation coverage. How to fix: Implement the missing behavior in production code and update this test to assert functional semantics rather than unsupported status once support is complete.

### G0685 `test/Tools/circt-verilog-lsp-server/e2e/lit.local.cfg:5` (test-gap-reference)

At `test/Tools/circt-verilog-lsp-server/e2e/lit.local.cfg:5`, the marker `config.unsupported = True` indicates a gap signal in this path. What's missing: This test line references an unsupported behavior, indicating a known gap in upstream implementation coverage. How to fix: Implement the missing behavior in production code and update this test to assert functional semantics rather than unsupported status once support is complete.

### G0686 `test/Tools/run-sv-tests-circt-lec-drop-remarks.test:4` (test-expectation)

At `test/Tools/run-sv-tests-circt-lec-drop-remarks.test:4`, the marker `// RUN: printf '#!/bin/sh\necho \"module @top {\"\necho \" verif.assert %c0_i1\"\necho \"}\"\necho \"warning: unsupported construct will be dropped during lowering\" >&2\n' > %t/bin/circt-verilog` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0687 `test/Tools/run-sv-tests-circt-lec-drop-remarks.test:16` (test-gap-reference)

At `test/Tools/run-sv-tests-circt-lec-drop-remarks.test:16`, the marker `// REASON: drop_case{{[[:space:]]+}}{{.*}}drop_case.sv{{[[:space:]]+}}unsupported construct will be dropped during lowering` indicates a gap signal in this path. What's missing: This test line references an unsupported behavior, indicating a known gap in upstream implementation coverage. How to fix: Implement the missing behavior in production code and update this test to assert functional semantics rather than unsupported status once support is complete.

### G0688 `test/Tools/run-yosys-sva-circt-lec-drop-remarks.test:3` (test-expectation)

At `test/Tools/run-yosys-sva-circt-lec-drop-remarks.test:3`, the marker `// RUN: printf '#!/usr/bin/env bash\necho \"// dummy mlir\"\necho \"warning: unsupported construct will be dropped during lowering\" >&2\n' > %t/fake-verilog.sh` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0689 `test/Tools/run-yosys-sva-circt-lec-drop-remarks.test:21` (test-gap-reference)

At `test/Tools/run-yosys-sva-circt-lec-drop-remarks.test:21`, the marker `// REASON: simple{{[[:space:]]+}}{{.*}}simple.sv{{[[:space:]]+}}unsupported construct will be dropped during lowering` indicates a gap signal in this path. What's missing: This test line references an unsupported behavior, indicating a known gap in upstream implementation coverage. How to fix: Implement the missing behavior in production code and update this test to assert functional semantics rather than unsupported status once support is complete.

### G0690 `test/Tools/run-yosys-sva-bmc-drop-remarks.test:3` (test-expectation)

At `test/Tools/run-yosys-sva-bmc-drop-remarks.test:3`, the marker `// RUN: printf '#!/usr/bin/env bash\necho \"// dummy mlir\"\necho \"warning: unsupported construct will be dropped during lowering\" >&2\n' > %t/fake-verilog.sh` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0691 `test/Tools/run-yosys-sva-bmc-drop-remarks.test:28` (test-gap-reference)

At `test/Tools/run-yosys-sva-bmc-drop-remarks.test:28`, the marker `// REASON: simple{{[[:space:]]+}}{{.*}}simple.sv{{[[:space:]]+}}unsupported construct will be dropped during lowering` indicates a gap signal in this path. What's missing: This test line references an unsupported behavior, indicating a known gap in upstream implementation coverage. How to fix: Implement the missing behavior in production code and update this test to assert functional semantics rather than unsupported status once support is complete.

### G0692 `test/Tools/run-verilator-verification-circt-lec-drop-remarks.test:4` (test-expectation)

At `test/Tools/run-verilator-verification-circt-lec-drop-remarks.test:4`, the marker `// RUN: printf '#!/bin/sh\necho \"module @top {\"\necho \" verif.assert %c0_i1\"\necho \"}\"\necho \"warning: unsupported construct will be dropped during lowering\" >&2\n' > %t/bin/circt-verilog` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0693 `test/Tools/run-verilator-verification-circt-lec-drop-remarks.test:16` (test-gap-reference)

At `test/Tools/run-verilator-verification-circt-lec-drop-remarks.test:16`, the marker `// REASON: drop_case{{[[:space:]]+}}{{.*}}drop_case.sv{{[[:space:]]+}}unsupported construct will be dropped during lowering` indicates a gap signal in this path. What's missing: This test line references an unsupported behavior, indicating a known gap in upstream implementation coverage. How to fix: Implement the missing behavior in production code and update this test to assert functional semantics rather than unsupported status once support is complete.

### G0694 `test/Tools/run-formal-all-strict-gate-bmc-drop-remark-cases-verilator.test:4` (test-expectation)

At `test/Tools/run-formal-all-strict-gate-bmc-drop-remark-cases-verilator.test:4`, the marker `// RUN: printf '#!/usr/bin/env bash\nset -euo pipefail\n: "${OUT:?}"\nmkdir -p "$(dirname "$OUT")"\nprintf "PASS\\tcase_pass\\t/tests/pass.sv\\n" > "$OUT"\necho "warning: unsupported construct will be dropped during lowering"\necho "verilator-verification summary: total=1 pass=1 fail=0 xfail=0 xpass=0 error=0 skip=0 unknown=0 timeout=0"\n' > %t/utils/run_verilator_verification_circt_bmc.sh` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0695 `test/Tools/run-formal-all-strict-gate-bmc-drop-remark-cases-verilator.test:8` (test-expectation)

At `test/Tools/run-formal-all-strict-gate-bmc-drop-remark-cases-verilator.test:8`, the marker `// RUN: printf '#!/usr/bin/env bash\nset -euo pipefail\n: "${OUT:?}"\nmkdir -p "$(dirname "$OUT")"\nprintf "PASS\\tcase_pass\\t/tests/pass.sv\\n" > "$OUT"\necho "warning: unsupported construct will be dropped during lowering"\necho "warning: unsupported construct will be dropped during lowering"\necho "verilator-verification summary: total=1 pass=1 fail=0 xfail=0 xpass=0 error=0 skip=0 unknown=0 timeout=0"\n' > %t/utils/run_verilator_verification_circt_bmc.sh` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0696 `test/Tools/circt-verilog-lsp-server/textdocument-didclose.test:4` (test-expectation)

At `test/Tools/circt-verilog-lsp-server/textdocument-didclose.test:4`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0697 `test/Tools/circt-verilog-lsp-server/textdocument-didchange.test:4` (test-expectation)

At `test/Tools/circt-verilog-lsp-server/textdocument-didchange.test:4`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0698 `test/Tools/circt-verilog-lsp-server/package-indexing.test:3` (test-expectation)

At `test/Tools/circt-verilog-lsp-server/package-indexing.test:3`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0699 `test/Tools/run-regression-unified-manifest-profile-internal-space.test:5` (test-expectation)

At `test/Tools/run-regression-unified-manifest-profile-internal-space.test:5`, the marker `// CHECK: invalid manifest row 1: unsupported profile token 's moke' (expected smoke|nightly|full|all)` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0700 `test/Tools/run-avip-circt-sim-jit-policy-gate.test:3` (test-expectation)

At `test/Tools/run-avip-circt-sim-jit-policy-gate.test:3`, the marker `// RUN: printf '#!/usr/bin/env bash\nset -euo pipefail\nout="${1:?out_dir required}"\nmkdir -p "$out/apb"\nif [[ "${CIRCT_SIM_MODE:-}" != "compile" ]]; then\n echo "bad_mode=${CIRCT_SIM_MODE:-}" >&2\n exit 41\nfi\nif [[ "${CIRCT_SIM_WRITE_JIT_REPORT:-0}" == "0" ]]; then\n echo "bad_write_jit_report=${CIRCT_SIM_WRITE_JIT_REPORT:-}" >&2\n exit 42\nfi\ncat > "$out/matrix.tsv" <<"MATRIX"\navip\tseed\tcompile_status\tcompile_sec\tsim_status\tsim_exit\tsim_sec\tsim_time_fs\tuvm_fatal\tuvm_error\tcov_1_pct\tcov_2_pct\tpeak_rss_kb\tcompile_log\tsim_log\napb\t1\tOK\t1\tOK\t0\t2\t200\t0\t0\t100\t100\t123\t/tmp/c.log\t/tmp/s.log\nMATRIX\ncat > "$out/apb/sim_seed_1.jit-report.json" <<"JSON"\n{\n "jit": {\n "jit_deopt_processes": [\n {"process_id": 1, "process_name": "llhd_process_1", "reason": "unsupported_operation", "detail": "first_op:llvm.call"},\n {"process_id": 2, "process_name": "llhd_process_2", "reason": "missing_thunk", "detail": "compile_budget_zero"}\n ]\n }\n}\nJSON\n' > %t/fake-run-avip-circt-sim.sh` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0701 `test/Tools/run-avip-circt-sim-jit-policy-gate.test:5` (test-expectation)

At `test/Tools/run-avip-circt-sim-jit-policy-gate.test:5`, the marker `// RUN: printf 'exact:reason:unsupported_operation\n' > %t/allowlist.txt` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0702 `test/Tools/run-avip-circt-sim-jit-policy-gate.test:6` (test-expectation)

At `test/Tools/run-avip-circt-sim-jit-policy-gate.test:6`, the marker `// RUN: env RUN_MATRIX=%t/fake-run-avip-circt-sim.sh SUMMARIZE_JIT_REPORTS=%S/../../utils/summarize_circt_sim_jit_reports.py JIT_POLICY_ALLOWLIST_FILE=%t/allowlist.txt JIT_POLICY_FAIL_ON_ANY_NON_ALLOWLISTED_DEOPT=0 JIT_POLICY_FAIL_ON_REASON=unsupported_operation %S/../../utils/run_avip_circt_sim_jit_policy_gate.sh %t/pass > %t/pass.log 2>&1` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0703 `test/Tools/run-avip-circt-sim-jit-policy-gate.test:9` (test-expectation)

At `test/Tools/run-avip-circt-sim-jit-policy-gate.test:9`, the marker `// RUN: not env RUN_MATRIX=%t/fake-run-avip-circt-sim.sh SUMMARIZE_JIT_REPORTS=%S/../../utils/summarize_circt_sim_jit_reports.py JIT_POLICY_FAIL_ON_ANY_NON_ALLOWLISTED_DEOPT=0 JIT_POLICY_FAIL_ON_REASON=unsupported_operation %S/../../utils/run_avip_circt_sim_jit_policy_gate.sh %t/fail > %t/fail.log 2>&1` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0704 `test/Tools/run-avip-circt-sim-jit-policy-gate.test:20` (unclassified)

At `test/Tools/run-avip-circt-sim-jit-policy-gate.test:20`, the marker `// REASON: 2{{[[:space:]]+}}unsupported_operation{{[[:space:]]+}}1` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0705 `test/Tools/run-avip-circt-sim-jit-policy-gate.test:23` (unclassified)

At `test/Tools/run-avip-circt-sim-jit-policy-gate.test:23`, the marker `// FAILLOG: top_non_allowlisted_reason{{\[[12]\]}} reason=unsupported_operation count=1` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0706 `test/Tools/run-avip-circt-sim-jit-policy-gate.test:24` (unclassified)

At `test/Tools/run-avip-circt-sim-jit-policy-gate.test:24`, the marker `// FAILLOG: blocked reason matched: reason=unsupported_operation count=1` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0707 `test/Tools/summarize-circt-sim-jit-reports-policy.test:3` (test-expectation)

At `test/Tools/summarize-circt-sim-jit-reports-policy.test:3`, the marker `// RUN: python3 -c "import json,pathlib; p=pathlib.Path(r'%t/reports/a.json'); p.write_text(json.dumps({'jit':{'jit_deopt_processes':[{'process_id':1,'process_name':'llhd_process_1','reason':'unsupported_operation','detail':'prewait_impure:sim.proc.print'},{'process_id':2,'process_name':'llhd_process_2','reason':'missing_thunk','detail':'compile_budget_zero'}]}}, indent=2), encoding='utf-8')"` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0708 `test/Tools/summarize-circt-sim-jit-reports-policy.test:4` (test-expectation)

At `test/Tools/summarize-circt-sim-jit-reports-policy.test:4`, the marker `// RUN: python3 -c "import json,pathlib; p=pathlib.Path(r'%t/reports/b.json'); p.write_text(json.dumps({'jit':{'jit_deopt_processes':[{'process_id':7,'process_name':'llhd_process_7','reason':'unsupported_operation','detail':'first_op:llvm.call'}]}}, indent=2), encoding='utf-8')"` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0709 `test/Tools/summarize-circt-sim-jit-reports-policy.test:5` (test-expectation)

At `test/Tools/summarize-circt-sim-jit-reports-policy.test:5`, the marker `// RUN: printf 'exact:reason:missing_thunk\nexact:reason_detail:unsupported_operation:first_op:llvm.call\n' > %t/allowlist.partial` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0710 `test/Tools/summarize-circt-sim-jit-reports-policy.test:6` (test-expectation)

At `test/Tools/summarize-circt-sim-jit-reports-policy.test:6`, the marker `// RUN: printf 'exact:reason:missing_thunk\nexact:reason_detail:unsupported_operation:first_op:llvm.call\nexact:reason_detail:unsupported_operation:prewait_impure:sim.proc.print\n' > %t/allowlist.full` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0711 `test/Tools/summarize-circt-sim-jit-reports-policy.test:8` (test-expectation)

At `test/Tools/summarize-circt-sim-jit-reports-policy.test:8`, the marker `// RUN: not python3 %S/../../utils/summarize_circt_sim_jit_reports.py %t/reports --allowlist-file %t/allowlist.partial --fail-on-reason unsupported_operation 2>&1 | FileCheck %s --check-prefix=FAIL_REASON` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0712 `test/Tools/summarize-circt-sim-jit-reports-policy.test:9` (test-expectation)

At `test/Tools/summarize-circt-sim-jit-reports-policy.test:9`, the marker `// RUN: not python3 %S/../../utils/summarize_circt_sim_jit_reports.py %t/reports --allowlist-file %t/allowlist.partial --fail-on-reason-detail unsupported_operation:prewait_impure:sim.proc.print 2>&1 | FileCheck %s --check-prefix=FAIL_DETAIL` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0713 `test/Tools/summarize-circt-sim-jit-reports-policy.test:10` (test-expectation)

At `test/Tools/summarize-circt-sim-jit-reports-policy.test:10`, the marker `// RUN: python3 %S/../../utils/summarize_circt_sim_jit_reports.py %t/reports --allowlist-file %t/allowlist.full --fail-on-any-non-allowlisted-deopt --fail-on-reason unsupported_operation --fail-on-reason-detail unsupported_operation:prewait_impure:sim.proc.print > %t/log.txt 2>&1` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0714 `test/Tools/summarize-circt-sim-jit-reports-policy.test:16` (unclassified)

At `test/Tools/summarize-circt-sim-jit-reports-policy.test:16`, the marker `// FAIL_REASON: top_non_allowlisted_reason[1] reason=unsupported_operation count=1` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0715 `test/Tools/summarize-circt-sim-jit-reports-policy.test:17` (unclassified)

At `test/Tools/summarize-circt-sim-jit-reports-policy.test:17`, the marker `// FAIL_REASON: blocked reason matched: reason=unsupported_operation count=1` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0716 `test/Tools/summarize-circt-sim-jit-reports-policy.test:19` (unclassified)

At `test/Tools/summarize-circt-sim-jit-reports-policy.test:19`, the marker `// FAIL_DETAIL: top_non_allowlisted_reason_detail[1] reason=unsupported_operation detail=prewait_impure:sim.proc.print count=1` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0717 `test/Tools/summarize-circt-sim-jit-reports-policy.test:20` (unclassified)

At `test/Tools/summarize-circt-sim-jit-reports-policy.test:20`, the marker `// FAIL_DETAIL: blocked reason/detail matched: reason=unsupported_operation detail=prewait_impure:sim.proc.print count=1` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0718 `test/Tools/summarize-circt-sim-jit-reports.test:3` (test-expectation)

At `test/Tools/summarize-circt-sim-jit-reports.test:3`, the marker `// RUN: python3 -c "import json,pathlib; p=pathlib.Path(r'%t/reports/a.json'); p.write_text(json.dumps({'jit':{'jit_deopt_processes':[{'process_id':1,'process_name':'llhd_process_1','reason':'unsupported_operation','detail':'prewait_impure:sim.proc.print'},{'process_id':2,'process_name':'llhd_process_2','reason':'missing_thunk','detail':'compile_budget_zero'}]}}, indent=2), encoding='utf-8')"` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0719 `test/Tools/summarize-circt-sim-jit-reports.test:4` (test-expectation)

At `test/Tools/summarize-circt-sim-jit-reports.test:4`, the marker `// RUN: python3 -c "import json,pathlib; p=pathlib.Path(r'%t/reports/b.json'); p.write_text(json.dumps({'jit':{'jit_deopt_processes':[{'process_id':7,'process_name':'llhd_process_7','reason':'unsupported_operation','detail':'first_op:llvm.call'}]}}, indent=2), encoding='utf-8')"` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0720 `test/Tools/summarize-circt-sim-jit-reports.test:16` (unclassified)

At `test/Tools/summarize-circt-sim-jit-reports.test:16`, the marker `// LOG: top_reason[1] reason=unsupported_operation count=2` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0721 `test/Tools/summarize-circt-sim-jit-reports.test:19` (unclassified)

At `test/Tools/summarize-circt-sim-jit-reports.test:19`, the marker `// LOG: top_reason_detail[2] reason=unsupported_operation detail=first_op:llvm.call count=1` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0722 `test/Tools/summarize-circt-sim-jit-reports.test:20` (unclassified)

At `test/Tools/summarize-circt-sim-jit-reports.test:20`, the marker `// LOG: top_reason_detail[3] reason=unsupported_operation detail=prewait_impure:sim.proc.print count=1` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0723 `test/Tools/summarize-circt-sim-jit-reports.test:23` (unclassified)

At `test/Tools/summarize-circt-sim-jit-reports.test:23`, the marker `// REASON: 1{{[[:space:]]+}}unsupported_operation{{[[:space:]]+}}2` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0724 `test/Tools/summarize-circt-sim-jit-reports.test:28` (unclassified)

At `test/Tools/summarize-circt-sim-jit-reports.test:28`, the marker `// DETAIL: 2{{[[:space:]]+}}unsupported_operation{{[[:space:]]+}}first_op:llvm.call{{[[:space:]]+}}1` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0725 `test/Tools/summarize-circt-sim-jit-reports.test:29` (unclassified)

At `test/Tools/summarize-circt-sim-jit-reports.test:29`, the marker `// DETAIL: 3{{[[:space:]]+}}unsupported_operation{{[[:space:]]+}}prewait_impure:sim.proc.print{{[[:space:]]+}}1` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0726 `test/Tools/summarize-circt-sim-jit-reports.test:32` (unclassified)

At `test/Tools/summarize-circt-sim-jit-reports.test:32`, the marker `// PROC: {{.*}}/reports/a.json{{[[:space:]]+}}1{{[[:space:]]+}}llhd_process_1{{[[:space:]]+}}unsupported_operation{{[[:space:]]+}}prewait_impure:sim.proc.print` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0727 `test/Tools/summarize-circt-sim-jit-reports.test:34` (unclassified)

At `test/Tools/summarize-circt-sim-jit-reports.test:34`, the marker `// PROC: {{.*}}/reports/b.json{{[[:space:]]+}}7{{[[:space:]]+}}llhd_process_7{{[[:space:]]+}}unsupported_operation{{[[:space:]]+}}first_op:llvm.call` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0728 `test/Tools/run-regression-unified-manifest-profile-invalid.test:7` (test-expectation)

At `test/Tools/run-regression-unified-manifest-profile-invalid.test:7`, the marker `// CHECK: invalid manifest row 1: unsupported profile token 'broken' (expected smoke|nightly|full|all)` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0729 `test/Tools/run-verilator-verification-circt-bmc-drop-remarks.test:4` (test-expectation)

At `test/Tools/run-verilator-verification-circt-bmc-drop-remarks.test:4`, the marker `// RUN: printf '#!/bin/sh\necho \"module @top {\"\necho \" verif.assert %c0_i1\"\necho \"}\"\necho \"warning: unsupported construct will be dropped during lowering\" >&2\n' > %t/bin/circt-verilog` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0730 `lib/Support/PrettyPrinter.cpp:43` (todo)

At `lib/Support/PrettyPrinter.cpp:43`, the marker `// (TODO)` indicates a gap signal in this path. What's missing: The marker documents unfinished work: ). The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0731 `include/circt/Analysis/FIRRTLInstanceInfo.h:145` (unclassified)

At `include/circt/Analysis/FIRRTLInstanceInfo.h:145`, the marker `bool anyInstanceUnderDut(igraph::ModuleOpInterface op);` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0732 `include/circt/Analysis/FIRRTLInstanceInfo.h:155` (unclassified)

At `include/circt/Analysis/FIRRTLInstanceInfo.h:155`, the marker `bool anyInstanceUnderEffectiveDut(igraph::ModuleOpInterface op);` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0733 `include/circt/Analysis/FIRRTLInstanceInfo.h:164` (unclassified)

At `include/circt/Analysis/FIRRTLInstanceInfo.h:164`, the marker `bool anyInstanceUnderLayer(igraph::ModuleOpInterface op);` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0734 `include/circt/Analysis/FIRRTLInstanceInfo.h:172` (unclassified)

At `include/circt/Analysis/FIRRTLInstanceInfo.h:172`, the marker `bool anyInstanceInDesign(igraph::ModuleOpInterface op);` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0735 `include/circt/Analysis/FIRRTLInstanceInfo.h:180` (unclassified)

At `include/circt/Analysis/FIRRTLInstanceInfo.h:180`, the marker `bool anyInstanceInEffectiveDesign(igraph::ModuleOpInterface op);` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0736 `include/circt/Analysis/DependenceAnalysis.h:66` (todo)

At `include/circt/Analysis/DependenceAnalysis.h:66`, the marker `/// TODO(mikeurbach): consider upstreaming this to MLIR's AffineAnalysis.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: (mikeurbach): consider upstreaming this to MLIR's AffineAnalysis.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0737 `test/Tools/circt-lec/lec-prune-unreachable-before-smt.mlir:12` (test-gap-reference)

At `test/Tools/circt-lec/lec-prune-unreachable-before-smt.mlir:12`, the marker `// `hw.type_scope` is intentionally unsupported by HWToSMT.` indicates a gap signal in this path. What's missing: This test line references an unsupported behavior, indicating a known gap in upstream implementation coverage. How to fix: Implement the missing behavior in production code and update this test to assert functional semantics rather than unsupported status once support is complete.

### G0738 `lib/Scheduling/SimplexSchedulers.cpp:924` (todo)

At `lib/Scheduling/SimplexSchedulers.cpp:924`, the marker `// TODO: Implement more sophisticated priority function.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Implement more sophisticated priority function.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0739 `lib/Scheduling/SimplexSchedulers.cpp:1062` (implementation-gap)

At `lib/Scheduling/SimplexSchedulers.cpp:1062`, the marker `llvm_unreachable("Unsupported objective requested");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0740 `lib/Scheduling/SimplexSchedulers.cpp:1149` (todo)

At `lib/Scheduling/SimplexSchedulers.cpp:1149`, the marker `// TODO: Replace the last condition with a proper graph analysis.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Replace the last condition with a proper graph analysis.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0741 `lib/Analysis/FIRRTLInstanceInfo.cpp:229` (unclassified)

At `lib/Analysis/FIRRTLInstanceInfo.cpp:229`, the marker `bool InstanceInfo::anyInstanceUnderDut(igraph::ModuleOpInterface op) {` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0742 `lib/Analysis/FIRRTLInstanceInfo.cpp:239` (unclassified)

At `lib/Analysis/FIRRTLInstanceInfo.cpp:239`, the marker `bool InstanceInfo::anyInstanceUnderEffectiveDut(igraph::ModuleOpInterface op) {` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0743 `lib/Analysis/FIRRTLInstanceInfo.cpp:240` (unclassified)

At `lib/Analysis/FIRRTLInstanceInfo.cpp:240`, the marker `return !hasDut() || anyInstanceUnderDut(op);` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0744 `lib/Analysis/FIRRTLInstanceInfo.cpp:247` (unclassified)

At `lib/Analysis/FIRRTLInstanceInfo.cpp:247`, the marker `bool InstanceInfo::anyInstanceUnderLayer(igraph::ModuleOpInterface op) {` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0745 `lib/Analysis/FIRRTLInstanceInfo.cpp:257` (unclassified)

At `lib/Analysis/FIRRTLInstanceInfo.cpp:257`, the marker `bool InstanceInfo::anyInstanceInDesign(igraph::ModuleOpInterface op) {` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0746 `lib/Analysis/FIRRTLInstanceInfo.cpp:267` (unclassified)

At `lib/Analysis/FIRRTLInstanceInfo.cpp:267`, the marker `bool InstanceInfo::anyInstanceInEffectiveDesign(igraph::ModuleOpInterface op) {` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0747 `lib/Firtool/Firtool.cpp:59` (todo)

At `lib/Firtool/Firtool.cpp:59`, the marker `// TODO: Ensure instance graph and other passes can handle instance choice` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Ensure instance graph and other passes can handle instance choice. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0748 `lib/Firtool/Firtool.cpp:69` (todo)

At `lib/Firtool/Firtool.cpp:69`, the marker `// TODO: This pass should be deleted.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: This pass should be deleted.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0749 `lib/Firtool/Firtool.cpp:183` (todo)

At `lib/Firtool/Firtool.cpp:183`, the marker `// TODO: This pass should be deleted along with InjectDUTHierarchy.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: This pass should be deleted along with InjectDUTHierarchy.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0750 `lib/Firtool/Firtool.cpp:250` (todo)

At `lib/Firtool/Firtool.cpp:250`, the marker `// TODO: Improve LowerLayers to avoid the need for canonicalization. See:` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Improve LowerLayers to avoid the need for canonicalization. See:. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0751 `lib/Firtool/Firtool.cpp:382` (implementation-gap)

At `lib/Firtool/Firtool.cpp:382`, the marker `// Legalize unsupported operations within the modules.` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0752 `lib/Firtool/Firtool.cpp:800` (todo)

At `lib/Firtool/Firtool.cpp:800`, the marker `// TODO: Change this default to 'true' once this has been better tested and` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Change this default to 'true' once this has been better tested and. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0753 `lib/Analysis/CMakeLists.txt:67` (todo)

At `lib/Analysis/CMakeLists.txt:67`, the marker `# TODO: Linting code needs slang header path fixes` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Linting code needs slang header path fixes. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0754 `lib/Bindings/Tcl/circt_tcl.cpp:67` (todo)

At `lib/Bindings/Tcl/circt_tcl.cpp:67`, the marker `// TODO` indicates a gap signal in this path. What's missing: The marker documents deferred or unfinished work on this path, which means behavior here is not fully implemented yet. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0755 `lib/Bindings/Tcl/circt_tcl.cpp:68` (implementation-gap)

At `lib/Bindings/Tcl/circt_tcl.cpp:68`, the marker `return returnErrorStr(interp, "loading FIR files is unimplemented :(");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0756 `lib/Transforms/FlattenMemRefs.cpp:387` (todo)

At `lib/Transforms/FlattenMemRefs.cpp:387`, the marker `// TODO: This is also possible for dynamically shaped memories.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: This is also possible for dynamically shaped memories.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0757 `lib/Analysis/TestPasses.cpp:238` (unclassified)

At `lib/Analysis/TestPasses.cpp:238`, the marker `<< " anyInstanceUnderDut: " << iInfo.anyInstanceUnderDut(op)` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0758 `lib/Analysis/TestPasses.cpp:242` (unclassified)

At `lib/Analysis/TestPasses.cpp:242`, the marker `<< " anyInstanceUnderEffectiveDut: "` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0759 `lib/Analysis/TestPasses.cpp:243` (unclassified)

At `lib/Analysis/TestPasses.cpp:243`, the marker `<< iInfo.anyInstanceUnderEffectiveDut(op) << "\n"` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0760 `lib/Analysis/TestPasses.cpp:246` (unclassified)

At `lib/Analysis/TestPasses.cpp:246`, the marker `<< " anyInstanceUnderLayer: "` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0761 `lib/Analysis/TestPasses.cpp:247` (unclassified)

At `lib/Analysis/TestPasses.cpp:247`, the marker `<< iInfo.anyInstanceUnderLayer(op) << "\n"` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0762 `lib/Analysis/TestPasses.cpp:250` (unclassified)

At `lib/Analysis/TestPasses.cpp:250`, the marker `<< " anyInstanceInDesign: " << iInfo.anyInstanceInDesign(op)` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0763 `lib/Analysis/TestPasses.cpp:254` (unclassified)

At `lib/Analysis/TestPasses.cpp:254`, the marker `<< " anyInstanceInEffectiveDesign: "` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0764 `lib/Analysis/TestPasses.cpp:255` (unclassified)

At `lib/Analysis/TestPasses.cpp:255`, the marker `<< iInfo.anyInstanceInEffectiveDesign(op) << "\n"` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0765 `lib/Bindings/Python/support.py:281` (todo)

At `lib/Bindings/Python/support.py:281`, the marker `# TODO: Make this use `UnconnectedSignalError`.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Make this use `UnconnectedSignalError`.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0766 `lib/Analysis/DependenceAnalysis.cpp:169` (todo)

At `lib/Analysis/DependenceAnalysis.cpp:169`, the marker `// TODO(mikeurbach): consider adding an inverted index to avoid this scan.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: (mikeurbach): consider adding an inverted index to avoid this scan.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0767 `test/Tools/circt-sim/format-radix-fourstate-compact-rules.sv:18` (unclassified)

At `test/Tools/circt-sim/format-radix-fourstate-compact-rules.sv:18`, the marker `f = 7'b0zxxx0z;` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0768 `test/Tools/circt-sim/format-radix-fourstate-compact-rules.sv:19` (unclassified)

At `test/Tools/circt-sim/format-radix-fourstate-compact-rules.sv:19`, the marker `g = 8'bxxxxxxxx;` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0769 `lib/Analysis/DebugInfo.cpp:165` (todo)

At `lib/Analysis/DebugInfo.cpp:165`, the marker `// TODO: What do we do with the port assignments? These should be` indicates a gap signal in this path. What's missing: The marker documents unfinished work: What do we do with the port assignments? These should be. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0770 `test/Tools/circt-sim/aot-trampoline-unsupported-abi-diagnostic.mlir:3` (test-gap-reference)

At `test/Tools/circt-sim/aot-trampoline-unsupported-abi-diagnostic.mlir:3`, the marker `// Regression: referenced externs with unsupported trampoline ABI types must be` indicates a gap signal in this path. What's missing: This test line references an unsupported behavior, indicating a known gap in upstream implementation coverage. How to fix: Implement the missing behavior in production code and update this test to assert functional semantics rather than unsupported status once support is complete.

### G0771 `test/Tools/circt-sim/aot-trampoline-unsupported-abi-diagnostic.mlir:6` (test-expectation)

At `test/Tools/circt-sim/aot-trampoline-unsupported-abi-diagnostic.mlir:6`, the marker `// CHECK: error: cannot generate interpreter trampoline for referenced external function: unsupported trampoline ABI (return type vector<2xi32>)` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0772 `lib/Conversion/ImportLiberty/ImportLiberty.cpp:705` (todo)

At `lib/Conversion/ImportLiberty/ImportLiberty.cpp:705`, the marker `// TODO: Support more group types` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Support more group types. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0773 `lib/Conversion/ImportLiberty/ImportLiberty.cpp:802` (todo)

At `lib/Conversion/ImportLiberty/ImportLiberty.cpp:802`, the marker `// TODO: Properly handle timing subgroups etc.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Properly handle timing subgroups etc.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0774 `lib/Conversion/ImportLiberty/ImportLiberty.cpp:935` (todo)

At `lib/Conversion/ImportLiberty/ImportLiberty.cpp:935`, the marker `// TODO: Support define.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Support define.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0775 `lib/Conversion/ImportLiberty/ImportLiberty.cpp:983` (todo)

At `lib/Conversion/ImportLiberty/ImportLiberty.cpp:983`, the marker `// TODO: Add array for timing attributes` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Add array for timing attributes. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0776 `lib/CAPI/Dialect/FIRRTL.cpp:327` (todo)

At `lib/CAPI/Dialect/FIRRTL.cpp:327`, the marker `// FIXME: The `reinterpret_cast` here may voilate strict aliasing rule. Is` indicates a gap signal in this path. What's missing: The marker documents unfinished work: The `reinterpret_cast` here may voilate strict aliasing rule. Is. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0777 `lib/CAPI/Dialect/CMakeLists.txt:1` (todo)

At `lib/CAPI/Dialect/CMakeLists.txt:1`, the marker `# TODO: Make the check source feature optional as an argument on *_add_library.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Make the check source feature optional as an argument on *_add_library.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0778 `lib/Bindings/Python/dialects/synth.py:57` (todo)

At `lib/Bindings/Python/dialects/synth.py:57`, the marker `# TODO: Associate with an MLIR value/op` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Associate with an MLIR value/op. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0779 `test/Tools/circt-sim/aot-trampoline-native-fallback-f64-incompatible-abi.mlir:10` (test-gap-reference)

At `test/Tools/circt-sim/aot-trampoline-native-fallback-f64-incompatible-abi.mlir:10`, the marker `// RUNTIME: WARNING: unsupported trampoline native fallback ABI` indicates a gap signal in this path. What's missing: This test line references an unsupported behavior, indicating a known gap in upstream implementation coverage. How to fix: Implement the missing behavior in production code and update this test to assert functional semantics rather than unsupported status once support is complete.

### G0780 `lib/CAPI/Dialect/OM.cpp:261` (todo)

At `lib/CAPI/Dialect/OM.cpp:261`, the marker `/// TODO: This can be removed.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: This can be removed.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0781 `lib/Dialect/SystemC/Transforms/SystemCLowerInstanceInterop.cpp:45` (todo)

At `lib/Dialect/SystemC/Transforms/SystemCLowerInstanceInterop.cpp:45`, the marker `// TODO: instead of hardcoding the verilated module's class name, it should` indicates a gap signal in this path. What's missing: The marker documents unfinished work: instead of hardcoding the verilated module's class name, it should. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0782 `lib/Dialect/SystemC/Transforms/SystemCLowerInstanceInterop.cpp:125` (todo)

At `lib/Dialect/SystemC/Transforms/SystemCLowerInstanceInterop.cpp:125`, the marker `// TODO: this has to be changed to a systemc::CallIndirectOp once the PR is` indicates a gap signal in this path. What's missing: The marker documents unfinished work: this has to be changed to a systemc::CallIndirectOp once the PR is. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0783 `lib/Dialect/LLHD/Transforms/Deseq.cpp:501` (implementation-gap)

At `lib/Dialect/LLHD/Transforms/Deseq.cpp:501`, the marker `<< ": unsupported terminator " << op->getName()` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0784 `lib/Dialect/LLHD/Transforms/Deseq.cpp:796` (todo)

At `lib/Dialect/LLHD/Transforms/Deseq.cpp:796`, the marker `// TODO: This should probably also check non-i1 values to see if they` indicates a gap signal in this path. What's missing: The marker documents unfinished work: This should probably also check non-i1 values to see if they. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0785 `lib/Dialect/LLHD/Transforms/Deseq.cpp:824` (todo)

At `lib/Dialect/LLHD/Transforms/Deseq.cpp:824`, the marker `// TODO: Reject values that depend on the triggers.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Reject values that depend on the triggers.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0786 `lib/Dialect/SystemC/SystemCOps.cpp:746` (todo)

At `lib/Dialect/SystemC/SystemCOps.cpp:746`, the marker `// TODO: The implementation for this operation was copy-pasted from the` indicates a gap signal in this path. What's missing: The marker documents unfinished work: The implementation for this operation was copy-pasted from the. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0787 `lib/Dialect/SystemC/SystemCOps.cpp:751` (todo)

At `lib/Dialect/SystemC/SystemCOps.cpp:751`, the marker `// FIXME: This is an exact copy from upstream` indicates a gap signal in this path. What's missing: The marker documents unfinished work: This is an exact copy from upstream. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0788 `lib/Dialect/SystemC/SystemCOps.cpp:816` (todo)

At `lib/Dialect/SystemC/SystemCOps.cpp:816`, the marker `// TODO: Most of the implementation for this operation was copy-pasted from the` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Most of the implementation for this operation was copy-pasted from the. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0789 `lib/Dialect/SystemC/SystemCOps.cpp:878` (todo)

At `lib/Dialect/SystemC/SystemCOps.cpp:878`, the marker `// FIXME: below is an exact copy of the` indicates a gap signal in this path. What's missing: The marker documents unfinished work: below is an exact copy of the. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0790 `lib/Dialect/SystemC/SystemCOps.cpp:981` (todo)

At `lib/Dialect/SystemC/SystemCOps.cpp:981`, the marker `// FIXME: inlined mlir::function_interface_impl::printFunctionOp because we` indicates a gap signal in this path. What's missing: The marker documents unfinished work: inlined mlir::function_interface_impl::printFunctionOp because we. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0791 `lib/Dialect/SystemC/SystemCOps.cpp:1012` (todo)

At `lib/Dialect/SystemC/SystemCOps.cpp:1012`, the marker `// FIXME: the below clone operation are exact copies from upstream.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: the below clone operation are exact copies from upstream.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0792 `lib/Dialect/SystemC/SystemCOps.cpp:1123` (todo)

At `lib/Dialect/SystemC/SystemCOps.cpp:1123`, the marker `// TODO: The implementation for this operation was copy-pasted from the` indicates a gap signal in this path. What's missing: The marker documents unfinished work: The implementation for this operation was copy-pasted from the. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0793 `lib/Dialect/LLHD/Transforms/UnrollLoops.cpp:167` (implementation-gap)

At `lib/Dialect/LLHD/Transforms/UnrollLoops.cpp:167`, the marker `return failMatch("unsupported exit branch");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0794 `lib/Dialect/LLHD/Transforms/UnrollLoops.cpp:181` (implementation-gap)

At `lib/Dialect/LLHD/Transforms/UnrollLoops.cpp:181`, the marker `return failMatch("unsupported exit condition");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0795 `lib/Dialect/LLHD/Transforms/UnrollLoops.cpp:211` (implementation-gap)

At `lib/Dialect/LLHD/Transforms/UnrollLoops.cpp:211`, the marker `return failMatch("unsupported induction variable value");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0796 `lib/Dialect/LLHD/Transforms/UnrollLoops.cpp:228` (implementation-gap)

At `lib/Dialect/LLHD/Transforms/UnrollLoops.cpp:228`, the marker `return failMatch("unsupported increment");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0797 `lib/Dialect/LLHD/Transforms/UnrollLoops.cpp:250` (implementation-gap)

At `lib/Dialect/LLHD/Transforms/UnrollLoops.cpp:250`, the marker `return failMatch("unsupported loop bounds");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0798 `test/Tools/circt-sim/aot-native-module-init-hw-struct-create-fourstate.mlir:7` (unclassified)

At `test/Tools/circt-sim/aot-native-module-init-hw-struct-create-fourstate.mlir:7`, the marker `// COMPILE-NOT: unsupported_op:hw.struct_create` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0799 `test/Tools/circt-sim/aot-native-module-init-scf-if-struct-extract.mlir:7` (unclassified)

At `test/Tools/circt-sim/aot-native-module-init-scf-if-struct-extract.mlir:7`, the marker `// COMPILE-NOT: unsupported_op:hw.struct_extract` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0800 `lib/Dialect/LLHD/Transforms/RemoveControlFlow.cpp:108` (todo)

At `lib/Dialect/LLHD/Transforms/RemoveControlFlow.cpp:108`, the marker `/// TODO: This eagerly aggregates all control flow decisions. It may be more` indicates a gap signal in this path. What's missing: The marker documents unfinished work: This eagerly aggregates all control flow decisions. It may be more. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0801 `lib/Dialect/LLHD/Transforms/RemoveControlFlow.cpp:206` (implementation-gap)

At `lib/Dialect/LLHD/Transforms/RemoveControlFlow.cpp:206`, the marker `<< "- Has unsupported terminator "` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0802 `lib/Dialect/RTG/Transforms/ElaborationPass.cpp:1978` (todo)

At `lib/Dialect/RTG/Transforms/ElaborationPass.cpp:1978`, the marker `// FIXME: this is not how the MLIR MemoryEffects interface intends it.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: this is not how the MLIR MemoryEffects interface intends it.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0803 `lib/Dialect/RTG/Transforms/ElaborationPass.cpp:2094` (todo)

At `lib/Dialect/RTG/Transforms/ElaborationPass.cpp:2094`, the marker `// TODO: don't clone if this is the only remaining reference to this` indicates a gap signal in this path. What's missing: The marker documents unfinished work: don't clone if this is the only remaining reference to this. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0804 `test/Tools/circt-bmc/drop-unsupported-sva.mlir:2` (test-expectation)

At `test/Tools/circt-bmc/drop-unsupported-sva.mlir:2`, the marker `// RUN: circt-bmc --emit-mlir -b 1 --module top --drop-unsupported-sva %s 2>&1 | FileCheck %s --check-prefix=DROP` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0805 `test/Tools/circt-bmc/drop-unsupported-sva.mlir:7` (unclassified)

At `test/Tools/circt-bmc/drop-unsupported-sva.mlir:7`, the marker `verif.assert %true {circt.unsupported_sva} : i1` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0806 `test/Tools/circt-bmc/drop-unsupported-sva.mlir:12` (test-gap-reference)

At `test/Tools/circt-bmc/drop-unsupported-sva.mlir:12`, the marker `// DROP: circt-bmc: dropped 1 unsupported SVA assert-like op(s)` indicates a gap signal in this path. What's missing: This test line references an unsupported behavior, indicating a known gap in upstream implementation coverage. How to fix: Implement the missing behavior in production code and update this test to assert functional semantics rather than unsupported status once support is complete.

### G0807 `lib/Dialect/RTG/Transforms/LinearScanRegisterAllocationPass.cpp:53` (todo)

At `lib/Dialect/RTG/Transforms/LinearScanRegisterAllocationPass.cpp:53`, the marker `// TODO: use a better datastructure for 'active'` indicates a gap signal in this path. What's missing: The marker documents unfinished work: use a better datastructure for 'active'. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0808 `lib/Dialect/RTG/Transforms/LinearScanRegisterAllocationPass.cpp:82` (todo)

At `lib/Dialect/RTG/Transforms/LinearScanRegisterAllocationPass.cpp:82`, the marker `// TODO: ideally check that the IR is already fully elaborated` indicates a gap signal in this path. What's missing: The marker documents unfinished work: ideally check that the IR is already fully elaborated. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0809 `lib/Dialect/RTG/Transforms/LinearScanRegisterAllocationPass.cpp:119` (todo)

At `lib/Dialect/RTG/Transforms/LinearScanRegisterAllocationPass.cpp:119`, the marker `// TODO: support labels and control-flow loops (jumps in general)` indicates a gap signal in this path. What's missing: The marker documents unfinished work: support labels and control-flow loops (jumps in general). The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0810 `lib/Dialect/RTG/Transforms/LinearScanRegisterAllocationPass.cpp:131` (todo)

At `lib/Dialect/RTG/Transforms/LinearScanRegisterAllocationPass.cpp:131`, the marker `// TODO: don't overapproximate that much` indicates a gap signal in this path. What's missing: The marker documents unfinished work: don't overapproximate that much. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0811 `test/Tools/circt-sim/aot-native-module-init-skip-telemetry.mlir:6` (test-expectation)

At `test/Tools/circt-sim/aot-native-module-init-skip-telemetry.mlir:6`, the marker `// CHECK: 1x unsupported_call:puts` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0812 `lib/Dialect/RTG/Transforms/EmitRTGISAAssemblyPass.cpp:42` (unclassified)

At `lib/Dialect/RTG/Transforms/EmitRTGISAAssemblyPass.cpp:42`, the marker `Emitter(llvm::raw_ostream &os, const DenseSet<StringAttr> &unsupportedInstr)` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0813 `lib/Dialect/RTG/Transforms/EmitRTGISAAssemblyPass.cpp:43` (unclassified)

At `lib/Dialect/RTG/Transforms/EmitRTGISAAssemblyPass.cpp:43`, the marker `: os(os), unsupportedInstr(unsupportedInstr) {}` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0814 `lib/Dialect/RTG/Transforms/EmitRTGISAAssemblyPass.cpp:94` (unclassified)

At `lib/Dialect/RTG/Transforms/EmitRTGISAAssemblyPass.cpp:94`, the marker `unsupportedInstr.contains(instr->getName().getIdentifier());` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0815 `lib/Dialect/RTG/Transforms/EmitRTGISAAssemblyPass.cpp:96` (todo)

At `lib/Dialect/RTG/Transforms/EmitRTGISAAssemblyPass.cpp:96`, the marker `// TODO: we cannot just assume that double-slash is the way to do a line` indicates a gap signal in this path. What's missing: The marker documents unfinished work: we cannot just assume that double-slash is the way to do a line. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0816 `lib/Dialect/RTG/Transforms/EmitRTGISAAssemblyPass.cpp:120` (todo)

At `lib/Dialect/RTG/Transforms/EmitRTGISAAssemblyPass.cpp:120`, the marker `// TODO: don't hardcode '.word'` indicates a gap signal in this path. What's missing: The marker documents unfinished work: don't hardcode '.word'. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0817 `lib/Dialect/RTG/Transforms/EmitRTGISAAssemblyPass.cpp:191` (unclassified)

At `lib/Dialect/RTG/Transforms/EmitRTGISAAssemblyPass.cpp:191`, the marker `const DenseSet<StringAttr> &unsupportedInstr;` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0818 `lib/Dialect/RTG/Transforms/EmitRTGISAAssemblyPass.cpp:200` (unclassified)

At `lib/Dialect/RTG/Transforms/EmitRTGISAAssemblyPass.cpp:200`, the marker `parseUnsupportedInstructionsFile(MLIRContext *ctxt,` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0819 `lib/Dialect/RTG/Transforms/EmitRTGISAAssemblyPass.cpp:201` (unclassified)

At `lib/Dialect/RTG/Transforms/EmitRTGISAAssemblyPass.cpp:201`, the marker `const std::string &unsupportedInstructionsFile,` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0820 `lib/Dialect/RTG/Transforms/EmitRTGISAAssemblyPass.cpp:202` (unclassified)

At `lib/Dialect/RTG/Transforms/EmitRTGISAAssemblyPass.cpp:202`, the marker `DenseSet<StringAttr> &unsupportedInstrs) {` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0821 `lib/Dialect/RTG/Transforms/EmitRTGISAAssemblyPass.cpp:203` (unclassified)

At `lib/Dialect/RTG/Transforms/EmitRTGISAAssemblyPass.cpp:203`, the marker `if (!unsupportedInstructionsFile.empty()) {` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0822 `lib/Dialect/RTG/Transforms/EmitRTGISAAssemblyPass.cpp:204` (unclassified)

At `lib/Dialect/RTG/Transforms/EmitRTGISAAssemblyPass.cpp:204`, the marker `std::ifstream input(unsupportedInstructionsFile, std::ios::in);` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0823 `lib/Dialect/RTG/Transforms/EmitRTGISAAssemblyPass.cpp:209` (unclassified)

At `lib/Dialect/RTG/Transforms/EmitRTGISAAssemblyPass.cpp:209`, the marker `unsupportedInstrs.insert(StringAttr::get(ctxt, trimmed));` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0824 `lib/Dialect/RTG/Transforms/EmitRTGISAAssemblyPass.cpp:228` (unclassified)

At `lib/Dialect/RTG/Transforms/EmitRTGISAAssemblyPass.cpp:228`, the marker `DenseSet<StringAttr> unsupportedInstr;` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0825 `lib/Dialect/RTG/Transforms/EmitRTGISAAssemblyPass.cpp:229` (unclassified)

At `lib/Dialect/RTG/Transforms/EmitRTGISAAssemblyPass.cpp:229`, the marker `for (const auto &instr : unsupportedInstructions)` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0826 `lib/Dialect/RTG/Transforms/EmitRTGISAAssemblyPass.cpp:230` (unclassified)

At `lib/Dialect/RTG/Transforms/EmitRTGISAAssemblyPass.cpp:230`, the marker `unsupportedInstr.insert(StringAttr::get(&getContext(), instr));` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0827 `lib/Dialect/RTG/Transforms/EmitRTGISAAssemblyPass.cpp:231` (unclassified)

At `lib/Dialect/RTG/Transforms/EmitRTGISAAssemblyPass.cpp:231`, the marker `parseUnsupportedInstructionsFile(` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0828 `lib/Dialect/RTG/Transforms/EmitRTGISAAssemblyPass.cpp:232` (unclassified)

At `lib/Dialect/RTG/Transforms/EmitRTGISAAssemblyPass.cpp:232`, the marker `&getContext(), unsupportedInstructionsFile.getValue(), unsupportedInstr);` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0829 `lib/Dialect/RTG/Transforms/EmitRTGISAAssemblyPass.cpp:249` (unclassified)

At `lib/Dialect/RTG/Transforms/EmitRTGISAAssemblyPass.cpp:249`, the marker `unsupportedInstr);` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0830 `lib/Dialect/Synth/Transforms/CutRewriter.cpp:62` (todo)

At `lib/Dialect/Synth/Transforms/CutRewriter.cpp:62`, the marker `// TODO: Extend this to allow comb.and/xor/or as well.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Extend this to allow comb.and/xor/or as well.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0831 `lib/Dialect/Synth/Transforms/CutRewriter.cpp:87` (implementation-gap)

At `lib/Dialect/Synth/Transforms/CutRewriter.cpp:87`, the marker `"Unsupported operation for simulation. isSupportedLogicOp should "` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0832 `lib/Dialect/Synth/Transforms/CutRewriter.cpp:169` (implementation-gap)

At `lib/Dialect/Synth/Transforms/CutRewriter.cpp:169`, the marker `return op->emitError("Unsupported operation for truth table simulation");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0833 `lib/Dialect/Synth/Transforms/CutRewriter.cpp:174` (todo)

At `lib/Dialect/Synth/Transforms/CutRewriter.cpp:174`, the marker `// TODO: Currently numOutputs is always 1, so we can just return the first` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Currently numOutputs is always 1, so we can just return the first. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0834 `lib/Dialect/Synth/Transforms/CutRewriter.cpp:343` (todo)

At `lib/Dialect/Synth/Transforms/CutRewriter.cpp:343`, the marker `// TODO: Merge-sort `operations` and `other.operations` by operation index` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Merge-sort `operations` and `other.operations` by operation index. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0835 `lib/Dialect/Synth/Transforms/CutRewriter.cpp:402` (todo)

At `lib/Dialect/Synth/Transforms/CutRewriter.cpp:402`, the marker `// TODO: Sort the inputs by their defining operation.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Sort the inputs by their defining operation.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0836 `lib/Dialect/Synth/Transforms/CutRewriter.cpp:403` (todo)

At `lib/Dialect/Synth/Transforms/CutRewriter.cpp:403`, the marker `// TODO: Update area and delay based on the merged cuts.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Update area and delay based on the merged cuts.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0837 `lib/Dialect/Synth/Transforms/CutRewriter.cpp:552` (todo)

At `lib/Dialect/Synth/Transforms/CutRewriter.cpp:552`, the marker `// TODO: Use a priority queue instead of sorting for better performance.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Use a priority queue instead of sorting for better performance.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0838 `lib/Dialect/Synth/Transforms/CutRewriter.cpp:684` (todo)

At `lib/Dialect/Synth/Transforms/CutRewriter.cpp:684`, the marker `// TODO: Variadic operations and non-single-bit results can be supported` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Variadic operations and non-single-bit results can be supported. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0839 `lib/Dialect/Synth/Transforms/CutRewriter.cpp:887` (todo)

At `lib/Dialect/Synth/Transforms/CutRewriter.cpp:887`, the marker `// TODO: This must be removed when we support multiple outputs.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: This must be removed when we support multiple outputs.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0840 `lib/Dialect/Synth/Transforms/CutRewriter.cpp:957` (todo)

At `lib/Dialect/Synth/Transforms/CutRewriter.cpp:957`, the marker `// TODO: This doesn't consider a global delay. Need to capture` indicates a gap signal in this path. What's missing: The marker documents unfinished work: This doesn't consider a global delay. Need to capture. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0841 `lib/Dialect/Synth/Transforms/SynthesisPipeline.cpp:122` (todo)

At `lib/Dialect/Synth/Transforms/SynthesisPipeline.cpp:122`, the marker `// FIXME: The following is very small compared to the default value of ABC` indicates a gap signal in this path. What's missing: The marker documents unfinished work: The following is very small compared to the default value of ABC. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0842 `lib/Dialect/Synth/Transforms/SynthesisPipeline.cpp:139` (todo)

At `lib/Dialect/Synth/Transforms/SynthesisPipeline.cpp:139`, the marker `// TODO: Add balancing, rewriting, FRAIG conversion, etc.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Add balancing, rewriting, FRAIG conversion, etc.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0843 `lib/Conversion/CalyxNative/CalyxNative.cpp:158` (todo)

At `lib/Conversion/CalyxNative/CalyxNative.cpp:158`, the marker `// XXX(rachitnigam): This is quite baroque. We insert the new block before the` indicates a gap signal in this path. What's missing: The marker documents unfinished work: (rachitnigam): This is quite baroque. We insert the new block before the. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0844 `lib/Dialect/LLHD/Transforms/InlineCalls.cpp:246` (todo)

At `lib/Dialect/LLHD/Transforms/InlineCalls.cpp:246`, the marker `// TODO: Compute actual offsets from class metadata or pass them as` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Compute actual offsets from class metadata or pass them as. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0845 `lib/Dialect/LLHD/Transforms/InlineCalls.cpp:460` (implementation-gap)

At `lib/Dialect/LLHD/Transforms/InlineCalls.cpp:460`, the marker `"recursive function call cannot be inlined (unsupported in --ir-hw)");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0846 `lib/Dialect/LLHD/Transforms/InlineCalls.cpp:488` (implementation-gap)

At `lib/Dialect/LLHD/Transforms/InlineCalls.cpp:488`, the marker `"recursive function call cannot be inlined (unsupported in --ir-hw)");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0847 `lib/Conversion/CombToDatapath/CombToDatapath.cpp:63` (todo)

At `lib/Conversion/CombToDatapath/CombToDatapath.cpp:63`, the marker `// TODO: support for variadic multipliers` indicates a gap signal in this path. What's missing: The marker documents unfinished work: support for variadic multipliers. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0848 `lib/Conversion/CombToDatapath/CombToDatapath.cpp:107` (todo)

At `lib/Conversion/CombToDatapath/CombToDatapath.cpp:107`, the marker `// TODO: determine lowering of multi-input multipliers` indicates a gap signal in this path. What's missing: The marker documents unfinished work: determine lowering of multi-input multipliers. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0849 `lib/Conversion/HWToSMT/HWToSMT.cpp:305` (implementation-gap)

At `lib/Conversion/HWToSMT/HWToSMT.cpp:305`, the marker `op, "unsupported aggregate constant attribute/type combination");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0850 `lib/Conversion/HWToSMT/HWToSMT.cpp:441` (implementation-gap)

At `lib/Conversion/HWToSMT/HWToSMT.cpp:441`, the marker `"unsupported bitcast result type");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0851 `lib/Conversion/HWToSMT/HWToSMT.cpp:569` (implementation-gap)

At `lib/Conversion/HWToSMT/HWToSMT.cpp:569`, the marker `return rewriter.notifyMatchFailure(op.getLoc(), "unsupported array type");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0852 `lib/Conversion/HWToSMT/HWToSMT.cpp:601` (implementation-gap)

At `lib/Conversion/HWToSMT/HWToSMT.cpp:601`, the marker `"unsupported array element type");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0853 `lib/Conversion/HWToSMT/HWToSMT.cpp:641` (implementation-gap)

At `lib/Conversion/HWToSMT/HWToSMT.cpp:641`, the marker `return rewriter.notifyMatchFailure(op.getLoc(), "unsupported array type");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0854 `lib/Dialect/Synth/Transforms/TechMapper.cpp:135` (todo)

At `lib/Dialect/Synth/Transforms/TechMapper.cpp:135`, the marker `// TODO: Give a better name to the instance` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Give a better name to the instance. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0855 `lib/Dialect/Synth/Transforms/TechMapper.cpp:174` (todo)

At `lib/Dialect/Synth/Transforms/TechMapper.cpp:174`, the marker `// TODO: This attribute should be replaced with a more structured` indicates a gap signal in this path. What's missing: The marker documents unfinished work: This attribute should be replaced with a more structured. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0856 `lib/Dialect/Synth/Transforms/TechMapper.cpp:183` (todo)

At `lib/Dialect/Synth/Transforms/TechMapper.cpp:183`, the marker `// TODO: Run mapping only when the module is under the specific` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Run mapping only when the module is under the specific. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0857 `lib/Dialect/Synth/Transforms/TechMapper.cpp:201` (unclassified)

At `lib/Dialect/Synth/Transforms/TechMapper.cpp:201`, the marker `double area = areaAttr.getValue().convertToDouble();` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0858 `lib/Dialect/Synth/Transforms/TechMapper.cpp:207` (todo)

At `lib/Dialect/Synth/Transforms/TechMapper.cpp:207`, the marker `// FIXME: Currently we assume delay is given as integer attributes,` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Currently we assume delay is given as integer attributes,. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0859 `lib/Dialect/LLHD/Transforms/HoistSignals.cpp:652` (todo)

At `lib/Dialect/LLHD/Transforms/HoistSignals.cpp:652`, the marker `// TODO: This should probably create something like a `llhd.dontcare`.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: This should probably create something like a `llhd.dontcare`.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0860 `lib/Conversion/CombToArith/CombToArith.cpp:290` (todo)

At `lib/Conversion/CombToArith/CombToArith.cpp:290`, the marker `// TODO: building a tree would be better here` indicates a gap signal in this path. What's missing: The marker documents unfinished work: building a tree would be better here. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0861 `lib/Conversion/CombToArith/CombToArith.cpp:406` (todo)

At `lib/Conversion/CombToArith/CombToArith.cpp:406`, the marker `// TODO: a pattern for comb.parity` indicates a gap signal in this path. What's missing: The marker documents unfinished work: a pattern for comb.parity. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0862 `lib/Conversion/HWToLLVM/HWToLLVM.cpp:704` (todo)

At `lib/Conversion/HWToLLVM/HWToLLVM.cpp:704`, the marker `// TODO: Only arrays and structs supported at the moment.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Only arrays and structs supported at the moment.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0863 `lib/Dialect/Synth/Transforms/LowerWordToBits.cpp:189` (todo)

At `lib/Dialect/Synth/Transforms/LowerWordToBits.cpp:189`, the marker `// TODO: This is not optimal as it has a depth limit and does not check` indicates a gap signal in this path. What's missing: The marker documents unfinished work: This is not optimal as it has a depth limit and does not check. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0864 `lib/Dialect/Synth/Transforms/LowerVariadic.cpp:134` (todo)

At `lib/Dialect/Synth/Transforms/LowerVariadic.cpp:134`, the marker `// FIXME: Currently only top-level operations are lowered due to the lack of` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Currently only top-level operations are lowered due to the lack of. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0865 `lib/Conversion/VerifToSMT/VerifToSMT.cpp:4242` (implementation-gap)

At `lib/Conversion/VerifToSMT/VerifToSMT.cpp:4242`, the marker `op.emitError("unsupported sequence lowering for block argument");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0866 `lib/Conversion/VerifToSMT/VerifToSMT.cpp:5049` (todo)

At `lib/Conversion/VerifToSMT/VerifToSMT.cpp:5049`, the marker `// TODO: the init and loop regions should be able to be concrete instead of` indicates a gap signal in this path. What's missing: The marker documents unfinished work: the init and loop regions should be able to be concrete instead of. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0867 `lib/Conversion/VerifToSMT/VerifToSMT.cpp:6270` (implementation-gap)

At `lib/Conversion/VerifToSMT/VerifToSMT.cpp:6270`, the marker `op.emitError("unsupported boolean type in BMC conversion");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0868 `lib/Conversion/VerifToSMT/VerifToSMT.cpp:6418` (implementation-gap)

At `lib/Conversion/VerifToSMT/VerifToSMT.cpp:6418`, the marker `op.emitError("unsupported integer initial value in BMC conversion");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0869 `lib/Conversion/VerifToSMT/VerifToSMT.cpp:6435` (implementation-gap)

At `lib/Conversion/VerifToSMT/VerifToSMT.cpp:6435`, the marker `op.emitError("unsupported bool initial value in BMC conversion");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0870 `lib/Conversion/VerifToSMT/VerifToSMT.cpp:9607` (todo)

At `lib/Conversion/VerifToSMT/VerifToSMT.cpp:9607`, the marker `// TODO: swapping to a whileOp here would allow early exit once the property` indicates a gap signal in this path. What's missing: The marker documents unfinished work: swapping to a whileOp here would allow early exit once the property. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0871 `lib/Conversion/VerifToSMT/VerifToSMT.cpp:10084` (todo)

At `lib/Conversion/VerifToSMT/VerifToSMT.cpp:10084`, the marker `// TODO: we create a lot of ITEs here that will slow things down` indicates a gap signal in this path. What's missing: The marker documents unfinished work: we create a lot of ITEs here that will slow things down. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0872 `lib/Conversion/VerifToSMT/VerifToSMT.cpp:10388` (implementation-gap)

At `lib/Conversion/VerifToSMT/VerifToSMT.cpp:10388`, the marker `// Keep properties and enables in the live roots so unsupported ops that` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0873 `lib/Conversion/VerifToSMT/VerifToSMT.cpp:11001` (implementation-gap)

At `lib/Conversion/VerifToSMT/VerifToSMT.cpp:11001`, the marker `// path so they remain explicit unsupported-syntax diagnostics.` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0874 `lib/Conversion/VerifToSMT/VerifToSMT.cpp:11016` (implementation-gap)

At `lib/Conversion/VerifToSMT/VerifToSMT.cpp:11016`, the marker `// Keep non-scalar zeros on the generic unsupported-op diagnostics path.` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0875 `lib/Conversion/VerifToSMT/VerifToSMT.cpp:11763` (implementation-gap)

At `lib/Conversion/VerifToSMT/VerifToSMT.cpp:11763`, the marker `// Skip SMT-LIB unsupported-LLVM diagnostics in this case so unrelated` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0876 `lib/Conversion/VerifToSMT/VerifToSMT.cpp:11769` (unclassified)

At `lib/Conversion/VerifToSMT/VerifToSMT.cpp:11769`, the marker `Operation *unsupportedOp = nullptr;` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0877 `lib/Conversion/VerifToSMT/VerifToSMT.cpp:11780` (unclassified)

At `lib/Conversion/VerifToSMT/VerifToSMT.cpp:11780`, the marker `unsupportedOp = &nested;` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0878 `lib/Conversion/VerifToSMT/VerifToSMT.cpp:11783` (unclassified)

At `lib/Conversion/VerifToSMT/VerifToSMT.cpp:11783`, the marker `if (unsupportedOp)` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0879 `lib/Conversion/VerifToSMT/VerifToSMT.cpp:11786` (unclassified)

At `lib/Conversion/VerifToSMT/VerifToSMT.cpp:11786`, the marker `if (unsupportedOp)` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0880 `lib/Conversion/VerifToSMT/VerifToSMT.cpp:11789` (unclassified)

At `lib/Conversion/VerifToSMT/VerifToSMT.cpp:11789`, the marker `if (unsupportedOp) {` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0881 `lib/Conversion/VerifToSMT/VerifToSMT.cpp:11790` (unclassified)

At `lib/Conversion/VerifToSMT/VerifToSMT.cpp:11790`, the marker `unsupportedOp->emitError(` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0882 `lib/Conversion/VerifToSMT/VerifToSMT.cpp:11793` (unclassified)

At `lib/Conversion/VerifToSMT/VerifToSMT.cpp:11793`, the marker `<< unsupportedOp->getName() << "'";` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0883 `lib/Conversion/VerifToSMT/VerifToSMT.cpp:11828` (implementation-gap)

At `lib/Conversion/VerifToSMT/VerifToSMT.cpp:11828`, the marker `op->emitError("unsupported initial value for register");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0884 `lib/Conversion/VerifToSMT/VerifToSMT.cpp:11838` (todo)

At `lib/Conversion/VerifToSMT/VerifToSMT.cpp:11838`, the marker `// TODO: this can be removed once we have a way to associate reg` indicates a gap signal in this path. What's missing: The marker documents unfinished work: this can be removed once we have a way to associate reg. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0885 `lib/Conversion/CFToHandshake/CFToHandshake.cpp:726` (todo)

At `lib/Conversion/CFToHandshake/CFToHandshake.cpp:726`, the marker `// TODO how to size these?` indicates a gap signal in this path. What's missing: The marker documents unfinished work: how to size these?. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0886 `lib/Dialect/Synth/Transforms/AIGERRunner.cpp:207` (todo)

At `lib/Dialect/Synth/Transforms/AIGERRunner.cpp:207`, the marker `// TODO: Consider caching extract operations for efficiency` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Consider caching extract operations for efficiency. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0887 `lib/Dialect/RTG/IR/RTGAttributes.cpp:26` (todo)

At `lib/Dialect/RTG/IR/RTGAttributes.cpp:26`, the marker `// TODO: improve collision resistance` indicates a gap signal in this path. What's missing: The marker documents unfinished work: improve collision resistance. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0888 `lib/Conversion/HWToBTOR2/HWToBTOR2.cpp:956` (unclassified)

At `lib/Conversion/HWToBTOR2/HWToBTOR2.cpp:956`, the marker `.Default([&](auto expr) { visitUnsupportedOp(op); });` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0889 `lib/Conversion/HWToBTOR2/HWToBTOR2.cpp:1025` (unclassified)

At `lib/Conversion/HWToBTOR2/HWToBTOR2.cpp:1025`, the marker `void visitUnsupportedOp(Operation *op) {` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0890 `lib/Conversion/HWToBTOR2/HWToBTOR2.cpp:1026` (implementation-gap)

At `lib/Conversion/HWToBTOR2/HWToBTOR2.cpp:1026`, the marker `// Check for explicitly ignored ops vs unsupported ops (which cause a` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0891 `lib/Conversion/HWToBTOR2/HWToBTOR2.cpp:1048` (implementation-gap)

At `lib/Conversion/HWToBTOR2/HWToBTOR2.cpp:1048`, the marker `// Anything else is considered unsupported and might cause a wrong` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0892 `lib/Conversion/HWToBTOR2/HWToBTOR2.cpp:1051` (implementation-gap)

At `lib/Conversion/HWToBTOR2/HWToBTOR2.cpp:1051`, the marker `op->emitOpError("is an unsupported operation");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0893 `lib/Dialect/Synth/SynthOps.cpp:68` (todo)

At `lib/Dialect/Synth/SynthOps.cpp:68`, the marker `// TODO: Implement maj(x, 1, 1) = 1, maj(x, 0, 0) = 0` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Implement maj(x, 1, 1) = 1, maj(x, 0, 0) = 0. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0894 `lib/Dialect/Synth/SynthOps.cpp:298` (todo)

At `lib/Dialect/Synth/SynthOps.cpp:298`, the marker `// TODO: This is a naive implementation that creates a balanced binary tree.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: This is a naive implementation that creates a balanced binary tree.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0895 `lib/Conversion/SimToSV/SimToSV.cpp:240` (todo)

At `lib/Conversion/SimToSV/SimToSV.cpp:240`, the marker `// TODO: If there is a return value and no output argument, use an` indicates a gap signal in this path. What's missing: The marker documents unfinished work: If there is a return value and no output argument, use an. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0896 `lib/Dialect/LLHD/IR/LLHDOps.cpp:917` (implementation-gap)

At `lib/Dialect/LLHD/IR/LLHDOps.cpp:917`, the marker `llvm_unreachable("Not implemented");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0897 `lib/Conversion/ArcToLLVM/LowerArcToLLVM.cpp:389` (todo)

At `lib/Conversion/ArcToLLVM/LowerArcToLLVM.cpp:389`, the marker `// FIXME: like the rest of MLIR, this assumes sizeof(intptr_t) ==` indicates a gap signal in this path. What's missing: The marker documents unfinished work: like the rest of MLIR, this assumes sizeof(intptr_t) ==. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0898 `lib/Conversion/ArcToLLVM/LowerArcToLLVM.cpp:940` (implementation-gap)

At `lib/Conversion/ArcToLLVM/LowerArcToLLVM.cpp:940`, the marker `// For unsupported format operations, emit a placeholder.` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0899 `lib/Conversion/ArcToLLVM/LowerArcToLLVM.cpp:941` (implementation-gap)

At `lib/Conversion/ArcToLLVM/LowerArcToLLVM.cpp:941`, the marker `formatStr.append("<unsupported>");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0900 `lib/Dialect/HW/Transforms/HWAggregateToComb.cpp:340` (todo)

At `lib/Dialect/HW/Transforms/HWAggregateToComb.cpp:340`, the marker `// TODO: Add ArraySliceOp as well.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Add ArraySliceOp as well.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0901 `lib/Conversion/SeqToSV/SeqToSV.cpp:763` (todo)

At `lib/Conversion/SeqToSV/SeqToSV.cpp:763`, the marker `// TODO: We could have an operation for macros and uses of them, and` indicates a gap signal in this path. What's missing: The marker documents unfinished work: We could have an operation for macros and uses of them, and. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0902 `lib/Conversion/AffineToLoopSchedule/AffineToLoopSchedule.cpp:154` (todo)

At `lib/Conversion/AffineToLoopSchedule/AffineToLoopSchedule.cpp:154`, the marker `/// TODO(mikeurbach): this is copied from AffineToStandard, see if we can reuse.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: (mikeurbach): this is copied from AffineToStandard, see if we can reuse.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0903 `lib/Conversion/AffineToLoopSchedule/AffineToLoopSchedule.cpp:188` (todo)

At `lib/Conversion/AffineToLoopSchedule/AffineToLoopSchedule.cpp:188`, the marker `/// TODO(mikeurbach): this is copied from AffineToStandard, see if we can reuse.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: (mikeurbach): this is copied from AffineToStandard, see if we can reuse.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0904 `lib/Conversion/AffineToLoopSchedule/AffineToLoopSchedule.cpp:304` (implementation-gap)

At `lib/Conversion/AffineToLoopSchedule/AffineToLoopSchedule.cpp:304`, the marker `Operation *unsupported;` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0905 `lib/Conversion/AffineToLoopSchedule/AffineToLoopSchedule.cpp:365` (implementation-gap)

At `lib/Conversion/AffineToLoopSchedule/AffineToLoopSchedule.cpp:365`, the marker `unsupported = op;` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0906 `lib/Conversion/AffineToLoopSchedule/AffineToLoopSchedule.cpp:371` (implementation-gap)

At `lib/Conversion/AffineToLoopSchedule/AffineToLoopSchedule.cpp:371`, the marker `return forOp.emitError("unsupported operation ") << *unsupported;` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0907 `lib/Conversion/FSMToSV/FSMToSV.cpp:342` (implementation-gap)

At `lib/Conversion/FSMToSV/FSMToSV.cpp:342`, the marker `<< "is unsupported (op from the "` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0908 `lib/Dialect/Synth/Analysis/LongestPathAnalysis.cpp:211` (todo)

At `lib/Dialect/Synth/Analysis/LongestPathAnalysis.cpp:211`, the marker `// TODO: Handle other operations.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Handle other operations.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0909 `lib/Dialect/Synth/Analysis/LongestPathAnalysis.cpp:729` (todo)

At `lib/Dialect/Synth/Analysis/LongestPathAnalysis.cpp:729`, the marker `// TODO: Make debug points optional.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Make debug points optional.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0910 `lib/Dialect/Synth/Analysis/LongestPathAnalysis.cpp:1307` (todo)

At `lib/Dialect/Synth/Analysis/LongestPathAnalysis.cpp:1307`, the marker `// TODO: Add address.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Add address.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0911 `lib/Dialect/Synth/Analysis/LongestPathAnalysis.cpp:1312` (todo)

At `lib/Dialect/Synth/Analysis/LongestPathAnalysis.cpp:1312`, the marker `// TODO: Add address.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Add address.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0912 `lib/Dialect/Synth/Analysis/LongestPathAnalysis.cpp:1383` (implementation-gap)

At `lib/Dialect/Synth/Analysis/LongestPathAnalysis.cpp:1383`, the marker `return Type(); // Unsupported type` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0913 `lib/Dialect/Synth/Analysis/LongestPathAnalysis.cpp:1400` (implementation-gap)

At `lib/Dialect/Synth/Analysis/LongestPathAnalysis.cpp:1400`, the marker `return failure(); // Unsupported operand type` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0914 `lib/Dialect/Synth/Analysis/LongestPathAnalysis.cpp:1409` (implementation-gap)

At `lib/Dialect/Synth/Analysis/LongestPathAnalysis.cpp:1409`, the marker `return failure(); // Unsupported result type` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0915 `lib/Dialect/HW/Transforms/HWStopatSymbolic.cpp:68` (implementation-gap)

At `lib/Dialect/HW/Transforms/HWStopatSymbolic.cpp:68`, the marker `error = "unsupported selector form (expected 'inst[.inst...].port' or '*inst[.inst...].port')";` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0916 `lib/Dialect/Pipeline/PipelineOps.cpp:719` (todo)

At `lib/Dialect/Pipeline/PipelineOps.cpp:719`, the marker `// TODO: This should be a pre-computed property.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: This should be a pre-computed property.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0917 `lib/Dialect/Pipeline/PipelineOps.cpp:980` (todo)

At `lib/Dialect/Pipeline/PipelineOps.cpp:980`, the marker `// TODO: This could be optimized quite a bit if we didn't store clock` indicates a gap signal in this path. What's missing: The marker documents unfinished work: This could be optimized quite a bit if we didn't store clock. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0918 `lib/Conversion/FSMToCore/FSMToCore.cpp:428` (implementation-gap)

At `lib/Conversion/FSMToCore/FSMToCore.cpp:428`, the marker `<< "is unsupported (op from the "` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0919 `lib/Conversion/SeqToSV/FirRegLowering.cpp:466` (todo)

At `lib/Conversion/SeqToSV/FirRegLowering.cpp:466`, the marker `// TODO: Handle struct if necessary.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Handle struct if necessary.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0920 `lib/Conversion/SeqToSV/FirRegLowering.cpp:793` (implementation-gap)

At `lib/Conversion/SeqToSV/FirRegLowering.cpp:793`, the marker `assert(false && "unsupported type");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0921 `lib/Dialect/Kanagawa/Transforms/KanagawaPortrefLowering.cpp:97` (todo)

At `lib/Dialect/Kanagawa/Transforms/KanagawaPortrefLowering.cpp:97`, the marker `// TODO: RewriterBase::replaceAllUsesWith is not currently supported by` indicates a gap signal in this path. What's missing: The marker documents unfinished work: RewriterBase::replaceAllUsesWith is not currently supported by. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0922 `lib/Dialect/Kanagawa/Transforms/KanagawaPortrefLowering.cpp:319` (todo)

At `lib/Dialect/Kanagawa/Transforms/KanagawaPortrefLowering.cpp:319`, the marker `// TODO: RewriterBase::replaceAllUsesWith is not currently supported` indicates a gap signal in this path. What's missing: The marker documents unfinished work: RewriterBase::replaceAllUsesWith is not currently supported. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0923 `lib/Dialect/Kanagawa/Transforms/KanagawaPortrefLowering.cpp:359` (todo)

At `lib/Dialect/Kanagawa/Transforms/KanagawaPortrefLowering.cpp:359`, the marker `// TODO: RewriterBase::replaceAllUsesWith is not currently supported by` indicates a gap signal in this path. What's missing: The marker documents unfinished work: RewriterBase::replaceAllUsesWith is not currently supported by. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0924 `lib/Dialect/Kanagawa/Transforms/KanagawaPassPipelines.cpp:62` (todo)

At `lib/Dialect/Kanagawa/Transforms/KanagawaPassPipelines.cpp:62`, the marker `// TODO @mortbopet: Add a verification pass to ensure that there are no more` indicates a gap signal in this path. What's missing: The marker documents unfinished work: @mortbopet: Add a verification pass to ensure that there are no more. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0925 `lib/Conversion/SeqToSV/FirMemLowering.cpp:42` (todo)

At `lib/Conversion/SeqToSV/FirMemLowering.cpp:42`, the marker `// TODO: Check if this module is in the DUT hierarchy.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Check if this module is in the DUT hierarchy.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0926 `lib/Conversion/SeqToSV/FirMemLowering.cpp:89` (todo)

At `lib/Conversion/SeqToSV/FirMemLowering.cpp:89`, the marker `// TODO: Handle modName (maybe not?)` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Handle modName (maybe not?). The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0927 `lib/Conversion/SeqToSV/FirMemLowering.cpp:90` (todo)

At `lib/Conversion/SeqToSV/FirMemLowering.cpp:90`, the marker `// TODO: Handle groupID (maybe not?)` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Handle groupID (maybe not?). The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0928 `lib/Dialect/HW/Transforms/HWConvertBitcasts.cpp:55` (todo)

At `lib/Dialect/HW/Transforms/HWConvertBitcasts.cpp:55`, the marker `// TODO: Add support for: union, packed array, enum` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Add support for: union, packed array, enum. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0929 `lib/Dialect/HW/Transforms/HWConvertBitcasts.cpp:96` (implementation-gap)

At `lib/Dialect/HW/Transforms/HWConvertBitcasts.cpp:96`, the marker `assert(false && "Unsupported type");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0930 `lib/Dialect/HW/Transforms/HWConvertBitcasts.cpp:167` (implementation-gap)

At `lib/Dialect/HW/Transforms/HWConvertBitcasts.cpp:167`, the marker `assert(false && "Unsupported type");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0931 `lib/Dialect/HW/Transforms/HWConvertBitcasts.cpp:177` (implementation-gap)

At `lib/Dialect/HW/Transforms/HWConvertBitcasts.cpp:177`, the marker `bitcastOp.emitOpError("has unsupported input type");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0932 `lib/Dialect/HW/Transforms/HWConvertBitcasts.cpp:179` (implementation-gap)

At `lib/Dialect/HW/Transforms/HWConvertBitcasts.cpp:179`, the marker `bitcastOp.emitOpError("has unsupported output type");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0933 `lib/Conversion/SMTToZ3LLVM/LowerSMTToZ3LLVM.cpp:1387` (todo)

At `lib/Conversion/SMTToZ3LLVM/LowerSMTToZ3LLVM.cpp:1387`, the marker `// FIXME: ideally we don't want to use i1 here, since bools can sometimes be` indicates a gap signal in this path. What's missing: The marker documents unfinished work: ideally we don't want to use i1 here, since bools can sometimes be. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0934 `lib/Dialect/Kanagawa/Transforms/KanagawaContainersToHW.cpp:293` (todo)

At `lib/Dialect/Kanagawa/Transforms/KanagawaContainersToHW.cpp:293`, the marker `// TODO: @mortbopet - this should be part of ModulePortInfo` indicates a gap signal in this path. What's missing: The marker documents unfinished work: @mortbopet - this should be part of ModulePortInfo. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0935 `lib/Dialect/Kanagawa/Transforms/KanagawaContainersToHW.cpp:314` (todo)

At `lib/Dialect/Kanagawa/Transforms/KanagawaContainersToHW.cpp:314`, the marker `// TODO: RewriterBase::replaceAllUsesWith is not currently supported by` indicates a gap signal in this path. What's missing: The marker documents unfinished work: RewriterBase::replaceAllUsesWith is not currently supported by. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0936 `lib/Dialect/HW/ModuleImplementation.cpp:157` (todo)

At `lib/Dialect/HW/ModuleImplementation.cpp:157`, the marker `// TODO: `printOptionalLocationSpecifier` will emit aliases for locations,` indicates a gap signal in this path. What's missing: The marker documents unfinished work: `printOptionalLocationSpecifier` will emit aliases for locations,. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0937 `lib/Dialect/HW/ModuleImplementation.cpp:189` (todo)

At `lib/Dialect/HW/ModuleImplementation.cpp:189`, the marker `// TODO: `printOptionalLocationSpecifier` will emit aliases for locations,` indicates a gap signal in this path. What's missing: The marker documents unfinished work: `printOptionalLocationSpecifier` will emit aliases for locations,. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0938 `lib/Dialect/HW/ModuleImplementation.cpp:441` (todo)

At `lib/Dialect/HW/ModuleImplementation.cpp:441`, the marker `// TODO: `printOptionalLocationSpecifier` will emit aliases for locations,` indicates a gap signal in this path. What's missing: The marker documents unfinished work: `printOptionalLocationSpecifier` will emit aliases for locations,. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0939 `lib/Dialect/Kanagawa/Transforms/KanagawaAddOperatorLibrary.cpp:46` (todo)

At `lib/Dialect/Kanagawa/Transforms/KanagawaAddOperatorLibrary.cpp:46`, the marker `// TODO @mortbopet: once we have c++20, change this to a templated lambda to` indicates a gap signal in this path. What's missing: The marker documents unfinished work: @mortbopet: once we have c++20, change this to a templated lambda to. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0940 `lib/Dialect/OM/Transforms/FreezePaths.cpp:170` (todo)

At `lib/Dialect/OM/Transforms/FreezePaths.cpp:170`, the marker `// TODO: add support for instance choices.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: add support for instance choices.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0941 `lib/Dialect/OM/Transforms/FreezePaths.cpp:173` (implementation-gap)

At `lib/Dialect/OM/Transforms/FreezePaths.cpp:173`, the marker `return op->emitError("unsupported instance operation");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0942 `lib/Dialect/HW/HWTypes.cpp:595` (implementation-gap)

At `lib/Dialect/HW/HWTypes.cpp:595`, the marker `p.emitError(p.getNameLoc(), "unsupported dimension kind in hw.array");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0943 `lib/Conversion/SCFToCalyx/SCFToCalyx.cpp:1480` (implementation-gap)

At `lib/Conversion/SCFToCalyx/SCFToCalyx.cpp:1480`, the marker `<< "Unsupported empty yieldOp outside ForOp or IfOp.";` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0944 `lib/Conversion/SCFToCalyx/SCFToCalyx.cpp:1683` (implementation-gap)

At `lib/Conversion/SCFToCalyx/SCFToCalyx.cpp:1683`, the marker `llvm_unreachable("unsupported comparison predicate");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0945 `lib/Conversion/SCFToCalyx/SCFToCalyx.cpp:1833` (todo)

At `lib/Conversion/SCFToCalyx/SCFToCalyx.cpp:1833`, the marker `/// TODO(#1850) evaluate the usefulness of this lowering pattern.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: (#1850) evaluate the usefulness of this lowering pattern.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0946 `lib/Conversion/SCFToCalyx/SCFToCalyx.cpp:2029` (todo)

At `lib/Conversion/SCFToCalyx/SCFToCalyx.cpp:2029`, the marker `/// TODO(mortbopet) can we support these? for now, do not support loops` indicates a gap signal in this path. What's missing: The marker documents unfinished work: (mortbopet) can we support these? for now, do not support loops. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0947 `lib/Conversion/SCFToCalyx/SCFToCalyx.cpp:2494` (todo)

At `lib/Conversion/SCFToCalyx/SCFToCalyx.cpp:2494`, the marker `/// TODO(mortbopet): we could choose to support ie. std.switch, but it` indicates a gap signal in this path. What's missing: The marker documents unfinished work: (mortbopet): we could choose to support ie. std.switch, but it. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0948 `lib/Conversion/SCFToCalyx/SCFToCalyx.cpp:2861` (todo)

At `lib/Conversion/SCFToCalyx/SCFToCalyx.cpp:2861`, the marker `// TODO (https://github.com/llvm/circt/issues/7764)` indicates a gap signal in this path. What's missing: The marker documents unfinished work: (https://github.com/llvm/circt/issues/7764). The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0949 `lib/Conversion/SCFToCalyx/SCFToCalyx.cpp:2878` (implementation-gap)

At `lib/Conversion/SCFToCalyx/SCFToCalyx.cpp:2878`, the marker `llvm::report_fatal_error("Unsupported operation in TypeSwitch");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0950 `test/Tools/circt-sim/syscall-save-restart-warning.sv:5` (test-gap-reference)

At `test/Tools/circt-sim/syscall-save-restart-warning.sv:5`, the marker `// An unimplemented $save should at minimum warn the user so they know` indicates a gap signal in this path. What's missing: This test line references an unsupported behavior, indicating a known gap in upstream implementation coverage. How to fix: Implement the missing behavior in production code and update this test to assert functional semantics rather than unsupported status once support is complete.

### G0951 `lib/Conversion/FIRRTLToHW/LowerToHW.cpp:586` (unclassified)

At `lib/Conversion/FIRRTLToHW/LowerToHW.cpp:586`, the marker `// If the accompanying pass runs on the HW dialect, then LowerToHW` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0952 `lib/Conversion/FIRRTLToHW/LowerToHW.cpp:1136` (todo)

At `lib/Conversion/FIRRTLToHW/LowerToHW.cpp:1136`, the marker `// TODO: handle this in some sensible way based on what the SFC does with` indicates a gap signal in this path. What's missing: The marker documents unfinished work: handle this in some sensible way based on what the SFC does with. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0953 `lib/Conversion/FIRRTLToHW/LowerToHW.cpp:1151` (todo)

At `lib/Conversion/FIRRTLToHW/LowerToHW.cpp:1151`, the marker `// TODO: handle this with annotation verification.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: handle this with annotation verification.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0954 `lib/Conversion/FIRRTLToHW/LowerToHW.cpp:1165` (todo)

At `lib/Conversion/FIRRTLToHW/LowerToHW.cpp:1165`, the marker `// TODO: this error behavior can be relaxed to always overwrite with the` indicates a gap signal in this path. What's missing: The marker documents unfinished work: this error behavior can be relaxed to always overwrite with the. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0955 `lib/Conversion/FIRRTLToHW/LowerToHW.cpp:1565` (todo)

At `lib/Conversion/FIRRTLToHW/LowerToHW.cpp:1565`, the marker `return {}; // TODO: Emit an sv.constant here since it is unconnected.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Emit an sv.constant here since it is unconnected.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0956 `lib/Conversion/FIRRTLToHW/LowerToHW.cpp:1858` (todo)

At `lib/Conversion/FIRRTLToHW/LowerToHW.cpp:1858`, the marker `// TODO: we should be checking for symbol collisions here and renaming as` indicates a gap signal in this path. What's missing: The marker documents unfinished work: we should be checking for symbol collisions here and renaming as. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0957 `lib/Conversion/FIRRTLToHW/LowerToHW.cpp:2611` (todo)

At `lib/Conversion/FIRRTLToHW/LowerToHW.cpp:2611`, the marker `// TODO: We don't support partial connects for bundles for now.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: We don't support partial connects for bundles for now.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0958 `lib/Conversion/FIRRTLToHW/LowerToHW.cpp:2856` (todo)

At `lib/Conversion/FIRRTLToHW/LowerToHW.cpp:2856`, the marker `// TODO: This is not printf specific anymore. Replace "Printf" with "FD"` indicates a gap signal in this path. What's missing: The marker documents unfinished work: This is not printf specific anymore. Replace "Printf" with "FD". The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0959 `lib/Conversion/FIRRTLToHW/LowerToHW.cpp:3700` (todo)

At `lib/Conversion/FIRRTLToHW/LowerToHW.cpp:3700`, the marker `// TODO: Remove this restriction and preserve aggregates in` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Remove this restriction and preserve aggregates in. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0960 `lib/Conversion/FIRRTLToHW/LowerToHW.cpp:4224` (todo)

At `lib/Conversion/FIRRTLToHW/LowerToHW.cpp:4224`, the marker `// TODO: Introduce elementwise operations to HW dialect instead of abusing` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Introduce elementwise operations to HW dialect instead of abusing. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0961 `lib/Conversion/FIRRTLToHW/LowerToHW.cpp:4560` (todo)

At `lib/Conversion/FIRRTLToHW/LowerToHW.cpp:4560`, the marker `// We lower invalid to 0. TODO: the FIRRTL spec mentions something about` indicates a gap signal in this path. What's missing: The marker documents unfinished work: the FIRRTL spec mentions something about. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0962 `lib/Conversion/FIRRTLToHW/LowerToHW.cpp:4576` (implementation-gap)

At `lib/Conversion/FIRRTLToHW/LowerToHW.cpp:4576`, the marker `op.emitOpError("unsupported type");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0963 `lib/Conversion/FIRRTLToHW/LowerToHW.cpp:5099` (implementation-gap)

At `lib/Conversion/FIRRTLToHW/LowerToHW.cpp:5099`, the marker `emitError(loc, "has a substitution with an unimplemented "` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0964 `lib/Conversion/FIRRTLToHW/LowerToHW.cpp:5102` (implementation-gap)

At `lib/Conversion/FIRRTLToHW/LowerToHW.cpp:5102`, the marker `<< "op with an unimplemented lowering is here";` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0965 `lib/Conversion/FIRRTLToHW/LowerToHW.cpp:5292` (todo)

At `lib/Conversion/FIRRTLToHW/LowerToHW.cpp:5292`, the marker `// TODO: This should *not* be part of the op, but rather a lowering` indicates a gap signal in this path. What's missing: The marker documents unfinished work: This should *not* be part of the op, but rather a lowering. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0966 `lib/Conversion/FIRRTLToHW/LowerToHW.cpp:5444` (todo)

At `lib/Conversion/FIRRTLToHW/LowerToHW.cpp:5444`, the marker `// TODO : Need to figure out if there is a cleaner way to get the string which` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Need to figure out if there is a cleaner way to get the string which. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0967 `lib/Dialect/OM/Evaluator/Evaluator.cpp:208` (todo)

At `lib/Dialect/OM/Evaluator/Evaluator.cpp:208`, the marker `// FIXME: `diag << actualParams` doesn't work for some reason.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: `diag << actualParams` doesn't work for some reason.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0968 `lib/Dialect/Sim/SimOps.cpp:97` (unclassified)

At `lib/Dialect/Sim/SimOps.cpp:97`, the marker `floatAttr.getValue().convertToDouble());` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0969 `lib/Dialect/Sim/SimOps.cpp:100` (unclassified)

At `lib/Dialect/Sim/SimOps.cpp:100`, the marker `floatAttr.getValue().convertToDouble());` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0970 `lib/Dialect/Handshake/Transforms/Materialization.cpp:83` (todo)

At `lib/Dialect/Handshake/Transforms/Materialization.cpp:83`, the marker `// TODO: should we use other indicator for op that has been erased?` indicates a gap signal in this path. What's missing: The marker documents unfinished work: should we use other indicator for op that has been erased?. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0971 `lib/Dialect/FIRRTL/FIRRTLUtils.cpp:322` (todo)

At `lib/Dialect/FIRRTL/FIRRTLUtils.cpp:322`, the marker `// TODO: If needed, this could be modified to look through unary ops which` indicates a gap signal in this path. What's missing: The marker documents unfinished work: If needed, this could be modified to look through unary ops which. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0972 `lib/Dialect/FIRRTL/FIRRTLUtils.cpp:333` (todo)

At `lib/Dialect/FIRRTL/FIRRTLUtils.cpp:333`, the marker `// TODO: what do we want to happen when there are flips in the type? Do we` indicates a gap signal in this path. What's missing: The marker documents unfinished work: what do we want to happen when there are flips in the type? Do we. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0973 `lib/Dialect/FIRRTL/FIRRTLUtils.cpp:452` (todo)

At `lib/Dialect/FIRRTL/FIRRTLUtils.cpp:452`, the marker `// TODO: If needed, this could be modified to look through unary ops which` indicates a gap signal in this path. What's missing: The marker documents unfinished work: If needed, this could be modified to look through unary ops which. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0974 `lib/Dialect/FIRRTL/FIRRTLUtils.cpp:688` (implementation-gap)

At `lib/Dialect/FIRRTL/FIRRTLUtils.cpp:688`, the marker `llvm_unreachable("unsupported type");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0975 `lib/Dialect/FIRRTL/FIRRTLUtils.cpp:726` (todo)

At `lib/Dialect/FIRRTL/FIRRTLUtils.cpp:726`, the marker `// TODO: Plumb error case out and handle in callers.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Plumb error case out and handle in callers.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0976 `lib/Dialect/FIRRTL/FIRRTLUtils.cpp:768` (todo)

At `lib/Dialect/FIRRTL/FIRRTLUtils.cpp:768`, the marker `// TODO: are enums aggregates or not? Where is walkGroundTypes called` indicates a gap signal in this path. What's missing: The marker documents unfinished work: are enums aggregates or not? Where is walkGroundTypes called. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0977 `lib/Dialect/FIRRTL/FIRRTLUtils.cpp:827` (todo)

At `lib/Dialect/FIRRTL/FIRRTLUtils.cpp:827`, the marker `// TODO: store/ensure always sorted, insert directly, faster search.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: store/ensure always sorted, insert directly, faster search.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0978 `lib/Dialect/Handshake/Transforms/LockFunctions.cpp:61` (todo)

At `lib/Dialect/Handshake/Transforms/LockFunctions.cpp:61`, the marker `// TODO is this UB?` indicates a gap signal in this path. What's missing: The marker documents unfinished work: is this UB?. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0979 `lib/Dialect/Handshake/HandshakeUtils.cpp:276` (todo)

At `lib/Dialect/Handshake/HandshakeUtils.cpp:276`, the marker `// todo: change when handshake switches to i0` indicates a gap signal in this path. What's missing: The marker documents unfinished work: change when handshake switches to i0. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0980 `test/Tools/run-formal-all-opentitan-connectivity-contract-parity-fail.test:7` (test-expectation)

At `test/Tools/run-formal-all-opentitan-connectivity-contract-parity-fail.test:7`, the marker `// RUN: printf '#!/usr/bin/env bash\nset -euo pipefail\n: "${OUT:?}"\n: "${LEC_RESOLVED_CONTRACTS_OUT:?}"\nprintf "PASS\\tconnectivity::chip.csv:RULE_CLK\\t%t/out/opentitan-connectivity-lec-work\\topentitan\\tCONNECTIVITY_LEC\\tEQ\\n" > "$OUT"\nprintf "#resolved_contract_schema_version=1\\nconnectivity::chip.csv:RULE_CLK\\t%t/ot/hw/top_earlgrey/formal/conn_csvs/chip.csv:2\\tmanifest\\tsmtlib\\t300\\tlec\\t0\\t0\\t\\txxxx9999xxxx9999\\n" > "$LEC_RESOLVED_CONTRACTS_OUT"\nprintf "rule_id\\tcase_total\\tcase_pass\\tcase_fail\\tcase_xfail\\tcase_xpass\\tcase_error\\tcase_skip\\nchip.csv:RULE_CLK\\t1\\t1\\t0\\t0\\t0\\t0\\t0\\n" > "${LEC_CONNECTIVITY_STATUS_SUMMARY_OUT:-/dev/null}"\n' > %t/utils/run_opentitan_connectivity_circt_lec.py` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0981 `test/Tools/run-formal-all-opentitan-connectivity-contract-parity-fail.test:24` (unclassified)

At `test/Tools/run-formal-all-opentitan-connectivity-contract-parity-fail.test:24`, the marker `// PARITY: connectivity::chip.csv:RULE_CLK{{[[:space:]]+}}xxxx9999xxxx9999{{[[:space:]]+}}missing_in_bmc{{[[:space:]]+}}absent{{[[:space:]]+}}present{{[[:space:]]+}}0` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0982 `lib/Dialect/Handshake/HandshakeOps.cpp:95` (implementation-gap)

At `lib/Dialect/Handshake/HandshakeOps.cpp:95`, the marker `return op->emitError("unsupported type for indexing value: ") << indexType;` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0983 `test/Tools/check-opentitan-connectivity-contract-fingerprint-parity-allowlist.test:4` (test-expectation)

At `test/Tools/check-opentitan-connectivity-contract-fingerprint-parity-allowlist.test:4`, the marker `// RUN: printf '#resolved_contract_schema_version=1\nconnectivity::chip.csv:RULE_CLK\t/tmp/chip.csv:2\tmanifest\tsmtlib\t300\tlec\t0\t0\t\txxxx9999xxxx9999\n' > %t/lec.tsv` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0984 `test/Tools/check-opentitan-connectivity-contract-fingerprint-parity-allowlist.test:5` (test-expectation)

At `test/Tools/check-opentitan-connectivity-contract-fingerprint-parity-allowlist.test:5`, the marker `// RUN: printf 'exact:connectivity::chip.csv:RULE_CLK::aaaa1111aaaa1111\nexact:connectivity::chip.csv:RULE_CLK::xxxx9999xxxx9999\n' > %t/allow.txt` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0985 `test/Tools/check-opentitan-connectivity-contract-fingerprint-parity-allowlist.test:12` (unclassified)

At `test/Tools/check-opentitan-connectivity-contract-fingerprint-parity-allowlist.test:12`, the marker `// PARITY: connectivity::chip.csv:RULE_CLK{{[[:space:]]+}}xxxx9999xxxx9999{{[[:space:]]+}}missing_in_bmc{{[[:space:]]+}}absent{{[[:space:]]+}}present{{[[:space:]]+}}1` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0986 `test/Tools/check-opentitan-connectivity-contract-fingerprint-parity-fail.test:4` (test-expectation)

At `test/Tools/check-opentitan-connectivity-contract-fingerprint-parity-fail.test:4`, the marker `// RUN: printf '#resolved_contract_schema_version=1\nconnectivity::chip.csv:RULE_CLK\t/tmp/chip.csv:2\tmanifest\tsmtlib\t300\tlec\t0\t0\t\txxxx9999xxxx9999\n' > %t/lec.tsv` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G0987 `test/Tools/check-opentitan-connectivity-contract-fingerprint-parity-fail.test:12` (unclassified)

At `test/Tools/check-opentitan-connectivity-contract-fingerprint-parity-fail.test:12`, the marker `// PARITY: connectivity::chip.csv:RULE_CLK{{[[:space:]]+}}xxxx9999xxxx9999{{[[:space:]]+}}missing_in_bmc{{[[:space:]]+}}absent{{[[:space:]]+}}present{{[[:space:]]+}}0` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G0988 `test/Tools/circt-sim/tlul-bfm-a-ready-timeout-short-circuit.sv:2` (todo)

At `test/Tools/circt-sim/tlul-bfm-a-ready-timeout-short-circuit.sv:2`, the marker `// TODO: BFM timeout messages not printed — task-level $display in while loop not working.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: BFM timeout messages not printed — task-level $display in while loop not working.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0989 `lib/Dialect/MSFT/ExportQuartusTcl.cpp:80` (todo)

At `lib/Dialect/MSFT/ExportQuartusTcl.cpp:80`, the marker `// TODO: Currently assumes Stratix 10 and QuartusPro. Make more general.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Currently assumes Stratix 10 and QuartusPro. Make more general.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0990 `lib/Dialect/Moore/Transforms/LowerConcatRef.cpp:75` (todo)

At `lib/Dialect/Moore/Transforms/LowerConcatRef.cpp:75`, the marker `// FIXME: Need to estimate whether the bits range is from large to` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Need to estimate whether the bits range is from large to. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0991 `lib/Dialect/FIRRTL/FIRRTLReductions.cpp:1821` (todo)

At `lib/Dialect/FIRRTL/FIRRTLReductions.cpp:1821`, the marker `// TODO: The namespace should be unnecessary. However, some FIRRTL` indicates a gap signal in this path. What's missing: The marker documents unfinished work: The namespace should be unnecessary. However, some FIRRTL. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0992 `lib/Dialect/Moore/Transforms/CreateVTables.cpp:151` (implementation-gap)

At `lib/Dialect/Moore/Transforms/CreateVTables.cpp:151`, the marker `// Last, emit any own method symbol entries (skip pure-virtual / unimplemented)` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0993 `lib/Dialect/Moore/Transforms/CreateVTables.cpp:168` (implementation-gap)

At `lib/Dialect/Moore/Transforms/CreateVTables.cpp:168`, the marker `// methods still get a vtable - unimplemented slots stay null and the` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0994 `lib/Dialect/MSFT/DeviceDB.cpp:357` (todo)

At `lib/Dialect/MSFT/DeviceDB.cpp:357`, the marker `// TODO: Since the data structures we're using aren't sorted, the best we can` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Since the data structures we're using aren't sorted, the best we can. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0995 `test/Tools/circt-sim/syscall-ungetc.sv:2` (todo)

At `test/Tools/circt-sim/syscall-ungetc.sv:2`, the marker `// TODO: $ungetc returns 66 ('B') instead of 65 ('A') — pushback not working correctly.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: $ungetc returns 66 ('B') instead of 65 ('A') — pushback not working correctly.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0996 `lib/Dialect/Sim/Transforms/ProceduralizeSim.cpp:106` (implementation-gap)

At `lib/Dialect/Sim/Transforms/ProceduralizeSim.cpp:106`, the marker `"argument is unsupported.");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0997 `lib/Dialect/Sim/Transforms/LowerDPIFunc.cpp:80` (todo)

At `lib/Dialect/Sim/Transforms/LowerDPIFunc.cpp:80`, the marker `// TODO: Support a non-integer type.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Support a non-integer type.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G0998 `lib/Dialect/Sim/Transforms/LowerDPIFunc.cpp:83` (implementation-gap)

At `lib/Dialect/Sim/Transforms/LowerDPIFunc.cpp:83`, the marker `<< "non-integer type argument is unsupported now";` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G0999 `lib/Dialect/Sim/Transforms/LowerDPIFunc.cpp:100` (todo)

At `lib/Dialect/Sim/Transforms/LowerDPIFunc.cpp:100`, the marker `// TODO: Check if function type matches.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Check if function type matches.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1000 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:298` (implementation-gap)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:298`, the marker `mlir::emitError(loc, "value has an unsupported verilog type ") << type;` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1001 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:726` (todo)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:726`, the marker `// TODO: We could handle concat and other operators here.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: We could handle concat and other operators here.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1002 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:1426` (todo)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:1426`, the marker `// TODO: handle other common patterns.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: handle other common patterns.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1003 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:1864` (implementation-gap)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:1864`, the marker `mlir::emitError(loc, "value has an unsupported verilog type ") << type;` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1004 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:1964` (todo)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:1964`, the marker `// TODO: relying on float printing to be precise is not a good idea.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: relying on float printing to be precise is not a good idea.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1005 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:1977` (todo)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:1977`, the marker `// TODO: Should we support signed parameters?` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Should we support signed parameters?. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1006 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:2082` (todo)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:2082`, the marker `// TODO: This could try harder to omit redundant casts like the mainline` indicates a gap signal in this path. What's missing: The marker documents unfinished work: This could try harder to omit redundant casts like the mainline. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1007 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:2111` (todo)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:2111`, the marker `// TODO: Also handle (a + b + x*-1).` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Also handle (a + b + x*-1).. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1008 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:2504` (implementation-gap)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:2504`, the marker `emitError(op, "SV attributes emission is unimplemented for the op");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1009 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:2512` (todo)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:2512`, the marker `// TODO: Build tree capturing precedence/fixity at same level, group those!` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Build tree capturing precedence/fixity at same level, group those!. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1010 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:2529` (todo)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:2529`, the marker `// TODO: MLIR should have general "Associative" trait.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: MLIR should have general "Associative" trait.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1011 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:2564` (implementation-gap)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:2564`, the marker `emitError(op, "SV attributes emission is unimplemented for the op");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1012 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:2781` (implementation-gap)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:2781`, the marker `emitError(op, "SV attributes emission is unimplemented for the op");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1013 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:2809` (implementation-gap)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:2809`, the marker `emitError(op, "SV attributes emission is unimplemented for the op");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1014 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:2819` (implementation-gap)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:2819`, the marker `emitError(op, "SV attributes emission is unimplemented for the op");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1015 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:2833` (implementation-gap)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:2833`, the marker `emitError(op, "SV attributes emission is unimplemented for the op");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1016 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:2844` (implementation-gap)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:2844`, the marker `emitError(op, "SV attributes emission is unimplemented for the op");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1017 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:2854` (todo)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:2854`, the marker `// TODO: This shares a lot of code with the getNameRemotely mtehod. Combine` indicates a gap signal in this path. What's missing: The marker documents unfinished work: This shares a lot of code with the getNameRemotely mtehod. Combine. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1018 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:2858` (implementation-gap)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:2858`, the marker `emitError(op, "SV attributes emission is unimplemented for the op");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1019 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:2885` (implementation-gap)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:2885`, the marker `emitError(op, "SV attributes emission is unimplemented for the op");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1020 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:2897` (implementation-gap)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:2897`, the marker `emitError(op, "SV attributes emission is unimplemented for the op");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1021 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:2925` (implementation-gap)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:2925`, the marker `emitError(op, "SV attributes emission is unimplemented for the op");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1022 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:2934` (implementation-gap)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:2934`, the marker `emitError(op, "SV attributes emission is unimplemented for the op");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1023 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:2942` (implementation-gap)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:2942`, the marker `emitError(op, "SV attributes emission is unimplemented for the op");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1024 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:2981` (implementation-gap)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:2981`, the marker `emitError(op, "SV attributes emission is unimplemented for the op");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1025 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:2989` (implementation-gap)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:2989`, the marker `ps << "<<unsupported zero width constant: "` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1026 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:3018` (todo)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:3018`, the marker `// TODO: Ideally we should emit zero bit values as comments, e.g. `{/*a:` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Ideally we should emit zero bit values as comments, e.g. `{/*a:. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1027 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:3082` (implementation-gap)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:3082`, the marker `emitError(op, "SV attributes emission is unimplemented for the op");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1028 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:3094` (implementation-gap)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:3094`, the marker `emitError(op, "SV attributes emission is unimplemented for the op");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1029 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:3107` (implementation-gap)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:3107`, the marker `emitError(op, "SV attributes emission is unimplemented for the op");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1030 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:3135` (implementation-gap)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:3135`, the marker `emitError(op, "SV attributes emission is unimplemented for the op");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1031 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:3158` (implementation-gap)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:3158`, the marker `emitError(op, "SV attributes emission is unimplemented for the op");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1032 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:3168` (implementation-gap)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:3168`, the marker `emitError(op, "SV attributes emission is unimplemented for the op");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1033 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:3176` (implementation-gap)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:3176`, the marker `emitError(op, "SV attributes emission is unimplemented for the op");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1034 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:3191` (implementation-gap)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:3191`, the marker `emitError(op, "SV attributes emission is unimplemented for the op");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1035 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:3207` (implementation-gap)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:3207`, the marker `emitError(op, "SV attributes emission is unimplemented for the op");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1036 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:3223` (implementation-gap)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:3223`, the marker `emitError(op, "SV attributes emission is unimplemented for the op");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1037 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:3233` (implementation-gap)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:3233`, the marker `emitError(op, "SV attributes emission is unimplemented for the op");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1038 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:3243` (implementation-gap)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:3243`, the marker `emitError(op, "SV attributes emission is unimplemented for the op");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1039 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:3248` (todo)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:3248`, the marker `// TODO: if any of these breaks, it'd be "nice" to break` indicates a gap signal in this path. What's missing: The marker documents unfinished work: if any of these breaks, it'd be "nice" to break. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1040 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:3265` (implementation-gap)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:3265`, the marker `emitError(op, "SV attributes emission is unimplemented for the op");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1041 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:3273` (implementation-gap)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:3273`, the marker `emitError(op, "SV attributes emission is unimplemented for the op");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1042 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:3320` (implementation-gap)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:3320`, the marker `emitError(op, "SV attributes emission is unimplemented for the op");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1043 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:3368` (implementation-gap)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:3368`, the marker `emitError(op, "SV attributes emission is unimplemented for the op");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1044 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:3370` (todo)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:3370`, the marker `// TODO: For unpacked structs, once we have support for them, `printAsPattern`` indicates a gap signal in this path. What's missing: The marker documents unfinished work: For unpacked structs, once we have support for them, `printAsPattern`. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1045 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:3386` (implementation-gap)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:3386`, the marker `emitError(op, "SV attributes emission is unimplemented for the op");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1046 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:3396` (implementation-gap)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:3396`, the marker `emitError(op, "SV attributes emission is unimplemented for the op");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1047 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:3398` (todo)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:3398`, the marker `// TODO: For unpacked structs, once we have support for them, `printAsPattern`` indicates a gap signal in this path. What's missing: The marker documents unfinished work: For unpacked structs, once we have support for them, `printAsPattern`. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1048 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:3423` (implementation-gap)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:3423`, the marker `emitError(op, "SV attributes emission is unimplemented for the op");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1049 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:3433` (implementation-gap)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:3433`, the marker `emitError(op, "SV attributes emission is unimplemented for the op");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1050 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:3475` (implementation-gap)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:3475`, the marker `emitError(op, "SV attributes emission is unimplemented for the op");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1051 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:3498` (implementation-gap)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:3498`, the marker `ps << "<<unsupported expr: " << PPExtString(op->getName().getStringRef())` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1052 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:3719` (implementation-gap)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:3719`, the marker `ps << "<<unsupported: " << PPExtString(op->getName().getStringRef()) << ">>";` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1053 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:4300` (implementation-gap)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:4300`, the marker `emitError(op, "SV attributes emission is unimplemented for the op");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1054 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:4307` (implementation-gap)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:4307`, the marker `emitError(op, "SV attributes emission is unimplemented for the op");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1055 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:4325` (implementation-gap)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:4325`, the marker `emitError(op, "SV attributes emission is unimplemented for the op");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1056 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:4351` (implementation-gap)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:4351`, the marker `emitError(op, "SV attributes emission is unimplemented for the op");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1057 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:4456` (implementation-gap)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:4456`, the marker `emitError(op, "SV attributes emission is unimplemented for the op");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1058 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:4606` (implementation-gap)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:4606`, the marker `emitError(op, "SV attributes emission is unimplemented for the op");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1059 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:4625` (implementation-gap)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:4625`, the marker `emitError(op, "SV attributes emission is unimplemented for the op");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1060 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:4639` (todo)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:4639`, the marker `// TODO: if any of these breaks, it'd be "nice" to break` indicates a gap signal in this path. What's missing: The marker documents unfinished work: if any of these breaks, it'd be "nice" to break. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1061 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:4658` (implementation-gap)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:4658`, the marker `emitError(op, "SV attributes emission is unimplemented for the op");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1062 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:4700` (implementation-gap)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:4700`, the marker `emitError(op, "SV attributes emission is unimplemented for the op");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1063 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:4732` (implementation-gap)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:4732`, the marker `emitError(op, "SV attributes emission is unimplemented for the op");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1064 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:4770` (implementation-gap)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:4770`, the marker `emitError(op, "SV attributes emission is unimplemented for the op");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1065 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:4794` (todo)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:4794`, the marker `// TODO: good comma/wrapping behavior as elsewhere.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: good comma/wrapping behavior as elsewhere.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1066 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:4872` (todo)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:4872`, the marker `// TODO: location info?` indicates a gap signal in this path. What's missing: The marker documents unfinished work: location info?. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1067 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:4889` (todo)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:4889`, the marker `// TODO: location info?` indicates a gap signal in this path. What's missing: The marker documents unfinished work: location info?. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1068 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:4908` (todo)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:4908`, the marker `// TODO: We'll probably need to store the legalized names somewhere for` indicates a gap signal in this path. What's missing: The marker documents unfinished work: We'll probably need to store the legalized names somewhere for. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1069 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:5010` (todo)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:5010`, the marker `// TODO: box, break/wrap behavior!` indicates a gap signal in this path. What's missing: The marker documents unfinished work: box, break/wrap behavior!. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1070 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:5022` (implementation-gap)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:5022`, the marker `emitError(op, "SV attributes emission is unimplemented for the op");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1071 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:5071` (implementation-gap)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:5071`, the marker `emitError(op, "SV attributes emission is unimplemented for the op");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1072 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:5116` (implementation-gap)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:5116`, the marker `emitError(op, "SV attributes emission is unimplemented for the op");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1073 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:5180` (implementation-gap)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:5180`, the marker `emitError(op, "SV attributes emission is unimplemented for the op");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1074 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:5411` (todo)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:5411`, the marker `// TODO: group, like normal 'if'.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: group, like normal 'if'.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1075 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:5484` (todo)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:5484`, the marker `// TODO: We could emit in hex if/when the size is a multiple of` indicates a gap signal in this path. What's missing: The marker documents unfinished work: We could emit in hex if/when the size is a multiple of. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1076 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:5745` (todo)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:5745`, the marker `// TODO: source info!` indicates a gap signal in this path. What's missing: The marker documents unfinished work: source info!. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1077 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:5750` (todo)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:5750`, the marker `// FIXME: Don't emit the body of this as general statements, they aren't!` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Don't emit the body of this as general statements, they aren't!. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1078 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:5798` (todo)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:5798`, the marker `// TODO: revisit, better breaks/grouping.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: revisit, better breaks/grouping.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1079 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:5816` (todo)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:5816`, the marker `// TODO: emit like emitAssignLike does, maybe refactor.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: emit like emitAssignLike does, maybe refactor.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1080 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:5836` (todo)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:5836`, the marker `// TODO: source info!` indicates a gap signal in this path. What's missing: The marker documents unfinished work: source info!. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1081 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:5989` (todo)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:5989`, the marker `/// TODO: Use MLIR DominanceInfo.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Use MLIR DominanceInfo.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1082 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:6098` (todo)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:6098`, the marker `// FIXME: Unpacked array is not inlined since several tools doesn't support` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Unpacked array is not inlined since several tools doesn't support. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1083 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:6123` (todo)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:6123`, the marker `// FIXME: Unpacked array is not inlined since several tools doesn't support` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Unpacked array is not inlined since several tools doesn't support. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1084 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:6230` (implementation-gap)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:6230`, the marker `emitError(op, "SV attributes emission is unimplemented for the op");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1085 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:6339` (implementation-gap)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:6339`, the marker `emitError(op, "SV attributes emission is unimplemented for the op");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1086 `lib/Conversion/ExportVerilog/ExportVerilog.cpp:6951` (todo)

At `lib/Conversion/ExportVerilog/ExportVerilog.cpp:6951`, the marker `// TODO: How do we want to handle typedefs in a split output?` indicates a gap signal in this path. What's missing: The marker documents unfinished work: How do we want to handle typedefs in a split output?. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1087 `lib/Dialect/Seq/SeqOps.cpp:610` (todo)

At `lib/Dialect/Seq/SeqOps.cpp:610`, the marker `// TODO: Once HW aggregate constant values are supported, move this` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Once HW aggregate constant values are supported, move this. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1088 `lib/Dialect/Seq/SeqOps.cpp:693` (todo)

At `lib/Dialect/Seq/SeqOps.cpp:693`, the marker `// TODO: Support nested arrays and bundles.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Support nested arrays and bundles.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1089 `lib/Dialect/Seq/SeqOps.cpp:743` (todo)

At `lib/Dialect/Seq/SeqOps.cpp:743`, the marker `// TODO: Handle a preset value.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Handle a preset value.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1090 `test/Tools/circt-sim/syscall-getpattern.sv:2` (test-gap-reference)

At `test/Tools/circt-sim/syscall-getpattern.sv:2`, the marker `// Test $getpattern — legacy function, returns 0 (not implemented)` indicates a gap signal in this path. What's missing: This test line references an unsupported behavior, indicating a known gap in upstream implementation coverage. How to fix: Implement the missing behavior in production code and update this test to assert functional semantics rather than unsupported status once support is complete.

### G1091 `test/Tools/circt-sim/syscall-randomize-with.sv:2` (todo)

At `test/Tools/circt-sim/syscall-randomize-with.sv:2`, the marker `// TODO: randomize() with inline constraints — data_constrained=0, constraint not applied.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: randomize() with inline constraints — data_constrained=0, constraint not applied.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1092 `test/Tools/circt-sim/syscall-generate.sv:2` (todo)

At `test/Tools/circt-sim/syscall-generate.sv:2`, the marker `// TODO: Generate-for produces data=101 instead of data=0101 — width/padding issue.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Generate-for produces data=101 instead of data=0101 — width/padding issue.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1093 `test/Tools/circt-sim/syscall-random.sv:2` (todo)

At `test/Tools/circt-sim/syscall-random.sv:2`, the marker `// TODO: $random(seed) does not update seed variable — seed output not wired.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: $random(seed) does not update seed variable — seed output not wired.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1094 `test/Tools/circt-sim/syscall-fread.sv:2` (todo)

At `test/Tools/circt-sim/syscall-fread.sv:2`, the marker `// TODO: $fread not yet implemented in ImportVerilog/interpreter.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: $fread not yet implemented in ImportVerilog/interpreter.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1095 `test/Tools/circt-sim/syscall-queue-stochastic.sv:8` (test-expectation)

At `test/Tools/circt-sim/syscall-queue-stochastic.sv:8`, the marker `// CHECK: error: unsupported legacy stochastic queue task '$q_initialize'` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1096 `test/Tools/circt-sim/syscall-feof.sv:2` (todo)

At `test/Tools/circt-sim/syscall-feof.sv:2`, the marker `// TODO: Comprehensive file I/O test — individual ops work but combined sequence fails.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Comprehensive file I/O test — individual ops work but combined sequence fails.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1097 `lib/Dialect/Seq/Transforms/LowerSeqHLMem.cpp:67` (implementation-gap)

At `lib/Dialect/Seq/Transforms/LowerSeqHLMem.cpp:67`, the marker `return rewriter.notifyMatchFailure(user, "unsupported port type");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1098 `lib/Dialect/Seq/Transforms/LowerSeqHLMem.cpp:120` (todo)

At `lib/Dialect/Seq/Transforms/LowerSeqHLMem.cpp:120`, the marker `// TODO: When latency > 2, and assuming that the exact read time is flexible` indicates a gap signal in this path. What's missing: The marker documents unfinished work: When latency > 2, and assuming that the exact read time is flexible. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1099 `lib/Conversion/ExportVerilog/PrepareForEmission.cpp:218` (todo)

At `lib/Conversion/ExportVerilog/PrepareForEmission.cpp:218`, the marker `// TODO: Use a result name as suffix.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Use a result name as suffix.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1100 `lib/Conversion/ExportVerilog/PrepareForEmission.cpp:728` (todo)

At `lib/Conversion/ExportVerilog/PrepareForEmission.cpp:728`, the marker `// TODO: Consider using virtual functions.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Consider using virtual functions.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1101 `lib/Conversion/ExportVerilog/PrepareForEmission.cpp:787` (todo)

At `lib/Conversion/ExportVerilog/PrepareForEmission.cpp:787`, the marker `// TODO: Handle procedural regions as well.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Handle procedural regions as well.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1102 `lib/Conversion/ExportVerilog/PrepareForEmission.cpp:1210` (todo)

At `lib/Conversion/ExportVerilog/PrepareForEmission.cpp:1210`, the marker `// TODO: This is checking the Commutative property, which doesn't seem` indicates a gap signal in this path. What's missing: The marker documents unfinished work: This is checking the Commutative property, which doesn't seem. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1103 `test/Tools/circt-sim/syscall-q-full.sv:8` (test-expectation)

At `test/Tools/circt-sim/syscall-q-full.sv:8`, the marker `// CHECK: error: unsupported legacy stochastic queue task '$q_initialize'` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1104 `test/Tools/circt-sim/syscall-pld-sync-array.sv:14` (test-expectation)

At `test/Tools/circt-sim/syscall-pld-sync-array.sv:14`, the marker `// CHECK: error: unsupported legacy PLD array task '$sync$and$array'` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1105 `test/Tools/circt-sim/syscall-pld-array.sv:14` (test-expectation)

At `test/Tools/circt-sim/syscall-pld-array.sv:14`, the marker `// CHECK: error: unsupported legacy PLD array task '$async$and$array'` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1106 `test/Tools/circt-sim/syscall-monitor.sv:2` (todo)

At `test/Tools/circt-sim/syscall-monitor.sv:2`, the marker `// TODO: $monitor fires only once (val=0) — value-change callback not re-triggering.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: $monitor fires only once (val=0) — value-change callback not re-triggering.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1107 `lib/Dialect/Seq/Transforms/HWMemSimImpl.cpp:652` (todo)

At `lib/Dialect/Seq/Transforms/HWMemSimImpl.cpp:652`, the marker `// TODO: This shares a lot of common logic with LowerToHW. Combine` indicates a gap signal in this path. What's missing: The marker documents unfinished work: This shares a lot of common logic with LowerToHW. Combine. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1108 `test/Tools/circt-sim/syscall-isunbounded.sv:2` (todo)

At `test/Tools/circt-sim/syscall-isunbounded.sv:2`, the marker `// TODO: $isunbounded with class type parameters — compilation or runtime failure.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: $isunbounded with class type parameters — compilation or runtime failure.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1109 `test/Tools/circt-sim/syscall-shortrealtobits.sv:2` (todo)

At `test/Tools/circt-sim/syscall-shortrealtobits.sv:2`, the marker `// TODO: $shortrealtobits partial — negative shortreal values produce x instead of bits.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: $shortrealtobits partial — negative shortreal values produce x instead of bits.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1110 `test/Tools/circt-sim/syscall-strobe.sv:2` (todo)

At `test/Tools/circt-sim/syscall-strobe.sv:2`, the marker `// TODO: $strobe shows val=10 (immediate) instead of val=20 (end-of-timestep).` indicates a gap signal in this path. What's missing: The marker documents unfinished work: $strobe shows val=10 (immediate) instead of val=20 (end-of-timestep).. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1111 `lib/Dialect/SV/SVOps.cpp:285` (implementation-gap)

At `lib/Dialect/SV/SVOps.cpp:285`, the marker `return emitError("unsupported type");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1112 `lib/Dialect/SV/SVOps.cpp:300` (implementation-gap)

At `lib/Dialect/SV/SVOps.cpp:300`, the marker `return emitError("unsupported type");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1113 `lib/Dialect/FIRRTL/FIRRTLOps.cpp:211` (implementation-gap)

At `lib/Dialect/FIRRTL/FIRRTLOps.cpp:211`, the marker `llvm_unreachable("Unsupported Flow type.");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1114 `lib/Dialect/FIRRTL/FIRRTLOps.cpp:226` (implementation-gap)

At `lib/Dialect/FIRRTL/FIRRTLOps.cpp:226`, the marker `llvm_unreachable("Unsupported Flow type.");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1115 `lib/Dialect/FIRRTL/FIRRTLOps.cpp:304` (todo)

At `lib/Dialect/FIRRTL/FIRRTLOps.cpp:304`, the marker `// TODO: This is doing the same walk as foldFlow. These two functions can be` indicates a gap signal in this path. What's missing: The marker documents unfinished work: This is doing the same walk as foldFlow. These two functions can be. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1116 `lib/Dialect/FIRRTL/FIRRTLOps.cpp:1331` (todo)

At `lib/Dialect/FIRRTL/FIRRTLOps.cpp:1331`, the marker `// TODO: `printOptionalLocationSpecifier` will emit aliases for locations,` indicates a gap signal in this path. What's missing: The marker documents unfinished work: `printOptionalLocationSpecifier` will emit aliases for locations,. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1117 `lib/Dialect/FIRRTL/FIRRTLOps.cpp:1676` (todo)

At `lib/Dialect/FIRRTL/FIRRTLOps.cpp:1676`, the marker `// TODO: this should be using properties.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: this should be using properties.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1118 `lib/Dialect/FIRRTL/FIRRTLOps.cpp:2100` (todo)

At `lib/Dialect/FIRRTL/FIRRTLOps.cpp:2100`, the marker `// TODO: this should use properties.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: this should use properties.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1119 `lib/Dialect/FIRRTL/FIRRTLOps.cpp:3461` (todo)

At `lib/Dialect/FIRRTL/FIRRTLOps.cpp:3461`, the marker `// FIXME error on missing bundle flip` indicates a gap signal in this path. What's missing: The marker documents unfinished work: error on missing bundle flip. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1120 `lib/Dialect/FIRRTL/FIRRTLOps.cpp:4076` (todo)

At `lib/Dialect/FIRRTL/FIRRTLOps.cpp:4076`, the marker `// TODO: Relax this to allow reads from output ports,` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Relax this to allow reads from output ports,. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1121 `lib/Dialect/FIRRTL/FIRRTLOps.cpp:4321` (todo)

At `lib/Dialect/FIRRTL/FIRRTLOps.cpp:4321`, the marker `// TODO: Make ref.sub only source flow?` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Make ref.sub only source flow?. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1122 `lib/Dialect/FIRRTL/FIRRTLOps.cpp:4949` (todo)

At `lib/Dialect/FIRRTL/FIRRTLOps.cpp:4949`, the marker `// TODO: check flow` indicates a gap signal in this path. What's missing: The marker documents unfinished work: check flow. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1123 `lib/Dialect/FIRRTL/FIRRTLOps.cpp:4962` (todo)

At `lib/Dialect/FIRRTL/FIRRTLOps.cpp:4962`, the marker `// TODO: check flow` indicates a gap signal in this path. What's missing: The marker documents unfinished work: check flow. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1124 `lib/Dialect/FIRRTL/FIRRTLOps.cpp:6791` (todo)

At `lib/Dialect/FIRRTL/FIRRTLOps.cpp:6791`, the marker `// TODO: Determine ref.sub + rwprobe behavior, test.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Determine ref.sub + rwprobe behavior, test.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1125 `lib/Dialect/FIRRTL/FIRRTLOps.cpp:6923` (todo)

At `lib/Dialect/FIRRTL/FIRRTLOps.cpp:6923`, the marker `// TODO: Verify that the target's type matches the type of this op.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Verify that the target's type matches the type of this op.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1126 `lib/Dialect/FIRRTL/FIRRTLOps.cpp:6935` (todo)

At `lib/Dialect/FIRRTL/FIRRTLOps.cpp:6935`, the marker `// TODO: Verify that the target's type matches the type of this op.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Verify that the target's type matches the type of this op.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1127 `lib/Dialect/FIRRTL/FIRRTLOps.cpp:7023` (todo)

At `lib/Dialect/FIRRTL/FIRRTLOps.cpp:7023`, the marker `// TODO: Improve this verifier. This is intentionally _not_ verifying` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Improve this verifier. This is intentionally _not_ verifying. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1128 `lib/Dialect/ESI/ESIServices.cpp:98` (todo)

At `lib/Dialect/ESI/ESIServices.cpp:98`, the marker `// TODO: The cosim op should probably be able to take a bundle type and get` indicates a gap signal in this path. What's missing: The marker documents unfinished work: The cosim op should probably be able to take a bundle type and get. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1129 `lib/Dialect/SV/Transforms/SVExtractTestCode.cpp:82` (todo)

At `lib/Dialect/SV/Transforms/SVExtractTestCode.cpp:82`, the marker `// TODO: determine whether we want to recurse backward into the other` indicates a gap signal in this path. What's missing: The marker documents unfinished work: determine whether we want to recurse backward into the other. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1130 `lib/Dialect/FIRRTL/Export/FIREmitter.cpp:512` (implementation-gap)

At `lib/Dialect/FIRRTL/Export/FIREmitter.cpp:512`, the marker `emitOpError(op, "with unsupported parameter attribute: ") << attr;` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1131 `lib/Dialect/FIRRTL/Export/FIREmitter.cpp:513` (implementation-gap)

At `lib/Dialect/FIRRTL/Export/FIREmitter.cpp:513`, the marker `ps << "<unsupported-attr ";` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1132 `lib/Dialect/FIRRTL/Export/FIREmitter.cpp:956` (implementation-gap)

At `lib/Dialect/FIRRTL/Export/FIREmitter.cpp:956`, the marker `emitError(op, "unsupported fstring substitution type");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1133 `lib/Dialect/FIRRTL/Export/FIREmitter.cpp:1229` (implementation-gap)

At `lib/Dialect/FIRRTL/Export/FIREmitter.cpp:1229`, the marker `emitOpError(op, "has unsupported 'debug' port");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1134 `lib/Dialect/FIRRTL/Export/FIREmitter.cpp:1282` (todo)

At `lib/Dialect/FIRRTL/Export/FIREmitter.cpp:1282`, the marker `// TODO: emitAssignLike` indicates a gap signal in this path. What's missing: The marker documents unfinished work: emitAssignLike. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1135 `lib/Dialect/FIRRTL/Export/FIREmitter.cpp:1386` (todo)

At `lib/Dialect/FIRRTL/Export/FIREmitter.cpp:1386`, the marker `// TODO: emitAssignLike ?` indicates a gap signal in this path. What's missing: The marker documents unfinished work: emitAssignLike ?. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1136 `lib/Dialect/FIRRTL/Export/FIREmitter.cpp:1457` (implementation-gap)

At `lib/Dialect/FIRRTL/Export/FIREmitter.cpp:1457`, the marker `ps << "<unsupported-expr-" << PPExtString(op->getName().stripDialect())` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1137 `lib/Dialect/FIRRTL/Export/FIREmitter.cpp:1465` (todo)

At `lib/Dialect/FIRRTL/Export/FIREmitter.cpp:1465`, the marker `// TODO: Add option to control base-2/8/10/16 output here.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Add option to control base-2/8/10/16 output here.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1138 `lib/Dialect/FIRRTL/Export/FIREmitter.cpp:1477` (todo)

At `lib/Dialect/FIRRTL/Export/FIREmitter.cpp:1477`, the marker `// TODO: Emit type decl for type alias.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Emit type decl for type alias.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1139 `lib/Dialect/FIRRTL/Export/FIREmitter.cpp:1743` (todo)

At `lib/Dialect/FIRRTL/Export/FIREmitter.cpp:1743`, the marker `// TODO: Emit type decl for type alias.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Emit type decl for type alias.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1140 `lib/Dialect/FIRRTL/Export/FIREmitter.cpp:1843` (todo)

At `lib/Dialect/FIRRTL/Export/FIREmitter.cpp:1843`, the marker `// TODO: Handle FusedLoc and uniquify locations, avoid repeated file names.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Handle FusedLoc and uniquify locations, avoid repeated file names.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1141 `lib/Dialect/SV/Transforms/HWEliminateInOutPorts.cpp:463` (implementation-gap)

At `lib/Dialect/SV/Transforms/HWEliminateInOutPorts.cpp:463`, the marker `<< " but the operation itself is unsupported.";` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1142 `lib/Dialect/SV/Transforms/HWEliminateInOutPorts.cpp:527` (implementation-gap)

At `lib/Dialect/SV/Transforms/HWEliminateInOutPorts.cpp:527`, the marker `<< " is unsupported.";` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1143 `lib/Dialect/SV/Transforms/HWEliminateInOutPorts.cpp:998` (todo)

At `lib/Dialect/SV/Transforms/HWEliminateInOutPorts.cpp:998`, the marker `// FIXME: hw.inout cannot be used in outputs.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: hw.inout cannot be used in outputs.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1144 `lib/Conversion/DatapathToComb/DatapathToComb.cpp:286` (todo)

At `lib/Conversion/DatapathToComb/DatapathToComb.cpp:286`, the marker `// TODO - replace with a concatenation to aid longest path analysis` indicates a gap signal in this path. What's missing: The marker documents unfinished work: replace with a concatenation to aid longest path analysis. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1145 `lib/Conversion/DatapathToComb/DatapathToComb.cpp:293` (todo)

At `lib/Conversion/DatapathToComb/DatapathToComb.cpp:293`, the marker `// TODO: sort a and b based on non-zero bits to encode the smaller input` indicates a gap signal in this path. What's missing: The marker documents unfinished work: sort a and b based on non-zero bits to encode the smaller input. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1146 `lib/Conversion/DatapathToComb/DatapathToComb.cpp:466` (todo)

At `lib/Conversion/DatapathToComb/DatapathToComb.cpp:466`, the marker `// TODO: Implement Booth lowering` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Implement Booth lowering. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1147 `lib/Conversion/DatapathToComb/DatapathToComb.cpp:571` (todo)

At `lib/Conversion/DatapathToComb/DatapathToComb.cpp:571`, the marker `// TODO: Topologically sort the operations in the module to ensure that all` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Topologically sort the operations in the module to ensure that all. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1148 `lib/Dialect/SV/Transforms/PrettifyVerilog.cpp:167` (todo)

At `lib/Dialect/SV/Transforms/PrettifyVerilog.cpp:167`, the marker `// TODO: consider breaking up array assignments into assignments` indicates a gap signal in this path. What's missing: The marker documents unfinished work: consider breaking up array assignments into assignments. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1149 `lib/Dialect/SV/Transforms/PrettifyVerilog.cpp:184` (todo)

At `lib/Dialect/SV/Transforms/PrettifyVerilog.cpp:184`, the marker `// TODO: generalise to ranges and arbitrary concatenations.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: generalise to ranges and arbitrary concatenations.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1150 `lib/Dialect/SV/Transforms/PrettifyVerilog.cpp:351` (todo)

At `lib/Dialect/SV/Transforms/PrettifyVerilog.cpp:351`, the marker `// TODO: We should use the condition used in ExportVerilog regarding` indicates a gap signal in this path. What's missing: The marker documents unfinished work: We should use the condition used in ExportVerilog regarding. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1151 `lib/Dialect/FIRRTL/FIRRTLFolds.cpp:541` (todo)

At `lib/Dialect/FIRRTL/FIRRTLFolds.cpp:541`, the marker `// TODO: Move to DRR.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Move to DRR.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1152 `lib/Dialect/FIRRTL/FIRRTLFolds.cpp:854` (todo)

At `lib/Dialect/FIRRTL/FIRRTLFolds.cpp:854`, the marker `/// TODO: Support SInt<1> on the LHS etc.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Support SInt<1> on the LHS etc.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1153 `lib/Dialect/FIRRTL/FIRRTLFolds.cpp:904` (todo)

At `lib/Dialect/FIRRTL/FIRRTLFolds.cpp:904`, the marker `/// TODO: Support SInt<1> on the LHS etc.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Support SInt<1> on the LHS etc.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1154 `lib/Dialect/FIRRTL/FIRRTLFolds.cpp:950` (todo)

At `lib/Dialect/FIRRTL/FIRRTLFolds.cpp:950`, the marker `// TODO: implement constant folding, etc.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: implement constant folding, etc.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1155 `lib/Dialect/FIRRTL/FIRRTLFolds.cpp:956` (todo)

At `lib/Dialect/FIRRTL/FIRRTLFolds.cpp:956`, the marker `// TODO: implement constant folding, etc.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: implement constant folding, etc.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1156 `lib/Dialect/FIRRTL/FIRRTLFolds.cpp:1657` (todo)

At `lib/Dialect/FIRRTL/FIRRTLFolds.cpp:1657`, the marker `// TODO: x ? ~0 : 0 -> sext(x)` indicates a gap signal in this path. What's missing: The marker documents unfinished work: x ? ~0 : 0 -> sext(x). The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1157 `lib/Dialect/FIRRTL/FIRRTLFolds.cpp:1658` (todo)

At `lib/Dialect/FIRRTL/FIRRTLFolds.cpp:1658`, the marker `// TODO: "x ? c1 : c2" -> many tricks` indicates a gap signal in this path. What's missing: The marker documents unfinished work: "x ? c1 : c2" -> many tricks. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1158 `lib/Dialect/FIRRTL/FIRRTLFolds.cpp:1660` (todo)

At `lib/Dialect/FIRRTL/FIRRTLFolds.cpp:1660`, the marker `// TODO: "x ? a : 0" -> sext(x) & a` indicates a gap signal in this path. What's missing: The marker documents unfinished work: "x ? a : 0" -> sext(x) & a. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1159 `lib/Dialect/FIRRTL/FIRRTLFolds.cpp:1663` (todo)

At `lib/Dialect/FIRRTL/FIRRTLFolds.cpp:1663`, the marker `// TODO: "x ? c1 : y" -> "~x ? y : c1"` indicates a gap signal in this path. What's missing: The marker documents unfinished work: "x ? c1 : y" -> "~x ? y : c1". The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1160 `lib/Dialect/FIRRTL/FIRRTLFolds.cpp:2042` (todo)

At `lib/Dialect/FIRRTL/FIRRTLFolds.cpp:2042`, the marker `// TODO: Handle even when `index` doesn't have uint<1>.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Handle even when `index` doesn't have uint<1>.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1161 `lib/Dialect/FIRRTL/FIRRTLFolds.cpp:2155` (todo)

At `lib/Dialect/FIRRTL/FIRRTLFolds.cpp:2155`, the marker `// TODO: Canonicalize towards explicit extensions and flips here.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Canonicalize towards explicit extensions and flips here.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1162 `lib/Dialect/FIRRTL/FIRRTLFolds.cpp:2207` (todo)

At `lib/Dialect/FIRRTL/FIRRTLFolds.cpp:2207`, the marker `// TODO: May need to be sensitive to "don't touch" or other` indicates a gap signal in this path. What's missing: The marker documents unfinished work: May need to be sensitive to "don't touch" or other. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1163 `lib/Dialect/SV/Transforms/HWLegalizeModules.cpp:1` (implementation-gap)

At `lib/Dialect/SV/Transforms/HWLegalizeModules.cpp:1`, the marker `//===- HWLegalizeModulesPass.cpp - Lower unsupported IR features away -----===//` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1164 `lib/Dialect/SV/Transforms/HWLegalizeModules.cpp:10` (implementation-gap)

At `lib/Dialect/SV/Transforms/HWLegalizeModules.cpp:10`, the marker `// unsupported by some tools (e.g. multidimensional arrays) as specified by` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1165 `lib/Dialect/SV/Transforms/HWLegalizeModules.cpp:357` (implementation-gap)

At `lib/Dialect/SV/Transforms/HWLegalizeModules.cpp:357`, the marker `user->emitError("unsupported packed array expression");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1166 `lib/Dialect/SV/Transforms/HWLegalizeModules.cpp:438` (implementation-gap)

At `lib/Dialect/SV/Transforms/HWLegalizeModules.cpp:438`, the marker `op.emitError("unsupported packed array expression");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1167 `lib/Dialect/FIRRTL/FIRRTLAnnotationHelper.cpp:227` (todo)

At `lib/Dialect/FIRRTL/FIRRTLAnnotationHelper.cpp:227`, the marker `// TODO: containsReference().` indicates a gap signal in this path. What's missing: The marker documents unfinished work: containsReference().. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1168 `lib/Dialect/FIRRTL/FIRRTLAnnotationHelper.cpp:268` (todo)

At `lib/Dialect/FIRRTL/FIRRTLAnnotationHelper.cpp:268`, the marker `// TODO: containsReference().` indicates a gap signal in this path. What's missing: The marker documents unfinished work: containsReference().. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1169 `lib/Dialect/FIRRTL/FIRRTLAnnotationHelper.cpp:467` (implementation-gap)

At `lib/Dialect/FIRRTL/FIRRTLAnnotationHelper.cpp:467`, the marker `"unsupported operation, only CombMem can be used as the source of "` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1170 `lib/Conversion/DCToHW/DCToHW.cpp:217` (todo)

At `lib/Conversion/DCToHW/DCToHW.cpp:217`, the marker `/// @todo: should be moved to support.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: should be moved to support.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1171 `lib/Dialect/ESI/runtime/python/CMakeLists.txt:76` (todo)

At `lib/Dialect/ESI/runtime/python/CMakeLists.txt:76`, the marker `# TODO: have the patience to make this work.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: have the patience to make this work.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1172 `test/Tools/select-opentitan-connectivity-cfg-invalid-row-kind.test:7` (test-expectation)

At `test/Tools/select-opentitan-connectivity-cfg-invalid-row-kind.test:7`, the marker `// CHECK: unsupported connectivity CSV row kind 'BROKEN'` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1173 `lib/Dialect/FIRRTL/Import/FIRParser.cpp:578` (implementation-gap)

At `lib/Dialect/FIRRTL/Import/FIRParser.cpp:578`, the marker `"String-encoded integer literals are unsupported after FIRRTL 3.0.0");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1174 `lib/Dialect/FIRRTL/Import/FIRParser.cpp:754` (todo)

At `lib/Dialect/FIRRTL/Import/FIRParser.cpp:754`, the marker `// FIXME: Handle RelaxedId` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Handle RelaxedId. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1175 `lib/Dialect/FIRRTL/Import/FIRParser.cpp:2122` (todo)

At `lib/Dialect/FIRRTL/Import/FIRParser.cpp:2122`, the marker `// TODO: This is very similar to connect expansion in the LowerTypes pass` indicates a gap signal in this path. What's missing: The marker documents unfinished work: This is very similar to connect expansion in the LowerTypes pass. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1176 `lib/Dialect/FIRRTL/Import/FIRParser.cpp:2167` (todo)

At `lib/Dialect/FIRRTL/Import/FIRParser.cpp:2167`, the marker `/// XX ::= exp '.' DoubleLit // TODO Workaround for #470` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Workaround for #470. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1177 `lib/Dialect/FIRRTL/Import/FIRParser.cpp:2426` (todo)

At `lib/Dialect/FIRRTL/Import/FIRParser.cpp:2426`, the marker `/// XX ::= exp '.' DoubleLit // TODO Workaround for #470` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Workaround for #470. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1178 `lib/Dialect/FIRRTL/Import/FIRParser.cpp:3517` (todo)

At `lib/Dialect/FIRRTL/Import/FIRParser.cpp:3517`, the marker `// TODO(firrtl spec): There is no reason for the 'else :' grammar to take an` indicates a gap signal in this path. What's missing: The marker documents unfinished work: (firrtl spec): There is no reason for the 'else :' grammar to take an. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1179 `lib/Dialect/FIRRTL/Import/FIRParser.cpp:4056` (todo)

At `lib/Dialect/FIRRTL/Import/FIRParser.cpp:4056`, the marker `/// XXX: spec says static_reference, allow ref_expr anyway for read(probe(x)).` indicates a gap signal in this path. What's missing: The marker documents unfinished work: spec says static_reference, allow ref_expr anyway for read(probe(x)).. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1180 `lib/Dialect/FIRRTL/Import/FIRParser.cpp:4096` (implementation-gap)

At `lib/Dialect/FIRRTL/Import/FIRParser.cpp:4096`, the marker `// Check for other unsupported reference sources.` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1181 `lib/Dialect/FIRRTL/Import/FIRParser.cpp:4097` (todo)

At `lib/Dialect/FIRRTL/Import/FIRParser.cpp:4097`, the marker `// TODO: Add to ref.send verifier / inferReturnTypes.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Add to ref.send verifier / inferReturnTypes.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1182 `lib/Dialect/FIRRTL/Import/FIRParser.cpp:4341` (todo)

At `lib/Dialect/FIRRTL/Import/FIRParser.cpp:4341`, the marker `// TODO: Once support lands for agg-of-ref, add test for this check!` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Once support lands for agg-of-ref, add test for this check!. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1183 `lib/Dialect/FIRRTL/Import/FIRParser.cpp:4514` (todo)

At `lib/Dialect/FIRRTL/Import/FIRParser.cpp:4514`, the marker `// TODO: Once support lands for agg-of-ref, add test for this check!` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Once support lands for agg-of-ref, add test for this check!. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1184 `lib/Dialect/FIRRTL/Import/FIRParser.cpp:4722` (todo)

At `lib/Dialect/FIRRTL/Import/FIRParser.cpp:4722`, the marker `// TODO(firrtl spec) cmem is completely undocumented.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: (firrtl spec) cmem is completely undocumented.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1185 `lib/Dialect/FIRRTL/Import/FIRParser.cpp:4754` (todo)

At `lib/Dialect/FIRRTL/Import/FIRParser.cpp:4754`, the marker `// TODO(firrtl spec) smem is completely undocumented.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: (firrtl spec) smem is completely undocumented.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1186 `lib/Dialect/FIRRTL/Import/FIRParser.cpp:4900` (todo)

At `lib/Dialect/FIRRTL/Import/FIRParser.cpp:4900`, the marker `// TODO: Move this into MemOp construction/canonicalization.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Move this into MemOp construction/canonicalization.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1187 `lib/Dialect/FIRRTL/Import/FIRParser.cpp:5038` (todo)

At `lib/Dialect/FIRRTL/Import/FIRParser.cpp:5038`, the marker `// TODO(firrtl spec): info? should come after the clock expression before` indicates a gap signal in this path. What's missing: The marker documents unfinished work: (firrtl spec): info? should come after the clock expression before. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1188 `lib/Dialect/FIRRTL/Import/FIRParser.cpp:5059` (todo)

At `lib/Dialect/FIRRTL/Import/FIRParser.cpp:5059`, the marker `// TODO(firrtl spec): Simplify the grammar for register reset logic.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: (firrtl spec): Simplify the grammar for register reset logic.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1189 `lib/Dialect/FIRRTL/Import/FIRParser.cpp:5082` (todo)

At `lib/Dialect/FIRRTL/Import/FIRParser.cpp:5082`, the marker `// TODO(firrtl scala impl): pretty print registers without resets right.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: (firrtl scala impl): pretty print registers without resets right.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1190 `lib/Dialect/FIRRTL/Import/FIRParser.cpp:5339` (implementation-gap)

At `lib/Dialect/FIRRTL/Import/FIRParser.cpp:5339`, the marker `auto diag = emitError(loc, "Invalid/unsupported annotation format");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1191 `lib/Dialect/FIRRTL/Import/FIRParser.cpp:5932` (todo)

At `lib/Dialect/FIRRTL/Import/FIRParser.cpp:5932`, the marker `// TODO: Remove the old `, bound = N` variant in favor of the new parameters.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Remove the old `, bound = N` variant in favor of the new parameters.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1192 `test/Tools/circt-bmc/commandline.mlir:17` (test-gap-reference)

At `test/Tools/circt-bmc/commandline.mlir:17`, the marker `// CHECK-DAG: --drop-unsupported-sva` indicates a gap signal in this path. What's missing: This test line references an unsupported behavior, indicating a known gap in upstream implementation coverage. How to fix: Implement the missing behavior in production code and update this test to assert functional semantics rather than unsupported status once support is complete.

### G1193 `lib/Dialect/ESI/runtime/python/esiaccel/types.py:73` (implementation-gap)

At `lib/Dialect/ESI/runtime/python/esiaccel/types.py:73`, the marker `assert False, "unimplemented"` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1194 `lib/Dialect/ESI/runtime/python/esiaccel/types.py:78` (implementation-gap)

At `lib/Dialect/ESI/runtime/python/esiaccel/types.py:78`, the marker `assert False, "unimplemented"` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1195 `lib/Dialect/ESI/runtime/python/esiaccel/types.py:90` (implementation-gap)

At `lib/Dialect/ESI/runtime/python/esiaccel/types.py:90`, the marker `assert False, "unimplemented"` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1196 `lib/Dialect/ESI/runtime/python/esiaccel/types.py:95` (implementation-gap)

At `lib/Dialect/ESI/runtime/python/esiaccel/types.py:95`, the marker `assert False, "unimplemented"` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1197 `lib/Dialect/ESI/runtime/python/esiaccel/types.py:464` (implementation-gap)

At `lib/Dialect/ESI/runtime/python/esiaccel/types.py:464`, the marker `raise TypeError(f"unsupported type: {reason}")` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1198 `lib/Dialect/ESI/runtime/python/esiaccel/types.py:527` (todo)

At `lib/Dialect/ESI/runtime/python/esiaccel/types.py:527`, the marker `# TODO: add a proper registration mechanism for service ports.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: add a proper registration mechanism for service ports.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1199 `lib/Dialect/ESI/runtime/python/esiaccel/types.py:565` (todo)

At `lib/Dialect/ESI/runtime/python/esiaccel/types.py:565`, the marker `# TODO: respect timeout` indicates a gap signal in this path. What's missing: The marker documents unfinished work: respect timeout. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1200 `lib/Dialect/ESI/runtime/python/esiaccel/types.py:574` (implementation-gap)

At `lib/Dialect/ESI/runtime/python/esiaccel/types.py:574`, the marker `raise NotImplementedError("add_done_callback is not implemented")` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1201 `test/Tools/circt-bmc/circt-bmc-prune-unreachable-hw-before-smt.mlir:11` (test-gap-reference)

At `test/Tools/circt-bmc/circt-bmc-prune-unreachable-hw-before-smt.mlir:11`, the marker `// `hw.type_scope` is intentionally unsupported by HWToSMT.` indicates a gap signal in this path. What's missing: This test line references an unsupported behavior, indicating a known gap in upstream implementation coverage. How to fix: Implement the missing behavior in production code and update this test to assert functional semantics rather than unsupported status once support is complete.

### G1202 `lib/Dialect/FIRRTL/Import/FIRParserAsserts.cpp:13` (implementation-gap)

At `lib/Dialect/FIRRTL/Import/FIRParserAsserts.cpp:13`, the marker `// operations so that we may error on their unsupported use.` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1203 `lib/Conversion/ConvertToArcs/ConvertToArcs.cpp:241` (todo)

At `lib/Conversion/ConvertToArcs/ConvertToArcs.cpp:241`, the marker `// TODO: Remove the elements from the post order as we go.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Remove the elements from the post order as we go.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1204 `lib/Dialect/ESI/runtime/python/esiaccel/codegen.py:302` (implementation-gap)

At `lib/Dialect/ESI/runtime/python/esiaccel/codegen.py:302`, the marker `raise ValueError(f"Unsupported integer width: {type.bit_width}")` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1205 `lib/Dialect/ESI/runtime/python/esiaccel/codegen.py:373` (todo)

At `lib/Dialect/ESI/runtime/python/esiaccel/codegen.py:373`, the marker `# TODO: Bitfield layout is implementation-defined; consider` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Bitfield layout is implementation-defined; consider. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1206 `lib/Dialect/FIRRTL/Import/FIRLexer.cpp:99` (todo)

At `lib/Dialect/FIRRTL/Import/FIRLexer.cpp:99`, the marker `// TODO: Handle the rest of the escapes (octal and unicode).` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Handle the rest of the escapes (octal and unicode).. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1207 `lib/Dialect/FIRRTL/Transforms/IMConstProp.cpp:365` (todo)

At `lib/Dialect/FIRRTL/Transforms/IMConstProp.cpp:365`, the marker `// TODO: handle annotations: [[OptimizableExtModuleAnnotation]]` indicates a gap signal in this path. What's missing: The marker documents unfinished work: handle annotations: [[OptimizableExtModuleAnnotation]]. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1208 `lib/Dialect/FIRRTL/Transforms/IMConstProp.cpp:462` (implementation-gap)

At `lib/Dialect/FIRRTL/Transforms/IMConstProp.cpp:462`, the marker `assert(intAttr && "unsupported lattice attribute kind");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1209 `lib/Dialect/FIRRTL/Transforms/IMConstProp.cpp:539` (todo)

At `lib/Dialect/FIRRTL/Transforms/IMConstProp.cpp:539`, the marker `// TODO: We should handle aggregate operations such as` indicates a gap signal in this path. What's missing: The marker documents unfinished work: We should handle aggregate operations such as. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1210 `lib/Dialect/FIRRTL/Transforms/IMConstProp.cpp:861` (todo)

At `lib/Dialect/FIRRTL/Transforms/IMConstProp.cpp:861`, the marker `// TODO: Handle 'when' operations.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Handle 'when' operations.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1211 `lib/Dialect/FIRRTL/Transforms/IMConstProp.cpp:957` (implementation-gap)

At `lib/Dialect/FIRRTL/Transforms/IMConstProp.cpp:957`, the marker `else // Treat unsupported constants as overdefined.` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1212 `lib/Dialect/FIRRTL/Transforms/IMConstProp.cpp:1027` (todo)

At `lib/Dialect/FIRRTL/Transforms/IMConstProp.cpp:1027`, the marker `// TODO: Replace entire aggregate.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Replace entire aggregate.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1213 `lib/Dialect/FIRRTL/Transforms/IMConstProp.cpp:1034` (todo)

At `lib/Dialect/FIRRTL/Transforms/IMConstProp.cpp:1034`, the marker `// TODO: Let materializeConstant tell us what it supports instead of this.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Let materializeConstant tell us what it supports instead of this.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1214 `lib/Dialect/FIRRTL/Transforms/IMConstProp.cpp:1035` (implementation-gap)

At `lib/Dialect/FIRRTL/Transforms/IMConstProp.cpp:1035`, the marker `// Presently it asserts on unsupported combinations, so check this here.` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1215 `lib/Dialect/FIRRTL/Transforms/IMConstProp.cpp:1056` (todo)

At `lib/Dialect/FIRRTL/Transforms/IMConstProp.cpp:1056`, the marker `// TODO: Handle WhenOps correctly.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Handle WhenOps correctly.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1216 `lib/Dialect/ESI/runtime/python/esiaccel/esiCppAccel.cpp:113` (todo)

At `lib/Dialect/ESI/runtime/python/esiaccel/esiCppAccel.cpp:113`, the marker `// TODO: Fix leaks! The one I know of is in the callback code -- if one` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Fix leaks! The one I know of is in the callback code -- if one. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1217 `lib/Dialect/ESI/runtime/python/esiaccel/esiCppAccel.cpp:209` (todo)

At `lib/Dialect/ESI/runtime/python/esiaccel/esiCppAccel.cpp:209`, the marker `// TODO: "extra" field.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: "extra" field.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1218 `lib/Dialect/ESI/runtime/python/esiaccel/esiCppAccel.cpp:373` (todo)

At `lib/Dialect/ESI/runtime/python/esiaccel/esiCppAccel.cpp:373`, the marker `// TODO: Under certain conditions this will cause python to crash. I` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Under certain conditions this will cause python to crash. I. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1219 `lib/Dialect/SSP/Transforms/Schedule.cpp:73` (implementation-gap)

At `lib/Dialect/SSP/Transforms/Schedule.cpp:73`, the marker `llvm::errs() << "ssp-schedule: Unsupported problem '" << problemName` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1220 `lib/Dialect/SSP/Transforms/Schedule.cpp:163` (implementation-gap)

At `lib/Dialect/SSP/Transforms/Schedule.cpp:163`, the marker `llvm::errs() << "ssp-schedule: Unsupported problem '" << problemName` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1221 `lib/Dialect/SSP/Transforms/Schedule.cpp:201` (implementation-gap)

At `lib/Dialect/SSP/Transforms/Schedule.cpp:201`, the marker `llvm::errs() << "ssp-schedule: Unsupported problem '" << problemName` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1222 `lib/Dialect/SSP/Transforms/Schedule.cpp:223` (implementation-gap)

At `lib/Dialect/SSP/Transforms/Schedule.cpp:223`, the marker `llvm::errs() << "ssp-schedule: Unsupported problem '" << problemName` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1223 `lib/Dialect/SSP/Transforms/Schedule.cpp:254` (implementation-gap)

At `lib/Dialect/SSP/Transforms/Schedule.cpp:254`, the marker `llvm::errs() << "ssp-schedule: Unsupported scheduler '" << scheduler` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1224 `lib/Dialect/FIRRTL/FIRRTLTypes.cpp:832` (todo)

At `lib/Dialect/FIRRTL/FIRRTLTypes.cpp:832`, the marker `{elt.name, false /* FIXME */, elt.type.getMaskType()});` indicates a gap signal in this path. What's missing: The marker documents unfinished work: */, elt.type.getMaskType()});. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1225 `lib/Dialect/FIRRTL/FIRRTLTypes.cpp:1698` (todo)

At `lib/Dialect/FIRRTL/FIRRTLTypes.cpp:1698`, the marker `// TODO: Maybe just have elementType be FieldIDTypeInterface ?` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Maybe just have elementType be FieldIDTypeInterface ?. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1226 `lib/Dialect/FIRRTL/FIRRTLTypes.cpp:1863` (todo)

At `lib/Dialect/FIRRTL/FIRRTLTypes.cpp:1863`, the marker `// TODO: ConstTypeInterface / Trait ?` indicates a gap signal in this path. What's missing: The marker documents unfinished work: ConstTypeInterface / Trait ?. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1227 `lib/Dialect/FIRRTL/FIRRTLTypes.cpp:2137` (todo)

At `lib/Dialect/FIRRTL/FIRRTLTypes.cpp:2137`, the marker `// TODO: ConstTypeInterface / Trait ?` indicates a gap signal in this path. What's missing: The marker documents unfinished work: ConstTypeInterface / Trait ?. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1228 `lib/Dialect/FIRRTL/FIRRTLTypes.cpp:2369` (todo)

At `lib/Dialect/FIRRTL/FIRRTLTypes.cpp:2369`, the marker `// TODO: exclude reference containing` indicates a gap signal in this path. What's missing: The marker documents unfinished work: exclude reference containing. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1229 `lib/Dialect/FIRRTL/FIRRTLOpInterfaces.cpp:66` (todo)

At `lib/Dialect/FIRRTL/FIRRTLOpInterfaces.cpp:66`, the marker `// TODO: bitwidth is 1 when there are no ports, since APInt previously did not` indicates a gap signal in this path. What's missing: The marker documents unfinished work: bitwidth is 1 when there are no ports, since APInt previously did not. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1230 `lib/Dialect/FIRRTL/FIRRTLOpInterfaces.cpp:86` (todo)

At `lib/Dialect/FIRRTL/FIRRTLOpInterfaces.cpp:86`, the marker `// TODO: it seems weird to allow empty port annotations.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: it seems weird to allow empty port annotations.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1231 `lib/Dialect/FIRRTL/FIRRTLOpInterfaces.cpp:89` (todo)

At `lib/Dialect/FIRRTL/FIRRTLOpInterfaces.cpp:89`, the marker `// TODO: Move this into an annotation verifier.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Move this into an annotation verifier.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1232 `lib/Dialect/FIRRTL/FIRRTLOpInterfaces.cpp:207` (todo)

At `lib/Dialect/FIRRTL/FIRRTLOpInterfaces.cpp:207`, the marker `// TODO: Find a way to not check same things RefType::get/verify does.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Find a way to not check same things RefType::get/verify does.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1233 `lib/Conversion/CombToSynth/CombToSynth.cpp:178` (todo)

At `lib/Conversion/CombToSynth/CombToSynth.cpp:178`, the marker `// TODO: We can handle replicate, extract, etc.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: We can handle replicate, extract, etc.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1234 `lib/Conversion/CombToSynth/CombToSynth.cpp:488` (todo)

At `lib/Conversion/CombToSynth/CombToSynth.cpp:488`, the marker `// TODO: Perform a more thorough analysis to motivate the choices or` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Perform a more thorough analysis to motivate the choices or. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1235 `lib/Conversion/CombToSynth/CombToSynth.cpp:703` (todo)

At `lib/Conversion/CombToSynth/CombToSynth.cpp:703`, the marker `// TODO: Generalize to other parallel prefix trees.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Generalize to other parallel prefix trees.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1236 `lib/Conversion/CombToSynth/CombToSynth.cpp:1038` (todo)

At `lib/Conversion/CombToSynth/CombToSynth.cpp:1038`, the marker `// TODO: Implement a signed division lowering at least for power of two.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Implement a signed division lowering at least for power of two.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1237 `lib/Conversion/CombToSynth/CombToSynth.cpp:1056` (todo)

At `lib/Conversion/CombToSynth/CombToSynth.cpp:1056`, the marker `// TODO: Implement a signed modulus lowering at least for power of two.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Implement a signed modulus lowering at least for power of two.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1238 `lib/Conversion/CombToSynth/CombToSynth.cpp:1102` (todo)

At `lib/Conversion/CombToSynth/CombToSynth.cpp:1102`, the marker `// TODO: Lazily compute only the required prefix values. Kogge-Stone is` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Lazily compute only the required prefix values. Kogge-Stone is. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1239 `lib/Conversion/CombToSynth/CombToSynth.cpp:1243` (implementation-gap)

At `lib/Conversion/CombToSynth/CombToSynth.cpp:1243`, the marker `op.getLoc(), "i0 signed comparison is unsupported");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1240 `lib/Conversion/CombToSynth/CombToSynth.cpp:1361` (implementation-gap)

At `lib/Conversion/CombToSynth/CombToSynth.cpp:1361`, the marker `"i0 signed shift is unsupported");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1241 `lib/Dialect/FIRRTL/Transforms/CreateSiFiveMetadata.cpp:273` (todo)

At `lib/Dialect/FIRRTL/Transforms/CreateSiFiveMetadata.cpp:273`, the marker `// TODO: This incongruity seems bad. Can we instead not generate metadata` indicates a gap signal in this path. What's missing: The marker documents unfinished work: This incongruity seems bad. Can we instead not generate metadata. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1242 `lib/Dialect/FIRRTL/Transforms/CreateSiFiveMetadata.cpp:275` (unclassified)

At `lib/Dialect/FIRRTL/Transforms/CreateSiFiveMetadata.cpp:275`, the marker `bool inDut = instanceInfo.anyInstanceInEffectiveDesign(mem);` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1243 `lib/Dialect/FIRRTL/Transforms/CreateSiFiveMetadata.cpp:321` (todo)

At `lib/Dialect/FIRRTL/Transforms/CreateSiFiveMetadata.cpp:321`, the marker `// TODO: This unresolvable distinct seems sketchy.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: This unresolvable distinct seems sketchy.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1244 `lib/Dialect/FIRRTL/Transforms/CreateSiFiveMetadata.cpp:624` (unclassified)

At `lib/Dialect/FIRRTL/Transforms/CreateSiFiveMetadata.cpp:624`, the marker `if (!instanceInfo->anyInstanceInEffectiveDesign(mem))` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1245 `lib/Dialect/FIRRTL/Transforms/CreateSiFiveMetadata.cpp:823` (unclassified)

At `lib/Dialect/FIRRTL/Transforms/CreateSiFiveMetadata.cpp:823`, the marker `!instanceInfo->anyInstanceInEffectiveDesign(module))` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1246 `lib/Dialect/FIRRTL/Transforms/CreateSiFiveMetadata.cpp:909` (unclassified)

At `lib/Dialect/FIRRTL/Transforms/CreateSiFiveMetadata.cpp:909`, the marker `if (instanceInfo->anyInstanceInEffectiveDesign(extModule)) {` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1247 `lib/Dialect/FIRRTL/Transforms/RemoveUnusedPorts.cpp:85` (todo)

At `lib/Dialect/FIRRTL/Transforms/RemoveUnusedPorts.cpp:85`, the marker `// TODO: Handle inout ports.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Handle inout ports.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1248 `lib/Dialect/FIRRTL/Transforms/ModuleInliner.cpp:420` (todo)

At `lib/Dialect/FIRRTL/Transforms/ModuleInliner.cpp:420`, the marker `// TODO: what to do with users that aren't local (or not mapped?).` indicates a gap signal in this path. What's missing: The marker documents unfinished work: what to do with users that aren't local (or not mapped?).. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1249 `lib/Dialect/FIRRTL/Transforms/ModuleInliner.cpp:739` (todo)

At `lib/Dialect/FIRRTL/Transforms/ModuleInliner.cpp:739`, the marker `// TODO: There is currently no good way to annotate an explicit parent scope` indicates a gap signal in this path. What's missing: The marker documents unfinished work: There is currently no good way to annotate an explicit parent scope. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1250 `lib/Dialect/FIRRTL/Transforms/ModuleInliner.cpp:979` (implementation-gap)

At `lib/Dialect/FIRRTL/Transforms/ModuleInliner.cpp:979`, the marker `return source->emitError("unsupported operation '")` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1251 `lib/Dialect/FIRRTL/Transforms/ModuleInliner.cpp:1227` (todo)

At `lib/Dialect/FIRRTL/Transforms/ModuleInliner.cpp:1227`, the marker `// TODO: Update any symbol renames which need to be used by the next` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Update any symbol renames which need to be used by the next. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1252 `lib/Dialect/FIRRTL/Transforms/ModuleInliner.cpp:1316` (todo)

At `lib/Dialect/FIRRTL/Transforms/ModuleInliner.cpp:1316`, the marker `// TODO: Update any symbol renames which need to be used by the next` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Update any symbol renames which need to be used by the next. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1253 `lib/Dialect/FIRRTL/Transforms/ExtractInstances.cpp:248` (todo)

At `lib/Dialect/FIRRTL/Transforms/ExtractInstances.cpp:248`, the marker `// TODO: This is tech debt. This was accepted on condition that work is done` indicates a gap signal in this path. What's missing: The marker documents unfinished work: This is tech debt. This was accepted on condition that work is done. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1254 `lib/Dialect/FIRRTL/Transforms/ExtractInstances.cpp:376` (todo)

At `lib/Dialect/FIRRTL/Transforms/ExtractInstances.cpp:376`, the marker `// TODO: This defname matching is a terrible hack and should be replaced with` indicates a gap signal in this path. What's missing: The marker documents unfinished work: This defname matching is a terrible hack and should be replaced with. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1255 `lib/Dialect/FIRRTL/Transforms/ExtractInstances.cpp:384` (unclassified)

At `lib/Dialect/FIRRTL/Transforms/ExtractInstances.cpp:384`, the marker `if (!instanceInfo->anyInstanceInDesign(module)) {` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1256 `lib/Dialect/FIRRTL/Transforms/ExtractInstances.cpp:391` (todo)

At `lib/Dialect/FIRRTL/Transforms/ExtractInstances.cpp:391`, the marker `info.prefix = "clock_gate"; // TODO: Don't hardcode this` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Don't hardcode this. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1257 `lib/Dialect/FIRRTL/Transforms/ExtractInstances.cpp:416` (unclassified)

At `lib/Dialect/FIRRTL/Transforms/ExtractInstances.cpp:416`, the marker `if (!instanceInfo->anyInstanceInDesign(module)) {` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1258 `lib/Dialect/FIRRTL/Transforms/ExtractInstances.cpp:423` (todo)

At `lib/Dialect/FIRRTL/Transforms/ExtractInstances.cpp:423`, the marker `info.prefix = "mem_wiring"; // TODO: Don't hardcode this` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Don't hardcode this. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1259 `lib/Dialect/FIRRTL/Transforms/ExtractInstances.cpp:544` (unclassified)

At `lib/Dialect/FIRRTL/Transforms/ExtractInstances.cpp:544`, the marker `!instanceInfo->anyInstanceInDesign(parent) ||` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1260 `lib/Dialect/FIRRTL/Transforms/ExtractInstances.cpp:1130` (unclassified)

At `lib/Dialect/FIRRTL/Transforms/ExtractInstances.cpp:1130`, the marker `!instanceInfo->anyInstanceInDesign(cast<igraph::ModuleOpInterface>(` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1261 `lib/Dialect/FIRRTL/Transforms/InferReadWrite.cpp:57` (todo)

At `lib/Dialect/FIRRTL/Transforms/InferReadWrite.cpp:57`, the marker `// TODO: This would be better handled if WhenOps were moved into the` indicates a gap signal in this path. What's missing: The marker documents unfinished work: This would be better handled if WhenOps were moved into the. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1262 `lib/Dialect/FIRRTL/Transforms/InferReadWrite.cpp:62` (implementation-gap)

At `lib/Dialect/FIRRTL/Transforms/InferReadWrite.cpp:62`, the marker `<< "is unsupported by InferReadWrite as this pass cannot trace "` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1263 `lib/Dialect/FIRRTL/Transforms/MergeConnections.cpp:52` (todo)

At `lib/Dialect/FIRRTL/Transforms/MergeConnections.cpp:52`, the marker `// TODO: Add unrealized_conversion, asUInt, asSInt` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Add unrealized_conversion, asUInt, asSInt. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1264 `lib/Dialect/FIRRTL/Transforms/MemToRegOfVec.cpp:50` (unclassified)

At `lib/Dialect/FIRRTL/Transforms/MemToRegOfVec.cpp:50`, the marker `if (instanceInfo.anyInstanceInEffectiveDesign(moduleOp))` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1265 `lib/Dialect/FIRRTL/Transforms/IMDeadCodeElim.cpp:272` (todo)

At `lib/Dialect/FIRRTL/Transforms/IMDeadCodeElim.cpp:272`, the marker `// TODO: Handle attach etc.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Handle attach etc.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1266 `lib/Dialect/FIRRTL/Transforms/IMDeadCodeElim.cpp:396` (todo)

At `lib/Dialect/FIRRTL/Transforms/IMDeadCodeElim.cpp:396`, the marker `// FIXME: We copy the list of modules into a vector first to avoid iterator` indicates a gap signal in this path. What's missing: The marker documents unfinished work: We copy the list of modules into a vector first to avoid iterator. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1267 `test/Target/ExportSystemC/errors.mlir:3` (test-expectation)

At `test/Target/ExportSystemC/errors.mlir:3`, the marker `// CHECK: <<UNSUPPORTED OPERATION (hw.module)>>` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1268 `test/Target/ExportSystemC/errors.mlir:9` (test-expectation)

At `test/Target/ExportSystemC/errors.mlir:9`, the marker `// CHECK: <<UNSUPPORTED TYPE (!hw.inout<i2>)>>` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1269 `lib/Conversion/HandshakeToHW/HandshakeToHW.cpp:165` (implementation-gap)

At `lib/Conversion/HandshakeToHW/HandshakeToHW.cpp:165`, the marker `emitError(loc) << "unsupported data type '" << type << "'";` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1270 `lib/Conversion/HandshakeToHW/HandshakeToHW.cpp:189` (implementation-gap)

At `lib/Conversion/HandshakeToHW/HandshakeToHW.cpp:189`, the marker `oldOp->emitError("unsupported constant type");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1271 `lib/Conversion/HandshakeToHW/HandshakeToHW.cpp:456` (todo)

At `lib/Conversion/HandshakeToHW/HandshakeToHW.cpp:456`, the marker `// @todo: should be moved to support.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: should be moved to support.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1272 `lib/Conversion/HandshakeToHW/HandshakeToHW.cpp:665` (todo)

At `lib/Conversion/HandshakeToHW/HandshakeToHW.cpp:665`, the marker `// Todo: clean up when handshake supports i0.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: clean up when handshake supports i0.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1273 `lib/Conversion/HandshakeToHW/HandshakeToHW.cpp:703` (implementation-gap)

At `lib/Conversion/HandshakeToHW/HandshakeToHW.cpp:703`, the marker `emitError(loc) << "unsupported type for zero value: " << type;` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1274 `lib/Conversion/HandshakeToHW/HandshakeToHW.cpp:1667` (todo)

At `lib/Conversion/HandshakeToHW/HandshakeToHW.cpp:1667`, the marker `// Todo: Change when i0 support is added.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Change when i0 support is added.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1275 `test/Target/ExportSystemC/basic.mlir:159` (todo)

At `test/Target/ExportSystemC/basic.mlir:159`, the marker `// TODO: add this test-case once we have support for an inlinable operation that has lower precedence` indicates a gap signal in this path. What's missing: The marker documents unfinished work: add this test-case once we have support for an inlinable operation that has lower precedence. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1276 `test/Target/ExportSystemC/basic.mlir:167` (todo)

At `test/Target/ExportSystemC/basic.mlir:167`, the marker `// TODO: add this test-case once we have support for an inlinable operation that has lower precedence` indicates a gap signal in this path. What's missing: The marker documents unfinished work: add this test-case once we have support for an inlinable operation that has lower precedence. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1277 `test/Target/ExportSystemC/basic.mlir:193` (todo)

At `test/Target/ExportSystemC/basic.mlir:193`, the marker `// TODO: no operation having COMMA precedence supported yet` indicates a gap signal in this path. What's missing: The marker documents unfinished work: no operation having COMMA precedence supported yet. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1278 `test/Target/ExportSystemC/basic.mlir:196` (todo)

At `test/Target/ExportSystemC/basic.mlir:196`, the marker `// TODO: no applicable operation having lower precedence than CAST supported yet` indicates a gap signal in this path. What's missing: The marker documents unfinished work: no applicable operation having lower precedence than CAST supported yet. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1279 `test/Target/ExportSystemC/basic.mlir:230` (todo)

At `test/Target/ExportSystemC/basic.mlir:230`, the marker `// TODO: there is currently no appropriate inlinable operation implemented with lower precedence` indicates a gap signal in this path. What's missing: The marker documents unfinished work: there is currently no appropriate inlinable operation implemented with lower precedence. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1280 `test/Target/ExportSystemC/basic.mlir:237` (todo)

At `test/Target/ExportSystemC/basic.mlir:237`, the marker `// TODO: there is currently no appropriate inlinable operation implemented with lower precedence` indicates a gap signal in this path. What's missing: The marker documents unfinished work: there is currently no appropriate inlinable operation implemented with lower precedence. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1281 `lib/Dialect/FIRRTL/Transforms/LowerOpenAggs.cpp:597` (todo)

At `lib/Dialect/FIRRTL/Transforms/LowerOpenAggs.cpp:597`, the marker `// TODO: add and erase ports without intermediate + various array attributes.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: add and erase ports without intermediate + various array attributes.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1282 `lib/Dialect/FIRRTL/Transforms/LinkCircuits.cpp:232` (todo)

At `lib/Dialect/FIRRTL/Transforms/LinkCircuits.cpp:232`, the marker `// FIXME: definition and declaration may have different defname and` indicates a gap signal in this path. What's missing: The marker documents unfinished work: definition and declaration may have different defname and. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1283 `lib/Dialect/FIRRTL/Transforms/LinkCircuits.cpp:263` (todo)

At `lib/Dialect/FIRRTL/Transforms/LinkCircuits.cpp:263`, the marker `// TODO: other circuit attributes (such as enable_layers...)` indicates a gap signal in this path. What's missing: The marker documents unfinished work: other circuit attributes (such as enable_layers...). The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1284 `test/Tools/run-opentitan-fpv-circt-bmc-stopat-selector-validation.test:11` (unclassified)

At `test/Tools/run-opentitan-fpv-circt-bmc-stopat-selector-validation.test:11`, the marker `// ROWS: ERROR{{[[:space:]]+}}foo_sec_cm{{[[:space:]]+}}hw/top_earlgrey/formal/sec_cm/foo{{[[:space:]]+}}opentitan{{[[:space:]]+}}FPV_BMC{{[[:space:]]+}}CIRCT_BMC_ERROR{{[[:space:]]+}}unsupported_stopat_selector` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1285 `lib/Dialect/FIRRTL/Transforms/LowerMemory.cpp:497` (todo)

At `lib/Dialect/FIRRTL/Transforms/LowerMemory.cpp:497`, the marker `// TODO: how do we lower port annotations?` indicates a gap signal in this path. What's missing: The marker documents unfinished work: how do we lower port annotations?. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1286 `lib/Dialect/FIRRTL/Transforms/LowerMemory.cpp:557` (unclassified)

At `lib/Dialect/FIRRTL/Transforms/LowerMemory.cpp:557`, the marker `auto shouldDedup = instanceInfo.anyInstanceInEffectiveDesign(moduleOp);` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1287 `lib/Dialect/FIRRTL/Transforms/CheckLayers.cpp:43` (unclassified)

At `lib/Dialect/FIRRTL/Transforms/CheckLayers.cpp:43`, the marker `if (!iInfo.anyInstanceUnderLayer(moduleOp))` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1288 `lib/Dialect/FIRRTL/Transforms/CheckLayers.cpp:73` (unclassified)

At `lib/Dialect/FIRRTL/Transforms/CheckLayers.cpp:73`, the marker `iInfo.anyInstanceUnderLayer(parent)) {` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1289 `lib/Conversion/MooreToCore/MooreToCore.cpp:1969` (todo)

At `lib/Conversion/MooreToCore/MooreToCore.cpp:1969`, the marker `// TODO: Handle vtable generation over ClassMethodDeclOp here.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Handle vtable generation over ClassMethodDeclOp here.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1290 `lib/Conversion/MooreToCore/MooreToCore.cpp:2100` (implementation-gap)

At `lib/Conversion/MooreToCore/MooreToCore.cpp:2100`, the marker `<< port.name << "' has unsupported type " << port.type` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1291 `lib/Conversion/MooreToCore/MooreToCore.cpp:2107` (todo)

At `lib/Conversion/MooreToCore/MooreToCore.cpp:2107`, the marker `// FIXME: Once we support net<...>, ref<...> type to represent type of` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Once we support net<...>, ref<...> type to represent type of. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1292 `lib/Conversion/MooreToCore/MooreToCore.cpp:3761` (todo)

At `lib/Conversion/MooreToCore/MooreToCore.cpp:3761`, the marker `// TODO: Once the core dialects support four-valued integers, this code` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Once the core dialects support four-valued integers, this code. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1293 `lib/Conversion/MooreToCore/MooreToCore.cpp:6031` (todo)

At `lib/Conversion/MooreToCore/MooreToCore.cpp:6031`, the marker `// TODO: Generate loop-based validation for complex element constraints.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Generate loop-based validation for complex element constraints.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1294 `lib/Conversion/MooreToCore/MooreToCore.cpp:6054` (todo)

At `lib/Conversion/MooreToCore/MooreToCore.cpp:6054`, the marker `// TODO: Generate weighted random number generation calls.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Generate weighted random number generation calls.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1295 `lib/Conversion/MooreToCore/MooreToCore.cpp:7126` (implementation-gap)

At `lib/Conversion/MooreToCore/MooreToCore.cpp:7126`, the marker `loc, "unsupported net kind");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1296 `lib/Conversion/MooreToCore/MooreToCore.cpp:7132` (todo)

At `lib/Conversion/MooreToCore/MooreToCore.cpp:7132`, the marker `// TODO: Once the core dialects support four-valued integers, this code` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Once the core dialects support four-valued integers, this code. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1297 `lib/Conversion/MooreToCore/MooreToCore.cpp:7819` (todo)

At `lib/Conversion/MooreToCore/MooreToCore.cpp:7819`, the marker `// TODO: return X if the domain is four-valued for out-of-bounds accesses` indicates a gap signal in this path. What's missing: The marker documents unfinished work: return X if the domain is four-valued for out-of-bounds accesses. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1298 `lib/Conversion/MooreToCore/MooreToCore.cpp:8108` (todo)

At `lib/Conversion/MooreToCore/MooreToCore.cpp:8108`, the marker `// TODO: properly handle out-of-bounds accesses` indicates a gap signal in this path. What's missing: The marker documents unfinished work: properly handle out-of-bounds accesses. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1299 `lib/Conversion/MooreToCore/MooreToCore.cpp:8878` (todo)

At `lib/Conversion/MooreToCore/MooreToCore.cpp:8878`, the marker `// TODO: properly handle out-of-bounds accesses` indicates a gap signal in this path. What's missing: The marker documents unfinished work: properly handle out-of-bounds accesses. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1300 `lib/Conversion/MooreToCore/MooreToCore.cpp:11233` (todo)

At `lib/Conversion/MooreToCore/MooreToCore.cpp:11233`, the marker `// TODO: Once the core dialects support four-valued integers, we will have` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Once the core dialects support four-valued integers, we will have. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1301 `lib/Conversion/MooreToCore/MooreToCore.cpp:12736` (unclassified)

At `lib/Conversion/MooreToCore/MooreToCore.cpp:12736`, the marker `// 1. Free functions (5 args): uvm_pkg::uvm_report_xxx(id, msg, verbosity, filename, line)` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1302 `lib/Conversion/MooreToCore/MooreToCore.cpp:12737` (unclassified)

At `lib/Conversion/MooreToCore/MooreToCore.cpp:12737`, the marker `// 2. Class methods (6 args): uvm_pkg::uvm_report_object::uvm_report_xxx(self, id, msg, verbosity, filename, line)` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1303 `lib/Conversion/MooreToCore/MooreToCore.cpp:12739` (unclassified)

At `lib/Conversion/MooreToCore/MooreToCore.cpp:12739`, the marker `// __moore_uvm_report_xxx(id_ptr, id_len, msg_ptr, msg_len, verbosity,` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1304 `lib/Conversion/MooreToCore/MooreToCore.cpp:12811` (unclassified)

At `lib/Conversion/MooreToCore/MooreToCore.cpp:12811`, the marker `// void __moore_uvm_report_xxx(` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1305 `lib/Conversion/MooreToCore/MooreToCore.cpp:13009` (unclassified)

At `lib/Conversion/MooreToCore/MooreToCore.cpp:13009`, the marker `// void __moore_uvm_report_xxx(` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1306 `lib/Conversion/MooreToCore/MooreToCore.cpp:14637` (todo)

At `lib/Conversion/MooreToCore/MooreToCore.cpp:14637`, the marker `// TODO: This lowering is only correct if the condition is two-valued. If` indicates a gap signal in this path. What's missing: The marker documents unfinished work: This lowering is only correct if the condition is two-valued. If. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1307 `lib/Conversion/MooreToCore/MooreToCore.cpp:15148` (implementation-gap)

At `lib/Conversion/MooreToCore/MooreToCore.cpp:15148`, the marker `return rewriter.notifyMatchFailure(op, "unsupported int format");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1308 `lib/Conversion/MooreToCore/MooreToCore.cpp:16035` (implementation-gap)

At `lib/Conversion/MooreToCore/MooreToCore.cpp:16035`, the marker `// Unsupported format string type - return empty string` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1309 `lib/Conversion/MooreToCore/MooreToCore.cpp:17884` (implementation-gap)

At `lib/Conversion/MooreToCore/MooreToCore.cpp:17884`, the marker `return rewriter.notifyMatchFailure(op, "unsupported queue type");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1310 `lib/Conversion/MooreToCore/MooreToCore.cpp:18190` (implementation-gap)

At `lib/Conversion/MooreToCore/MooreToCore.cpp:18190`, the marker `return rewriter.notifyMatchFailure(op, "unsupported queue type");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1311 `lib/Conversion/MooreToCore/MooreToCore.cpp:19004` (implementation-gap)

At `lib/Conversion/MooreToCore/MooreToCore.cpp:19004`, the marker `return rewriter.notifyMatchFailure(op, "unsupported input type");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1312 `lib/Conversion/MooreToCore/MooreToCore.cpp:19149` (implementation-gap)

At `lib/Conversion/MooreToCore/MooreToCore.cpp:19149`, the marker `"unsupported destination ref type");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1313 `lib/Conversion/MooreToCore/MooreToCore.cpp:19925` (implementation-gap)

At `lib/Conversion/MooreToCore/MooreToCore.cpp:19925`, the marker `// Unsupported type in path` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1314 `lib/Conversion/MooreToCore/MooreToCore.cpp:21188` (implementation-gap)

At `lib/Conversion/MooreToCore/MooreToCore.cpp:21188`, the marker `// Unsupported operation` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1315 `lib/Conversion/MooreToCore/MooreToCore.cpp:21584` (implementation-gap)

At `lib/Conversion/MooreToCore/MooreToCore.cpp:21584`, the marker `return rewriter.notifyMatchFailure(op, "unsupported array type");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1316 `lib/Conversion/MooreToCore/MooreToCore.cpp:22223` (implementation-gap)

At `lib/Conversion/MooreToCore/MooreToCore.cpp:22223`, the marker `return rewriter.notifyMatchFailure(op, "unsupported array type");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1317 `lib/Conversion/MooreToCore/MooreToCore.cpp:22610` (implementation-gap)

At `lib/Conversion/MooreToCore/MooreToCore.cpp:22610`, the marker `return rewriter.notifyMatchFailure(loc, "unsupported key type");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1318 `lib/Conversion/MooreToCore/MooreToCore.cpp:23960` (implementation-gap)

At `lib/Conversion/MooreToCore/MooreToCore.cpp:23960`, the marker `op, "sscanf: unsupported destination type for arg " +` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1319 `lib/Conversion/MooreToCore/MooreToCore.cpp:24064` (implementation-gap)

At `lib/Conversion/MooreToCore/MooreToCore.cpp:24064`, the marker `op, "fscanf: unsupported destination type for arg " +` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1320 `lib/Conversion/MooreToCore/MooreToCore.cpp:24335` (unclassified)

At `lib/Conversion/MooreToCore/MooreToCore.cpp:24335`, the marker `// void __moore_xxx(MooreString *message)` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1321 `lib/Conversion/MooreToCore/MooreToCore.cpp:30654` (todo)

At `lib/Conversion/MooreToCore/MooreToCore.cpp:30654`, the marker `// FIXME: Unpacked arrays support more element types than their packed` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Unpacked arrays support more element types than their packed. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1322 `lib/Conversion/MooreToCore/MooreToCore.cpp:30683` (todo)

At `lib/Conversion/MooreToCore/MooreToCore.cpp:30683`, the marker `// FIXME: Mapping unpacked struct type to struct type in hw dialect may be a` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Mapping unpacked struct type to struct type in hw dialect may be a. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1323 `lib/Dialect/FIRRTL/Transforms/LowerDPI.cpp:109` (todo)

At `lib/Dialect/FIRRTL/Transforms/LowerDPI.cpp:109`, the marker `// TODO: Support bundle or enum types.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Support bundle or enum types.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1324 `lib/Dialect/FIRRTL/Transforms/LowerDPI.cpp:206` (todo)

At `lib/Dialect/FIRRTL/Transforms/LowerDPI.cpp:206`, the marker `// TODO: This should be implemented as a verifier once function is added` indicates a gap signal in this path. What's missing: The marker documents unfinished work: This should be implemented as a verifier once function is added. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1325 `lib/Dialect/ESI/runtime/cpp/lib/Manifest.cpp:57` (todo)

At `lib/Dialect/ESI/runtime/cpp/lib/Manifest.cpp:57`, the marker `/// activeServices table. TODO: re-using this for the engines section is a` indicates a gap signal in this path. What's missing: The marker documents unfinished work: re-using this for the engines section is a. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1326 `lib/Dialect/ESI/runtime/cpp/lib/Manifest.cpp:178` (todo)

At `lib/Dialect/ESI/runtime/cpp/lib/Manifest.cpp:178`, the marker `// TODO: Check or guide the conversion of the value to the type based on the` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Check or guide the conversion of the value to the type based on the. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1327 `lib/Dialect/ESI/runtime/cpp/lib/Manifest.cpp:290` (todo)

At `lib/Dialect/ESI/runtime/cpp/lib/Manifest.cpp:290`, the marker `// TODO: support engines at lower levels.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: support engines at lower levels.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1328 `lib/Dialect/ESI/runtime/cpp/lib/Manifest.cpp:324` (todo)

At `lib/Dialect/ESI/runtime/cpp/lib/Manifest.cpp:324`, the marker `/// TODO: Hack. This method is a giant hack to reuse the getService method for` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Hack. This method is a giant hack to reuse the getService method for. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1329 `lib/Conversion/HWToSystemC/HWToSystemC.cpp:71` (todo)

At `lib/Conversion/HWToSystemC/HWToSystemC.cpp:71`, the marker `// TODO: implement logic to extract a better name and properly unique it.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: implement logic to extract a better name and properly unique it.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1330 `lib/Conversion/HWToSystemC/HWToSystemC.cpp:77` (todo)

At `lib/Conversion/HWToSystemC/HWToSystemC.cpp:77`, the marker `// TODO: do some dominance analysis to detect use-before-def and cycles in` indicates a gap signal in this path. What's missing: The marker documents unfinished work: do some dominance analysis to detect use-before-def and cycles in. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1331 `lib/Dialect/FIRRTL/Transforms/CheckCombLoops.cpp:336` (todo)

At `lib/Dialect/FIRRTL/Transforms/CheckCombLoops.cpp:336`, the marker `// TODO: External modules not handled !!` indicates a gap signal in this path. What's missing: The marker documents unfinished work: External modules not handled !!. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1332 `lib/Dialect/FIRRTL/Transforms/LowerCHIRRTL.cpp:308` (todo)

At `lib/Dialect/FIRRTL/Transforms/LowerCHIRRTL.cpp:308`, the marker `// should just be deleted. TODO: this is mirroring SFC, but should we be` indicates a gap signal in this path. What's missing: The marker documents unfinished work: this is mirroring SFC, but should we be. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1333 `lib/Dialect/FIRRTL/Transforms/LowerCHIRRTL.cpp:454` (todo)

At `lib/Dialect/FIRRTL/Transforms/LowerCHIRRTL.cpp:454`, the marker `// TODO: SFC does not infer any enable when using a module port as the` indicates a gap signal in this path. What's missing: The marker documents unfinished work: SFC does not infer any enable when using a module port as the. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1334 `lib/Dialect/FIRRTL/Transforms/Dedup.cpp:307` (todo)

At `lib/Dialect/FIRRTL/Transforms/Dedup.cpp:307`, the marker `// TODO: properly handle DistinctAttr, including its use in paths.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: properly handle DistinctAttr, including its use in paths.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1335 `lib/Dialect/FIRRTL/Transforms/Dedup.cpp:589` (todo)

At `lib/Dialect/FIRRTL/Transforms/Dedup.cpp:589`, the marker `// TODO: we should print the port number if there are no port names, but` indicates a gap signal in this path. What's missing: The marker documents unfinished work: we should print the port number if there are no port names, but. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1336 `lib/Dialect/FIRRTL/Transforms/Dedup.cpp:733` (todo)

At `lib/Dialect/FIRRTL/Transforms/Dedup.cpp:733`, the marker `// TODO: properly handle DistinctAttr, including its use in paths.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: properly handle DistinctAttr, including its use in paths.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1337 `lib/Dialect/FIRRTL/Transforms/Lint.cpp:107` (unclassified)

At `lib/Dialect/FIRRTL/Transforms/Lint.cpp:107`, the marker `if (!instanceInfo.anyInstanceInDesign(fModule))` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1338 `lib/Dialect/ESI/runtime/cpp/lib/Services.cpp:167` (implementation-gap)

At `lib/Dialect/ESI/runtime/cpp/lib/Services.cpp:167`, the marker `throw std::runtime_error("Unsupported ESI header version: " +` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1339 `lib/Dialect/ESI/runtime/cpp/lib/Services.cpp:422` (todo)

At `lib/Dialect/ESI/runtime/cpp/lib/Services.cpp:422`, the marker `// TODO: Add a proper registration mechanism.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Add a proper registration mechanism.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1340 `lib/Dialect/ESI/runtime/cpp/lib/Services.cpp:435` (todo)

At `lib/Dialect/ESI/runtime/cpp/lib/Services.cpp:435`, the marker `// TODO: Add a proper registration mechanism.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Add a proper registration mechanism.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1341 `test/Tools/run-formal-all-strict-gate-bmc-lec-contract-fingerprint-parity-defaults.test:8` (test-expectation)

At `test/Tools/run-formal-all-strict-gate-bmc-lec-contract-fingerprint-parity-defaults.test:8`, the marker `// RUN: printf '#!/usr/bin/env bash\nset -euo pipefail\n: "${OUT:?}"\n: "${LEC_RESOLVED_CONTRACTS_OUT:?}"\nmkdir -p "$(dirname "$OUT")"\nprintf "PASS\tcase_a\t/tests/case_a.sv\tsv-tests\tLEC\tEQ\n" > "$OUT"\nprintf "case_a\t/tests/case_a.sv\tmanifest\tsmtlib\t300\tlec\t0\t0\t\txxxx9999xxxx9999\n" > "$LEC_RESOLVED_CONTRACTS_OUT"\necho "sv-tests LEC summary: total=1 pass=1 fail=0 error=0 skip=0"\n' > %t/utils/run_sv_tests_circt_lec.sh` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1342 `lib/Dialect/ESI/runtime/cpp/lib/backends/RpcClient.cpp:180` (todo)

At `lib/Dialect/ESI/runtime/cpp/lib/backends/RpcClient.cpp:180`, the marker `// loop until it is. TODO: fix this with the DPI API change.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: fix this with the DPI API change.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1343 `lib/Conversion/LoopScheduleToCalyx/LoopScheduleToCalyx.cpp:736` (implementation-gap)

At `lib/Conversion/LoopScheduleToCalyx/LoopScheduleToCalyx.cpp:736`, the marker `llvm_unreachable("unsupported comparison predicate");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1344 `lib/Conversion/LoopScheduleToCalyx/LoopScheduleToCalyx.cpp:994` (implementation-gap)

At `lib/Conversion/LoopScheduleToCalyx/LoopScheduleToCalyx.cpp:994`, the marker `"unsupported pipeline result type");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1345 `lib/Conversion/LoopScheduleToCalyx/LoopScheduleToCalyx.cpp:1256` (implementation-gap)

At `lib/Conversion/LoopScheduleToCalyx/LoopScheduleToCalyx.cpp:1256`, the marker `"Unsupported block schedulable");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1346 `lib/Conversion/LoopScheduleToCalyx/LoopScheduleToCalyx.cpp:1316` (todo)

At `lib/Conversion/LoopScheduleToCalyx/LoopScheduleToCalyx.cpp:1316`, the marker `/// TODO(mortbopet): we could choose to support ie. std.switch, but it` indicates a gap signal in this path. What's missing: The marker documents unfinished work: (mortbopet): we could choose to support ie. std.switch, but it. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1347 `lib/Dialect/FIRRTL/Transforms/AnnotateInputOnlyModules.cpp:58` (unclassified)

At `lib/Dialect/FIRRTL/Transforms/AnnotateInputOnlyModules.cpp:58`, the marker `if (!instanceInfo.anyInstanceInEffectiveDesign(module) ||` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1348 `lib/Conversion/LTLToCore/LTLToCore.cpp:1102` (implementation-gap)

At `lib/Conversion/LTLToCore/LTLToCore.cpp:1102`, the marker `andOp.emitError("unsupported abort_on action on and");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1349 `lib/Conversion/LTLToCore/LTLToCore.cpp:1133` (implementation-gap)

At `lib/Conversion/LTLToCore/LTLToCore.cpp:1133`, the marker `orOp.emitError("unsupported abort_on action on or");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1350 `lib/Conversion/LTLToCore/LTLToCore.cpp:1265` (implementation-gap)

At `lib/Conversion/LTLToCore/LTLToCore.cpp:1265`, the marker `prop.getDefiningOp()->emitError("unsupported property lowering");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1351 `lib/Dialect/FIRRTL/Transforms/LowerLayers.cpp:446` (todo)

At `lib/Dialect/FIRRTL/Transforms/LowerLayers.cpp:446`, the marker `// TODO: Change this to recursively clone. This will matter once FString` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Change this to recursively clone. This will matter once FString. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1352 `lib/Dialect/FIRRTL/Transforms/LowerLayers.cpp:513` (todo)

At `lib/Dialect/FIRRTL/Transforms/LowerLayers.cpp:513`, the marker `// TODO: Move to before parallel region to avoid the lock.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Move to before parallel region to avoid the lock.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1353 `lib/Dialect/FIRRTL/Transforms/LowerLayers.cpp:561` (todo)

At `lib/Dialect/FIRRTL/Transforms/LowerLayers.cpp:561`, the marker `// TODO: Simplify this once wires have domain kind information [1].` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Simplify this once wires have domain kind information [1].. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1354 `lib/Dialect/FIRRTL/Transforms/LowerLayers.cpp:912` (todo)

At `lib/Dialect/FIRRTL/Transforms/LowerLayers.cpp:912`, the marker `// TODO: Stop looking through wires when wires support domain info [1].` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Stop looking through wires when wires support domain info [1].. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1355 `lib/Dialect/FIRRTL/Transforms/LowerLayers.cpp:1361` (todo)

At `lib/Dialect/FIRRTL/Transforms/LowerLayers.cpp:1361`, the marker `// TODO: This unnecessarily computes a new namepath for every hw::HierPathOp` indicates a gap signal in this path. What's missing: The marker documents unfinished work: This unnecessarily computes a new namepath for every hw::HierPathOp. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1356 `lib/Dialect/ESI/runtime/cpp/lib/backends/Cosim.cpp:427` (todo)

At `lib/Dialect/ESI/runtime/cpp/lib/backends/Cosim.cpp:427`, the marker `// TODO: The types here are WRONG. They need to be wrapped in Channels! Fix` indicates a gap signal in this path. What's missing: The marker documents unfinished work: The types here are WRONG. They need to be wrapped in Channels! Fix. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1357 `lib/Dialect/ESI/runtime/cpp/lib/backends/Cosim.cpp:489` (todo)

At `lib/Dialect/ESI/runtime/cpp/lib/backends/Cosim.cpp:489`, the marker `// location specified. TODO: check that the memory has been mapped.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: check that the memory has been mapped.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1358 `lib/Dialect/ESI/runtime/cpp/lib/backends/Cosim.cpp:517` (todo)

At `lib/Dialect/ESI/runtime/cpp/lib/backends/Cosim.cpp:517`, the marker `// location specified. TODO: check that the memory has been mapped.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: check that the memory has been mapped.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1359 `lib/Dialect/FIRRTL/Transforms/LowerIntmodules.cpp:164` (todo)

At `lib/Dialect/FIRRTL/Transforms/LowerIntmodules.cpp:164`, the marker `// FIXME: Dedup group annotation could be annotated to EICG_wrapper but` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Dedup group annotation could be annotated to EICG_wrapper but. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1360 `lib/Dialect/FIRRTL/Transforms/LowerDomains.cpp:305` (todo)

At `lib/Dialect/FIRRTL/Transforms/LowerDomains.cpp:305`, the marker `/// TODO: The mutation of this is _not_ thread safe. This needs to be fixed` indicates a gap signal in this path. What's missing: The marker documents unfinished work: The mutation of this is _not_ thread safe. This needs to be fixed. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1361 `lib/Dialect/FIRRTL/Transforms/LowerDomains.cpp:311` (unclassified)

At `lib/Dialect/FIRRTL/Transforms/LowerDomains.cpp:311`, the marker `llvm::MapVector<unsigned, DomainInfo> indexToDomain;` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1362 `lib/Dialect/FIRRTL/Transforms/LowerDomains.cpp:360` (unclassified)

At `lib/Dialect/FIRRTL/Transforms/LowerDomains.cpp:360`, the marker `indexToDomain[i] = port.direction == Direction::In` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1363 `lib/Dialect/FIRRTL/Transforms/LowerDomains.cpp:376` (unclassified)

At `lib/Dialect/FIRRTL/Transforms/LowerDomains.cpp:376`, the marker `indexToDomain[i].op = object;` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1364 `lib/Dialect/FIRRTL/Transforms/LowerDomains.cpp:377` (unclassified)

At `lib/Dialect/FIRRTL/Transforms/LowerDomains.cpp:377`, the marker `indexToDomain[i].temp = stubOut(body->getArgument(i));` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1365 `lib/Dialect/FIRRTL/Transforms/LowerDomains.cpp:441` (unclassified)

At `lib/Dialect/FIRRTL/Transforms/LowerDomains.cpp:441`, the marker `indexToDomain[indexAttr.getUInt()].associations.push_back({id, port.loc});` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1366 `lib/Dialect/FIRRTL/Transforms/LowerDomains.cpp:457` (unclassified)

At `lib/Dialect/FIRRTL/Transforms/LowerDomains.cpp:457`, the marker `for (auto const &[_, info] : indexToDomain) {` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1367 `lib/Dialect/FIRRTL/Transforms/LowerDomains.cpp:644` (implementation-gap)

At `lib/Dialect/FIRRTL/Transforms/LowerDomains.cpp:644`, the marker `diag.attachNote(src->getLoc()) << "unsupported source is here";` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1368 `lib/Dialect/FIRRTL/Transforms/LowerDomains.cpp:671` (todo)

At `lib/Dialect/FIRRTL/Transforms/LowerDomains.cpp:671`, the marker `// TODO: There is nothing to do unless this instance is a module or external` indicates a gap signal in this path. What's missing: The marker documents unfinished work: There is nothing to do unless this instance is a module or external. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1369 `lib/Dialect/FIRRTL/Transforms/LowerDomains.cpp:683` (implementation-gap)

At `lib/Dialect/FIRRTL/Transforms/LowerDomains.cpp:683`, the marker `<< "has an unimplemented lowering in LowerDomains";` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1370 `lib/Dialect/FIRRTL/Transforms/LowerDomains.cpp:690` (unclassified)

At `lib/Dialect/FIRRTL/Transforms/LowerDomains.cpp:690`, the marker `indexToDomain[i].temp = stubOut(instanceOp.getResult(i));` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1371 `lib/Dialect/FIRRTL/Transforms/LowerDomains.cpp:696` (unclassified)

At `lib/Dialect/FIRRTL/Transforms/LowerDomains.cpp:696`, the marker `for (auto &[i, info] : indexToDomain) {` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1372 `lib/Dialect/ESI/runtime/cpp/lib/backends/Trace.cpp:59` (implementation-gap)

At `lib/Dialect/ESI/runtime/cpp/lib/backends/Trace.cpp:59`, the marker `assert(false && "not implemented");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1373 `lib/Dialect/ESI/runtime/cpp/lib/backends/Trace.cpp:211` (todo)

At `lib/Dialect/ESI/runtime/cpp/lib/backends/Trace.cpp:211`, the marker `// TODO: support other types.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: support other types.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1374 `lib/Dialect/ESI/runtime/cpp/lib/backends/Trace.cpp:212` (implementation-gap)

At `lib/Dialect/ESI/runtime/cpp/lib/backends/Trace.cpp:212`, the marker `throw std::runtime_error("unsupported type for read: " +` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1375 `lib/Dialect/ESI/runtime/cpp/lib/backends/RpcServer.cpp:153` (todo)

At `lib/Dialect/ESI/runtime/cpp/lib/backends/RpcServer.cpp:153`, the marker `// TODO: use secure credentials. Not so bad for now since we only accept` indicates a gap signal in this path. What's missing: The marker documents unfinished work: use secure credentials. Not so bad for now since we only accept. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1376 `lib/Dialect/ESI/runtime/cpp/lib/backends/RpcServer.cpp:310` (todo)

At `lib/Dialect/ESI/runtime/cpp/lib/backends/RpcServer.cpp:310`, the marker `// TODO: adapt this to a new notification mechanism which is forthcoming.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: adapt this to a new notification mechanism which is forthcoming.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1377 `lib/Dialect/FIRRTL/Transforms/GrandCentral.cpp:91` (todo)

At `lib/Dialect/FIRRTL/Transforms/GrandCentral.cpp:91`, the marker `/// TODO: This could be removed if we add `firrtl.DocStringAnnotation` support` indicates a gap signal in this path. What's missing: The marker documents unfinished work: This could be removed if we add `firrtl.DocStringAnnotation` support. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1378 `lib/Dialect/FIRRTL/Transforms/GrandCentral.cpp:106` (todo)

At `lib/Dialect/FIRRTL/Transforms/GrandCentral.cpp:106`, the marker `/// TODO: Fix this once we have a solution for #1464.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Fix this once we have a solution for #1464.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1379 `lib/Dialect/FIRRTL/Transforms/GrandCentral.cpp:358` (implementation-gap)

At `lib/Dialect/FIRRTL/Transforms/GrandCentral.cpp:358`, the marker `// This is an unsupported construct. Just drop it.` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1380 `lib/Dialect/FIRRTL/Transforms/GrandCentral.cpp:359` (implementation-gap)

At `lib/Dialect/FIRRTL/Transforms/GrandCentral.cpp:359`, the marker `if (tpe.getValue() == "unsupported") {` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1381 `lib/Dialect/FIRRTL/Transforms/GrandCentral.cpp:715` (todo)

At `lib/Dialect/FIRRTL/Transforms/GrandCentral.cpp:715`, the marker `/// TODO: Investigate a way to not use a pointer here like how `getNamespace`` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Investigate a way to not use a pointer here like how `getNamespace`. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1382 `lib/Dialect/FIRRTL/Transforms/GrandCentral.cpp:760` (todo)

At `lib/Dialect/FIRRTL/Transforms/GrandCentral.cpp:760`, the marker `// TODO: Add a comment op and lower the description to that.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Add a comment op and lower the description to that.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1383 `lib/Dialect/FIRRTL/Transforms/GrandCentral.cpp:761` (todo)

At `lib/Dialect/FIRRTL/Transforms/GrandCentral.cpp:761`, the marker `// TODO: Tracking issue: https://github.com/llvm/circt/issues/1677` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Tracking issue: https://github.com/llvm/circt/issues/1677. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1384 `lib/Dialect/FIRRTL/Transforms/GrandCentral.cpp:1716` (todo)

At `lib/Dialect/FIRRTL/Transforms/GrandCentral.cpp:1716`, the marker `// TODO: The `append(name.getValue())` in the following should actually be` indicates a gap signal in this path. What's missing: The marker documents unfinished work: The `append(name.getValue())` in the following should actually be. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1385 `lib/Dialect/FIRRTL/Transforms/GrandCentral.cpp:1749` (todo)

At `lib/Dialect/FIRRTL/Transforms/GrandCentral.cpp:1749`, the marker `// TODO: Require as part of attribute, structurally.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Require as part of attribute, structurally.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1386 `lib/Dialect/FIRRTL/Transforms/GrandCentral.cpp:1762` (todo)

At `lib/Dialect/FIRRTL/Transforms/GrandCentral.cpp:1762`, the marker `interfaceBuilder.emplace_back(iFaceName, IntegerAttr() /* XXX */);` indicates a gap signal in this path. What's missing: The marker documents unfinished work: */);. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1387 `lib/Dialect/FIRRTL/Transforms/GrandCentral.cpp:1775` (todo)

At `lib/Dialect/FIRRTL/Transforms/GrandCentral.cpp:1775`, the marker `// TODO: The `append(name.getValue())` in the following should actually be` indicates a gap signal in this path. What's missing: The marker documents unfinished work: The `append(name.getValue())` in the following should actually be. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1388 `lib/Dialect/FIRRTL/Transforms/GrandCentral.cpp:1906` (todo)

At `lib/Dialect/FIRRTL/Transforms/GrandCentral.cpp:1906`, the marker `/// TODO: Handle this differently to allow construction of an options` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Handle this differently to allow construction of an options. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1389 `lib/Dialect/FIRRTL/Transforms/GrandCentral.cpp:1998` (todo)

At `lib/Dialect/FIRRTL/Transforms/GrandCentral.cpp:1998`, the marker `// TODO: Figure out what to do with this.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Figure out what to do with this.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1390 `lib/Dialect/FIRRTL/Transforms/GrandCentral.cpp:2187` (unclassified)

At `lib/Dialect/FIRRTL/Transforms/GrandCentral.cpp:2187`, the marker `instanceInfo->anyInstanceInEffectiveDesign(` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1391 `lib/Dialect/FIRRTL/Transforms/GrandCentral.cpp:2477` (implementation-gap)

At `lib/Dialect/FIRRTL/Transforms/GrandCentral.cpp:2477`, the marker `builder.getStringAttr("unsupported"));` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1392 `lib/Dialect/FIRRTL/Transforms/GrandCentral.cpp:2633` (implementation-gap)

At `lib/Dialect/FIRRTL/Transforms/GrandCentral.cpp:2633`, the marker `builder.getStringAttr("unsupported"));` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1393 `lib/Dialect/FIRRTL/Transforms/LowerTypes.cpp:58` (todo)

At `lib/Dialect/FIRRTL/Transforms/LowerTypes.cpp:58`, the marker `// TODO: check all argument types` indicates a gap signal in this path. What's missing: The marker documents unfinished work: check all argument types. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1394 `lib/Dialect/FIRRTL/Transforms/LowerTypes.cpp:130` (todo)

At `lib/Dialect/FIRRTL/Transforms/LowerTypes.cpp:130`, the marker `// FIXME: Don't preserve read-only RefType for now. This is workaround for` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Don't preserve read-only RefType for now. This is workaround for. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1395 `lib/Dialect/FIRRTL/Transforms/LowerTypes.cpp:644` (implementation-gap)

At `lib/Dialect/FIRRTL/Transforms/LowerTypes.cpp:644`, the marker `"unable to partition symbol on unsupported type ")` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1396 `lib/Dialect/FIRRTL/Transforms/LowerTypes.cpp:696` (implementation-gap)

At `lib/Dialect/FIRRTL/Transforms/LowerTypes.cpp:696`, the marker `errorLoc, "unable to partition symbol on unsupported type ")` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1397 `lib/Dialect/FIRRTL/Transforms/LowerTypes.cpp:1622` (todo)

At `lib/Dialect/FIRRTL/Transforms/LowerTypes.cpp:1622`, the marker `// FIXME: annotation update` indicates a gap signal in this path. What's missing: The marker documents unfinished work: annotation update. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1398 `lib/Dialect/ESI/runtime/cpp/lib/Values.cpp:382` (implementation-gap)

At `lib/Dialect/ESI/runtime/cpp/lib/Values.cpp:382`, the marker `std::format("Unsupported base '{}' for BitVector::toString", base));` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1399 `lib/Conversion/ImportVerilog/ImportVerilog.cpp:215` (implementation-gap)

At `lib/Conversion/ImportVerilog/ImportVerilog.cpp:215`, the marker `/// Rewrite unsupported width / alignment modifiers for a subset of format` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1400 `lib/Conversion/ImportVerilog/ImportVerilog.cpp:1838` (todo)

At `lib/Conversion/ImportVerilog/ImportVerilog.cpp:1838`, the marker `// TODO: Enable the following once it not longer interferes with @(...)` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Enable the following once it not longer interferes with @(...). The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1401 `lib/Dialect/FIRRTL/Transforms/AddSeqMemPorts.cpp:463` (unclassified)

At `lib/Dialect/FIRRTL/Transforms/AddSeqMemPorts.cpp:463`, the marker `if (!instanceInfo->anyInstanceInEffectiveDesign(op))` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1402 `lib/Dialect/FIRRTL/Transforms/SpecializeLayers.cpp:31` (todo)

At `lib/Dialect/FIRRTL/Transforms/SpecializeLayers.cpp:31`, the marker `// TODO: this should be upstreamed.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: this should be upstreamed.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1403 `lib/Dialect/FIRRTL/Transforms/SpecializeLayers.cpp:790` (todo)

At `lib/Dialect/FIRRTL/Transforms/SpecializeLayers.cpp:790`, the marker `// TODO: if both arrays are empty we could preserve specific analyses, but` indicates a gap signal in this path. What's missing: The marker documents unfinished work: if both arrays are empty we could preserve specific analyses, but. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1404 `lib/Dialect/ESI/Passes/ESIBuildManifest.cpp:32` (todo)

At `lib/Dialect/ESI/Passes/ESIBuildManifest.cpp:32`, the marker `// TODO: The code herein is a bit ugly, but it works. Consider cleaning it up.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: The code herein is a bit ugly, but it works. Consider cleaning it up.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1405 `lib/Dialect/FIRRTL/Transforms/LowerAnnotations.cpp:131` (todo)

At `lib/Dialect/FIRRTL/Transforms/LowerAnnotations.cpp:131`, the marker `// FIXME: uniq annotation chain links` indicates a gap signal in this path. What's missing: The marker documents unfinished work: uniq annotation chain links. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1406 `lib/Dialect/Datapath/DatapathFolds.cpp:154` (todo)

At `lib/Dialect/Datapath/DatapathFolds.cpp:154`, the marker `// FIXME: This should be implemented as a canonicalization pattern for` indicates a gap signal in this path. What's missing: The marker documents unfinished work: This should be implemented as a canonicalization pattern for. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1407 `lib/Dialect/Datapath/DatapathFolds.cpp:316` (todo)

At `lib/Dialect/Datapath/DatapathFolds.cpp:316`, the marker `// TODO: use knownBits to extract all constant ones` indicates a gap signal in this path. What's missing: The marker documents unfinished work: use knownBits to extract all constant ones. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1408 `lib/Dialect/Datapath/DatapathFolds.cpp:426` (todo)

At `lib/Dialect/Datapath/DatapathFolds.cpp:426`, the marker `// TODO: implement a constant multiplication for the PartialProductOp` indicates a gap signal in this path. What's missing: The marker documents unfinished work: implement a constant multiplication for the PartialProductOp. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1409 `lib/Dialect/Datapath/DatapathFolds.cpp:484` (todo)

At `lib/Dialect/Datapath/DatapathFolds.cpp:484`, the marker `// TODO: add support for different width inputs` indicates a gap signal in this path. What's missing: The marker documents unfinished work: add support for different width inputs. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1410 `lib/Dialect/FIRRTL/Transforms/InjectDUTHierarchy.cpp:13` (unclassified)

At `lib/Dialect/FIRRTL/Transforms/InjectDUTHierarchy.cpp:13`, the marker `// below, and the accompanying description of terms is provided to clarify what` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1411 `lib/Dialect/FIRRTL/Transforms/InjectDUTHierarchy.cpp:340` (todo)

At `lib/Dialect/FIRRTL/Transforms/InjectDUTHierarchy.cpp:340`, the marker `// TODO: It _may_ be desirable to only do this in the `moveDut=true` case.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: It _may_ be desirable to only do this in the `moveDut=true` case.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1412 `lib/Dialect/ESI/runtime/cpp/lib/Accelerator.cpp:121` (implementation-gap)

At `lib/Dialect/ESI/runtime/cpp/lib/Accelerator.cpp:121`, the marker `#eror "Unsupported platform"` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1413 `lib/Dialect/ESI/runtime/cpp/lib/Accelerator.cpp:147` (implementation-gap)

At `lib/Dialect/ESI/runtime/cpp/lib/Accelerator.cpp:147`, the marker `#eror "Unsupported platform"` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1414 `lib/Dialect/ESI/runtime/cpp/lib/Accelerator.cpp:199` (implementation-gap)

At `lib/Dialect/ESI/runtime/cpp/lib/Accelerator.cpp:199`, the marker `#eror "Unsupported platform"` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1415 `lib/Dialect/ESI/runtime/cpp/lib/Accelerator.cpp:285` (implementation-gap)

At `lib/Dialect/ESI/runtime/cpp/lib/Accelerator.cpp:285`, the marker `#eror "Unsupported platform"` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1416 `lib/Dialect/ESI/runtime/cpp/lib/Accelerator.cpp:396` (todo)

At `lib/Dialect/ESI/runtime/cpp/lib/Accelerator.cpp:396`, the marker `// TODO: investigate better ways to do this. For now, just play nice with` indicates a gap signal in this path. What's missing: The marker documents unfinished work: investigate better ways to do this. For now, just play nice with. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1417 `lib/Conversion/ImportVerilog/CrossSelect.cpp:97` (implementation-gap)

At `lib/Conversion/ImportVerilog/CrossSelect.cpp:97`, the marker `<< "unsupported non-constant intersect value range in cross "` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1418 `lib/Conversion/ImportVerilog/CrossSelect.cpp:104` (implementation-gap)

At `lib/Conversion/ImportVerilog/CrossSelect.cpp:104`, the marker `<< "unsupported non-constant intersect value range in cross "` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1419 `lib/Conversion/ImportVerilog/CrossSelect.cpp:121` (implementation-gap)

At `lib/Conversion/ImportVerilog/CrossSelect.cpp:121`, the marker `<< "unsupported intersect value range in cross select expression";` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1420 `lib/Conversion/ImportVerilog/CrossSelect.cpp:150` (implementation-gap)

At `lib/Conversion/ImportVerilog/CrossSelect.cpp:150`, the marker `<< "unsupported cross select expression: non-constant 'matches' "` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1421 `lib/Conversion/ImportVerilog/CrossSelect.cpp:159` (implementation-gap)

At `lib/Conversion/ImportVerilog/CrossSelect.cpp:159`, the marker `<< "unsupported cross select expression: non-constant 'matches' "` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1422 `lib/Conversion/ImportVerilog/CrossSelect.cpp:1027` (implementation-gap)

At `lib/Conversion/ImportVerilog/CrossSelect.cpp:1027`, the marker `<< "unsupported negation of cross identifier in cross select "` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1423 `lib/Conversion/ImportVerilog/CrossSelect.cpp:1069` (implementation-gap)

At `lib/Conversion/ImportVerilog/CrossSelect.cpp:1069`, the marker `<< "unsupported cross select expression with nested 'with' clause";` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1424 `lib/Conversion/ImportVerilog/CrossSelect.cpp:1072` (implementation-gap)

At `lib/Conversion/ImportVerilog/CrossSelect.cpp:1072`, the marker `<< "unsupported cross select expression with nested cross set "` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1425 `lib/Conversion/ImportVerilog/CrossSelect.cpp:1109` (implementation-gap)

At `lib/Conversion/ImportVerilog/CrossSelect.cpp:1109`, the marker `<< "unsupported cross select expression with no cross targets";` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1426 `lib/Conversion/ImportVerilog/CrossSelect.cpp:1132` (implementation-gap)

At `lib/Conversion/ImportVerilog/CrossSelect.cpp:1132`, the marker `<< "unsupported cross set expression element; expected tuple value";` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1427 `lib/Conversion/ImportVerilog/CrossSelect.cpp:1137` (implementation-gap)

At `lib/Conversion/ImportVerilog/CrossSelect.cpp:1137`, the marker `<< "unsupported cross set expression element with tuple arity "` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1428 `lib/Conversion/ImportVerilog/CrossSelect.cpp:1144` (implementation-gap)

At `lib/Conversion/ImportVerilog/CrossSelect.cpp:1144`, the marker `<< "unsupported non-integer tuple value in cross set expression";` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1429 `lib/Conversion/ImportVerilog/CrossSelect.cpp:1165` (implementation-gap)

At `lib/Conversion/ImportVerilog/CrossSelect.cpp:1165`, the marker `<< "unsupported non-constant cross set expression";` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1430 `lib/Conversion/ImportVerilog/CrossSelect.cpp:1187` (implementation-gap)

At `lib/Conversion/ImportVerilog/CrossSelect.cpp:1187`, the marker `<< "unsupported cross set expression; expected queue or unpacked "` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1431 `lib/Conversion/ImportVerilog/CrossSelect.cpp:1273` (implementation-gap)

At `lib/Conversion/ImportVerilog/CrossSelect.cpp:1273`, the marker `<< "unsupported non-constant intersect value range in cross "` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1432 `lib/Conversion/ImportVerilog/CrossSelect.cpp:1307` (implementation-gap)

At `lib/Conversion/ImportVerilog/CrossSelect.cpp:1307`, the marker `<< "unsupported non-constant intersect value in cross select "` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1433 `lib/Conversion/ImportVerilog/CrossSelect.cpp:1357` (implementation-gap)

At `lib/Conversion/ImportVerilog/CrossSelect.cpp:1357`, the marker `<< "unsupported cross select iterator width in 'with' clause";` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1434 `lib/Conversion/ImportVerilog/CrossSelect.cpp:1457` (implementation-gap)

At `lib/Conversion/ImportVerilog/CrossSelect.cpp:1457`, the marker `<< "unsupported cross select expression due to large finite "` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1435 `lib/Conversion/ImportVerilog/CrossSelect.cpp:1469` (implementation-gap)

At `lib/Conversion/ImportVerilog/CrossSelect.cpp:1469`, the marker `<< "unsupported cross select expression due to large finite "` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1436 `lib/Conversion/ImportVerilog/CrossSelect.cpp:1482` (implementation-gap)

At `lib/Conversion/ImportVerilog/CrossSelect.cpp:1482`, the marker `<< "unsupported empty coverpoint bin in cross select expression";` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1437 `lib/Conversion/ImportVerilog/CrossSelect.cpp:1500` (implementation-gap)

At `lib/Conversion/ImportVerilog/CrossSelect.cpp:1500`, the marker `<< "unsupported non-constant coverpoint bin range in cross "` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1438 `lib/Conversion/ImportVerilog/CrossSelect.cpp:1512` (implementation-gap)

At `lib/Conversion/ImportVerilog/CrossSelect.cpp:1512`, the marker `<< "unsupported non-constant coverpoint bin value in cross "` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1439 `lib/Conversion/ImportVerilog/CrossSelect.cpp:1545` (implementation-gap)

At `lib/Conversion/ImportVerilog/CrossSelect.cpp:1545`, the marker `<< "unsupported coverpoint bin iterator width in cross select "` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1440 `lib/Conversion/ImportVerilog/CrossSelect.cpp:1585` (implementation-gap)

At `lib/Conversion/ImportVerilog/CrossSelect.cpp:1585`, the marker `<< "unsupported non-constant set coverpoint bin in cross select "` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1441 `lib/Conversion/ImportVerilog/CrossSelect.cpp:1592` (implementation-gap)

At `lib/Conversion/ImportVerilog/CrossSelect.cpp:1592`, the marker `<< "unsupported non-integer set coverpoint bin value in cross "` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1442 `lib/Conversion/ImportVerilog/CrossSelect.cpp:1630` (implementation-gap)

At `lib/Conversion/ImportVerilog/CrossSelect.cpp:1630`, the marker `<< "unsupported non-constant transition bin range in cross "` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1443 `lib/Conversion/ImportVerilog/CrossSelect.cpp:1642` (implementation-gap)

At `lib/Conversion/ImportVerilog/CrossSelect.cpp:1642`, the marker `<< "unsupported non-constant transition bin value in cross "` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1444 `lib/Conversion/ImportVerilog/CrossSelect.cpp:1668` (implementation-gap)

At `lib/Conversion/ImportVerilog/CrossSelect.cpp:1668`, the marker `<< "unsupported default coverpoint bin target in cross select "` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1445 `lib/Conversion/ImportVerilog/CrossSelect.cpp:1702` (implementation-gap)

At `lib/Conversion/ImportVerilog/CrossSelect.cpp:1702`, the marker `<< "unsupported cross select 'with' clause over non-integral "` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1446 `lib/Conversion/ImportVerilog/CrossSelect.cpp:1709` (implementation-gap)

At `lib/Conversion/ImportVerilog/CrossSelect.cpp:1709`, the marker `<< "unsupported cross select 'with' clause over coverpoint '"` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1447 `lib/Conversion/ImportVerilog/CrossSelect.cpp:1757` (unclassified)

At `lib/Conversion/ImportVerilog/CrossSelect.cpp:1757`, the marker `bool unsupportedBinShape = bin->isDefaultSequence;` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1448 `lib/Conversion/ImportVerilog/CrossSelect.cpp:1758` (unclassified)

At `lib/Conversion/ImportVerilog/CrossSelect.cpp:1758`, the marker `if (unsupportedBinShape) {` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1449 `lib/Conversion/ImportVerilog/CrossSelect.cpp:1830` (implementation-gap)

At `lib/Conversion/ImportVerilog/CrossSelect.cpp:1830`, the marker `<< "unsupported cross select expression over coverpoint '"` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1450 `lib/Conversion/ImportVerilog/CrossSelect.cpp:1835` (implementation-gap)

At `lib/Conversion/ImportVerilog/CrossSelect.cpp:1835`, the marker `<< "unsupported cross select expression due to large finite "` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1451 `lib/Conversion/ImportVerilog/CrossSelect.cpp:1898` (implementation-gap)

At `lib/Conversion/ImportVerilog/CrossSelect.cpp:1898`, the marker `<< "unsupported cross select condition target kind";` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1452 `lib/Conversion/ImportVerilog/CrossSelect.cpp:1904` (implementation-gap)

At `lib/Conversion/ImportVerilog/CrossSelect.cpp:1904`, the marker `<< "unsupported detached coverage bin in cross select expression";` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1453 `lib/Conversion/ImportVerilog/CrossSelect.cpp:1908` (implementation-gap)

At `lib/Conversion/ImportVerilog/CrossSelect.cpp:1908`, the marker `<< "unsupported coverage bin parent in cross select expression";` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1454 `lib/Conversion/ImportVerilog/CrossSelect.cpp:2013` (implementation-gap)

At `lib/Conversion/ImportVerilog/CrossSelect.cpp:2013`, the marker `<< "unsupported empty coverpoint bin in cross select expression";` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1455 `lib/Conversion/ImportVerilog/CrossSelect.cpp:2030` (implementation-gap)

At `lib/Conversion/ImportVerilog/CrossSelect.cpp:2030`, the marker `<< "unsupported cross select 'with' clause due to too many "` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1456 `lib/Conversion/ImportVerilog/CrossSelect.cpp:2067` (implementation-gap)

At `lib/Conversion/ImportVerilog/CrossSelect.cpp:2067`, the marker `<< "unsupported non-constant cross set expression";` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1457 `lib/Conversion/ImportVerilog/CrossSelect.cpp:2105` (implementation-gap)

At `lib/Conversion/ImportVerilog/CrossSelect.cpp:2105`, the marker `<< "unsupported cross set expression; expected queue or unpacked "` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1458 `lib/Conversion/ImportVerilog/CrossSelect.cpp:2165` (implementation-gap)

At `lib/Conversion/ImportVerilog/CrossSelect.cpp:2165`, the marker `<< "unsupported cross select 'with' clause due to too many "` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1459 `lib/Conversion/ImportVerilog/CrossSelect.cpp:2356` (implementation-gap)

At `lib/Conversion/ImportVerilog/CrossSelect.cpp:2356`, the marker `<< "unsupported cross select expression due to too many "` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1460 `lib/Conversion/ImportVerilog/CrossSelect.cpp:2444` (implementation-gap)

At `lib/Conversion/ImportVerilog/CrossSelect.cpp:2444`, the marker `<< "unsupported cross select expression due to large finite "` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1461 `lib/Conversion/ImportVerilog/CrossSelect.cpp:2457` (implementation-gap)

At `lib/Conversion/ImportVerilog/CrossSelect.cpp:2457`, the marker `<< "unsupported cross select expression due to invalid "` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1462 `lib/Conversion/ImportVerilog/CrossSelect.cpp:2499` (implementation-gap)

At `lib/Conversion/ImportVerilog/CrossSelect.cpp:2499`, the marker `<< "unsupported non-constant intersect value range in cross "` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1463 `lib/Conversion/ImportVerilog/CrossSelect.cpp:2523` (implementation-gap)

At `lib/Conversion/ImportVerilog/CrossSelect.cpp:2523`, the marker `<< "unsupported non-constant intersect value in cross select "` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1464 `lib/Conversion/ImportVerilog/CrossSelect.cpp:2562` (implementation-gap)

At `lib/Conversion/ImportVerilog/CrossSelect.cpp:2562`, the marker `<< "unsupported negation of cross set expression";` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1465 `lib/Conversion/ImportVerilog/CrossSelect.cpp:2570` (implementation-gap)

At `lib/Conversion/ImportVerilog/CrossSelect.cpp:2570`, the marker `<< "unsupported negation of cross select expression with 'with' "` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1466 `lib/Dialect/FIRRTL/Transforms/BlackBoxReader.cpp:223` (todo)

At `lib/Dialect/FIRRTL/Transforms/BlackBoxReader.cpp:223`, the marker `// TODO: Check that the new text is the _exact same_ as the prior best.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Check that the new text is the _exact same_ as the prior best.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1467 `lib/Dialect/FIRRTL/Transforms/BlackBoxReader.cpp:374` (unclassified)

At `lib/Dialect/FIRRTL/Transforms/BlackBoxReader.cpp:374`, the marker `!instanceInfo->anyInstanceInEffectiveDesign(` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1468 `lib/Dialect/FIRRTL/Transforms/InferWidths.cpp:1043` (todo)

At `lib/Dialect/FIRRTL/Transforms/InferWidths.cpp:1043`, the marker `// TODO: It's possible that this result is already sufficient to arrive at a` indicates a gap signal in this path. What's missing: The marker documents unfinished work: It's possible that this result is already sufficient to arrive at a. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1469 `lib/Dialect/FIRRTL/Transforms/InferWidths.cpp:1719` (implementation-gap)

At `lib/Dialect/FIRRTL/Transforms/InferWidths.cpp:1719`, the marker `op->emitError("target of rwprobe resolved to unsupported target");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1470 `lib/Dialect/FIRRTL/Transforms/InferWidths.cpp:2217` (todo)

At `lib/Dialect/FIRRTL/Transforms/InferWidths.cpp:2217`, the marker `// TODO: this should recurse into the element type of 0 length vectors and` indicates a gap signal in this path. What's missing: The marker documents unfinished work: this should recurse into the element type of 0 length vectors and. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1471 `lib/Dialect/FIRRTL/Transforms/ProbesToSignals.cpp:163` (implementation-gap)

At `lib/Dialect/FIRRTL/Transforms/ProbesToSignals.cpp:163`, the marker `// Force and release operations: reject as unsupported.` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1472 `lib/Dialect/FIRRTL/Transforms/ProbesToSignals.cpp:393` (implementation-gap)

At `lib/Dialect/FIRRTL/Transforms/ProbesToSignals.cpp:393`, the marker `return op.emitError("memory has unsupported debug port (memtap)");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1473 `lib/Dialect/Datapath/DatapathOps.cpp:129` (todo)

At `lib/Dialect/Datapath/DatapathOps.cpp:129`, the marker `// TODO: Fold Constant 1s` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Fold Constant 1s. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1474 `lib/Dialect/Datapath/DatapathOps.cpp:216` (todo)

At `lib/Dialect/Datapath/DatapathOps.cpp:216`, the marker `// TODO: Dadda's algorithm is redundant here since it assumes uniform arrival so` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Dadda's algorithm is redundant here since it assumes uniform arrival so. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1475 `lib/Dialect/ESI/ESIOps.cpp:478` (todo)

At `lib/Dialect/ESI/ESIOps.cpp:478`, the marker `// TODO: other container types.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: other container types.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1476 `lib/Dialect/ESI/runtime/cpp/include/esi/Types.h:50` (implementation-gap)

At `lib/Dialect/ESI/runtime/cpp/include/esi/Types.h:50`, the marker `throw std::runtime_error("Serialization not implemented for type " + id);` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1477 `lib/Dialect/ESI/runtime/cpp/include/esi/Types.h:57` (implementation-gap)

At `lib/Dialect/ESI/runtime/cpp/include/esi/Types.h:57`, the marker `throw std::runtime_error("Deserialization not implemented for type " + id);` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1478 `lib/Dialect/ESI/runtime/cpp/include/esi/Types.h:71` (implementation-gap)

At `lib/Dialect/ESI/runtime/cpp/include/esi/Types.h:71`, the marker `throw std::runtime_error("Validation not implemented for type " + id);` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1479 `lib/Dialect/ESI/runtime/cpp/include/esi/Types.h:98` (todo)

At `lib/Dialect/ESI/runtime/cpp/include/esi/Types.h:98`, the marker `/// TODO: Add a good description of direction?` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Add a good description of direction?. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1480 `lib/Conversion/ImportVerilog/AssertionExpr.cpp:41` (unclassified)

At `lib/Conversion/ImportVerilog/AssertionExpr.cpp:41`, the marker `static mlir::InFlightDiagnostic emitUnsupportedSvaDiagnostic(Context &context,` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1481 `lib/Conversion/ImportVerilog/AssertionExpr.cpp:43` (unclassified)

At `lib/Conversion/ImportVerilog/AssertionExpr.cpp:43`, the marker `if (context.options.continueOnUnsupportedSVA)` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1482 `lib/Conversion/ImportVerilog/AssertionExpr.cpp:118` (unclassified)

At `lib/Conversion/ImportVerilog/AssertionExpr.cpp:118`, the marker `emitUnsupportedSvaDiagnostic(context, loc)` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1483 `lib/Conversion/ImportVerilog/AssertionExpr.cpp:119` (implementation-gap)

At `lib/Conversion/ImportVerilog/AssertionExpr.cpp:119`, the marker `<< "unsupported sampled value type for " << funcName;` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1484 `lib/Conversion/ImportVerilog/AssertionExpr.cpp:394` (unclassified)

At `lib/Conversion/ImportVerilog/AssertionExpr.cpp:394`, the marker `emitUnsupportedSvaDiagnostic(context, loc)` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1485 `lib/Conversion/ImportVerilog/AssertionExpr.cpp:395` (implementation-gap)

At `lib/Conversion/ImportVerilog/AssertionExpr.cpp:395`, the marker `<< "unsupported sampled value type for " << funcName;` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1486 `lib/Conversion/ImportVerilog/AssertionExpr.cpp:405` (unclassified)

At `lib/Conversion/ImportVerilog/AssertionExpr.cpp:405`, the marker `emitUnsupportedSvaDiagnostic(context, loc)` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1487 `lib/Conversion/ImportVerilog/AssertionExpr.cpp:406` (implementation-gap)

At `lib/Conversion/ImportVerilog/AssertionExpr.cpp:406`, the marker `<< "unsupported sampled value type for " << funcName;` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1488 `lib/Conversion/ImportVerilog/AssertionExpr.cpp:633` (unclassified)

At `lib/Conversion/ImportVerilog/AssertionExpr.cpp:633`, the marker `emitUnsupportedSvaDiagnostic(context, loc)` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1489 `lib/Conversion/ImportVerilog/AssertionExpr.cpp:634` (implementation-gap)

At `lib/Conversion/ImportVerilog/AssertionExpr.cpp:634`, the marker `<< "unsupported sampled value type for " << funcName;` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1490 `lib/Conversion/ImportVerilog/AssertionExpr.cpp:649` (unclassified)

At `lib/Conversion/ImportVerilog/AssertionExpr.cpp:649`, the marker `emitUnsupportedSvaDiagnostic(context, loc)` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1491 `lib/Conversion/ImportVerilog/AssertionExpr.cpp:650` (implementation-gap)

At `lib/Conversion/ImportVerilog/AssertionExpr.cpp:650`, the marker `<< "unsupported sampled value type for " << funcName;` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1492 `lib/Conversion/ImportVerilog/AssertionExpr.cpp:656` (unclassified)

At `lib/Conversion/ImportVerilog/AssertionExpr.cpp:656`, the marker `emitUnsupportedSvaDiagnostic(context, loc)` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1493 `lib/Conversion/ImportVerilog/AssertionExpr.cpp:657` (implementation-gap)

At `lib/Conversion/ImportVerilog/AssertionExpr.cpp:657`, the marker `<< "unsupported sampled value type for " << funcName;` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1494 `lib/Conversion/ImportVerilog/AssertionExpr.cpp:985` (unclassified)

At `lib/Conversion/ImportVerilog/AssertionExpr.cpp:985`, the marker `emitUnsupportedSvaDiagnostic(context, loc)` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1495 `lib/Conversion/ImportVerilog/AssertionExpr.cpp:986` (implementation-gap)

At `lib/Conversion/ImportVerilog/AssertionExpr.cpp:986`, the marker `<< "unsupported sampled value type for " << funcName;` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1496 `lib/Conversion/ImportVerilog/AssertionExpr.cpp:1076` (unclassified)

At `lib/Conversion/ImportVerilog/AssertionExpr.cpp:1076`, the marker `emitUnsupportedSvaDiagnostic(context, loc)` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1497 `lib/Conversion/ImportVerilog/AssertionExpr.cpp:1077` (implementation-gap)

At `lib/Conversion/ImportVerilog/AssertionExpr.cpp:1077`, the marker `<< "unsupported sampled value type for " << funcName;` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1498 `lib/Conversion/ImportVerilog/AssertionExpr.cpp:1081` (unclassified)

At `lib/Conversion/ImportVerilog/AssertionExpr.cpp:1081`, the marker `emitUnsupportedSvaDiagnostic(context, loc)` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1499 `lib/Conversion/ImportVerilog/AssertionExpr.cpp:1082` (implementation-gap)

At `lib/Conversion/ImportVerilog/AssertionExpr.cpp:1082`, the marker `<< "unsupported sampled value type for " << funcName;` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1500 `lib/Conversion/ImportVerilog/AssertionExpr.cpp:1088` (unclassified)

At `lib/Conversion/ImportVerilog/AssertionExpr.cpp:1088`, the marker `emitUnsupportedSvaDiagnostic(context, loc)` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1501 `lib/Conversion/ImportVerilog/AssertionExpr.cpp:1089` (implementation-gap)

At `lib/Conversion/ImportVerilog/AssertionExpr.cpp:1089`, the marker `<< "unsupported sampled value type for " << funcName;` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1502 `lib/Conversion/ImportVerilog/AssertionExpr.cpp:1104` (unclassified)

At `lib/Conversion/ImportVerilog/AssertionExpr.cpp:1104`, the marker `emitUnsupportedSvaDiagnostic(context, loc)` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1503 `lib/Conversion/ImportVerilog/AssertionExpr.cpp:1105` (implementation-gap)

At `lib/Conversion/ImportVerilog/AssertionExpr.cpp:1105`, the marker `<< "unsupported sampled value type for " << funcName;` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1504 `lib/Conversion/ImportVerilog/AssertionExpr.cpp:1119` (unclassified)

At `lib/Conversion/ImportVerilog/AssertionExpr.cpp:1119`, the marker `emitUnsupportedSvaDiagnostic(context, loc)` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1505 `lib/Conversion/ImportVerilog/AssertionExpr.cpp:1120` (implementation-gap)

At `lib/Conversion/ImportVerilog/AssertionExpr.cpp:1120`, the marker `<< "unsupported sampled value type for " << funcName;` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1506 `lib/Conversion/ImportVerilog/AssertionExpr.cpp:1332` (unclassified)

At `lib/Conversion/ImportVerilog/AssertionExpr.cpp:1332`, the marker `emitUnsupportedSvaDiagnostic(context, loc)` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1507 `lib/Conversion/ImportVerilog/AssertionExpr.cpp:1333` (implementation-gap)

At `lib/Conversion/ImportVerilog/AssertionExpr.cpp:1333`, the marker `<< "unsupported $past value type with sampled-value controls (input "` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1508 `lib/Conversion/ImportVerilog/AssertionExpr.cpp:1336` (unclassified)

At `lib/Conversion/ImportVerilog/AssertionExpr.cpp:1336`, the marker `if (context.options.continueOnUnsupportedSVA && !context.inAssertionExpr) {` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1509 `lib/Conversion/ImportVerilog/AssertionExpr.cpp:1402` (unclassified)

At `lib/Conversion/ImportVerilog/AssertionExpr.cpp:1402`, the marker `emitUnsupportedSvaDiagnostic(context, loc)` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1510 `lib/Conversion/ImportVerilog/AssertionExpr.cpp:1403` (implementation-gap)

At `lib/Conversion/ImportVerilog/AssertionExpr.cpp:1403`, the marker `<< "unsupported $past value type with sampled-value controls "` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1511 `lib/Conversion/ImportVerilog/AssertionExpr.cpp:1411` (unclassified)

At `lib/Conversion/ImportVerilog/AssertionExpr.cpp:1411`, the marker `emitUnsupportedSvaDiagnostic(context, loc)` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1512 `lib/Conversion/ImportVerilog/AssertionExpr.cpp:1412` (implementation-gap)

At `lib/Conversion/ImportVerilog/AssertionExpr.cpp:1412`, the marker `<< "unsupported $past value type with sampled-value controls "` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1513 `lib/Conversion/ImportVerilog/AssertionExpr.cpp:1618` (unclassified)

At `lib/Conversion/ImportVerilog/AssertionExpr.cpp:1618`, the marker `emitUnsupportedSvaDiagnostic(context, loc)` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1514 `lib/Conversion/ImportVerilog/AssertionExpr.cpp:1619` (implementation-gap)

At `lib/Conversion/ImportVerilog/AssertionExpr.cpp:1619`, the marker `<< "unsupported local assertion variable type";` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1515 `lib/Conversion/ImportVerilog/AssertionExpr.cpp:1631` (unclassified)

At `lib/Conversion/ImportVerilog/AssertionExpr.cpp:1631`, the marker `emitUnsupportedSvaDiagnostic(context, loc)` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1516 `lib/Conversion/ImportVerilog/AssertionExpr.cpp:1632` (implementation-gap)

At `lib/Conversion/ImportVerilog/AssertionExpr.cpp:1632`, the marker `<< "unsupported match item assignment type"` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1517 `lib/Conversion/ImportVerilog/AssertionExpr.cpp:1650` (unclassified)

At `lib/Conversion/ImportVerilog/AssertionExpr.cpp:1650`, the marker `emitUnsupportedSvaDiagnostic(context, loc)` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1518 `lib/Conversion/ImportVerilog/AssertionExpr.cpp:1651` (implementation-gap)

At `lib/Conversion/ImportVerilog/AssertionExpr.cpp:1651`, the marker `<< "unsupported match item assignment type"` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1519 `lib/Conversion/ImportVerilog/AssertionExpr.cpp:1672` (unclassified)

At `lib/Conversion/ImportVerilog/AssertionExpr.cpp:1672`, the marker `emitUnsupportedSvaDiagnostic(context, loc)` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1520 `lib/Conversion/ImportVerilog/AssertionExpr.cpp:1673` (implementation-gap)

At `lib/Conversion/ImportVerilog/AssertionExpr.cpp:1673`, the marker `<< "unsupported match item unary operator";` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1521 `lib/Conversion/ImportVerilog/AssertionExpr.cpp:2449` (implementation-gap)

At `lib/Conversion/ImportVerilog/AssertionExpr.cpp:2449`, the marker `<< " (checkpoint/restart not implemented)";` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1522 `lib/Conversion/ImportVerilog/AssertionExpr.cpp:2456` (implementation-gap)

At `lib/Conversion/ImportVerilog/AssertionExpr.cpp:2456`, the marker `<< " (SDF timing annotation not implemented)";` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1523 `lib/Conversion/ImportVerilog/AssertionExpr.cpp:2747` (unclassified)

At `lib/Conversion/ImportVerilog/AssertionExpr.cpp:2747`, the marker `emitUnsupportedSvaDiagnostic(context, loc)` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1524 `lib/Conversion/ImportVerilog/AssertionExpr.cpp:2748` (implementation-gap)

At `lib/Conversion/ImportVerilog/AssertionExpr.cpp:2748`, the marker `<< "unsupported match item expression";` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1525 `lib/Conversion/ImportVerilog/AssertionExpr.cpp:3562` (unclassified)

At `lib/Conversion/ImportVerilog/AssertionExpr.cpp:3562`, the marker `emitUnsupportedSvaDiagnostic(context, loc)` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1526 `lib/Conversion/ImportVerilog/AssertionExpr.cpp:3563` (implementation-gap)

At `lib/Conversion/ImportVerilog/AssertionExpr.cpp:3563`, the marker `<< "unsupported expression: "` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1527 `lib/Conversion/ImportVerilog/AssertionExpr.cpp:3713` (unclassified)

At `lib/Conversion/ImportVerilog/AssertionExpr.cpp:3713`, the marker `auto emitUnsupportedNonConcurrentSampledPlaceholder = [&]() -> Value {` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1528 `lib/Conversion/ImportVerilog/AssertionExpr.cpp:3714` (unclassified)

At `lib/Conversion/ImportVerilog/AssertionExpr.cpp:3714`, the marker `if (!(options.continueOnUnsupportedSVA && !inAssertionExpr))` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1529 `lib/Conversion/ImportVerilog/AssertionExpr.cpp:3718` (implementation-gap)

At `lib/Conversion/ImportVerilog/AssertionExpr.cpp:3718`, the marker `<< funcName << " has unsupported sampled value type " << value.getType()` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1530 `lib/Conversion/ImportVerilog/AssertionExpr.cpp:3737` (unclassified)

At `lib/Conversion/ImportVerilog/AssertionExpr.cpp:3737`, the marker `if (auto fallback = emitUnsupportedNonConcurrentSampledPlaceholder())` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1531 `lib/Conversion/ImportVerilog/AssertionExpr.cpp:3739` (unclassified)

At `lib/Conversion/ImportVerilog/AssertionExpr.cpp:3739`, the marker `emitUnsupportedSvaDiagnostic(*this, loc)` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1532 `lib/Conversion/ImportVerilog/AssertionExpr.cpp:3740` (implementation-gap)

At `lib/Conversion/ImportVerilog/AssertionExpr.cpp:3740`, the marker `<< "unsupported sampled value type for " << funcName;` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1533 `lib/Conversion/ImportVerilog/AssertionExpr.cpp:3747` (unclassified)

At `lib/Conversion/ImportVerilog/AssertionExpr.cpp:3747`, the marker `if (auto fallback = emitUnsupportedNonConcurrentSampledPlaceholder())` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1534 `lib/Conversion/ImportVerilog/AssertionExpr.cpp:3749` (unclassified)

At `lib/Conversion/ImportVerilog/AssertionExpr.cpp:3749`, the marker `emitUnsupportedSvaDiagnostic(*this, loc)` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1535 `lib/Conversion/ImportVerilog/AssertionExpr.cpp:3750` (implementation-gap)

At `lib/Conversion/ImportVerilog/AssertionExpr.cpp:3750`, the marker `<< "unsupported sampled value type for " << funcName;` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1536 `lib/Conversion/ImportVerilog/AssertionExpr.cpp:3765` (unclassified)

At `lib/Conversion/ImportVerilog/AssertionExpr.cpp:3765`, the marker `emitUnsupportedSvaDiagnostic(*this, loc)` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1537 `lib/Conversion/ImportVerilog/AssertionExpr.cpp:3766` (implementation-gap)

At `lib/Conversion/ImportVerilog/AssertionExpr.cpp:3766`, the marker `<< "unsupported sampled value type for " << funcName;` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1538 `lib/Conversion/ImportVerilog/AssertionExpr.cpp:4197` (unclassified)

At `lib/Conversion/ImportVerilog/AssertionExpr.cpp:4197`, the marker `emitUnsupportedSvaDiagnostic(*this, loc)` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1539 `lib/Conversion/ImportVerilog/AssertionExpr.cpp:4198` (implementation-gap)

At `lib/Conversion/ImportVerilog/AssertionExpr.cpp:4198`, the marker `<< "unsupported system call `" << funcName << "`";` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1540 `lib/Dialect/FIRRTL/Transforms/InferResets.cpp:443` (todo)

At `lib/Dialect/FIRRTL/Transforms/InferResets.cpp:443`, the marker `/// TODO: This logic is *very* brittle and error-prone. It may make sense to` indicates a gap signal in this path. What's missing: The marker documents unfinished work: This logic is *very* brittle and error-prone. It may make sense to. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1541 `lib/Dialect/FIRRTL/Transforms/InferResets.cpp:746` (implementation-gap)

At `lib/Dialect/FIRRTL/Transforms/InferResets.cpp:746`, the marker `llvm_unreachable("unsupported type");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1542 `lib/Conversion/ImportVerilog/TimingControls.cpp:42` (todo)

At `lib/Conversion/ImportVerilog/TimingControls.cpp:42`, the marker `// TODO: SV 16.16, what to do when no edge is specified?` indicates a gap signal in this path. What's missing: The marker documents unfinished work: SV 16.16, what to do when no edge is specified?. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1543 `lib/Conversion/ImportVerilog/TimingControls.cpp:175` (implementation-gap)

At `lib/Conversion/ImportVerilog/TimingControls.cpp:175`, the marker `<< "unsupported global clocking symbol kind for $global_clock";` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1544 `lib/Conversion/ImportVerilog/TimingControls.cpp:245` (implementation-gap)

At `lib/Conversion/ImportVerilog/TimingControls.cpp:245`, the marker `<< "unsupported event control: " << slang::ast::toString(ctrl.kind);` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1545 `lib/Conversion/ImportVerilog/TimingControls.cpp:1584` (implementation-gap)

At `lib/Conversion/ImportVerilog/TimingControls.cpp:1584`, the marker `<< "unsupported event kind in sequence event list";` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1546 `lib/Conversion/ImportVerilog/TimingControls.cpp:1644` (implementation-gap)

At `lib/Conversion/ImportVerilog/TimingControls.cpp:1644`, the marker `<< "unsupported global clocking event kind in sequence "` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1547 `lib/Conversion/ImportVerilog/TimingControls.cpp:1649` (implementation-gap)

At `lib/Conversion/ImportVerilog/TimingControls.cpp:1649`, the marker `<< "unsupported global clocking symbol kind for $global_clock";` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1548 `lib/Conversion/ImportVerilog/TimingControls.cpp:1669` (implementation-gap)

At `lib/Conversion/ImportVerilog/TimingControls.cpp:1669`, the marker `<< "unsupported clocking block event kind in sequence event "` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1549 `lib/Conversion/ImportVerilog/TimingControls.cpp:1991` (implementation-gap)

At `lib/Conversion/ImportVerilog/TimingControls.cpp:1991`, the marker `<< "unsupported delay control: " << slang::ast::toString(ctrl.kind);` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1550 `lib/Conversion/ImportVerilog/TimingControls.cpp:2083` (implementation-gap)

At `lib/Conversion/ImportVerilog/TimingControls.cpp:2083`, the marker `<< "unsupported global clocking symbol kind for $global_clock";` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1551 `lib/Conversion/ImportVerilog/TimingControls.cpp:2274` (implementation-gap)

At `lib/Conversion/ImportVerilog/TimingControls.cpp:2274`, the marker `mlir::emitError(loc, "unsupported LTL clock control: ")` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1552 `lib/Conversion/ImportVerilog/TimingControls.cpp:2407` (implementation-gap)

At `lib/Conversion/ImportVerilog/TimingControls.cpp:2407`, the marker `<< "unsupported default clocking symbol kind for cycle delay";` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1553 `lib/Conversion/ImportVerilog/TimingControls.cpp:2417` (implementation-gap)

At `lib/Conversion/ImportVerilog/TimingControls.cpp:2417`, the marker `<< "unsupported global clocking symbol kind for cycle delay";` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1554 `lib/Conversion/ImportVerilog/TimingControls.cpp:2427` (implementation-gap)

At `lib/Conversion/ImportVerilog/TimingControls.cpp:2427`, the marker `<< "unsupported clocking event kind for cycle delay";` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1555 `lib/Conversion/ImportVerilog/TimingControls.cpp:2500` (implementation-gap)

At `lib/Conversion/ImportVerilog/TimingControls.cpp:2500`, the marker `return mlir::emitError(loc, "unsupported timing control: ")` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1556 `test/Dialect/LLHD/Transforms/unroll-loops.mlir:134` (unclassified)

At `test/Dialect/LLHD/Transforms/unroll-loops.mlir:134`, the marker `// CHECK-LABEL: @SkipLoopWithUnsupportedExitBranch` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1557 `test/Dialect/LLHD/Transforms/unroll-loops.mlir:135` (unclassified)

At `test/Dialect/LLHD/Transforms/unroll-loops.mlir:135`, the marker `hw.module @SkipLoopWithUnsupportedExitBranch() {` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1558 `test/Dialect/LLHD/Transforms/unroll-loops.mlir:176` (unclassified)

At `test/Dialect/LLHD/Transforms/unroll-loops.mlir:176`, the marker `// CHECK-LABEL: @SkipLoopWithUnsupportedExitCondition` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1559 `test/Dialect/LLHD/Transforms/unroll-loops.mlir:177` (unclassified)

At `test/Dialect/LLHD/Transforms/unroll-loops.mlir:177`, the marker `hw.module @SkipLoopWithUnsupportedExitCondition() {` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1560 `test/Dialect/LLHD/Transforms/unroll-loops.mlir:197` (unclassified)

At `test/Dialect/LLHD/Transforms/unroll-loops.mlir:197`, the marker `// CHECK-LABEL: @SkipLoopWithUnsupportedInductionVariable1` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1561 `test/Dialect/LLHD/Transforms/unroll-loops.mlir:198` (unclassified)

At `test/Dialect/LLHD/Transforms/unroll-loops.mlir:198`, the marker `hw.module @SkipLoopWithUnsupportedInductionVariable1() {` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1562 `test/Dialect/LLHD/Transforms/unroll-loops.mlir:244` (unclassified)

At `test/Dialect/LLHD/Transforms/unroll-loops.mlir:244`, the marker `// CHECK-LABEL: @SkipLoopWithUnsupportedInductionVariable2` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1563 `test/Dialect/LLHD/Transforms/unroll-loops.mlir:245` (unclassified)

At `test/Dialect/LLHD/Transforms/unroll-loops.mlir:245`, the marker `hw.module @SkipLoopWithUnsupportedInductionVariable2(in %i: i42) {` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1564 `test/Dialect/LLHD/Transforms/unroll-loops.mlir:317` (unclassified)

At `test/Dialect/LLHD/Transforms/unroll-loops.mlir:317`, the marker `// CHECK-LABEL: @SkipLoopWithUnsupportedInitialInductionVariableValue` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1565 `test/Dialect/LLHD/Transforms/unroll-loops.mlir:318` (unclassified)

At `test/Dialect/LLHD/Transforms/unroll-loops.mlir:318`, the marker `hw.module @SkipLoopWithUnsupportedInitialInductionVariableValue(in %a: i42) {` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1566 `test/Dialect/LLHD/Transforms/unroll-loops.mlir:360` (unclassified)

At `test/Dialect/LLHD/Transforms/unroll-loops.mlir:360`, the marker `// CHECK-LABEL: @SkipLoopWithUnsupportedIncrement` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1567 `test/Dialect/LLHD/Transforms/unroll-loops.mlir:361` (unclassified)

At `test/Dialect/LLHD/Transforms/unroll-loops.mlir:361`, the marker `hw.module @SkipLoopWithUnsupportedIncrement() {` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1568 `test/Dialect/LLHD/Transforms/unroll-loops.mlir:382` (unclassified)

At `test/Dialect/LLHD/Transforms/unroll-loops.mlir:382`, the marker `// CHECK-LABEL: @SkipLoopWithUnsupportedBounds` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1569 `test/Dialect/LLHD/Transforms/unroll-loops.mlir:383` (unclassified)

At `test/Dialect/LLHD/Transforms/unroll-loops.mlir:383`, the marker `hw.module @SkipLoopWithUnsupportedBounds() {` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1570 `lib/Conversion/ImportVerilog/Statements.cpp:191` (unclassified)

At `lib/Conversion/ImportVerilog/Statements.cpp:191`, the marker `constexpr const char kUnsupportedSvaAttr[] = "circt.unsupported_sva";` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1571 `lib/Conversion/ImportVerilog/Statements.cpp:192` (unclassified)

At `lib/Conversion/ImportVerilog/Statements.cpp:192`, the marker `constexpr const char kUnsupportedSvaReasonAttr[] =` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1572 `lib/Conversion/ImportVerilog/Statements.cpp:193` (unclassified)

At `lib/Conversion/ImportVerilog/Statements.cpp:193`, the marker `"circt.unsupported_sva_reason";` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1573 `lib/Conversion/ImportVerilog/Statements.cpp:435` (unclassified)

At `lib/Conversion/ImportVerilog/Statements.cpp:435`, the marker `emitUnsupportedConcurrentAssertionPlaceholder(` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1574 `lib/Conversion/ImportVerilog/Statements.cpp:438` (unclassified)

At `lib/Conversion/ImportVerilog/Statements.cpp:438`, the marker `if (!context.options.continueOnUnsupportedSVA)` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1575 `lib/Conversion/ImportVerilog/Statements.cpp:473` (unclassified)

At `lib/Conversion/ImportVerilog/Statements.cpp:473`, the marker `op->setAttr(kUnsupportedSvaAttr, builder.getUnitAttr());` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1576 `lib/Conversion/ImportVerilog/Statements.cpp:474` (unclassified)

At `lib/Conversion/ImportVerilog/Statements.cpp:474`, the marker `op->setAttr(kUnsupportedSvaReasonAttr, builder.getStringAttr(reason));` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1577 `lib/Conversion/ImportVerilog/Statements.cpp:476` (implementation-gap)

At `lib/Conversion/ImportVerilog/Statements.cpp:476`, the marker `<< "skipping unsupported SVA assertion in continue mode: " << reason;` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1578 `lib/Conversion/ImportVerilog/Statements.cpp:1919` (implementation-gap)

At `lib/Conversion/ImportVerilog/Statements.cpp:1919`, the marker `mlir::emitError(loc) << "unsupported pattern in case statement: "` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1579 `lib/Conversion/ImportVerilog/Statements.cpp:1973` (implementation-gap)

At `lib/Conversion/ImportVerilog/Statements.cpp:1973`, the marker `mlir::emitError(loc, "unsupported set membership pattern match");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1580 `lib/Conversion/ImportVerilog/Statements.cpp:2555` (implementation-gap)

At `lib/Conversion/ImportVerilog/Statements.cpp:2555`, the marker `mlir::emitError(loc) << "unsupported immediate assertion kind: "` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1581 `lib/Conversion/ImportVerilog/Statements.cpp:2843` (unclassified)

At `lib/Conversion/ImportVerilog/Statements.cpp:2843`, the marker `auto tolerateUnsupportedSVA = [&](StringRef reason) -> LogicalResult {` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1582 `lib/Conversion/ImportVerilog/Statements.cpp:2844` (unclassified)

At `lib/Conversion/ImportVerilog/Statements.cpp:2844`, the marker `return emitUnsupportedConcurrentAssertionPlaceholder(stmt, assertLabel,` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1583 `lib/Conversion/ImportVerilog/Statements.cpp:2894` (unclassified)

At `lib/Conversion/ImportVerilog/Statements.cpp:2894`, the marker `if (succeeded(tolerateUnsupportedSVA("property lowering failed")))` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1584 `lib/Conversion/ImportVerilog/Statements.cpp:3005` (unclassified)

At `lib/Conversion/ImportVerilog/Statements.cpp:3005`, the marker `if (succeeded(tolerateUnsupportedSVA("property lowering failed")))` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1585 `lib/Conversion/ImportVerilog/Statements.cpp:3164` (implementation-gap)

At `lib/Conversion/ImportVerilog/Statements.cpp:3164`, the marker `if (succeeded(tolerateUnsupportedSVA("unsupported concurrent assertion kind")))` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1586 `lib/Conversion/ImportVerilog/Statements.cpp:3166` (implementation-gap)

At `lib/Conversion/ImportVerilog/Statements.cpp:3166`, the marker `mlir::emitError(loc) << "unsupported concurrent assertion kind: "` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1587 `lib/Conversion/ImportVerilog/Statements.cpp:3557` (implementation-gap)

At `lib/Conversion/ImportVerilog/Statements.cpp:3557`, the marker `<< " (checkpoint/restart not implemented)";` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1588 `lib/Conversion/ImportVerilog/Statements.cpp:3632` (implementation-gap)

At `lib/Conversion/ImportVerilog/Statements.cpp:3632`, the marker `// Legacy gate-array modeling functions — deprecated, not implemented.` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1589 `lib/Conversion/ImportVerilog/Statements.cpp:3649` (implementation-gap)

At `lib/Conversion/ImportVerilog/Statements.cpp:3649`, the marker `mlir::emitError(loc) << "unsupported legacy PLD array task '"` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1590 `lib/Conversion/ImportVerilog/Statements.cpp:3655` (implementation-gap)

At `lib/Conversion/ImportVerilog/Statements.cpp:3655`, the marker `// Legacy abstract queue functions — deprecated, not implemented.` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1591 `lib/Conversion/ImportVerilog/Statements.cpp:3660` (implementation-gap)

At `lib/Conversion/ImportVerilog/Statements.cpp:3660`, the marker `mlir::emitError(loc) << "unsupported legacy stochastic queue task '"` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1592 `lib/Conversion/ImportVerilog/Statements.cpp:3669` (implementation-gap)

At `lib/Conversion/ImportVerilog/Statements.cpp:3669`, the marker `<< " (SDF timing annotation not implemented)";` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1593 `lib/Conversion/ImportVerilog/Statements.cpp:4304` (implementation-gap)

At `lib/Conversion/ImportVerilog/Statements.cpp:4304`, the marker `mlir::emitError(loc) << "unsupported delayed event trigger";` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1594 `lib/Conversion/ImportVerilog/Statements.cpp:5087` (implementation-gap)

At `lib/Conversion/ImportVerilog/Statements.cpp:5087`, the marker `mlir::emitError(loc, "unsupported statement: ")` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1595 `lib/Conversion/ImportVerilog/Types.cpp:350` (implementation-gap)

At `lib/Conversion/ImportVerilog/Types.cpp:350`, the marker `auto d = mlir::emitError(loc, "unsupported type: ")` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1596 `lib/Dialect/ESI/runtime/cpp/include/esi/backends/Trace.h:44` (todo)

At `lib/Dialect/ESI/runtime/cpp/include/esi/backends/Trace.h:44`, the marker `// TODO: Full trace mode not yet supported.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Full trace mode not yet supported.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1597 `lib/Conversion/ImportVerilog/FormatStrings.cpp:220` (implementation-gap)

At `lib/Conversion/ImportVerilog/FormatStrings.cpp:220`, the marker `<< "unsupported format specifier `" << fullSpecifier << "`";` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1598 `test/Dialect/Synth/tech-mapper.mlir:88` (test-gap-reference)

At `test/Dialect/Synth/tech-mapper.mlir:88`, the marker `// It produces sub-optimal mappings since currently area-flow is not implemented.` indicates a gap signal in this path. What's missing: This test line references an unsupported behavior, indicating a known gap in upstream implementation coverage. How to fix: Implement the missing behavior in production code and update this test to assert functional semantics rather than unsupported status once support is complete.

### G1599 `test/Dialect/Synth/tech-mapper.mlir:92` (todo)

At `test/Dialect/Synth/tech-mapper.mlir:92`, the marker `// FIXME: If area-flow is implemented, this should be mapped to @area_flow with area strategy.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: If area-flow is implemented, this should be mapped to @area_flow with area strategy.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1600 `test/Dialect/LLHD/Transforms/inline-calls-errors.mlir:30` (test-expectation)

At `test/Dialect/LLHD/Transforms/inline-calls-errors.mlir:30`, the marker `// expected-error @below {{recursive function call cannot be inlined (unsupported in --ir-hw)}}` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1601 `lib/Dialect/ESI/runtime/cpp/include/esi/backends/RpcServer.h:25` (todo)

At `lib/Dialect/ESI/runtime/cpp/include/esi/backends/RpcServer.h:25`, the marker `/// TODO: make this a proper backend (as much as possible).` indicates a gap signal in this path. What's missing: The marker documents unfinished work: make this a proper backend (as much as possible).. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1602 `lib/Dialect/ESI/runtime/cpp/include/esi/backends/RpcServer.h:36` (todo)

At `lib/Dialect/ESI/runtime/cpp/include/esi/backends/RpcServer.h:36`, the marker `/// before the manifest is set. TODO: rework the DPI API to require that the` indicates a gap signal in this path. What's missing: The marker documents unfinished work: rework the DPI API to require that the. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1603 `test/Dialect/Synth/tech-mapper-error.mlir:59` (test-expectation)

At `test/Dialect/Synth/tech-mapper-error.mlir:59`, the marker `// expected-error@+1 {{Unsupported operation for truth table simulation}}` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1604 `lib/Dialect/ESI/runtime/cosim_dpi_server/CMakeLists.txt:14` (todo)

At `lib/Dialect/ESI/runtime/cosim_dpi_server/CMakeLists.txt:14`, the marker `# the simulator, but without it we get runtime link errors. TODO: figure out how` indicates a gap signal in this path. What's missing: The marker documents unfinished work: figure out how. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1605 `lib/Dialect/ESI/runtime/cosim_dpi_server/DpiEntryPoints.cpp:133` (todo)

At `lib/Dialect/ESI/runtime/cosim_dpi_server/DpiEntryPoints.cpp:133`, the marker `// TODO: Change this by breaking it in two functions, one for read and one for` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Change this by breaking it in two functions, one for read and one for. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1606 `lib/Dialect/ESI/runtime/cosim_dpi_server/DpiEntryPoints.cpp:357` (todo)

At `lib/Dialect/ESI/runtime/cosim_dpi_server/DpiEntryPoints.cpp:357`, the marker `// TODO: These had the shit broken outta them in the gRPC conversion. We're not` indicates a gap signal in this path. What's missing: The marker documents unfinished work: These had the shit broken outta them in the gRPC conversion. We're not. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1607 `lib/Dialect/ESI/runtime/cosim_dpi_server/DpiEntryPoints.cpp:384` (implementation-gap)

At `lib/Dialect/ESI/runtime/cosim_dpi_server/DpiEntryPoints.cpp:384`, the marker `assert(false && "unimplemented");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1608 `lib/Dialect/ESI/runtime/cosim_dpi_server/DpiEntryPoints.cpp:396` (implementation-gap)

At `lib/Dialect/ESI/runtime/cosim_dpi_server/DpiEntryPoints.cpp:396`, the marker `assert(false && "unimplemented");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1609 `lib/Conversion/ImportVerilog/HierarchicalNames.cpp:1591` (todo)

At `lib/Conversion/ImportVerilog/HierarchicalNames.cpp:1591`, the marker `/// TODO:Skip all others.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Skip all others.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1610 `lib/Dialect/ESI/runtime/cosim_dpi_server/driver.cpp:86` (todo)

At `lib/Dialect/ESI/runtime/cosim_dpi_server/driver.cpp:86`, the marker `// TODO: Add max speed (cycles per second) option for small, interactive` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Add max speed (cycles per second) option for small, interactive. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1611 `lib/Dialect/ESI/runtime/cosim_dpi_server/driver.cpp:94` (todo)

At `lib/Dialect/ESI/runtime/cosim_dpi_server/driver.cpp:94`, the marker `// TODO: Support ESI reset handshake in the future.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Support ESI reset handshake in the future.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1612 `lib/Dialect/ESI/runtime/cpp/include/esi/Utils.h:78` (todo)

At `lib/Dialect/ESI/runtime/cpp/include/esi/Utils.h:78`, the marker `// is to copy the data. TODO: Avoid copying the data.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Avoid copying the data.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1613 `test/Dialect/Synth/lut-mapper.mlir:1` (todo)

At `test/Dialect/Synth/lut-mapper.mlir:1`, the marker `// FIXME: max-cuts-per-root=20 is due to a lack of non-minimal cut filtering.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: max-cuts-per-root=20 is due to a lack of non-minimal cut filtering.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1614 `lib/Dialect/ESI/runtime/cpp/include/esi/Ports.h:335` (todo)

At `lib/Dialect/ESI/runtime/cpp/include/esi/Ports.h:335`, the marker `// TODO: Have the callback return something upon which the caller can check,` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Have the callback return something upon which the caller can check,. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1615 `lib/Dialect/ESI/runtime/cpp/include/esi/Ports.h:458` (todo)

At `lib/Dialect/ESI/runtime/cpp/include/esi/Ports.h:458`, the marker `// TODO: this probably shouldn't be 'const', but bundle ports' user access are` indicates a gap signal in this path. What's missing: The marker documents unfinished work: this probably shouldn't be 'const', but bundle ports' user access are. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1616 `lib/Dialect/Comb/CombFolds.cpp:1091` (todo)

At `lib/Dialect/Comb/CombFolds.cpp:1091`, the marker `// TODO: Combine multiple constants together even if they aren't at the` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Combine multiple constants together even if they aren't at the. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1617 `lib/Dialect/Comb/CombFolds.cpp:1109` (todo)

At `lib/Dialect/Comb/CombFolds.cpp:1109`, the marker `// TODO: Generalize this for non-single-bit operands.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Generalize this for non-single-bit operands.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1618 `lib/Dialect/Comb/CombFolds.cpp:1205` (todo)

At `lib/Dialect/Comb/CombFolds.cpp:1205`, the marker `/// TODO: and(..., x, not(x)) -> and(..., 0) -- complement` indicates a gap signal in this path. What's missing: The marker documents unfinished work: and(..., x, not(x)) -> and(..., 0) -- complement. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1619 `lib/Dialect/Comb/CombFolds.cpp:1421` (todo)

At `lib/Dialect/Comb/CombFolds.cpp:1421`, the marker `/// TODO: or(..., x, not(x)) -> or(..., '1) -- complement` indicates a gap signal in this path. What's missing: The marker documents unfinished work: or(..., x, not(x)) -> or(..., '1) -- complement. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1620 `lib/Dialect/Comb/CombFolds.cpp:2194` (todo)

At `lib/Dialect/Comb/CombFolds.cpp:2194`, the marker `// TODO: We could handle things like "x < 2" as two entries.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: We could handle things like "x < 2" as two entries.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1621 `lib/Dialect/Comb/CombFolds.cpp:2507` (todo)

At `lib/Dialect/Comb/CombFolds.cpp:2507`, the marker `// TODO: Generalize this to and, or, xor, icmp(!), which all occur in practice` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Generalize this to and, or, xor, icmp(!), which all occur in practice. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1622 `lib/Dialect/Comb/CombFolds.cpp:3254` (todo)

At `lib/Dialect/Comb/CombFolds.cpp:3254`, the marker `// FIXME(llvm merge, cc697fc292b0): concat doesn't work with zero bit values` indicates a gap signal in this path. What's missing: The marker documents unfinished work: (llvm merge, cc697fc292b0): concat doesn't work with zero bit values. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1623 `lib/Conversion/ImportVerilog/Structure.cpp:554` (implementation-gap)

At `lib/Conversion/ImportVerilog/Structure.cpp:554`, the marker `<< "unsupported interface port type for `" << port.name << "`";` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1624 `lib/Conversion/ImportVerilog/Structure.cpp:570` (implementation-gap)

At `lib/Conversion/ImportVerilog/Structure.cpp:570`, the marker `<< "unsupported interface port type for `" << port.name << "`";` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1625 `lib/Conversion/ImportVerilog/Structure.cpp:590` (implementation-gap)

At `lib/Conversion/ImportVerilog/Structure.cpp:590`, the marker `<< "unsupported interface port type for `" << port.name << "`";` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1626 `lib/Conversion/ImportVerilog/Structure.cpp:641` (implementation-gap)

At `lib/Conversion/ImportVerilog/Structure.cpp:641`, the marker `<< "unsupported interface port `" << port->name << "` ("` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1627 `lib/Conversion/ImportVerilog/Structure.cpp:671` (implementation-gap)

At `lib/Conversion/ImportVerilog/Structure.cpp:671`, the marker `<< "unsupported interface port `" << port->name << "` ("` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1628 `lib/Conversion/ImportVerilog/Structure.cpp:680` (implementation-gap)

At `lib/Conversion/ImportVerilog/Structure.cpp:680`, the marker `<< "unsupported interface port `" << portSymbol->name << "` ("` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1629 `lib/Conversion/ImportVerilog/Structure.cpp:875` (implementation-gap)

At `lib/Conversion/ImportVerilog/Structure.cpp:875`, the marker `mlir::emitError(loc, "unsupported construct: ")` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1630 `lib/Conversion/ImportVerilog/Structure.cpp:904` (implementation-gap)

At `lib/Conversion/ImportVerilog/Structure.cpp:904`, the marker `mlir::emitError(loc, "unsupported package member: ")` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1631 `lib/Conversion/ImportVerilog/Structure.cpp:1269` (implementation-gap)

At `lib/Conversion/ImportVerilog/Structure.cpp:1269`, the marker `<< "unsupported unconnected interface port `"` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1632 `lib/Conversion/ImportVerilog/Structure.cpp:1343` (implementation-gap)

At `lib/Conversion/ImportVerilog/Structure.cpp:1343`, the marker `<< "unsupported interface port connection for `"` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1633 `lib/Conversion/ImportVerilog/Structure.cpp:1445` (implementation-gap)

At `lib/Conversion/ImportVerilog/Structure.cpp:1445`, the marker `<< "unsupported internal symbol for unconnected port `"` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1634 `lib/Conversion/ImportVerilog/Structure.cpp:1456` (todo)

At `lib/Conversion/ImportVerilog/Structure.cpp:1456`, the marker `// TODO: Mark Inout port as unsupported and it will be supported later.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Mark Inout port as unsupported and it will be supported later.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1635 `lib/Conversion/ImportVerilog/Structure.cpp:1459` (implementation-gap)

At `lib/Conversion/ImportVerilog/Structure.cpp:1459`, the marker `<< "unsupported port `" << port->name << "` ("` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1636 `lib/Conversion/ImportVerilog/Structure.cpp:1465` (implementation-gap)

At `lib/Conversion/ImportVerilog/Structure.cpp:1465`, the marker `<< "unsupported port `" << portSymbol->name << "` ("` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1637 `lib/Conversion/ImportVerilog/Structure.cpp:1517` (implementation-gap)

At `lib/Conversion/ImportVerilog/Structure.cpp:1517`, the marker `mlir::emitError(loc) << "unsupported instance port `" << con->port.name` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1638 `lib/Conversion/ImportVerilog/Structure.cpp:1837` (unclassified)

At `lib/Conversion/ImportVerilog/Structure.cpp:1837`, the marker `bool hasAnyIff = false;` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1639 `lib/Conversion/ImportVerilog/Structure.cpp:1841` (unclassified)

At `lib/Conversion/ImportVerilog/Structure.cpp:1841`, the marker `hasAnyIff = true;` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1640 `lib/Conversion/ImportVerilog/Structure.cpp:1858` (unclassified)

At `lib/Conversion/ImportVerilog/Structure.cpp:1858`, the marker `if (!hasAnyIff)` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1641 `lib/Conversion/ImportVerilog/Structure.cpp:1901` (implementation-gap)

At `lib/Conversion/ImportVerilog/Structure.cpp:1901`, the marker `return mlir::emitError(loc, "unsupported net kind `")` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1642 `lib/Conversion/ImportVerilog/Structure.cpp:1948` (implementation-gap)

At `lib/Conversion/ImportVerilog/Structure.cpp:1948`, the marker `mlir::emitError(loc) << "unsupported lvalue type in continuous assign: "` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1643 `lib/Conversion/ImportVerilog/Structure.cpp:1976` (implementation-gap)

At `lib/Conversion/ImportVerilog/Structure.cpp:1976`, the marker `<< "unsupported continuous assignment timing control: "` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1644 `lib/Conversion/ImportVerilog/Structure.cpp:2441` (implementation-gap)

At `lib/Conversion/ImportVerilog/Structure.cpp:2441`, the marker `return emitDropped("unsupported UDP initial value");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1645 `lib/Conversion/ImportVerilog/Structure.cpp:2498` (unclassified)

At `lib/Conversion/ImportVerilog/Structure.cpp:2498`, the marker `bool &unsupportedSymbol, Value value,` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1646 `lib/Conversion/ImportVerilog/Structure.cpp:2522` (unclassified)

At `lib/Conversion/ImportVerilog/Structure.cpp:2522`, the marker `unsupportedSymbol = true;` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1647 `lib/Conversion/ImportVerilog/Structure.cpp:2530` (unclassified)

At `lib/Conversion/ImportVerilog/Structure.cpp:2530`, the marker `bool &unsupportedSymbol,` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1648 `lib/Conversion/ImportVerilog/Structure.cpp:2537` (implementation-gap)

At `lib/Conversion/ImportVerilog/Structure.cpp:2537`, the marker `bool unsupported = false;` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1649 `lib/Conversion/ImportVerilog/Structure.cpp:2538` (implementation-gap)

At `lib/Conversion/ImportVerilog/Structure.cpp:2538`, the marker `if (failed(addInputCharMatch(altCond, altImpossible, unsupported,` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1650 `lib/Conversion/ImportVerilog/Structure.cpp:2541` (implementation-gap)

At `lib/Conversion/ImportVerilog/Structure.cpp:2541`, the marker `if (unsupported) {` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1651 `lib/Conversion/ImportVerilog/Structure.cpp:2542` (unclassified)

At `lib/Conversion/ImportVerilog/Structure.cpp:2542`, the marker `unsupportedSymbol = true;` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1652 `lib/Conversion/ImportVerilog/Structure.cpp:2546` (implementation-gap)

At `lib/Conversion/ImportVerilog/Structure.cpp:2546`, the marker `if (failed(addInputCharMatch(altCond, altImpossible, unsupported,` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1653 `lib/Conversion/ImportVerilog/Structure.cpp:2549` (implementation-gap)

At `lib/Conversion/ImportVerilog/Structure.cpp:2549`, the marker `if (unsupported) {` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1654 `lib/Conversion/ImportVerilog/Structure.cpp:2550` (unclassified)

At `lib/Conversion/ImportVerilog/Structure.cpp:2550`, the marker `unsupportedSymbol = true;` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1655 `lib/Conversion/ImportVerilog/Structure.cpp:2611` (implementation-gap)

At `lib/Conversion/ImportVerilog/Structure.cpp:2611`, the marker `return emitDropped("unsupported UDP row shape");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1656 `lib/Conversion/ImportVerilog/Structure.cpp:2624` (implementation-gap)

At `lib/Conversion/ImportVerilog/Structure.cpp:2624`, the marker `return emitDropped("unsupported UDP row shape");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1657 `lib/Conversion/ImportVerilog/Structure.cpp:2634` (unclassified)

At `lib/Conversion/ImportVerilog/Structure.cpp:2634`, the marker `bool unsupportedSymbol = false;` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1658 `lib/Conversion/ImportVerilog/Structure.cpp:2636` (unclassified)

At `lib/Conversion/ImportVerilog/Structure.cpp:2636`, the marker `unsupportedSymbol, rowInputValues[i],` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1659 `lib/Conversion/ImportVerilog/Structure.cpp:2639` (unclassified)

At `lib/Conversion/ImportVerilog/Structure.cpp:2639`, the marker `if (unsupportedSymbol)` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1660 `lib/Conversion/ImportVerilog/Structure.cpp:2640` (implementation-gap)

At `lib/Conversion/ImportVerilog/Structure.cpp:2640`, the marker `return emitDropped("unsupported UDP input row symbol");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1661 `lib/Conversion/ImportVerilog/Structure.cpp:2684` (implementation-gap)

At `lib/Conversion/ImportVerilog/Structure.cpp:2684`, the marker `return emitDropped("unsupported UDP edge transition symbol");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1662 `lib/Conversion/ImportVerilog/Structure.cpp:2688` (unclassified)

At `lib/Conversion/ImportVerilog/Structure.cpp:2688`, the marker `bool unsupportedSymbol = false;` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1663 `lib/Conversion/ImportVerilog/Structure.cpp:2692` (unclassified)

At `lib/Conversion/ImportVerilog/Structure.cpp:2692`, the marker `edgeCond, unsupportedSymbol, impossibleEdge)))` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1664 `lib/Conversion/ImportVerilog/Structure.cpp:2694` (unclassified)

At `lib/Conversion/ImportVerilog/Structure.cpp:2694`, the marker `if (unsupportedSymbol)` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1665 `lib/Conversion/ImportVerilog/Structure.cpp:2695` (implementation-gap)

At `lib/Conversion/ImportVerilog/Structure.cpp:2695`, the marker `return emitDropped("unsupported UDP edge transition symbol");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1666 `lib/Conversion/ImportVerilog/Structure.cpp:2735` (implementation-gap)

At `lib/Conversion/ImportVerilog/Structure.cpp:2735`, the marker `return emitDropped("unsupported UDP current-state row symbol");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1667 `lib/Conversion/ImportVerilog/Structure.cpp:2742` (implementation-gap)

At `lib/Conversion/ImportVerilog/Structure.cpp:2742`, the marker `return emitDropped("unsupported UDP output row symbol");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1668 `lib/Conversion/ImportVerilog/Structure.cpp:2747` (implementation-gap)

At `lib/Conversion/ImportVerilog/Structure.cpp:2747`, the marker `return emitDropped("unsupported UDP output row symbol");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1669 `lib/Conversion/ImportVerilog/Structure.cpp:3075` (todo)

At `lib/Conversion/ImportVerilog/Structure.cpp:3075`, the marker `// TODO: Implement proper tristate modeling with high-Z support.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Implement proper tristate modeling with high-Z support.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1670 `lib/Conversion/ImportVerilog/Structure.cpp:3132` (todo)

At `lib/Conversion/ImportVerilog/Structure.cpp:3132`, the marker `// TODO: Implement proper tristate/high-Z modeling.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Implement proper tristate/high-Z modeling.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1671 `lib/Conversion/ImportVerilog/Structure.cpp:3188` (todo)

At `lib/Conversion/ImportVerilog/Structure.cpp:3188`, the marker `// TODO: Implement proper complementary MOS modeling with tristate.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Implement proper complementary MOS modeling with tristate.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1672 `lib/Conversion/ImportVerilog/Structure.cpp:3228` (todo)

At `lib/Conversion/ImportVerilog/Structure.cpp:3228`, the marker `// TODO: Implement proper bidirectional modeling.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Implement proper bidirectional modeling.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1673 `lib/Conversion/ImportVerilog/Structure.cpp:3289` (todo)

At `lib/Conversion/ImportVerilog/Structure.cpp:3289`, the marker `// TODO: Implement proper conditional bidirectional modeling.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Implement proper conditional bidirectional modeling.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1674 `lib/Conversion/ImportVerilog/Structure.cpp:3311` (implementation-gap)

At `lib/Conversion/ImportVerilog/Structure.cpp:3311`, the marker `mlir::emitError(loc) << "unsupported primitive type: " << primName;` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1675 `lib/Conversion/ImportVerilog/Structure.cpp:3324` (implementation-gap)

At `lib/Conversion/ImportVerilog/Structure.cpp:3324`, the marker `mlir::emitError(loc, "unsupported module member: ")` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1676 `lib/Conversion/ImportVerilog/Structure.cpp:3601` (implementation-gap)

At `lib/Conversion/ImportVerilog/Structure.cpp:3601`, the marker `mlir::emitError(loc) << "unsupported definition: "` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1677 `lib/Conversion/ImportVerilog/Structure.cpp:3657` (implementation-gap)

At `lib/Conversion/ImportVerilog/Structure.cpp:3657`, the marker `<< "unsupported generic interface port `" << port.name << "`";` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1678 `lib/Conversion/ImportVerilog/Structure.cpp:3730` (implementation-gap)

At `lib/Conversion/ImportVerilog/Structure.cpp:3730`, the marker `<< "unsupported module port `" << symbol->name << "` ("` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1679 `lib/Conversion/ImportVerilog/Structure.cpp:4100` (implementation-gap)

At `lib/Conversion/ImportVerilog/Structure.cpp:4100`, the marker `return mlir::emitError(port.loc, "unsupported port: `")` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1680 `lib/Conversion/ImportVerilog/Structure.cpp:4111` (implementation-gap)

At `lib/Conversion/ImportVerilog/Structure.cpp:4111`, the marker `return mlir::emitError(port.loc, "unsupported port: `")` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1681 `lib/Conversion/ImportVerilog/Structure.cpp:7138` (implementation-gap)

At `lib/Conversion/ImportVerilog/Structure.cpp:7138`, the marker `// currently not implemented. Since forward declarations of non-interface` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1682 `lib/Conversion/ImportVerilog/Structure.cpp:7328` (implementation-gap)

At `lib/Conversion/ImportVerilog/Structure.cpp:7328`, the marker `mlir::emitError(loc) << "unsupported construct in ClassType members: "` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1683 `test/Dialect/Emit/Reduction/pattern-registration.mlir:1` (test-expectation)

At `test/Dialect/Emit/Reduction/pattern-registration.mlir:1`, the marker `// UNSUPPORTED: system-windows` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1684 `test/Dialect/Emit/Reduction/emit-op-eraser.mlir:1` (test-expectation)

At `test/Dialect/Emit/Reduction/emit-op-eraser.mlir:1`, the marker `// UNSUPPORTED: system-windows` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1685 `test/Dialect/Kanagawa/Transforms/scoperef_tunneling.mlir:198` (todo)

At `test/Dialect/Kanagawa/Transforms/scoperef_tunneling.mlir:198`, the marker `// TODO: support this case. Too hard to support now. Problem is that hw.module` indicates a gap signal in this path. What's missing: The marker documents unfinished work: support this case. Too hard to support now. Problem is that hw.module. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1686 `lib/Dialect/Comb/Transforms/IntRangeAnnotations.cpp:110` (todo)

At `lib/Dialect/Comb/Transforms/IntRangeAnnotations.cpp:110`, the marker `// TODO: determine how to support subtraction` indicates a gap signal in this path. What's missing: The marker documents unfinished work: determine how to support subtraction. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1687 `test/Dialect/HW/svEmitErrors.mlir:3` (test-expectation)

At `test/Dialect/HW/svEmitErrors.mlir:3`, the marker `// expected-error @+1 {{value has an unsupported verilog type 'vector<3xi1>'}}` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1688 `test/Analysis/firrtl-test-instance-info.mlir:15` (unclassified)

At `test/Analysis/firrtl-test-instance-info.mlir:15`, the marker `// CHECK-NEXT: anyInstanceUnderDut: false` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1689 `test/Analysis/firrtl-test-instance-info.mlir:17` (unclassified)

At `test/Analysis/firrtl-test-instance-info.mlir:17`, the marker `// CHECK-NEXT: anyInstanceUnderEffectiveDut: false` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1690 `test/Analysis/firrtl-test-instance-info.mlir:19` (unclassified)

At `test/Analysis/firrtl-test-instance-info.mlir:19`, the marker `// CHECK-NEXT: anyInstanceUnderLayer: true` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1691 `test/Analysis/firrtl-test-instance-info.mlir:24` (unclassified)

At `test/Analysis/firrtl-test-instance-info.mlir:24`, the marker `// CHECK-NEXT: anyInstanceUnderDut: false` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1692 `test/Analysis/firrtl-test-instance-info.mlir:26` (unclassified)

At `test/Analysis/firrtl-test-instance-info.mlir:26`, the marker `// CHECK-NEXT: anyInstanceUnderEffectiveDut: false` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1693 `test/Analysis/firrtl-test-instance-info.mlir:28` (unclassified)

At `test/Analysis/firrtl-test-instance-info.mlir:28`, the marker `// CHECK-NEXT: anyInstanceUnderLayer: true` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1694 `test/Analysis/firrtl-test-instance-info.mlir:33` (unclassified)

At `test/Analysis/firrtl-test-instance-info.mlir:33`, the marker `// CHECK-NEXT: anyInstanceUnderDut: true` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1695 `test/Analysis/firrtl-test-instance-info.mlir:35` (unclassified)

At `test/Analysis/firrtl-test-instance-info.mlir:35`, the marker `// CHECK-NEXT: anyInstanceUnderEffectiveDut: true` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1696 `test/Analysis/firrtl-test-instance-info.mlir:37` (unclassified)

At `test/Analysis/firrtl-test-instance-info.mlir:37`, the marker `// CHECK-NEXT: anyInstanceUnderLayer: false` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1697 `test/Analysis/firrtl-test-instance-info.mlir:42` (unclassified)

At `test/Analysis/firrtl-test-instance-info.mlir:42`, the marker `// CHECK-NEXT: anyInstanceUnderDut: true` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1698 `test/Analysis/firrtl-test-instance-info.mlir:44` (unclassified)

At `test/Analysis/firrtl-test-instance-info.mlir:44`, the marker `// CHECK-NEXT: anyInstanceUnderEffectiveDut: true` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1699 `test/Analysis/firrtl-test-instance-info.mlir:46` (unclassified)

At `test/Analysis/firrtl-test-instance-info.mlir:46`, the marker `// CHECK-NEXT: anyInstanceUnderLayer: false` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1700 `test/Analysis/firrtl-test-instance-info.mlir:51` (unclassified)

At `test/Analysis/firrtl-test-instance-info.mlir:51`, the marker `// CHECK-NEXT: anyInstanceUnderDut: true` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1701 `test/Analysis/firrtl-test-instance-info.mlir:53` (unclassified)

At `test/Analysis/firrtl-test-instance-info.mlir:53`, the marker `// CHECK-NEXT: anyInstanceUnderEffectiveDut: true` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1702 `test/Analysis/firrtl-test-instance-info.mlir:55` (unclassified)

At `test/Analysis/firrtl-test-instance-info.mlir:55`, the marker `// CHECK-NEXT: anyInstanceUnderLayer: false` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1703 `test/Analysis/firrtl-test-instance-info.mlir:67` (unclassified)

At `test/Analysis/firrtl-test-instance-info.mlir:67`, the marker `// CHECK-NEXT: anyInstanceUnderDut: false` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1704 `test/Analysis/firrtl-test-instance-info.mlir:69` (unclassified)

At `test/Analysis/firrtl-test-instance-info.mlir:69`, the marker `// CHECK-NEXT: anyInstanceUnderEffectiveDut: false` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1705 `test/Analysis/firrtl-test-instance-info.mlir:71` (unclassified)

At `test/Analysis/firrtl-test-instance-info.mlir:71`, the marker `// CHECK-NEXT: anyInstanceUnderLayer: false` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1706 `test/Analysis/firrtl-test-instance-info.mlir:95` (unclassified)

At `test/Analysis/firrtl-test-instance-info.mlir:95`, the marker `// CHECK-NEXT: anyInstanceUnderDut: false` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1707 `test/Analysis/firrtl-test-instance-info.mlir:97` (unclassified)

At `test/Analysis/firrtl-test-instance-info.mlir:97`, the marker `// CHECK-NEXT: anyInstanceUnderEffectiveDut: true` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1708 `test/Analysis/firrtl-test-instance-info.mlir:99` (unclassified)

At `test/Analysis/firrtl-test-instance-info.mlir:99`, the marker `// CHECK-NEXT: anyInstanceUnderLayer: false` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1709 `test/Analysis/firrtl-test-instance-info.mlir:110` (unclassified)

At `test/Analysis/firrtl-test-instance-info.mlir:110`, the marker `// CHECK-NEXT: anyInstanceUnderDut: true` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1710 `test/Analysis/firrtl-test-instance-info.mlir:112` (unclassified)

At `test/Analysis/firrtl-test-instance-info.mlir:112`, the marker `// CHECK-NEXT: anyInstanceUnderEffectiveDut: true` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1711 `test/Analysis/firrtl-test-instance-info.mlir:114` (unclassified)

At `test/Analysis/firrtl-test-instance-info.mlir:114`, the marker `// CHECK-NEXT: anyInstanceUnderLayer: false` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1712 `test/Analysis/firrtl-test-instance-info.mlir:153` (unclassified)

At `test/Analysis/firrtl-test-instance-info.mlir:153`, the marker `// CHECK-NEXT: anyInstanceUnderDut: true` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1713 `test/Analysis/firrtl-test-instance-info.mlir:155` (unclassified)

At `test/Analysis/firrtl-test-instance-info.mlir:155`, the marker `// CHECK-NEXT: anyInstanceUnderEffectiveDut: true` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1714 `test/Analysis/firrtl-test-instance-info.mlir:173` (unclassified)

At `test/Analysis/firrtl-test-instance-info.mlir:173`, the marker `// CHECK-NEXT: anyInstanceUnderDut: true` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1715 `test/Analysis/firrtl-test-instance-info.mlir:175` (unclassified)

At `test/Analysis/firrtl-test-instance-info.mlir:175`, the marker `// CHECK-NEXT: anyInstanceUnderEffectiveDut: true` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1716 `test/Analysis/firrtl-test-instance-info.mlir:177` (unclassified)

At `test/Analysis/firrtl-test-instance-info.mlir:177`, the marker `// CHECK-NEXT: anyInstanceUnderLayer: true` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1717 `test/Analysis/firrtl-test-instance-info.mlir:179` (unclassified)

At `test/Analysis/firrtl-test-instance-info.mlir:179`, the marker `// CHECK-NEXT: anyInstanceInDesign: false` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1718 `test/Analysis/firrtl-test-instance-info.mlir:181` (unclassified)

At `test/Analysis/firrtl-test-instance-info.mlir:181`, the marker `// CHECK-NEXT: anyInstanceInEffectiveDesign: false` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1719 `test/Analysis/firrtl-test-instance-info.mlir:186` (unclassified)

At `test/Analysis/firrtl-test-instance-info.mlir:186`, the marker `// CHECK-NEXT: anyInstanceUnderDut: true` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1720 `test/Analysis/firrtl-test-instance-info.mlir:188` (unclassified)

At `test/Analysis/firrtl-test-instance-info.mlir:188`, the marker `// CHECK-NEXT: anyInstanceUnderEffectiveDut: true` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1721 `test/Analysis/firrtl-test-instance-info.mlir:190` (unclassified)

At `test/Analysis/firrtl-test-instance-info.mlir:190`, the marker `// CHECK-NEXT: anyInstanceUnderLayer: false` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1722 `test/Analysis/firrtl-test-instance-info.mlir:192` (unclassified)

At `test/Analysis/firrtl-test-instance-info.mlir:192`, the marker `// CHECK-NEXT: anyInstanceInDesign: true` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1723 `test/Analysis/firrtl-test-instance-info.mlir:194` (unclassified)

At `test/Analysis/firrtl-test-instance-info.mlir:194`, the marker `// CHECK-NEXT: anyInstanceInEffectiveDesign: true` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1724 `test/Analysis/firrtl-test-instance-info.mlir:209` (unclassified)

At `test/Analysis/firrtl-test-instance-info.mlir:209`, the marker `// CHECK-NEXT: anyInstanceUnderDut: false` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1725 `test/Analysis/firrtl-test-instance-info.mlir:211` (unclassified)

At `test/Analysis/firrtl-test-instance-info.mlir:211`, the marker `// CHECK-NEXT: anyInstanceUnderEffectiveDut: false` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1726 `test/Analysis/firrtl-test-instance-info.mlir:213` (unclassified)

At `test/Analysis/firrtl-test-instance-info.mlir:213`, the marker `// CHECK-NEXT: anyInstanceUnderLayer: false` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1727 `test/Analysis/firrtl-test-instance-info.mlir:215` (unclassified)

At `test/Analysis/firrtl-test-instance-info.mlir:215`, the marker `// CHECK-NEXT: anyInstanceInDesign: false` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1728 `test/Analysis/firrtl-test-instance-info.mlir:217` (unclassified)

At `test/Analysis/firrtl-test-instance-info.mlir:217`, the marker `// CHECK-NEXT: anyInstanceInEffectiveDesign: false` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1729 `test/Analysis/firrtl-test-instance-info.mlir:231` (test-expectation)

At `test/Analysis/firrtl-test-instance-info.mlir:231`, the marker `// CHECK: anyInstanceInDesign: false` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1730 `test/Analysis/firrtl-test-instance-info.mlir:233` (unclassified)

At `test/Analysis/firrtl-test-instance-info.mlir:233`, the marker `// CHECK-NEXT: anyInstanceInEffectiveDesign: true` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1731 `test/Analysis/firrtl-test-instance-info.mlir:237` (test-expectation)

At `test/Analysis/firrtl-test-instance-info.mlir:237`, the marker `// CHECK: anyInstanceInDesign: false` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1732 `test/Analysis/firrtl-test-instance-info.mlir:239` (unclassified)

At `test/Analysis/firrtl-test-instance-info.mlir:239`, the marker `// CHECK-NEXT: anyInstanceInEffectiveDesign: true` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1733 `test/Analysis/firrtl-test-instance-info.mlir:247` (test-expectation)

At `test/Analysis/firrtl-test-instance-info.mlir:247`, the marker `// CHECK: anyInstanceInDesign: false` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1734 `test/Analysis/firrtl-test-instance-info.mlir:249` (unclassified)

At `test/Analysis/firrtl-test-instance-info.mlir:249`, the marker `// CHECK-NEXT: anyInstanceInEffectiveDesign: true` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1735 `test/Analysis/firrtl-test-instance-info.mlir:262` (test-expectation)

At `test/Analysis/firrtl-test-instance-info.mlir:262`, the marker `// CHECK: anyInstanceInEffectiveDesign: true` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1736 `test/Analysis/firrtl-test-instance-info.mlir:284` (test-expectation)

At `test/Analysis/firrtl-test-instance-info.mlir:284`, the marker `// CHECK: anyInstanceInDesign: false` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1737 `test/Analysis/firrtl-test-instance-info.mlir:286` (unclassified)

At `test/Analysis/firrtl-test-instance-info.mlir:286`, the marker `// CHECK-NEXT: anyInstanceInEffectiveDesign: false` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1738 `test/Analysis/firrtl-test-instance-info.mlir:290` (test-expectation)

At `test/Analysis/firrtl-test-instance-info.mlir:290`, the marker `// CHECK: anyInstanceInDesign: true` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1739 `test/Analysis/firrtl-test-instance-info.mlir:292` (unclassified)

At `test/Analysis/firrtl-test-instance-info.mlir:292`, the marker `// CHECK-NEXT: anyInstanceInEffectiveDesign: true` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1740 `test/Analysis/firrtl-test-instance-info.mlir:296` (test-expectation)

At `test/Analysis/firrtl-test-instance-info.mlir:296`, the marker `// CHECK: anyInstanceInDesign: false` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1741 `test/Analysis/firrtl-test-instance-info.mlir:298` (unclassified)

At `test/Analysis/firrtl-test-instance-info.mlir:298`, the marker `// CHECK-NEXT: anyInstanceInEffectiveDesign: false` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1742 `test/Analysis/firrtl-test-instance-info.mlir:302` (test-expectation)

At `test/Analysis/firrtl-test-instance-info.mlir:302`, the marker `// CHECK: anyInstanceInDesign: true` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1743 `test/Analysis/firrtl-test-instance-info.mlir:304` (unclassified)

At `test/Analysis/firrtl-test-instance-info.mlir:304`, the marker `// CHECK-NEXT: anyInstanceInEffectiveDesign: true` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1744 `test/Analysis/firrtl-test-instance-info.mlir:308` (test-expectation)

At `test/Analysis/firrtl-test-instance-info.mlir:308`, the marker `// CHECK: anyInstanceInDesign: true` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1745 `test/Analysis/firrtl-test-instance-info.mlir:310` (unclassified)

At `test/Analysis/firrtl-test-instance-info.mlir:310`, the marker `// CHECK-NEXT: anyInstanceInEffectiveDesign: true` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1746 `test/Analysis/firrtl-test-instance-info.mlir:314` (test-expectation)

At `test/Analysis/firrtl-test-instance-info.mlir:314`, the marker `// CHECK: anyInstanceInDesign: false` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1747 `test/Analysis/firrtl-test-instance-info.mlir:316` (unclassified)

At `test/Analysis/firrtl-test-instance-info.mlir:316`, the marker `// CHECK-NEXT: anyInstanceInEffectiveDesign: false` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1748 `test/Analysis/firrtl-test-instance-info.mlir:320` (test-expectation)

At `test/Analysis/firrtl-test-instance-info.mlir:320`, the marker `// CHECK: anyInstanceInDesign: true` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1749 `test/Analysis/firrtl-test-instance-info.mlir:322` (unclassified)

At `test/Analysis/firrtl-test-instance-info.mlir:322`, the marker `// CHECK-NEXT: anyInstanceInEffectiveDesign: true` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1750 `test/Analysis/firrtl-test-instance-info.mlir:385` (test-expectation)

At `test/Analysis/firrtl-test-instance-info.mlir:385`, the marker `// CHECK: anyInstanceInDesign: false` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1751 `test/Analysis/firrtl-test-instance-info.mlir:387` (unclassified)

At `test/Analysis/firrtl-test-instance-info.mlir:387`, the marker `// CHECK-NEXT: anyInstanceInEffectiveDesign: true` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1752 `test/Analysis/firrtl-test-instance-info.mlir:391` (test-expectation)

At `test/Analysis/firrtl-test-instance-info.mlir:391`, the marker `// CHECK: anyInstanceInDesign: false` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1753 `test/Analysis/firrtl-test-instance-info.mlir:393` (unclassified)

At `test/Analysis/firrtl-test-instance-info.mlir:393`, the marker `// CHECK-NEXT: anyInstanceInEffectiveDesign: true` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1754 `test/Analysis/firrtl-test-instance-info.mlir:397` (test-expectation)

At `test/Analysis/firrtl-test-instance-info.mlir:397`, the marker `// CHECK: anyInstanceInDesign: false` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1755 `test/Analysis/firrtl-test-instance-info.mlir:399` (unclassified)

At `test/Analysis/firrtl-test-instance-info.mlir:399`, the marker `// CHECK-NEXT: anyInstanceInEffectiveDesign: false` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1756 `test/Analysis/firrtl-test-instance-info.mlir:403` (test-expectation)

At `test/Analysis/firrtl-test-instance-info.mlir:403`, the marker `// CHECK: anyInstanceInDesign: false` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1757 `test/Analysis/firrtl-test-instance-info.mlir:405` (unclassified)

At `test/Analysis/firrtl-test-instance-info.mlir:405`, the marker `// CHECK-NEXT: anyInstanceInEffectiveDesign: true` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1758 `test/Analysis/firrtl-test-instance-info.mlir:409` (test-expectation)

At `test/Analysis/firrtl-test-instance-info.mlir:409`, the marker `// CHECK: anyInstanceInDesign: false` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1759 `test/Analysis/firrtl-test-instance-info.mlir:411` (unclassified)

At `test/Analysis/firrtl-test-instance-info.mlir:411`, the marker `// CHECK-NEXT: anyInstanceInEffectiveDesign: true` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1760 `test/Analysis/firrtl-test-instance-info.mlir:415` (test-expectation)

At `test/Analysis/firrtl-test-instance-info.mlir:415`, the marker `// CHECK: anyInstanceInDesign: false` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1761 `test/Analysis/firrtl-test-instance-info.mlir:417` (unclassified)

At `test/Analysis/firrtl-test-instance-info.mlir:417`, the marker `// CHECK-NEXT: anyInstanceInEffectiveDesign: true` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1762 `test/Analysis/firrtl-test-instance-info.mlir:421` (test-expectation)

At `test/Analysis/firrtl-test-instance-info.mlir:421`, the marker `// CHECK: anyInstanceInDesign: false` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1763 `test/Analysis/firrtl-test-instance-info.mlir:423` (unclassified)

At `test/Analysis/firrtl-test-instance-info.mlir:423`, the marker `// CHECK-NEXT: anyInstanceInEffectiveDesign: true` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1764 `test/Analysis/firrtl-test-instance-info.mlir:471` (test-expectation)

At `test/Analysis/firrtl-test-instance-info.mlir:471`, the marker `// CHECK: anyInstanceUnderLayer: true` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1765 `test/Analysis/firrtl-test-instance-info.mlir:473` (test-expectation)

At `test/Analysis/firrtl-test-instance-info.mlir:473`, the marker `// CHECK: anyInstanceInEffectiveDesign: false` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1766 `test/Analysis/firrtl-test-instance-info.mlir:478` (test-expectation)

At `test/Analysis/firrtl-test-instance-info.mlir:478`, the marker `// CHECK: anyInstanceUnderLayer: true` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1767 `test/Analysis/firrtl-test-instance-info.mlir:480` (test-expectation)

At `test/Analysis/firrtl-test-instance-info.mlir:480`, the marker `// CHECK: anyInstanceInEffectiveDesign: false` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1768 `test/Analysis/firrtl-test-instance-info.mlir:501` (unclassified)

At `test/Analysis/firrtl-test-instance-info.mlir:501`, the marker `// CHECK-NEXT: anyInstanceUnderDut: false` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1769 `test/Analysis/firrtl-test-instance-info.mlir:510` (unclassified)

At `test/Analysis/firrtl-test-instance-info.mlir:510`, the marker `// CHECK-NEXT: anyInstanceUnderDut: true` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1770 `test/Analysis/firrtl-test-instance-info.mlir:522` (unclassified)

At `test/Analysis/firrtl-test-instance-info.mlir:522`, the marker `// CHECK-NEXT: anyInstanceUnderDut: false` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1771 `lib/Dialect/Comb/Transforms/BalanceMux.cpp:216` (todo)

At `lib/Dialect/Comb/Transforms/BalanceMux.cpp:216`, the marker `// TODO: Ideally the separator index should be selected based on arrival times` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Ideally the separator index should be selected based on arrival times. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1772 `test/Dialect/HW/parameters.mlir:33` (todo)

At `test/Dialect/HW/parameters.mlir:33`, the marker `// CHECK-SAME: #hw.param.expr.add<#hw.param.verbatim<"xxx">, 17>>() -> ()` indicates a gap signal in this path. What's missing: The marker documents unfinished work: ">, 17>>() -> (). The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1773 `test/Dialect/HW/parameters.mlir:34` (todo)

At `test/Dialect/HW/parameters.mlir:34`, the marker `hw.instance "verbatimparam" @NoArg<param: i42 = #hw.param.expr.add<#hw.param.verbatim<"xxx">, 17>>() -> ()` indicates a gap signal in this path. What's missing: The marker documents unfinished work: ">, 17>>() -> (). The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1774 `test/Dialect/HW/parameters.mlir:128` (todo)

At `test/Dialect/HW/parameters.mlir:128`, the marker `// CHECK-SAME: add<#hw.param.expr.mul<{{.*}}>, #hw.param.verbatim<"xxx">>` indicates a gap signal in this path. What's missing: The marker documents unfinished work: ">>. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1775 `test/Dialect/HW/parameters.mlir:129` (todo)

At `test/Dialect/HW/parameters.mlir:129`, the marker `%4 = hw.param.value i4 = #hw.param.expr.add<#hw.param.verbatim<"xxx">, #hw.param.expr.mul<#hw.param.decl.ref<"p1">, 4>>` indicates a gap signal in this path. What's missing: The marker documents unfinished work: ">, #hw.param.expr.mul<#hw.param.decl.ref<"p1">, 4>>. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1776 `lib/Conversion/ImportVerilog/Expressions.cpp:213` (unclassified)

At `lib/Conversion/ImportVerilog/Expressions.cpp:213`, the marker `bool hasAnyIff = false;` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1777 `lib/Conversion/ImportVerilog/Expressions.cpp:217` (unclassified)

At `lib/Conversion/ImportVerilog/Expressions.cpp:217`, the marker `hasAnyIff = true;` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1778 `lib/Conversion/ImportVerilog/Expressions.cpp:233` (unclassified)

At `lib/Conversion/ImportVerilog/Expressions.cpp:233`, the marker `if (!hasAnyIff)` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1779 `lib/Conversion/ImportVerilog/Expressions.cpp:1670` (implementation-gap)

At `lib/Conversion/ImportVerilog/Expressions.cpp:1670`, the marker `mlir::emitError(loc) << "unsupported expression: element select into "` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1780 `lib/Conversion/ImportVerilog/Expressions.cpp:2480` (implementation-gap)

At `lib/Conversion/ImportVerilog/Expressions.cpp:2480`, the marker `mlir::emitError(loc, "unsupported local assertion variable type");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1781 `lib/Conversion/ImportVerilog/Expressions.cpp:2937` (implementation-gap)

At `lib/Conversion/ImportVerilog/Expressions.cpp:2937`, the marker `auto d = mlir::emitError(loc, "unsupported arbitrary symbol reference `")` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1782 `lib/Conversion/ImportVerilog/Expressions.cpp:3132` (implementation-gap)

At `lib/Conversion/ImportVerilog/Expressions.cpp:3132`, the marker `mlir::emitError(loc) << "unsupported lvalue type in assignment: "` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1783 `lib/Conversion/ImportVerilog/Expressions.cpp:3396` (implementation-gap)

At `lib/Conversion/ImportVerilog/Expressions.cpp:3396`, the marker `mlir::emitError(loc, "unsupported unary operator");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1784 `lib/Conversion/ImportVerilog/Expressions.cpp:3830` (implementation-gap)

At `lib/Conversion/ImportVerilog/Expressions.cpp:3830`, the marker `<< "unsupported unpacked aggregate equality operands";` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1785 `lib/Conversion/ImportVerilog/Expressions.cpp:3844` (implementation-gap)

At `lib/Conversion/ImportVerilog/Expressions.cpp:3844`, the marker `<< "unsupported dynamic unpacked equality operands";` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1786 `lib/Conversion/ImportVerilog/Expressions.cpp:3991` (implementation-gap)

At `lib/Conversion/ImportVerilog/Expressions.cpp:3991`, the marker `<< "unsupported unpacked aggregate inequality operands";` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1787 `lib/Conversion/ImportVerilog/Expressions.cpp:4005` (implementation-gap)

At `lib/Conversion/ImportVerilog/Expressions.cpp:4005`, the marker `<< "unsupported dynamic unpacked inequality operands";` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1788 `lib/Conversion/ImportVerilog/Expressions.cpp:4143` (implementation-gap)

At `lib/Conversion/ImportVerilog/Expressions.cpp:4143`, the marker `<< "unsupported dynamic unpacked case equality operands";` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1789 `lib/Conversion/ImportVerilog/Expressions.cpp:4153` (implementation-gap)

At `lib/Conversion/ImportVerilog/Expressions.cpp:4153`, the marker `<< "unsupported unpacked aggregate case equality operands";` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1790 `lib/Conversion/ImportVerilog/Expressions.cpp:4182` (implementation-gap)

At `lib/Conversion/ImportVerilog/Expressions.cpp:4182`, the marker `<< "unsupported dynamic unpacked case inequality operands";` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1791 `lib/Conversion/ImportVerilog/Expressions.cpp:4192` (implementation-gap)

At `lib/Conversion/ImportVerilog/Expressions.cpp:4192`, the marker `<< "unsupported unpacked aggregate case inequality operands";` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1792 `lib/Conversion/ImportVerilog/Expressions.cpp:4261` (implementation-gap)

At `lib/Conversion/ImportVerilog/Expressions.cpp:4261`, the marker `mlir::emitError(loc, "unsupported binary operator");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1793 `lib/Conversion/ImportVerilog/Expressions.cpp:4486` (implementation-gap)

At `lib/Conversion/ImportVerilog/Expressions.cpp:4486`, the marker `<< "unsupported conditional expression with more than one condition";` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1794 `lib/Conversion/ImportVerilog/Expressions.cpp:4945` (implementation-gap)

At `lib/Conversion/ImportVerilog/Expressions.cpp:4945`, the marker `<< "' is not yet implemented; lowering as "` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1795 `lib/Conversion/ImportVerilog/Expressions.cpp:5030` (implementation-gap)

At `lib/Conversion/ImportVerilog/Expressions.cpp:5030`, the marker `<< "' is not yet implemented; lowering as "` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1796 `lib/Conversion/ImportVerilog/Expressions.cpp:5072` (unclassified)

At `lib/Conversion/ImportVerilog/Expressions.cpp:5072`, the marker `bool hasAnyIff = false;` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1797 `lib/Conversion/ImportVerilog/Expressions.cpp:5077` (unclassified)

At `lib/Conversion/ImportVerilog/Expressions.cpp:5077`, the marker `hasAnyIff = true;` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1798 `lib/Conversion/ImportVerilog/Expressions.cpp:5082` (unclassified)

At `lib/Conversion/ImportVerilog/Expressions.cpp:5082`, the marker `if (!hasAnyIff)` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1799 `lib/Conversion/ImportVerilog/Expressions.cpp:5253` (implementation-gap)

At `lib/Conversion/ImportVerilog/Expressions.cpp:5253`, the marker `<< "' is not yet implemented; lowering as "` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1800 `lib/Conversion/ImportVerilog/Expressions.cpp:5450` (implementation-gap)

At `lib/Conversion/ImportVerilog/Expressions.cpp:5450`, the marker `<< "' is not yet implemented; lowering as regular function call";` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1801 `lib/Conversion/ImportVerilog/Expressions.cpp:5717` (implementation-gap)

At `lib/Conversion/ImportVerilog/Expressions.cpp:5717`, the marker `<< "' is not yet implemented";` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1802 `lib/Conversion/ImportVerilog/Expressions.cpp:5839` (implementation-gap)

At `lib/Conversion/ImportVerilog/Expressions.cpp:5839`, the marker `<< "' is not yet implemented";` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1803 `lib/Conversion/ImportVerilog/Expressions.cpp:6986` (implementation-gap)

At `lib/Conversion/ImportVerilog/Expressions.cpp:6986`, the marker `mlir::emitError(loc) << "unsupported destination type in $cast: "` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1804 `lib/Conversion/ImportVerilog/Expressions.cpp:8503` (implementation-gap)

At `lib/Conversion/ImportVerilog/Expressions.cpp:8503`, the marker `mlir::emitError(loc) << "unsupported system call `" << subroutine.name` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1805 `lib/Conversion/ImportVerilog/Expressions.cpp:8762` (implementation-gap)

At `lib/Conversion/ImportVerilog/Expressions.cpp:8762`, the marker `mlir::emitError(loc) << "unsupported assignment pattern with type " << type;` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1806 `lib/Conversion/ImportVerilog/Expressions.cpp:8897` (implementation-gap)

At `lib/Conversion/ImportVerilog/Expressions.cpp:8897`, the marker `<< "unsupported streaming concat element type for queue: "` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1807 `lib/Conversion/ImportVerilog/Expressions.cpp:9692` (implementation-gap)

At `lib/Conversion/ImportVerilog/Expressions.cpp:9692`, the marker `mlir::emitError(loc, "unsupported expression: ")` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1808 `lib/Conversion/ImportVerilog/Expressions.cpp:9763` (implementation-gap)

At `lib/Conversion/ImportVerilog/Expressions.cpp:9763`, the marker `mlir::emitError(loc, "unsupported local assertion variable type")` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1809 `lib/Conversion/ImportVerilog/Expressions.cpp:9789` (implementation-gap)

At `lib/Conversion/ImportVerilog/Expressions.cpp:9789`, the marker `mlir::emitError(loc, "unsupported local assertion variable type");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1810 `lib/Conversion/ImportVerilog/Expressions.cpp:9798` (implementation-gap)

At `lib/Conversion/ImportVerilog/Expressions.cpp:9798`, the marker `mlir::emitError(loc, "unsupported local assertion variable type")` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1811 `lib/Conversion/ImportVerilog/Expressions.cpp:10449` (implementation-gap)

At `lib/Conversion/ImportVerilog/Expressions.cpp:10449`, the marker `mlir::emitError(loc) << "unsupported conversion: " << packedType` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1812 `lib/Conversion/ImportVerilog/Expressions.cpp:10491` (implementation-gap)

At `lib/Conversion/ImportVerilog/Expressions.cpp:10491`, the marker `mlir::emitError(loc) << "unsupported conversion: " << intType` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1813 `lib/Conversion/ImportVerilog/Expressions.cpp:10809` (todo)

At `lib/Conversion/ImportVerilog/Expressions.cpp:10809`, the marker `// TODO: Handle other conversions with dedicated ops.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Handle other conversions with dedicated ops.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1814 `lib/Conversion/ImportVerilog/Expressions.cpp:10977` (implementation-gap)

At `lib/Conversion/ImportVerilog/Expressions.cpp:10977`, the marker `mlir::emitError(loc) << "unsupported system call `"` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1815 `lib/Conversion/ImportVerilog/Expressions.cpp:11813` (implementation-gap)

At `lib/Conversion/ImportVerilog/Expressions.cpp:11813`, the marker `mlir::emitError(loc) << "unsupported system call `"` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1816 `lib/Conversion/ImportVerilog/Expressions.cpp:11930` (implementation-gap)

At `lib/Conversion/ImportVerilog/Expressions.cpp:11930`, the marker `// Legacy — deprecated, not implemented.` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1817 `lib/Conversion/ImportVerilog/Expressions.cpp:11934` (implementation-gap)

At `lib/Conversion/ImportVerilog/Expressions.cpp:11934`, the marker `<< "unsupported legacy stochastic queue function "` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1818 `lib/Conversion/ImportVerilog/Expressions.cpp:11941` (implementation-gap)

At `lib/Conversion/ImportVerilog/Expressions.cpp:11941`, the marker `<< "unsupported legacy stochastic queue function "` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1819 `lib/Conversion/ImportVerilog/Expressions.cpp:11952` (implementation-gap)

At `lib/Conversion/ImportVerilog/Expressions.cpp:11952`, the marker `mlir::emitError(loc) << "unsupported system call `"` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1820 `lib/Conversion/ImportVerilog/Expressions.cpp:11969` (implementation-gap)

At `lib/Conversion/ImportVerilog/Expressions.cpp:11969`, the marker `mlir::emitError(loc) << "unsupported system call `"` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1821 `test/Dialect/Arc/lower-clocks-to-funcs-errors.mlir:30` (test-expectation)

At `test/Dialect/Arc/lower-clocks-to-funcs-errors.mlir:30`, the marker `// expected-error @below {{op containing multiple InitialOps is currently unsupported.}}` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1822 `test/Dialect/Arc/lower-clocks-to-funcs-errors.mlir:31` (test-expectation)

At `test/Dialect/Arc/lower-clocks-to-funcs-errors.mlir:31`, the marker `// expected-error @below {{op containing multiple FinalOps is currently unsupported.}}` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1823 `test/Dialect/Seq/canonicalization.mlir:106` (todo)

At `test/Dialect/Seq/canonicalization.mlir:106`, the marker `// TODO: Use constant aggregate attribute once supported.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Use constant aggregate attribute once supported.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1824 `test/Dialect/Arc/latency-retiming.mlir:13` (todo)

At `test/Dialect/Arc/latency-retiming.mlir:13`, the marker `// COM: TODO: a canonicalization pattern could reorder the outputs such that` indicates a gap signal in this path. What's missing: The marker documents unfinished work: a canonicalization pattern could reorder the outputs such that. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1825 `test/Dialect/Arc/canonicalizers.mlir:135` (todo)

At `test/Dialect/Arc/canonicalizers.mlir:135`, the marker `// TODO: add op such that it is not folded away because it's just passthrough` indicates a gap signal in this path. What's missing: The marker documents unfinished work: add op such that it is not folded away because it's just passthrough. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1826 `test/Dialect/FIRRTL/infer-domains-infer-all-errors.mlir:122` (todo)

At `test/Dialect/FIRRTL/infer-domains-infer-all-errors.mlir:122`, the marker `// TODO: this just relies on the op-verifier for instance choice ops.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: this just relies on the op-verifier for instance choice ops.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1827 `test/Dialect/Arc/mux-to-control-flow.mlir:40` (todo)

At `test/Dialect/Arc/mux-to-control-flow.mlir:40`, the marker `// TODO: while above testcase should already provide high statement coverage,` indicates a gap signal in this path. What's missing: The marker documents unfinished work: while above testcase should already provide high statement coverage,. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1828 `test/Dialect/HW/hw-convert-bitcasts.mlir:49` (test-gap-reference)

At `test/Dialect/HW/hw-convert-bitcasts.mlir:49`, the marker `// Don't crash on unsupported types` indicates a gap signal in this path. What's missing: This test line references an unsupported behavior, indicating a known gap in upstream implementation coverage. How to fix: Implement the missing behavior in production code and update this test to assert functional semantics rather than unsupported status once support is complete.

### G1829 `test/Dialect/HW/hw-convert-bitcasts.mlir:50` (test-gap-reference)

At `test/Dialect/HW/hw-convert-bitcasts.mlir:50`, the marker `// NOCANON-LABEL: hw.module @unsupported` indicates a gap signal in this path. What's missing: This test line references an unsupported behavior, indicating a known gap in upstream implementation coverage. How to fix: Implement the missing behavior in production code and update this test to assert functional semantics rather than unsupported status once support is complete.

### G1830 `test/Dialect/HW/hw-convert-bitcasts.mlir:51` (test-gap-reference)

At `test/Dialect/HW/hw-convert-bitcasts.mlir:51`, the marker `hw.module @unsupported(in %i: i8, out o : i8) {` indicates a gap signal in this path. What's missing: This test line references an unsupported behavior, indicating a known gap in upstream implementation coverage. How to fix: Implement the missing behavior in production code and update this test to assert functional semantics rather than unsupported status once support is complete.

### G1831 `test/Dialect/Arc/infer-state-properties.mlir:248` (todo)

At `test/Dialect/Arc/infer-state-properties.mlir:248`, the marker `// TODO: test that patterns handle the case where the output is used for another thing as well properly` indicates a gap signal in this path. What's missing: The marker documents unfinished work: test that patterns handle the case where the output is used for another thing as well properly. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1832 `test/Dialect/Arc/infer-state-properties.mlir:249` (todo)

At `test/Dialect/Arc/infer-state-properties.mlir:249`, the marker `// TODO: test that reset and enable are only added when the latency is actually 1 or higher` indicates a gap signal in this path. What's missing: The marker documents unfinished work: test that reset and enable are only added when the latency is actually 1 or higher. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1833 `test/Dialect/HW/hw-convert-bitcasts-errors.mlir:3` (test-gap-reference)

At `test/Dialect/HW/hw-convert-bitcasts-errors.mlir:3`, the marker `hw.module @unsupported(in %i: i8, out o : i8) {` indicates a gap signal in this path. What's missing: This test line references an unsupported behavior, indicating a known gap in upstream implementation coverage. How to fix: Implement the missing behavior in production code and update this test to assert functional semantics rather than unsupported status once support is complete.

### G1834 `test/Dialect/HW/hw-convert-bitcasts-errors.mlir:4` (test-expectation)

At `test/Dialect/HW/hw-convert-bitcasts-errors.mlir:4`, the marker `// expected-error @+1 {{has unsupported output type}}` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1835 `test/Dialect/HW/hw-convert-bitcasts-errors.mlir:6` (test-expectation)

At `test/Dialect/HW/hw-convert-bitcasts-errors.mlir:6`, the marker `// expected-error @+1 {{has unsupported input type}}` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1836 `test/Dialect/FIRRTL/annotations-errors.fir:6` (test-expectation)

At `test/Dialect/FIRRTL/annotations-errors.fir:6`, the marker `; expected-error @+2 {{Invalid/unsupported annotation format}}` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1837 `test/Dialect/FIRRTL/annotations-errors.fir:17` (test-expectation)

At `test/Dialect/FIRRTL/annotations-errors.fir:17`, the marker `; expected-error @+2 {{Invalid/unsupported annotation format}}` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1838 `test/Dialect/Arc/Reduction/state-elimination.mlir:1` (test-expectation)

At `test/Dialect/Arc/Reduction/state-elimination.mlir:1`, the marker `// UNSUPPORTED: system-windows` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1839 `test/Dialect/RTG/Transform/linear-scan-register-allocation.mlir:69` (unclassified)

At `test/Dialect/RTG/Transform/linear-scan-register-allocation.mlir:69`, the marker `rtg.test @unsupportedUser() {` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1840 `test/Dialect/HW/errors.mlir:418` (test-expectation)

At `test/Dialect/HW/errors.mlir:418`, the marker `// expected-error @+1 {{unsupported dimension kind in hw.array}}` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1841 `test/Dialect/Arc/Reduction/pattern-registration.mlir:1` (test-expectation)

At `test/Dialect/Arc/Reduction/pattern-registration.mlir:1`, the marker `// UNSUPPORTED: system-windows` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1842 `lib/Dialect/Calyx/Transforms/CompileControl.cpp:34` (todo)

At `lib/Dialect/Calyx/Transforms/CompileControl.cpp:34`, the marker `/// TODO(Calyx): Probably a better built-in operation?` indicates a gap signal in this path. What's missing: The marker documents unfinished work: (Calyx): Probably a better built-in operation?. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1843 `lib/Dialect/Calyx/Transforms/CompileControl.cpp:116` (todo)

At `lib/Dialect/Calyx/Transforms/CompileControl.cpp:116`, the marker `// TODO(Calyx): Eventually, we should canonicalize the GroupDoneOp's guard` indicates a gap signal in this path. What's missing: The marker documents unfinished work: (Calyx): Eventually, we should canonicalize the GroupDoneOp's guard. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1844 `test/Dialect/RTG/Transform/emit-rtg-isa-assembly-errors.mlir:1` (test-expectation)

At `test/Dialect/RTG/Transform/emit-rtg-isa-assembly-errors.mlir:1`, the marker `// RUN: circt-opt --rtg-emit-isa-assembly=unsupported-instructions=rtgtest.rv32i.beq %s --split-input-file --verify-diagnostics` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1845 `lib/Dialect/Calyx/Transforms/CalyxLoweringUtils.cpp:798` (implementation-gap)

At `lib/Dialect/Calyx/Transforms/CalyxLoweringUtils.cpp:798`, the marker `assert(isa<IntegerType>(argType) && "unsupported block argument type");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1846 `lib/Dialect/Calyx/Transforms/CalyxLoweringUtils.cpp:822` (implementation-gap)

At `lib/Dialect/Calyx/Transforms/CalyxLoweringUtils.cpp:822`, the marker `"unsupported return type");` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1847 `test/Dialect/RTG/Transform/emit-rtg-isa-assembly.mlir:2` (test-expectation)

At `test/Dialect/RTG/Transform/emit-rtg-isa-assembly.mlir:2`, the marker `// RUN: circt-opt --rtg-emit-isa-assembly="unsupported-instructions=rtgtest.rv32i.ebreak,rtgtest.rv32i.ecall unsupported-instructions-file=%S/unsupported-instr.txt" %s 2>&1 >/dev/null | FileCheck %s --match-full-lines --strict-whitespace` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1848 `test/Dialect/FIRRTL/simplify-mems.mlir:471` (todo)

At `test/Dialect/FIRRTL/simplify-mems.mlir:471`, the marker `// TODO: It would be good to de-duplicate these either in the pass or in a canonicalizer.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: It would be good to de-duplicate these either in the pass or in a canonicalizer.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1849 `test/Dialect/Handshake/errors.mlir:12` (unclassified)

At `test/Dialect/Handshake/errors.mlir:12`, the marker `handshake.func @invalid_mux_unsupported_select(%arg0: tensor<i1>, %arg1: i32, %arg2: i32) {` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1850 `test/Dialect/Handshake/errors.mlir:13` (test-expectation)

At `test/Dialect/Handshake/errors.mlir:13`, the marker `// expected-error @+1 {{unsupported type for indexing value: 'tensor<i1>'}}` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1851 `test/Dialect/Handshake/errors.mlir:28` (unclassified)

At `test/Dialect/Handshake/errors.mlir:28`, the marker `handshake.func @invalid_cmerge_unsupported_index(%arg1: i32, %arg2: i32) -> tensor<i1> {` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1852 `test/Dialect/Handshake/errors.mlir:29` (test-expectation)

At `test/Dialect/Handshake/errors.mlir:29`, the marker `// expected-error @below {{unsupported type for indexing value: 'tensor<i1>'}}` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1853 `test/Dialect/FIRRTL/SFCTests/ExtractSeqMems/Simple2.fir:1` (test-expectation)

At `test/Dialect/FIRRTL/SFCTests/ExtractSeqMems/Simple2.fir:1`, the marker `; UNSUPPORTED: system-windows` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1854 `test/Dialect/HW/Reduction/pattern-registration.mlir:1` (test-expectation)

At `test/Dialect/HW/Reduction/pattern-registration.mlir:1`, the marker `// UNSUPPORTED: system-windows` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1855 `test/Dialect/FIRRTL/SFCTests/ExtractSeqMems/Compose.fir:1` (test-expectation)

At `test/Dialect/FIRRTL/SFCTests/ExtractSeqMems/Compose.fir:1`, the marker `; UNSUPPORTED: system-windows` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1856 `test/Dialect/HW/Reduction/hw-operand-forwarder.mlir:1` (test-expectation)

At `test/Dialect/HW/Reduction/hw-operand-forwarder.mlir:1`, the marker `// UNSUPPORTED: system-windows` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1857 `test/Dialect/SV/hw-legalize-modules-packed-arrays.mlir:14` (test-expectation)

At `test/Dialect/SV/hw-legalize-modules-packed-arrays.mlir:14`, the marker `// expected-error @+1 {{unsupported packed array expression}}` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1858 `test/Dialect/HW/Reduction/hw-module-output-pruner.mlir:1` (test-expectation)

At `test/Dialect/HW/Reduction/hw-module-output-pruner.mlir:1`, the marker `// UNSUPPORTED: system-windows` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1859 `test/Dialect/FIRRTL/ref.mlir:211` (todo)

At `test/Dialect/FIRRTL/ref.mlir:211`, the marker `// TODO: infer ref result existence + type based on "forceable" or other ref-kind(s) indicator.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: infer ref result existence + type based on "forceable" or other ref-kind(s) indicator.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1860 `test/Dialect/RTG/Reduction/virtual-register-constantifier.mlir:1` (test-expectation)

At `test/Dialect/RTG/Reduction/virtual-register-constantifier.mlir:1`, the marker `// UNSUPPORTED: system-windows` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1861 `test/Dialect/HW/Reduction/hw-module-input-pruner.mlir:1` (test-expectation)

At `test/Dialect/HW/Reduction/hw-module-input-pruner.mlir:1`, the marker `// UNSUPPORTED: system-windows` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1862 `lib/Dialect/Calyx/Export/CalyxEmitter.cpp:974` (unclassified)

At `lib/Dialect/Calyx/Export/CalyxEmitter.cpp:974`, the marker `double doubleValue = value.convertToDouble();` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1863 `test/Dialect/FIRRTL/Reduction/root-extmodule-port-pruner.mlir:1` (test-expectation)

At `test/Dialect/FIRRTL/Reduction/root-extmodule-port-pruner.mlir:1`, the marker `// UNSUPPORTED: system-windows` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1864 `test/Dialect/HW/Reduction/hw-module-externalizer.mlir:1` (test-expectation)

At `test/Dialect/HW/Reduction/hw-module-externalizer.mlir:1`, the marker `// UNSUPPORTED: system-windows` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1865 `test/Dialect/FIRRTL/Reduction/module-port-pruner.mlir:1` (test-expectation)

At `test/Dialect/FIRRTL/Reduction/module-port-pruner.mlir:1`, the marker `// UNSUPPORTED: system-windows` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1866 `test/Dialect/HW/Reduction/hw-constantifier.mlir:1` (test-expectation)

At `test/Dialect/HW/Reduction/hw-constantifier.mlir:1`, the marker `// UNSUPPORTED: system-windows` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1867 `test/Dialect/Moore/vtable-partial-impl.mlir:5` (test-gap-reference)

At `test/Dialect/Moore/vtable-partial-impl.mlir:5`, the marker `// *any* unimplemented inherited method (using allHaveImpl), which caused` indicates a gap signal in this path. What's missing: This test line references an unsupported behavior, indicating a known gap in upstream implementation coverage. How to fix: Implement the missing behavior in production code and update this test to assert functional semantics rather than unsupported status once support is complete.

### G1868 `test/Dialect/Moore/vtable-partial-impl.mlir:8` (test-gap-reference)

At `test/Dialect/Moore/vtable-partial-impl.mlir:8`, the marker `// some implemented methods still get vtables; unimplemented slots are` indicates a gap signal in this path. What's missing: This test line references an unsupported behavior, indicating a known gap in upstream implementation coverage. How to fix: Implement the missing behavior in production code and update this test to assert functional semantics rather than unsupported status once support is complete.

### G1869 `test/Dialect/SV/errors.mlir:253` (test-expectation)

At `test/Dialect/SV/errors.mlir:253`, the marker `// expected-error @+1 {{unsupported type}}` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1870 `test/Dialect/SV/errors.mlir:260` (test-expectation)

At `test/Dialect/SV/errors.mlir:260`, the marker `// expected-error @+1 {{unsupported type}}` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1871 `test/Dialect/FIRRTL/Reduction/extmodule-port-pruner.mlir:1` (test-expectation)

At `test/Dialect/FIRRTL/Reduction/extmodule-port-pruner.mlir:1`, the marker `// UNSUPPORTED: system-windows` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1872 `test/Dialect/FIRRTL/Reduction/list-create-element-remover.mlir:4` (test-expectation)

At `test/Dialect/FIRRTL/Reduction/list-create-element-remover.mlir:4`, the marker `// UNSUPPORTED: system-windows` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1873 `test/Dialect/FIRRTL/Reduction/layer-disable.mlir:1` (test-expectation)

At `test/Dialect/FIRRTL/Reduction/layer-disable.mlir:1`, the marker `// UNSUPPORTED: system-windows` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1874 `test/Dialect/FIRRTL/Reduction/extmodule-instance-remover.mlir:1` (test-expectation)

At `test/Dialect/FIRRTL/Reduction/extmodule-instance-remover.mlir:1`, the marker `// UNSUPPORTED: system-windows` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1875 `test/Dialect/FIRRTL/inferRW-errors.mlir:9` (test-expectation)

At `test/Dialect/FIRRTL/inferRW-errors.mlir:9`, the marker `// expected-error @below {{is unsupported by InferReadWrite}}` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1876 `test/Dialect/FIRRTL/Reduction/instance-stubber.mlir:1` (test-expectation)

At `test/Dialect/FIRRTL/Reduction/instance-stubber.mlir:1`, the marker `// UNSUPPORTED: system-windows` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1877 `test/Dialect/FIRRTL/Reduction/connect-forwarder.mlir:1` (test-expectation)

At `test/Dialect/FIRRTL/Reduction/connect-forwarder.mlir:1`, the marker `// UNSUPPORTED: system-windows` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1878 `test/Dialect/FIRRTL/parse-errors.fir:848` (unclassified)

At `test/Dialect/FIRRTL/parse-errors.fir:848`, the marker `circuit UnsupportedRadixSpecifiedIntegerLiterals:` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1879 `test/Dialect/FIRRTL/parse-errors.fir:849` (unclassified)

At `test/Dialect/FIRRTL/parse-errors.fir:849`, the marker `module UnsupportedRadixSpecifiedIntegerLiterals:` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1880 `test/Dialect/FIRRTL/parse-errors.fir:893` (unclassified)

At `test/Dialect/FIRRTL/parse-errors.fir:893`, the marker `circuit UnsupportedStringEncodedIntegerLiterals:` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1881 `test/Dialect/FIRRTL/parse-errors.fir:894` (unclassified)

At `test/Dialect/FIRRTL/parse-errors.fir:894`, the marker `module UnsupportedStringEncodedIntegerLiterals:` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1882 `test/Dialect/FIRRTL/parse-errors.fir:896` (test-expectation)

At `test/Dialect/FIRRTL/parse-errors.fir:896`, the marker `; expected-error @below {{String-encoded integer literals are unsupported after FIRRTL 3.0.0}}` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1883 `test/Dialect/FIRRTL/parse-errors.fir:929` (unclassified)

At `test/Dialect/FIRRTL/parse-errors.fir:929`, the marker `circuit UnsupportedVersionDeclGroups:` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1884 `test/Dialect/FIRRTL/parse-errors.fir:943` (unclassified)

At `test/Dialect/FIRRTL/parse-errors.fir:943`, the marker `circuit UnsupportedVersionLayer:` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1885 `test/Dialect/FIRRTL/parse-errors.fir:950` (unclassified)

At `test/Dialect/FIRRTL/parse-errors.fir:950`, the marker `circuit UnsupportedVersionGroups:` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1886 `test/Dialect/FIRRTL/parse-errors.fir:951` (unclassified)

At `test/Dialect/FIRRTL/parse-errors.fir:951`, the marker `module UnsupportedVersionGroups:` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1887 `test/Dialect/FIRRTL/parse-errors.fir:966` (unclassified)

At `test/Dialect/FIRRTL/parse-errors.fir:966`, the marker `circuit UnsupportedLayerBlock:` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1888 `test/Dialect/FIRRTL/parse-errors.fir:967` (unclassified)

At `test/Dialect/FIRRTL/parse-errors.fir:967`, the marker `module UnsupportedLayerBlock:` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1889 `test/Dialect/FIRRTL/parse-errors.fir:974` (unclassified)

At `test/Dialect/FIRRTL/parse-errors.fir:974`, the marker `circuit UnsupportedLayerConvention:` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1890 `test/Dialect/FIRRTL/parse-errors.fir:1396` (test-expectation)

At `test/Dialect/FIRRTL/parse-errors.fir:1396`, the marker `circuit PublicModuleUnsupported:` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1891 `test/Dialect/FIRRTL/parse-errors.fir:1398` (test-expectation)

At `test/Dialect/FIRRTL/parse-errors.fir:1398`, the marker `public module PublicModuleUnsupported:` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1892 `test/Dialect/FIRRTL/lower-domains.mlir:327` (todo)

At `test/Dialect/FIRRTL/lower-domains.mlir:327`, the marker `// TODO: If there was stronger verification that ExpandWhens ensures` indicates a gap signal in this path. What's missing: The marker documents unfinished work: If there was stronger verification that ExpandWhens ensures. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1893 `test/Dialect/FIRRTL/Reduction/simplify-resets.mlir:3` (test-expectation)

At `test/Dialect/FIRRTL/Reduction/simplify-resets.mlir:3`, the marker `// UNSUPPORTED: system-windows` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1894 `test/Dialect/Moore/canonicalizers.mlir:94` (unclassified)

At `test/Dialect/Moore/canonicalizers.mlir:94`, the marker `// CHECK-NEXT: moore.constant bXXXXX101 : l8` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1895 `test/Dialect/Moore/canonicalizers.mlir:146` (unclassified)

At `test/Dialect/Moore/canonicalizers.mlir:146`, the marker `// CHECK-DAG: [[V2:%.+]] = moore.constant hXXXXXX : l24` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1896 `test/Dialect/Moore/canonicalizers.mlir:164` (unclassified)

At `test/Dialect/Moore/canonicalizers.mlir:164`, the marker `// CHECK-DAG: [[X:%.+]] = moore.constant hXXXXXX : l24` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1897 `test/Dialect/Moore/canonicalizers.mlir:191` (unclassified)

At `test/Dialect/Moore/canonicalizers.mlir:191`, the marker `// CHECK-DAG: [[X:%.+]] = moore.constant hXXXXXX : l24` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1898 `test/Dialect/FIRRTL/Reduction/port-pruner.mlir:1` (test-expectation)

At `test/Dialect/FIRRTL/Reduction/port-pruner.mlir:1`, the marker `// UNSUPPORTED: system-windows` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1899 `lib/Dialect/Calyx/CalyxOps.cpp:100` (todo)

At `lib/Dialect/Calyx/CalyxOps.cpp:100`, the marker `/// TODO(Calyx): This is useful to verify current MLIR can be lowered to the` indicates a gap signal in this path. What's missing: The marker documents unfinished work: (Calyx): This is useful to verify current MLIR can be lowered to the. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1900 `lib/Dialect/Calyx/CalyxOps.cpp:1544` (unclassified)

At `lib/Dialect/Calyx/CalyxOps.cpp:1544`, the marker `// Since the guard is optional, we need to check if there is an accompanying` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1901 `test/Dialect/FIRRTL/Reduction/pattern-registration.mlir:1` (test-expectation)

At `test/Dialect/FIRRTL/Reduction/pattern-registration.mlir:1`, the marker `// UNSUPPORTED: system-windows` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1902 `test/Dialect/FIRRTL/Reduction/object-inliner.mlir:1` (test-expectation)

At `test/Dialect/FIRRTL/Reduction/object-inliner.mlir:1`, the marker `// UNSUPPORTED: system-windows` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1903 `test/Dialect/FIRRTL/Reduction/node-symbol-remover.mlir:1` (test-expectation)

At `test/Dialect/FIRRTL/Reduction/node-symbol-remover.mlir:1`, the marker `// UNSUPPORTED: system-windows` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1904 `test/Dialect/OM/Reduction/unused-class-remover.mlir:3` (test-expectation)

At `test/Dialect/OM/Reduction/unused-class-remover.mlir:3`, the marker `// UNSUPPORTED: system-windows` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1905 `test/Dialect/FIRRTL/Reduction/module-swapper.mlir:1` (test-expectation)

At `test/Dialect/FIRRTL/Reduction/module-swapper.mlir:1`, the marker `// UNSUPPORTED: system-windows` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1906 `test/Dialect/FIRRTL/lower-chirrtl.mlir:53` (todo)

At `test/Dialect/FIRRTL/lower-chirrtl.mlir:53`, the marker `// TODO: How do you get FileCheck to accept "[[[DATA]]]"?` indicates a gap signal in this path. What's missing: The marker documents unfinished work: How do you get FileCheck to accept "[[[DATA]]]"?. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1907 `test/Dialect/OM/Reduction/object-to-unknown-replacer.mlir:3` (test-expectation)

At `test/Dialect/OM/Reduction/object-to-unknown-replacer.mlir:3`, the marker `// UNSUPPORTED: system-windows` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1908 `test/Dialect/FIRRTL/Reduction/module-externalizer.mlir:1` (test-expectation)

At `test/Dialect/FIRRTL/Reduction/module-externalizer.mlir:1`, the marker `// UNSUPPORTED: system-windows` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1909 `test/Dialect/FIRRTL/parse-basic.fir:1570` (todo)

At `test/Dialect/FIRRTL/parse-basic.fir:1570`, the marker `; TODO: This test needs to be fixed up once flow-checking for objects is implemented correctly.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: This test needs to be fixed up once flow-checking for objects is implemented correctly.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1910 `test/Dialect/FIRRTL/canonicalization.mlir:2606` (todo)

At `test/Dialect/FIRRTL/canonicalization.mlir:2606`, the marker `// FIXME(Issue #1125): Add a test for zero width memory elimination.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: (Issue #1125): Add a test for zero width memory elimination.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1911 `test/Dialect/FIRRTL/canonicalization.mlir:2983` (todo)

At `test/Dialect/FIRRTL/canonicalization.mlir:2983`, the marker `// TODO: Move to an appropriate place` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Move to an appropriate place. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1912 `test/Dialect/FIRRTL/canonicalization.mlir:3036` (todo)

At `test/Dialect/FIRRTL/canonicalization.mlir:3036`, the marker `//TODO: Move to an appropriate place` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Move to an appropriate place. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1913 `test/Dialect/OM/Reduction/list-element-pruner.mlir:6` (test-expectation)

At `test/Dialect/OM/Reduction/list-element-pruner.mlir:6`, the marker `// UNSUPPORTED: system-windows` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1914 `test/Dialect/FIRRTL/Reduction/memory-stubber.mlir:1` (test-expectation)

At `test/Dialect/FIRRTL/Reduction/memory-stubber.mlir:1`, the marker `// UNSUPPORTED: system-windows` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1915 `test/Dialect/OM/Reduction/class-parameter-pruner.mlir:3` (test-expectation)

At `test/Dialect/OM/Reduction/class-parameter-pruner.mlir:3`, the marker `// UNSUPPORTED: system-windows` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1916 `test/Dialect/FIRRTL/Reduction/issue-3555.mlir:1` (test-expectation)

At `test/Dialect/FIRRTL/Reduction/issue-3555.mlir:1`, the marker `// UNSUPPORTED: system-windows` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1917 `test/Dialect/FIRRTL/infer-resets.mlir:140` (todo)

At `test/Dialect/FIRRTL/infer-resets.mlir:140`, the marker `// TODO: Enable the following once #1303 is fixed.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Enable the following once #1303 is fixed.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1918 `test/Dialect/FIRRTL/infer-resets.mlir:605` (todo)

At `test/Dialect/FIRRTL/infer-resets.mlir:605`, the marker `// TODO: Check that no extraReset port present` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Check that no extraReset port present. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1919 `test/Dialect/SV/EliminateInOutPorts/hw-eliminate-inout-ports-errors.mlir:3` (test-gap-reference)

At `test/Dialect/SV/EliminateInOutPorts/hw-eliminate-inout-ports-errors.mlir:3`, the marker `hw.module @unsupported(inout %a: i42) {` indicates a gap signal in this path. What's missing: This test line references an unsupported behavior, indicating a known gap in upstream implementation coverage. How to fix: Implement the missing behavior in production code and update this test to assert functional semantics rather than unsupported status once support is complete.

### G1920 `test/Dialect/SV/EliminateInOutPorts/hw-eliminate-inout-ports-errors.mlir:4` (test-expectation)

At `test/Dialect/SV/EliminateInOutPorts/hw-eliminate-inout-ports-errors.mlir:4`, the marker `// expected-error @+1 {{uses hw.inout port "a" but the operation itself is unsupported.}}` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1921 `test/Dialect/SV/EliminateInOutPorts/hw-eliminate-inout-ports-errors.mlir:10` (test-expectation)

At `test/Dialect/SV/EliminateInOutPorts/hw-eliminate-inout-ports-errors.mlir:10`, the marker `// expected-error @+1 {{multiple writers of inout port "a" is unsupported.}}` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1922 `test/Dialect/OM/Reduction/class-field-pruner.mlir:3` (test-expectation)

At `test/Dialect/OM/Reduction/class-field-pruner.mlir:3`, the marker `// UNSUPPORTED: system-windows` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1923 `test/Dialect/FIRRTL/Reduction/force-dedup.mlir:1` (test-expectation)

At `test/Dialect/FIRRTL/Reduction/force-dedup.mlir:1`, the marker `// UNSUPPORTED: system-windows` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1924 `test/Dialect/OM/Reduction/anycast-of-unknown-simplifier.mlir:3` (test-expectation)

At `test/Dialect/OM/Reduction/anycast-of-unknown-simplifier.mlir:3`, the marker `// UNSUPPORTED: system-windows` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1925 `lib/Dialect/Arc/ArcDialect.cpp:29` (todo)

At `lib/Dialect/Arc/ArcDialect.cpp:29`, the marker `// TODO` indicates a gap signal in this path. What's missing: The marker documents deferred or unfinished work on this path, which means behavior here is not fully implemented yet. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1926 `lib/Dialect/Arc/ArcDialect.cpp:34` (todo)

At `lib/Dialect/Arc/ArcDialect.cpp:34`, the marker `// TODO` indicates a gap signal in this path. What's missing: The marker documents deferred or unfinished work on this path, which means behavior here is not fully implemented yet. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1927 `test/Dialect/FIRRTL/Reduction/eager-inliner.mlir:1` (test-expectation)

At `test/Dialect/FIRRTL/Reduction/eager-inliner.mlir:1`, the marker `// UNSUPPORTED: system-windows` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1928 `test/Dialect/FIRRTL/inliner-errors.mlir:58` (test-expectation)

At `test/Dialect/FIRRTL/inliner-errors.mlir:58`, the marker `// expected-error @below {{unsupported operation 'sv.ifdef' cannot be inlined}}` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1929 `lib/Dialect/Arc/ArcFolds.cpp:241` (todo)

At `lib/Dialect/Arc/ArcFolds.cpp:241`, the marker `// TODO: we could also push out all operations that are not clocked/don't` indicates a gap signal in this path. What's missing: The marker documents unfinished work: we could also push out all operations that are not clocked/don't. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1930 `test/Dialect/FIRRTL/Reduction/constantifier.mlir:1` (test-expectation)

At `test/Dialect/FIRRTL/Reduction/constantifier.mlir:1`, the marker `// UNSUPPORTED: system-windows` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1931 `test/Dialect/FIRRTL/Reduction/connect-source-operand-forward.mlir:1` (test-expectation)

At `test/Dialect/FIRRTL/Reduction/connect-source-operand-forward.mlir:1`, the marker `// UNSUPPORTED: system-windows` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1932 `lib/Dialect/Arc/Interfaces/RuntimeCostEstimateInterfaceImpl.cpp:58` (todo)

At `lib/Dialect/Arc/Interfaces/RuntimeCostEstimateInterfaceImpl.cpp:58`, the marker `// TODO: improve this measure as it might lower to a sext or a mul` indicates a gap signal in this path. What's missing: The marker documents unfinished work: improve this measure as it might lower to a sext or a mul. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1933 `lib/Dialect/Arc/Interfaces/RuntimeCostEstimateInterfaceImpl.cpp:90` (todo)

At `lib/Dialect/Arc/Interfaces/RuntimeCostEstimateInterfaceImpl.cpp:90`, the marker `// TODO: ArraySliceOp, ArrayConcatOp` indicates a gap signal in this path. What's missing: The marker documents unfinished work: ArraySliceOp, ArrayConcatOp. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1934 `lib/Dialect/Arc/Interfaces/RuntimeCostEstimateInterfaceImpl.cpp:104` (todo)

At `lib/Dialect/Arc/Interfaces/RuntimeCostEstimateInterfaceImpl.cpp:104`, the marker `// TODO: this is chosen quite arbitrarily right now` indicates a gap signal in this path. What's missing: The marker documents unfinished work: this is chosen quite arbitrarily right now. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1935 `lib/Dialect/Arc/ArcCostModel.cpp:19` (todo)

At `lib/Dialect/Arc/ArcCostModel.cpp:19`, the marker `// FIXME: May be refined and we have more accurate operation costs` indicates a gap signal in this path. What's missing: The marker documents unfinished work: May be refined and we have more accurate operation costs. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1936 `test/Dialect/FIRRTL/Reduction/annotation-remover.mlir:4` (test-expectation)

At `test/Dialect/FIRRTL/Reduction/annotation-remover.mlir:4`, the marker `// UNSUPPORTED: system-windows` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1937 `lib/Dialect/Arc/Transforms/InferMemories.cpp:95` (implementation-gap)

At `lib/Dialect/Arc/Transforms/InferMemories.cpp:95`, the marker `instOp.emitError("unsupported memory write latency ") << writeLatency;` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1938 `test/Conversion/VerifToSMT/bmc-for-smtlib-no-property-live-llvm-call.mlir:4` (test-gap-reference)

At `test/Conversion/VerifToSMT/bmc-for-smtlib-no-property-live-llvm-call.mlir:4`, the marker `// carries unsupported LLVM ops (e.g. runtime helper calls), this should not` indicates a gap signal in this path. What's missing: This test line references an unsupported behavior, indicating a known gap in upstream implementation coverage. How to fix: Implement the missing behavior in production code and update this test to assert functional semantics rather than unsupported status once support is complete.

### G1939 `test/Conversion/VerifToSMT/bmc-concat-unknown-bounds.mlir:3` (test-expectation)

At `test/Conversion/VerifToSMT/bmc-concat-unknown-bounds.mlir:3`, the marker `// CHECK: unsupported sequence lowering for block argument` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1940 `lib/Dialect/Arc/Transforms/Dedup.cpp:204` (todo)

At `lib/Dialect/Arc/Transforms/Dedup.cpp:204`, the marker `// TODO: We should probably bail out if there are any operations in the` indicates a gap signal in this path. What's missing: The marker documents unfinished work: We should probably bail out if there are any operations in the. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1941 `test/Dialect/FIRRTL/lower-types.mlir:579` (todo)

At `test/Dialect/FIRRTL/lower-types.mlir:579`, the marker `// TODO: Enable this` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Enable this. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1942 `test/Dialect/FIRRTL/lower-types.mlir:602` (todo)

At `test/Dialect/FIRRTL/lower-types.mlir:602`, the marker `// TODO: Enable this` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Enable this. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1943 `test/Dialect/FIRRTL/lower-types.mlir:621` (todo)

At `test/Dialect/FIRRTL/lower-types.mlir:621`, the marker `// TODO: Enable this` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Enable this. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1944 `lib/Dialect/Arc/Transforms/LowerState.cpp:582` (unclassified)

At `lib/Dialect/Arc/Transforms/LowerState.cpp:582`, the marker `module.builder.setInsertionPoint(ifClockOp.thenYield());` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1945 `lib/Dialect/Arc/Transforms/LowerState.cpp:609` (unclassified)

At `lib/Dialect/Arc/Transforms/LowerState.cpp:609`, the marker `module.builder.setInsertionPoint(ifResetOp.thenYield());` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1946 `lib/Dialect/Arc/Transforms/LowerState.cpp:641` (unclassified)

At `lib/Dialect/Arc/Transforms/LowerState.cpp:641`, the marker `module.builder.setInsertionPoint(ifEnableOp.thenYield());` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1947 `lib/Dialect/Arc/Transforms/LowerState.cpp:731` (unclassified)

At `lib/Dialect/Arc/Transforms/LowerState.cpp:731`, the marker `module.builder.setInsertionPoint(ifClockOp.thenYield());` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1948 `lib/Dialect/Arc/Transforms/LowerState.cpp:749` (unclassified)

At `lib/Dialect/Arc/Transforms/LowerState.cpp:749`, the marker `module.builder.setInsertionPoint(ifEnableOp.thenYield());` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1949 `lib/Dialect/Arc/Transforms/AllocateState.cpp:97` (todo)

At `lib/Dialect/Arc/Transforms/AllocateState.cpp:97`, the marker `// TODO: Can a StateWriteOp be shared across models?` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Can a StateWriteOp be shared across models?. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1950 `lib/Dialect/Arc/Transforms/AllocateState.cpp:174` (implementation-gap)

At `lib/Dialect/Arc/Transforms/AllocateState.cpp:174`, the marker `assert("unsupported op for allocation" && false);` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1951 `test/Conversion/SeqToSV/error.mlir:3` (todo)

At `test/Conversion/SeqToSV/error.mlir:3`, the marker `// TODO: Improve the error message` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Improve the error message. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1952 `lib/Dialect/Arc/Transforms/ArcCanonicalizer.cpp:130` (todo)

At `lib/Dialect/Arc/Transforms/ArcCanonicalizer.cpp:130`, the marker `// TODO: if an operation is inserted that defines a symbol and the symbol` indicates a gap signal in this path. What's missing: The marker documents unfinished work: if an operation is inserted that defines a symbol and the symbol. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1953 `lib/Dialect/Arc/Transforms/ArcCanonicalizer.cpp:143` (todo)

At `lib/Dialect/Arc/Transforms/ArcCanonicalizer.cpp:143`, the marker `// TODO: if an operation is inserted that defines a symbol and the symbol` indicates a gap signal in this path. What's missing: The marker documents unfinished work: if an operation is inserted that defines a symbol and the symbol. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1954 `test/Dialect/FIRRTL/errors.mlir:325` (test-expectation)

At `test/Dialect/FIRRTL/errors.mlir:325`, the marker `// expected-error @+1 {{'firrtl.instance' op name for port 1 must be "arg1", but got "xxx"}}` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1955 `test/Dialect/FIRRTL/errors.mlir:326` (todo)

At `test/Dialect/FIRRTL/errors.mlir:326`, the marker `%a:2 = firrtl.instance "" @Callee(in arg0: !firrtl.uint<1>, in xxx: !firrtl.bundle<valid: uint<1>>)` indicates a gap signal in this path. What's missing: The marker documents unfinished work: !firrtl.bundle<valid: uint<1>>). The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1956 `test/Dialect/FIRRTL/errors.mlir:2169` (test-expectation)

At `test/Dialect/FIRRTL/errors.mlir:2169`, the marker `// expected-error @below {{'firrtl.module' op port #0 has wrong name, got "xxx", expected "str"}}` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1957 `test/Dialect/FIRRTL/errors.mlir:2170` (todo)

At `test/Dialect/FIRRTL/errors.mlir:2170`, the marker `firrtl.module @ClassTypeWrongPortName(out %port: !firrtl.class<@MyClass(out xxx: !firrtl.string)>) {}` indicates a gap signal in this path. What's missing: The marker documents unfinished work: !firrtl.string)>) {}. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1958 `test/Dialect/FIRRTL/errors.mlir:2978` (test-expectation)

At `test/Dialect/FIRRTL/errors.mlir:2978`, the marker `// expected-error @below {{target #hw.innerNameRef<@XXX::@YYY> cannot be resolved}}` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1959 `test/Dialect/FIRRTL/errors.mlir:2979` (todo)

At `test/Dialect/FIRRTL/errors.mlir:2979`, the marker `firrtl.bind <@XXX::@YYY>` indicates a gap signal in this path. What's missing: The marker documents unfinished work: :@YYY>. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1960 `lib/Dialect/Arc/Transforms/SplitLoops.cpp:240` (todo)

At `lib/Dialect/Arc/Transforms/SplitLoops.cpp:240`, the marker `// TODO: This is ugly and we should only split arcs that are truly involved in` indicates a gap signal in this path. What's missing: The marker documents unfinished work: This is ugly and we should only split arcs that are truly involved in. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1961 `lib/Dialect/Arc/Transforms/LowerLUT.cpp:318` (todo)

At `lib/Dialect/Arc/Transforms/LowerLUT.cpp:318`, the marker `// TODO: This class could be factored out into an analysis if there is a need` indicates a gap signal in this path. What's missing: The marker documents unfinished work: This class could be factored out into an analysis if there is a need. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1962 `test/Dialect/FIRRTL/grand-central-view-errors.mlir:146` (test-gap-reference)

At `test/Dialect/FIRRTL/grand-central-view-errors.mlir:146`, the marker `// Invalid / unsupported "class" in element.` indicates a gap signal in this path. What's missing: This test line references an unsupported behavior, indicating a known gap in upstream implementation coverage. How to fix: Implement the missing behavior in production code and update this test to assert functional semantics rather than unsupported status once support is complete.

### G1963 `lib/Dialect/Arc/Transforms/LowerClocksToFuncs.cpp:87` (implementation-gap)

At `lib/Dialect/Arc/Transforms/LowerClocksToFuncs.cpp:87`, the marker `<< "containing multiple InitialOps is currently unsupported.";` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1964 `lib/Dialect/Arc/Transforms/LowerClocksToFuncs.cpp:93` (implementation-gap)

At `lib/Dialect/Arc/Transforms/LowerClocksToFuncs.cpp:93`, the marker `<< "containing multiple FinalOps is currently unsupported.";` indicates a gap signal in this path. What's missing: The implementation explicitly rejects, drops, or cannot lower this construct yet, so users still hit an unsupported path. How to fix: Add full handling for this construct in the owning conversion/dialect code path, preserve correct diagnostics for invalid cases, and add positive plus negative tests to lock in behavior.

### G1965 `lib/Dialect/Arc/Transforms/MuxToControlFlow.cpp:249` (todo)

At `lib/Dialect/Arc/Transforms/MuxToControlFlow.cpp:249`, the marker `// TODO: we can also check for arith.select or other operations here` indicates a gap signal in this path. What's missing: The marker documents unfinished work: we can also check for arith.select or other operations here. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1966 `lib/Dialect/Arc/Transforms/MuxToControlFlow.cpp:256` (todo)

At `lib/Dialect/Arc/Transforms/MuxToControlFlow.cpp:256`, the marker `/// TODO: improve and fine-tune this` indicates a gap signal in this path. What's missing: The marker documents unfinished work: improve and fine-tune this. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1967 `lib/Dialect/Arc/Transforms/MuxToControlFlow.cpp:289` (todo)

At `lib/Dialect/Arc/Transforms/MuxToControlFlow.cpp:289`, the marker `// FIXME: Assumes that the regions in which muxes exist are topologically` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Assumes that the regions in which muxes exist are topologically. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1968 `lib/Dialect/Arc/Transforms/MuxToControlFlow.cpp:291` (todo)

At `lib/Dialect/Arc/Transforms/MuxToControlFlow.cpp:291`, the marker `// FIXME: does not consider side-effects` indicates a gap signal in this path. What's missing: The marker documents unfinished work: does not consider side-effects. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1969 `lib/Dialect/Arc/Transforms/MergeIfs.cpp:340` (unclassified)

At `lib/Dialect/Arc/Transforms/MergeIfs.cpp:340`, the marker `prevIfOp.thenYield().erase();` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1970 `lib/Dialect/Arc/Transforms/IsolateClocks.cpp:44` (unclassified)

At `lib/Dialect/Arc/Transforms/IsolateClocks.cpp:44`, the marker `bool moveToDomain(Operation *op);` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1971 `lib/Dialect/Arc/Transforms/IsolateClocks.cpp:64` (unclassified)

At `lib/Dialect/Arc/Transforms/IsolateClocks.cpp:64`, the marker `bool ClockDomain::moveToDomain(Operation *op) {` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1972 `lib/Dialect/Arc/Transforms/IsolateClocks.cpp:85` (todo)

At `lib/Dialect/Arc/Transforms/IsolateClocks.cpp:85`, the marker `// TODO: if we find a clock domain with the same clock we should merge it.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: if we find a clock domain with the same clock we should merge it.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1973 `lib/Dialect/Arc/Transforms/IsolateClocks.cpp:104` (unclassified)

At `lib/Dialect/Arc/Transforms/IsolateClocks.cpp:104`, the marker `if (moveToDomain(op))` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1974 `lib/Dialect/Arc/Transforms/IsolateClocks.cpp:210` (unclassified)

At `lib/Dialect/Arc/Transforms/IsolateClocks.cpp:210`, the marker `if (domain.moveToDomain(op)) {` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1975 `lib/Dialect/Arc/Transforms/InlineArcs.cpp:190` (todo)

At `lib/Dialect/Arc/Transforms/InlineArcs.cpp:190`, the marker `// TODO: make safe` indicates a gap signal in this path. What's missing: The marker documents unfinished work: make safe. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1976 `lib/Dialect/Arc/Transforms/InlineArcs.cpp:343` (todo)

At `lib/Dialect/Arc/Transforms/InlineArcs.cpp:343`, the marker `// TODO: instead of hardcoding these ops we might also be able to query the` indicates a gap signal in this path. What's missing: The marker documents unfinished work: instead of hardcoding these ops we might also be able to query the. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1977 `lib/Dialect/Arc/Transforms/InferStateProperties.cpp:113` (todo)

At `lib/Dialect/Arc/Transforms/InferStateProperties.cpp:113`, the marker `// TODO: split the arcs such that there is one for each reset kind, however,` indicates a gap signal in this path. What's missing: The marker documents unfinished work: split the arcs such that there is one for each reset kind, however,. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1978 `lib/Dialect/Arc/Transforms/InferStateProperties.cpp:120` (todo)

At `lib/Dialect/Arc/Transforms/InferStateProperties.cpp:120`, the marker `// TODO: arc.state operation only supports resets to zero at the moment.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: arc.state operation only supports resets to zero at the moment.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1979 `lib/Dialect/Arc/Transforms/InferStateProperties.cpp:176` (todo)

At `lib/Dialect/Arc/Transforms/InferStateProperties.cpp:176`, the marker `// TODO: split the arcs such that there is one for each enable kind,` indicates a gap signal in this path. What's missing: The marker documents unfinished work: split the arcs such that there is one for each enable kind,. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1980 `test/Conversion/FIRRTLToHW/errors.mlir:46` (test-gap-reference)

At `test/Conversion/FIRRTLToHW/errors.mlir:46`, the marker `// COM: Unknown widths are unsupported` indicates a gap signal in this path. What's missing: This test line references an unsupported behavior, indicating a known gap in upstream implementation coverage. How to fix: Implement the missing behavior in production code and update this test to assert functional semantics rather than unsupported status once support is complete.

### G1981 `test/Conversion/ArcToLLVM/lower-arc-to-llvm.mlir:151` (todo)

At `test/Conversion/ArcToLLVM/lower-arc-to-llvm.mlir:151`, the marker `// FIXME: this does not really belong here, but there is no better place either.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: this does not really belong here, but there is no better place either.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1982 `test/Conversion/ExportAIGER/errors.mlir:3` (test-gap-reference)

At `test/Conversion/ExportAIGER/errors.mlir:3`, the marker `// Test unsupported variadic AND gates (should be lowered first)` indicates a gap signal in this path. What's missing: This test line references an unsupported behavior, indicating a known gap in upstream implementation coverage. How to fix: Implement the missing behavior in production code and update this test to assert functional semantics rather than unsupported status once support is complete.

### G1983 `test/Conversion/ExportAIGER/errors.mlir:23` (test-gap-reference)

At `test/Conversion/ExportAIGER/errors.mlir:23`, the marker `// Test unsupported operation (when handleUnknownOperation is false)` indicates a gap signal in this path. What's missing: This test line references an unsupported behavior, indicating a known gap in upstream implementation coverage. How to fix: Implement the missing behavior in production code and update this test to assert functional semantics rather than unsupported status once support is complete.

### G1984 `test/Conversion/ExportVerilog/pretty.mlir:243` (unclassified)

At `test/Conversion/ExportVerilog/pretty.mlir:243`, the marker `hw.module @ForStatement(in %aaaaaaaaaaa: i5, in %xxxxxxxxxxxxxxx : i2, in %yyyyyyyyyyyyyyy : i2, in %zzzzzzzzzzzzzzz : i2) {` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1985 `test/Conversion/ExportVerilog/pretty.mlir:246` (unclassified)

At `test/Conversion/ExportVerilog/pretty.mlir:246`, the marker `%x_and_y = comb.and %xxxxxxxxxxxxxxx, %yyyyyyyyyyyyyyy : i2` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1986 `test/Conversion/ExportVerilog/pretty.mlir:247` (unclassified)

At `test/Conversion/ExportVerilog/pretty.mlir:247`, the marker `%x_or_y = comb.or %xxxxxxxxxxxxxxx, %yyyyyyyyyyyyyyy : i2` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1987 `test/Conversion/ExportVerilog/pretty.mlir:250` (unclassified)

At `test/Conversion/ExportVerilog/pretty.mlir:250`, the marker `%eq = comb.icmp eq %xxxxxxxxxxxxxxx, %yyyyyyyyyyyyyyy : i2` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1988 `test/Conversion/ExportVerilog/pretty.mlir:255` (unclassified)

At `test/Conversion/ExportVerilog/pretty.mlir:255`, the marker `// CHECK-NEXT: xxxxxxxxxxxxxxx == yyyyyyyyyyyyyyy ? _GEN : _GEN_0) begin` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G1989 `test/Conversion/ExportVerilog/name-legalize.mlir:84` (todo)

At `test/Conversion/ExportVerilog/name-legalize.mlir:84`, the marker `hw.module @useParametersNameConflict(in %xxx: i8) {` indicates a gap signal in this path. What's missing: The marker documents unfinished work: i8) {. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1990 `test/Conversion/ExportVerilog/name-legalize.mlir:89` (test-expectation)

At `test/Conversion/ExportVerilog/name-legalize.mlir:89`, the marker `// CHECK: .p1 (xxx)` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1991 `test/Conversion/ExportVerilog/name-legalize.mlir:91` (todo)

At `test/Conversion/ExportVerilog/name-legalize.mlir:91`, the marker `hw.instance "inst" @parametersNameConflict<p2: i42 = 27, wire: i1 = 0>(p1: %xxx: i8) -> ()` indicates a gap signal in this path. What's missing: The marker documents unfinished work: i8) -> (). The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1992 `test/Conversion/ExportVerilog/name-legalize.mlir:95` (test-expectation)

At `test/Conversion/ExportVerilog/name-legalize.mlir:95`, the marker `// CHECK: reg [3:0] xxx_0;` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1993 `test/Conversion/ExportVerilog/name-legalize.mlir:96` (todo)

At `test/Conversion/ExportVerilog/name-legalize.mlir:96`, the marker `%0 = sv.reg name "xxx" : !hw.inout<i4>` indicates a gap signal in this path. What's missing: The marker documents unfinished work: " : !hw.inout<i4>. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1994 `test/Conversion/ExportVerilog/hw-dialect.mlir:274` (todo)

At `test/Conversion/ExportVerilog/hw-dialect.mlir:274`, the marker `/// TODO: Specify parameter declarations.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Specify parameter declarations.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1995 `test/Conversion/ExportVerilog/hw-dialect.mlir:365` (todo)

At `test/Conversion/ExportVerilog/hw-dialect.mlir:365`, the marker `// FIXME: The MLIR parser doesn't accept an i0 even though it is valid IR,` indicates a gap signal in this path. What's missing: The marker documents unfinished work: The MLIR parser doesn't accept an i0 even though it is valid IR,. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1996 `test/Conversion/ExportVerilog/hw-dialect.mlir:1257` (todo)

At `test/Conversion/ExportVerilog/hw-dialect.mlir:1257`, the marker `// FIXME: Decl word should be localparam.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: Decl word should be localparam.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1997 `test/Conversion/ExportVerilog/verilog-errors.mlir:3` (test-expectation)

At `test/Conversion/ExportVerilog/verilog-errors.mlir:3`, the marker `// expected-error @+1 {{value has an unsupported verilog type 'f32'}}` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G1998 `test/Conversion/ExportVerilog/sv-dialect.mlir:1959` (todo)

At `test/Conversion/ExportVerilog/sv-dialect.mlir:1959`, the marker `sv.macro.error "my message xxx yyy"` indicates a gap signal in this path. What's missing: The marker documents unfinished work: yyy". The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G1999 `test/Conversion/ExportVerilog/sv-dialect.mlir:1961` (test-expectation)

At `test/Conversion/ExportVerilog/sv-dialect.mlir:1961`, the marker `// CHECK: `_ERROR_my_message_xxx_yyy` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G2000 `test/Conversion/MooreToCore/errors.mlir:7` (unclassified)

At `test/Conversion/MooreToCore/errors.mlir:7`, the marker `func.func @unsupportedConversion() -> !moore.string {` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G2001 `test/Conversion/MooreToCore/interface-timing-after-inlining.sv:8` (todo)

At `test/Conversion/MooreToCore/interface-timing-after-inlining.sv:8`, the marker `// FIXME: moore.wait_event/detect_event inside func.func are not yet lowered.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: moore.wait_event/detect_event inside func.func are not yet lowered.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G2002 `test/Conversion/MooreToCore/basic.mlir:173` (unclassified)

At `test/Conversion/MooreToCore/basic.mlir:173`, the marker `%0 = moore.constant bXXXXXX : l6` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G2003 `test/Conversion/MooreToCore/basic.mlir:628` (todo)

At `test/Conversion/MooreToCore/basic.mlir:628`, the marker `// TODO: moore.procedure always_comb` indicates a gap signal in this path. What's missing: The marker documents unfinished work: moore.procedure always_comb. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G2004 `test/Conversion/MooreToCore/basic.mlir:629` (todo)

At `test/Conversion/MooreToCore/basic.mlir:629`, the marker `// TODO: moore.procedure always_latch` indicates a gap signal in this path. What's missing: The marker documents unfinished work: moore.procedure always_latch. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G2005 `test/Conversion/FSMToSV/test_errors.mlir:4` (test-expectation)

At `test/Conversion/FSMToSV/test_errors.mlir:4`, the marker `// expected-error@+1 {{'arith.constant' op is unsupported (op from the arith dialect).}}` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G2006 `test/Conversion/FSMToCore/errors.mlir:4` (test-expectation)

At `test/Conversion/FSMToCore/errors.mlir:4`, the marker `// expected-error @below {{'arith.constant' op is unsupported (op from the arith dialect).}}` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G2007 `test/Conversion/ImportAIGER/basic-binary.mlir:3` (todo)

At `test/Conversion/ImportAIGER/basic-binary.mlir:3`, the marker `// TODO: After the AIGER exporter is upstreamed, generate AIG file from this MLIR.` indicates a gap signal in this path. What's missing: The marker documents unfinished work: After the AIGER exporter is upstreamed, generate AIG file from this MLIR.. The current implementation is partial along this code path. How to fix: Complete the deferred logic end-to-end in this subsystem, including type/semantic checks where needed, and add or update regression tests that fail before the change and pass after it.

### G2008 `test/Conversion/ImportVerilog/global-variable-init.sv:6` (test-expectation)

At `test/Conversion/ImportVerilog/global-variable-init.sv:6`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G2009 `test/Conversion/ImportVerilog/procedures.sv:6` (test-expectation)

At `test/Conversion/ImportVerilog/procedures.sv:6`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G2010 `test/Conversion/ImportVerilog/assoc_arrays.sv:6` (test-expectation)

At `test/Conversion/ImportVerilog/assoc_arrays.sv:6`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G2011 `test/Conversion/ImportVerilog/assoc_arrays.sv:32` (test-gap-reference)

At `test/Conversion/ImportVerilog/assoc_arrays.sv:32`, the marker `/// This tests the fix for "unsupported expression: element select into int$[*]"` indicates a gap signal in this path. What's missing: This test line references an unsupported behavior, indicating a known gap in upstream implementation coverage. How to fix: Implement the missing behavior in production code and update this test to assert functional semantics rather than unsupported status once support is complete.

### G2012 `test/Conversion/ImportVerilog/assoc_arrays.sv:345` (test-gap-reference)

At `test/Conversion/ImportVerilog/assoc_arrays.sv:345`, the marker `/// This tests the fix for "unsupported expression: element select into TypedefArray"` indicates a gap signal in this path. What's missing: This test line references an unsupported behavior, indicating a known gap in upstream implementation coverage. How to fix: Implement the missing behavior in production code and update this test to assert functional semantics rather than unsupported status once support is complete.

### G2013 `test/Conversion/ImportVerilog/constraint-solve.sv:5` (test-expectation)

At `test/Conversion/ImportVerilog/constraint-solve.sv:5`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G2014 `test/Conversion/ImportVerilog/constraint-method-call.sv:5` (test-expectation)

At `test/Conversion/ImportVerilog/constraint-method-call.sv:5`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G2015 `test/Conversion/ImportVerilog/constraint-implication.sv:5` (test-expectation)

At `test/Conversion/ImportVerilog/constraint-implication.sv:5`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G2016 `test/Conversion/ImportVerilog/queues.sv:6` (test-expectation)

At `test/Conversion/ImportVerilog/queues.sv:6`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G2017 `test/Conversion/ImportVerilog/types.sv:6` (test-expectation)

At `test/Conversion/ImportVerilog/types.sv:6`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G2018 `test/Conversion/ImportVerilog/basic.sv:6` (test-expectation)

At `test/Conversion/ImportVerilog/basic.sv:6`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G2019 `test/Conversion/ImportVerilog/basic.sv:773` (test-expectation)

At `test/Conversion/ImportVerilog/basic.sv:773`, the marker `// CHECK: moore.constant hXXXXXXXX : l32` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G2020 `test/Conversion/ImportVerilog/hierarchical-names.sv:6` (test-expectation)

At `test/Conversion/ImportVerilog/hierarchical-names.sv:6`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G2021 `test/Conversion/ImportVerilog/errors.sv:5` (test-expectation)

At `test/Conversion/ImportVerilog/errors.sv:5`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G2022 `test/Conversion/ImportVerilog/inherited-virtual-methods.sv:6` (test-expectation)

At `test/Conversion/ImportVerilog/inherited-virtual-methods.sv:6`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G2023 `test/Conversion/ImportVerilog/classes.sv:6` (test-expectation)

At `test/Conversion/ImportVerilog/classes.sv:6`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G2024 `test/Conversion/ImportVerilog/four-state-constants.sv:45` (unclassified)

At `test/Conversion/ImportVerilog/four-state-constants.sv:45`, the marker `assign all_x = 4'bxxxx;` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G2025 `test/Conversion/ImportVerilog/class-e2e.sv:5` (test-expectation)

At `test/Conversion/ImportVerilog/class-e2e.sv:5`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G2026 `test/Conversion/ImportVerilog/builtins.sv:6` (test-expectation)

At `test/Conversion/ImportVerilog/builtins.sv:6`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G2027 `test/Conversion/ImportVerilog/queue-max-min.sv:6` (test-expectation)

At `test/Conversion/ImportVerilog/queue-max-min.sv:6`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G2028 `test/Conversion/ImportVerilog/queue-delete-index.sv:6` (test-expectation)

At `test/Conversion/ImportVerilog/queue-delete-index.sv:6`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G2029 `test/Conversion/ImportVerilog/pre-post-randomize.sv:6` (test-expectation)

At `test/Conversion/ImportVerilog/pre-post-randomize.sv:6`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G2030 `test/Conversion/ImportVerilog/continuous-assign-delay-one-step-supported.sv:11` (test-expectation)

At `test/Conversion/ImportVerilog/continuous-assign-delay-one-step-supported.sv:11`, the marker `// DIAG-NOT: unsupported continuous assignment timing control: OneStepDelay` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G2031 `test/Conversion/ImportVerilog/static-property-fixes.sv:6` (test-expectation)

At `test/Conversion/ImportVerilog/static-property-fixes.sv:6`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G2032 `test/Conversion/ImportVerilog/delay-cycle-supported.sv:17` (test-expectation)

At `test/Conversion/ImportVerilog/delay-cycle-supported.sv:17`, the marker `// DIAG-NOT: unsupported delay control: CycleDelay` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G2033 `test/Conversion/ImportVerilog/delay-one-step-supported.sv:8` (test-expectation)

At `test/Conversion/ImportVerilog/delay-one-step-supported.sv:8`, the marker `// DIAG-NOT: unsupported delay control: OneStepDelay` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G2034 `test/Conversion/ImportVerilog/randomize.sv:6` (test-expectation)

At `test/Conversion/ImportVerilog/randomize.sv:6`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G2035 `test/Conversion/ImportVerilog/runtime-randomization.sv:6` (test-expectation)

At `test/Conversion/ImportVerilog/runtime-randomization.sv:6`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G2036 `test/Conversion/ImportVerilog/randomize-inline-control.sv:6` (test-expectation)

At `test/Conversion/ImportVerilog/randomize-inline-control.sv:6`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G2037 `test/Conversion/ImportVerilog/time-type-handling.sv:6` (test-expectation)

At `test/Conversion/ImportVerilog/time-type-handling.sv:6`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G2038 `test/Conversion/ImportVerilog/cross-select-intersect-open-range-wide-supported.sv:3` (unclassified)

At `test/Conversion/ImportVerilog/cross-select-intersect-open-range-wide-supported.sv:3`, the marker `module CrossSelectIntersectOpenRangeUnsupported;` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G2039 `test/Conversion/ImportVerilog/uvm_classes.sv:6` (test-expectation)

At `test/Conversion/ImportVerilog/uvm_classes.sv:6`, the marker `// UNSUPPORTED: valgrind` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G2040 `test/Conversion/ImportVerilog/system-calls-complete.sv:5` (test-gap-reference)

At `test/Conversion/ImportVerilog/system-calls-complete.sv:5`, the marker `// This test verifies that none of the system calls produce "unsupported system` indicates a gap signal in this path. What's missing: This test line references an unsupported behavior, indicating a known gap in upstream implementation coverage. How to fix: Implement the missing behavior in production code and update this test to assert functional semantics rather than unsupported status once support is complete.

### G2041 `test/Conversion/ImportVerilog/system-calls-complete.sv:8` (test-expectation)

At `test/Conversion/ImportVerilog/system-calls-complete.sv:8`, the marker `// CHECK-NOT: unsupported system call` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G2042 `test/Conversion/ImportVerilog/cross-select-intersect-plusminus-unsupported.sv:3` (unclassified)

At `test/Conversion/ImportVerilog/cross-select-intersect-plusminus-unsupported.sv:3`, the marker `module CrossSelectIntersectPlusMinusUnsupported;` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G2043 `test/Conversion/ImportVerilog/cross-select-intersect-plusminus-unsupported.sv:17` (test-expectation)

At `test/Conversion/ImportVerilog/cross-select-intersect-plusminus-unsupported.sv:17`, the marker `// CHECK: error: unsupported non-constant intersect value range in cross select expression` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G2044 `test/Conversion/ImportVerilog/sva-sequence-match-item-coverage-sdf-static-subroutine.sv:18` (test-expectation)

At `test/Conversion/ImportVerilog/sva-sequence-match-item-coverage-sdf-static-subroutine.sv:18`, the marker `// DIAG: warning: $sdf_annotate is not supported in circt-sim (SDF timing annotation not implemented)` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G2045 `test/Conversion/ImportVerilog/sva-continue-on-unsupported.sv:2` (test-expectation)

At `test/Conversion/ImportVerilog/sva-continue-on-unsupported.sv:2`, the marker `// RUN: circt-verilog --no-uvm-auto-include --sva-continue-on-unsupported --ir-moore %s 2>&1 | FileCheck %s --check-prefix=WARN` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G2046 `test/Conversion/ImportVerilog/sva-continue-on-unsupported.sv:3` (test-expectation)

At `test/Conversion/ImportVerilog/sva-continue-on-unsupported.sv:3`, the marker `// RUN: circt-verilog --no-uvm-auto-include --sva-continue-on-unsupported --ir-moore %s | FileCheck %s --check-prefix=IR` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G2047 `test/Conversion/ImportVerilog/sva-continue-on-unsupported.sv:6` (unclassified)

At `test/Conversion/ImportVerilog/sva-continue-on-unsupported.sv:6`, the marker `module SvaContinueOnUnsupported(input logic clk, a);` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G2048 `test/Conversion/ImportVerilog/sva-continue-on-unsupported.sv:10` (test-gap-reference)

At `test/Conversion/ImportVerilog/sva-continue-on-unsupported.sv:10`, the marker `// `event` + sampled-value controls in `$past` is currently unsupported.` indicates a gap signal in this path. What's missing: This test line references an unsupported behavior, indicating a known gap in upstream implementation coverage. How to fix: Implement the missing behavior in production code and update this test to assert functional semantics rather than unsupported status once support is complete.

### G2049 `test/Conversion/ImportVerilog/sva-continue-on-unsupported.sv:15` (test-gap-reference)

At `test/Conversion/ImportVerilog/sva-continue-on-unsupported.sv:15`, the marker `// ERR: error: unsupported $past value type with sampled-value controls` indicates a gap signal in this path. What's missing: This test line references an unsupported behavior, indicating a known gap in upstream implementation coverage. How to fix: Implement the missing behavior in production code and update this test to assert functional semantics rather than unsupported status once support is complete.

### G2050 `test/Conversion/ImportVerilog/sva-continue-on-unsupported.sv:17` (test-gap-reference)

At `test/Conversion/ImportVerilog/sva-continue-on-unsupported.sv:17`, the marker `// WARN: warning: unsupported $past value type with sampled-value controls` indicates a gap signal in this path. What's missing: This test line references an unsupported behavior, indicating a known gap in upstream implementation coverage. How to fix: Implement the missing behavior in production code and update this test to assert functional semantics rather than unsupported status once support is complete.

### G2051 `test/Conversion/ImportVerilog/sva-continue-on-unsupported.sv:18` (test-gap-reference)

At `test/Conversion/ImportVerilog/sva-continue-on-unsupported.sv:18`, the marker `// WARN: warning: skipping unsupported SVA assertion in continue mode: property lowering failed` indicates a gap signal in this path. What's missing: This test line references an unsupported behavior, indicating a known gap in upstream implementation coverage. How to fix: Implement the missing behavior in production code and update this test to assert functional semantics rather than unsupported status once support is complete.

### G2052 `test/Conversion/ImportVerilog/sva-continue-on-unsupported.sv:21` (unclassified)

At `test/Conversion/ImportVerilog/sva-continue-on-unsupported.sv:21`, the marker `// IR-SAME: {circt.unsupported_sva` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G2053 `test/Conversion/ImportVerilog/sva-continue-on-unsupported.sv:22` (unclassified)

At `test/Conversion/ImportVerilog/sva-continue-on-unsupported.sv:22`, the marker `// IR-SAME: circt.unsupported_sva_reason = "property lowering failed"` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G2054 `test/Conversion/ImportVerilog/sva-sequence-match-item-debug-checkpoint-subroutine.sv:22` (test-expectation)

At `test/Conversion/ImportVerilog/sva-sequence-match-item-debug-checkpoint-subroutine.sv:22`, the marker `// DIAG: warning: $save is not supported in circt-sim (checkpoint/restart not implemented)` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G2055 `test/Conversion/ImportVerilog/sva-sequence-match-item-debug-checkpoint-subroutine.sv:23` (test-expectation)

At `test/Conversion/ImportVerilog/sva-sequence-match-item-debug-checkpoint-subroutine.sv:23`, the marker `// DIAG: warning: $restart is not supported in circt-sim (checkpoint/restart not implemented)` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G2056 `test/Conversion/ImportVerilog/sva-sequence-match-item-debug-checkpoint-subroutine.sv:24` (test-expectation)

At `test/Conversion/ImportVerilog/sva-sequence-match-item-debug-checkpoint-subroutine.sv:24`, the marker `// DIAG: warning: $incsave is not supported in circt-sim (checkpoint/restart not implemented)` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G2057 `test/Conversion/ImportVerilog/sva-sequence-match-item-debug-checkpoint-subroutine.sv:25` (test-expectation)

At `test/Conversion/ImportVerilog/sva-sequence-match-item-debug-checkpoint-subroutine.sv:25`, the marker `// DIAG: warning: $reset is not supported in circt-sim (checkpoint/restart not implemented)` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G2058 `test/Conversion/ImportVerilog/sva-sequence-match-item-rewind-function.sv:20` (test-expectation)

At `test/Conversion/ImportVerilog/sva-sequence-match-item-rewind-function.sv:20`, the marker `// DIAG-NOT: unsupported system call `$rewind`` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G2059 `test/Conversion/ImportVerilog/sva-immediate-past-event-continue-on-unsupported.sv:2` (test-expectation)

At `test/Conversion/ImportVerilog/sva-immediate-past-event-continue-on-unsupported.sv:2`, the marker `// RUN: circt-verilog --no-uvm-auto-include --sva-continue-on-unsupported --ir-moore %s 2>&1 | FileCheck %s --check-prefix=WARN` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G2060 `test/Conversion/ImportVerilog/sva-immediate-past-event-continue-on-unsupported.sv:3` (test-expectation)

At `test/Conversion/ImportVerilog/sva-immediate-past-event-continue-on-unsupported.sv:3`, the marker `// RUN: circt-verilog --no-uvm-auto-include --sva-continue-on-unsupported --ir-moore %s | FileCheck %s --check-prefix=IR` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G2061 `test/Conversion/ImportVerilog/sva-immediate-past-event-continue-on-unsupported.sv:5` (unclassified)

At `test/Conversion/ImportVerilog/sva-immediate-past-event-continue-on-unsupported.sv:5`, the marker `module immediate_past_event_continue_on_unsupported(input logic clk);` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G2062 `test/Conversion/ImportVerilog/sva-immediate-past-event-continue-on-unsupported.sv:12` (test-gap-reference)

At `test/Conversion/ImportVerilog/sva-immediate-past-event-continue-on-unsupported.sv:12`, the marker `// ERR: error: unsupported $past value type with sampled-value controls` indicates a gap signal in this path. What's missing: This test line references an unsupported behavior, indicating a known gap in upstream implementation coverage. How to fix: Implement the missing behavior in production code and update this test to assert functional semantics rather than unsupported status once support is complete.

### G2063 `test/Conversion/ImportVerilog/sva-immediate-past-event-continue-on-unsupported.sv:14` (test-gap-reference)

At `test/Conversion/ImportVerilog/sva-immediate-past-event-continue-on-unsupported.sv:14`, the marker `// WARN: warning: unsupported $past value type with sampled-value controls` indicates a gap signal in this path. What's missing: This test line references an unsupported behavior, indicating a known gap in upstream implementation coverage. How to fix: Implement the missing behavior in production code and update this test to assert functional semantics rather than unsupported status once support is complete.

### G2064 `test/Conversion/ImportVerilog/sva-immediate-past-event-continue-on-unsupported.sv:16` (unclassified)

At `test/Conversion/ImportVerilog/sva-immediate-past-event-continue-on-unsupported.sv:16`, the marker `// IR-LABEL: moore.module @immediate_past_event_continue_on_unsupported` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G2065 `test/Conversion/ImportVerilog/cross-select-with-wide-auto-domain-supported.sv:3` (unclassified)

At `test/Conversion/ImportVerilog/cross-select-with-wide-auto-domain-supported.sv:3`, the marker `module CrossSelectWithWideAutoDomainSupported;` indicates a gap signal in this path. What's missing: This marker was captured by the project-wide gap scan and likely indicates an incomplete or constrained behavior in this location. How to fix: Confirm intent at this location, implement missing behavior if the marker is actionable, and add a focused regression test to prevent recurrence.

### G2066 `test/Conversion/ImportVerilog/sva-sequence-match-item-stacktrace-function.sv:20` (test-expectation)

At `test/Conversion/ImportVerilog/sva-sequence-match-item-stacktrace-function.sv:20`, the marker `// DIAG-NOT: unsupported system call `$stacktrace`` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G2067 `test/Conversion/ImportVerilog/sva-immediate-sampled-continue-on-unsupported.sv:2` (test-expectation)

At `test/Conversion/ImportVerilog/sva-immediate-sampled-continue-on-unsupported.sv:2`, the marker `// RUN: circt-verilog --no-uvm-auto-include --sva-continue-on-unsupported --ir-moore %s 2>&1 | FileCheck %s --check-prefix=WARN` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G2068 `test/Conversion/ImportVerilog/sva-immediate-sampled-continue-on-unsupported.sv:3` (test-expectation)

At `test/Conversion/ImportVerilog/sva-immediate-sampled-continue-on-unsupported.sv:3`, the marker `// RUN: circt-verilog --no-uvm-auto-include --sva-continue-on-unsupported --ir-moore %s | FileCheck %s --check-prefix=IR` indicates a gap signal in this path. What's missing: This line is primarily a regression expectation for behavior that is currently unsupported or intentionally skipped, so the underlying implementation is still incomplete in the product code. How to fix: Implement or enable the corresponding feature in the owning production path, then rewrite this test from expected failure/skip to a positive functional check that validates lowering and runtime behavior.

### G2069 `test/Conversion/ImportVerilog/sva-immediate-sampled-continue-on-unsupported.sv:19` (test-gap-reference)

At `test/Conversion/ImportVerilog/sva-immediate-sampled-continue-on-unsupported.sv:19`, the marker `// STRICT: error: unsupported sampled value type for $stable` indicates a gap signal in this path. What's missing: This test line references an unsupported behavior, indicating a known gap in upstream implementation coverage. How to fix: Implement the missing behavior in production code and update this test to assert functional semantics rather than unsupported status once support is complete.

### G2070 `test/Conversion/ImportVerilog/sva-immediate-sampled-continue-on-unsupported.sv:20` (test-gap-reference)

At `test/Conversion/ImportVerilog/sva-immediate-sampled-continue-on-unsupported.sv:20`, the marker `// WARN: warning: $stable has unsupported sampled value type` indicates a gap signal in this path. What's missing: This test line references an unsupported behavior, indicating a known gap in upstream implementation coverage. How to fix: Implement the missing behavior in production code and update this test to assert functional semantics rather than unsupported status once support is complete.

### G2071 `test/Conversion/ImportVerilog/sva-immediate-sampled-continue-on-unsupported.sv:21` (test-gap-reference)

At `test/Conversion/ImportVerilog/sva-immediate-sampled-continue-on-unsupported.sv:21`, the marker `// WARN: warning: $rose has unsupported sampled value type` indicates a gap signal in this path. What's missing: This test line references an unsupported behavior, indicating a known gap in upstream implementation coverage. How to fix: Implement the missing behavior in production code and update this test to assert functional semantics rather than unsupported status once support is complete.
