Toy LEC models live here for fast iteration loops.

- `toy_aes_equiv.sv`: minimal AES-style equivalence model.
- `run_toy_aes_lec.sh`: compiles and runs `circt-lec` with MLIR timing enabled.

Example:

```bash
toy_models/lec/run_toy_aes_lec.sh
```
